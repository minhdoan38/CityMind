---
phase: 12-dashboard-advisory-assistant-conversational-officer-chat-wid
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/server/ai/officer-assistant.ts
  - src/server/ai/officer-assistant.test.ts
  - src/server/services/officer-assistant.ts
  - src/server/services/officer-assistant.test.ts
  - src/app/api/officer/assistant/messages/route.ts
  - package.json
autonomous: true
requirements: [DASH-10, DASH-10a, DASH-10b, DASH-10c, DASH-10d]
must_haves:
  truths:
    - POST loads history from DB; client history field ignored (P12-D-01)
    - User and assistant turns persisted after each successful send
    - GET /api/officer/assistant/messages returns officer's thread for authenticated session
    - Optional report_id attach grounds reply via officer RLS read path
    - 429 rate limit, 404 invalid attach, 503 down health covered by unit tests
  artifacts:
    - path: src/server/services/officer-assistant.ts
      provides: handleOfficerAssistantMessageRequest, handleOfficerAssistantListRequest
      exports: [handleOfficerAssistantMessageRequest, handleOfficerAssistantListRequest]
    - path: src/server/ai/officer-assistant.ts
      provides: buildOfficerReportContext, generateOfficerAssistantReply with reportContext
      exports: [buildOfficerReportContext, OFFICER_ASSISTANT_SYSTEM_PROMPT]
    - path: src/app/api/officer/assistant/messages/route.ts
      provides: GET and POST exports
      exports: [GET, POST]
  key_links:
    - from: src/server/services/officer-assistant.ts
      to: src/server/repositories/officer-assistant-messages.ts
      via: listOfficerAssistantMessages before generate; insert user + assistant after reply
      pattern: listOfficerAssistantMessages
    - from: src/server/services/officer-assistant.ts
      to: src/server/repositories/reports.ts
      via: getOfficerReport(auth.context.client, reportId) on attach
      pattern: getOfficerReport
    - from: src/server/ai/officer-assistant.ts
      to: src/server/domain/analysis-projection.ts
      via: projectEvaluatorAnalysis in buildOfficerReportContext
      pattern: projectEvaluatorAnalysis
---

<objective>
Wave 2 backend: server-authoritative chat flow, report context injection, GET history endpoint, and expanded service/AI unit tests. Productionizes MVP officer-assistant service without widget changes yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-01-SUMMARY.md
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-RESEARCH.md
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-PATTERNS.md
@src/server/services/officer-assistant.ts
@src/server/services/citizen-coach.ts
@src/server/ai/coach.ts
@src/server/repositories/officer-assistant-messages.ts
@src/server/repositories/reports.ts
@src/server/domain/analysis-projection.ts

<interfaces>
From src/server/services/officer-assistant.ts (MVP — replace history trust):
- OfficerAssistantRequestSchema currently accepts client history — REMOVE history field per P12-D-01
- enforceOfficerAssistantRateLimit, checkAiHealth gate, requireOfficerContext — keep

From src/server/services/citizen-coach.ts (mirror send flow):
- sendCoachMessage: load history → insert user → generate → insert assistant → return messages
- serializeMessage(row) → { message_id, role, content, created_at }
- GET branch returns messages array

From src/server/ai/coach.ts:
- buildCoachReportContext(row) using projectEvaluatorAnalysis
- buildCoachContextBlock(context) appended to system prompt

From src/server/repositories/reports.ts:
- getOfficerReport(client, reportId) — use auth.context.client for IDOR safety per P12-D-03

From src/lib/supabase/admin.ts:
- getAdminClient() — use only for message persistence, NOT report load on attach path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildOfficerReportContext and AI reply with report grounding</name>
  <files>src/server/ai/officer-assistant.ts, src/server/ai/officer-assistant.test.ts</files>
  <read_first>
    <file>src/server/ai/coach.ts</file>
    <file>src/server/ai/coach.test.ts</file>
    <file>src/server/domain/analysis-projection.ts</file>
  </read_first>
  <behavior>
    - OfficerReportContext includes reportId, status, triage_status, routing_destination, category, severity, priority, observed_facts
    - buildOfficerReportContext(row) uses projectEvaluatorAnalysis for 11-key fields
    - generateOfficerAssistantReply accepts optional reportContext; when present, appends Report context block to system message
    - System message still contains OFFICER_ASSISTANT_SYSTEM_PROMPT; must NOT claim resolve/reject authority
    - Test: context block includes report_id and triage_status when attached
    - Test: completeConversationalChat receives system message with officer fields (mock fetch)
  </behavior>
  <action>
    Add OfficerReportContext type and buildOfficerReportContext mirroring coach pattern but with officer fields (status, triage_status, severity, priority) per 12-RESEARCH Pattern 4. Extend generateOfficerAssistantReply input with reportContext: OfficerReportContext | null. Create officer-assistant.test.ts with buildOfficerReportContext unit test and provider message shape test (mock completeConversationalChat or fetch). Do not reuse coach prompt or buildCoachReportContext exports.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:unit -- src/server/ai/officer-assistant.test.ts passes.</criterion>
    <criterion>System prompt includes report_id when reportContext provided.</criterion>
    <criterion>OFFICER_ASSISTANT_SYSTEM_PROMPT unchanged advisory constraints.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/ai/officer-assistant.test.ts</automated>
  </verify>
  <done>Report context injection ready for service attach path (DASH-10b).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server-authoritative send flow and GET history handler</name>
  <files>src/server/services/officer-assistant.ts, src/app/api/officer/assistant/messages/route.ts</files>
  <read_first>
    <file>src/server/services/citizen-coach.ts</file>
    <file>src/server/services/officer-assistant.ts</file>
    <file>src/server/repositories/officer-assistant-messages.ts</file>
  </read_first>
  <behavior>
    - OfficerAssistantRequestSchema: message required; optional report_id; NO history field
    - POST: load history from listOfficerAssistantMessages(getAdminClient(), officerUserId); cap last MAX_HISTORY_TURNS turns for AI input
    - If report_id provided: getOfficerReport(auth.context.client, reportId) → 404 if missing; buildOfficerReportContext for AI
    - Insert user message with report_id; call generateOfficerAssistantReply; insert assistant with model/latency from lineage if available
    - Response: { assistant_message, messages } with serialized persisted thread
    - GET handleOfficerAssistantListRequest: requireOfficerContext + rate limit; return { messages } for officer
    - Route exports GET and POST delegating to handlers
  </behavior>
  <action>
    Refactor handleOfficerAssistantMessageRequest per citizen-coach sendCoachMessage flow and P12-D-01. Remove client history from schema and generateOfficerAssistantReply call — ignore if sent by legacy clients. Add handleOfficerAssistantListRequest for GET. Wire route.ts with GET export. Use getAdminClient() for message repo only; report attach MUST use auth.context.client per IDOR mitigation. Serialize messages without leaking internal columns.
  </action>
  <acceptance_criteria>
    <criterion>POST no longer passes client history to generateOfficerAssistantReply.</criterion>
    <criterion>GET returns 401 when unauthenticated.</criterion>
    <criterion>Successful POST persists user and assistant rows.</criterion>
    <criterion>Invalid report_id returns 404 with generic detail.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/officer-assistant.test.ts</automated>
  </verify>
  <done>Server-authoritative persistence and GET history endpoint live (DASH-10a, DASH-10c, DASH-10d).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service tests — 429, persistence, attach 404, phase12:gate update</name>
  <files>src/server/services/officer-assistant.test.ts, package.json</files>
  <read_first>
    <file>src/server/services/officer-assistant.test.ts</file>
    <file>src/server/services/citizen-escalate.test.ts</file>
    <file>.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-VALIDATION.md</file>
  </read_first>
  <behavior>
    - 429: two rapid POSTs from same officer → second returns 429 with Retry-After 60
    - Persistence: mock repository; assert list called, insert called twice (user + assistant)
    - Attach 404: mock getOfficerReport null → 404
    - Existing 401, 503, 422 tests still pass
    - Update existing valid-request test to expect DB history not client history
  </behavior>
  <action>
    Extend officer-assistant.test.ts with vi.mock for repository and getOfficerReport. Add 429 test using resetOfficerAssistantLimiter and two sequential POSTs (stub env limit if needed). Add persistence integration test with mocked list/insert. Add attach 404 test. Add handleOfficerAssistantListRequest GET without session → 401 test (DASH-10a). Update phase12:gate in package.json to include officer-assistant.test.ts and officer-assistant.test.ts (ai). Remove dependency on client history in assertions.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:unit -- src/server/services/officer-assistant.test.ts includes passing 429 test.</criterion>
    <criterion>npm run test:unit -- src/server/services/officer-assistant.test.ts includes passing GET 401 test for handleOfficerAssistantListRequest.</criterion>
    <criterion>npm run phase12:gate passes after this task.</criterion>
    <criterion>All DASH-10a behaviors covered: 401 POST, 401 GET, 422, 429, 503.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run phase12:gate</automated>
  </verify>
  <done>Service hardening fully tested; phase gate includes backend tests (DASH-10a, DASH-10c, DASH-10d, DASH-10e).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/officer/assistant/messages | Untrusted message body; history must not be trusted |
| Officer → report_id attach | Must verify officer can read report via RLS client |
| Service role → message table | Inserts scoped by officer_user_id from JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | Client-sent history | mitigate | Remove history from schema; server loads persisted turns only (P12-D-01) |
| T-12-04 | Information Disclosure | Report IDOR on attach | mitigate | getOfficerReport(auth.context.client, id); uniform 404 |
| T-12-05 | Denial of Service | AI provider abuse | mitigate | Per-officer SlidingWindowLimiter; 429 + Retry-After test |
| T-12-06 | Information Disclosure | Provider errors | mitigate | Generic 502/503 responses per DATA-10 |
| T-12-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
Wave 2 gate: npm run phase12:gate
Per-plan: npm run test:unit -- src/server/services/officer-assistant.test.ts src/server/ai/officer-assistant.test.ts
</verification>

<success_criteria>
1. Client history no longer trusted
2. Messages persist and reload via GET
3. Report attach grounds AI with officer-visible fields
4. 429, 404 attach, 503 tests green
5. phase12:gate includes service + AI tests
</success_criteria>

<output>
Create `.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-02-SUMMARY.md` when done
</output>
