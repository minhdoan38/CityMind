---
phase: 12-dashboard-advisory-assistant-conversational-officer-chat-wid
plan: "03"
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx
  - messages/en.json
  - messages/vi.json
  - tests/advisory-assistant-widget.test.mjs
  - package.json
autonomous: true
requirements: [DASH-10, DASH-10e]
must_haves:
  truths:
    - Widget loads persisted history on mount via GET /api/officer/assistant/messages
    - Send disabled only when health is down (not unknown or degraded) per P12-D-04
    - Degraded health shows amber Alert; send remains enabled per 12-UI-SPEC
    - Optional report attach: chip + popover; POST includes report_id
    - EN/VI parity for new assistant keys; legacy widget contract test passes
  artifacts:
    - path: src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx
      provides: Production widget UX per 12-UI-SPEC
    - path: tests/advisory-assistant-widget.test.mjs
      provides: Static contract tests for widget + i18n keys
    - path: messages/en.json
      provides: assistantDegradedWarning, assistantLoading, assistantLoadError, attach keys
  key_links:
    - from: src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx
      to: /api/officer/assistant/messages
      via: GET on mount; POST without client history
      pattern: fetch\\(\"/api/officer/assistant/messages\"
    - from: src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx
      to: /api/health/ai
      via: 60s poll for up/degraded/down
      pattern: fetch\\(\"/api/health/ai\"
---

<objective>
Wave 3 UI: productionize AdvisoryAssistantWidget per 12-UI-SPEC — persisted thread load, health/degraded UX, report attach, bilingual copy, and legacy contract test. Voice input stays disabled per P12-D-05.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-02-SUMMARY.md
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-UI-SPEC.md
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-RESEARCH.md
@.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-VALIDATION.md
@src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx
@src/components/coach/CoachPanel.tsx
@src/components/dashboard/AiHealthChip.tsx
@messages/en.json
@messages/vi.json

<interfaces>
From AdvisoryAssistantWidget.tsx (MVP — update):
- aiUnavailable currently blocks unknown — change to down only (P12-D-04)
- POST sends client history — remove; use server messages
- Attach button disabled — enable with Popover

From CoachPanel.tsx (reference):
- Mount load: health poll + GET messages
- aiDown = status === "down" only
- Optimistic send + draft restore on error

From 12-UI-SPEC.md new i18n keys:
- assistantDegradedWarning, assistantLoading, assistantLoadError, assistantAttachedReport, assistantDetach, assistantAttachPick, assistantAttachEmpty

Optional prop:
- contextReportId?: string — pre-select attach when widget embedded with report context (Claude discretion per RESEARCH)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bilingual copy and message catalog parity</name>
  <files>messages/en.json, messages/vi.json</files>
  <read_first>
    <file>.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-UI-SPEC.md</file>
    <file>messages/en.json</file>
    <file>messages/vi.json</file>
  </read_first>
  <action>
    Add dashboard.widgets keys from 12-UI-SPEC Copywriting Contract (New keys section) to both en.json and vi.json with exact EN/VI strings from UI-SPEC. Preserve existing assistant* keys. Optionally remove or repurpose stale assistantComingSoon if present and unused. Do not add inline English in widget — all strings via useTranslations.
  </action>
  <acceptance_criteria>
    <criterion>grep finds assistantDegradedWarning in both en.json and vi.json.</criterion>
    <criterion>All seven new keys present in EN and VI with non-empty values.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node -e "const en=require('./messages/en.json');const vi=require('./messages/vi.json');const k=['assistantDegradedWarning','assistantLoading','assistantLoadError','assistantAttachedReport','assistantDetach','assistantAttachPick','assistantAttachEmpty'];for(const x of k){if(!en.dashboard?.widgets?.[x]||!vi.dashboard?.widgets?.[x]){console.error('missing',x);process.exit(1)}}"</automated>
  </verify>
  <done>Bilingual copy ready for widget (DASH-10e).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Legacy widget contract test scaffold</name>
  <files>tests/advisory-assistant-widget.test.mjs, package.json</files>
  <read_first>
    <file>tests/dashboard-table.test.mjs</file>
    <file>src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx</file>
    <file>.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test file asserts AdvisoryAssistantWidget.tsx exists
    - Baseline assertions on current MVP source (GET fetch, health poll) — extended in Task 3 after widget refactor
    - phase12:gate script scaffold includes legacy widget test path
  </behavior>
  <action>
    Create tests/advisory-assistant-widget.test.mjs following dashboard-table.test.mjs style (node:test + fs read). Initial static assertions: widget file exists, fetches /api/health/ai, POST to /api/officer/assistant/messages. Add phase12:gate script to package.json if not present from 12-01 (include paths per 12-VALIDATION.md). Test will be tightened in Task 3 after widget refactor.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:legacy -- tests/advisory-assistant-widget.test.mjs passes.</criterion>
    <criterion>package.json contains phase12:gate script.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:legacy -- tests/advisory-assistant-widget.test.mjs</automated>
  </verify>
  <done>Widget contract test scaffold exists before UX refactor (DASH-10e).</done>
</task>

<task type="auto">
  <name>Task 3: Widget UX — history load, health gating, degraded alert, attach</name>
  <files>src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx</files>
  <read_first>
    <file>src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx</file>
    <file>src/components/coach/CoachPanel.tsx</file>
    <file>.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - On mount: GET /api/officer/assistant/messages; show Loader2 + assistantLoading while loading
    - Load error: assistantLoadError caption; send still allowed if health not down
    - aiUnavailable = aiStatus === "down" only (P12-D-04)
    - degraded: shadcn Alert amber above disclaimer; hide disclaimer; send enabled
    - unknown: input enabled; show assistantDisclaimer (optionally opacity-70)
    - Messages use message_id as React key when from server
    - Attach: enable Paperclip; Popover with report ID input + confirm; Badge chip with detach X
    - POST body: { message, report_id? } only — no history
    - Optional prop contextReportId pre-fills attach on mount
    - Voice button stays disabled with assistantVoiceSoon (P12-D-05)
    - Preserve optimistic send, thinking row, draft restore, aria-live, sphere empty state
  </behavior>
  <action>
    Refactor AdvisoryAssistantWidget per 12-UI-SPEC and CoachPanel patterns. Import Alert from shadcn if not present. Implement attach Popover (report ID text input is acceptable for v1 — officer pastes ID from dashboard; optional contextReportId prop for detail-page embed). Fix canSend to exclude only down status. Map GET response messages to thread state. On successful POST, replace thread with server messages array if returned, else append assistant reply. Do not call coach API or send client history.
  </action>
  <acceptance_criteria>
    <criterion>Widget fetches GET /api/officer/assistant/messages on mount.</criterion>
    <criterion>aiUnavailable is true only when aiStatus === "down".</criterion>
    <criterion>Degraded shows Alert with assistantDegradedWarning.</criterion>
    <criterion>Attach enabled; POST includes report_id when attached.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx','utf8');const checks=[['/api/officer/assistant/messages','GET fetch'],['aiStatus === \"down\"','down-only gate'],['assistantDegradedWarning','degraded copy']];for(const [p,l] of checks){if(!s.includes(p)){console.error('missing',l);process.exit(1)}}"</automated>
  </verify>
  <done>Widget matches 12-UI-SPEC health, persistence, and attach contracts (DASH-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Final widget contract assertions and phase12:gate</name>
  <files>tests/advisory-assistant-widget.test.mjs, package.json</files>
  <read_first>
    <file>tests/dashboard-table.test.mjs</file>
    <file>.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-VALIDATION.md</file>
    <file>src/components/dashboard/widgets/AdvisoryAssistantWidget.tsx</file>
  </read_first>
  <behavior>
    - Test file asserts AdvisoryAssistantWidget.tsx exists
    - Asserts GET fetch to /api/officer/assistant/messages
    - Asserts POST does NOT include history in JSON.stringify body
    - Asserts aiUnavailable uses down-only check (not unknown)
    - Asserts assistantDegradedWarning key usage or Alert import
    - Asserts aria-live on thread container
    - phase12:gate includes legacy widget test + full unit paths
  </behavior>
  <action>
    Extend tests/advisory-assistant-widget.test.mjs with post-refactor assertions: GET fetch to /api/officer/assistant/messages; POST does NOT include history in JSON.stringify body; aiUnavailable uses down-only check (not unknown); assistantDegradedWarning key usage or Alert import; aria-live on thread container. Update package.json phase12:gate to run: officer-assistant repository test, officer-assistant service test, officer-assistant AI test, advisory-assistant-widget legacy test, and 12_phase12_contract.sql. Final gate must match 12-VALIDATION.md phase gate section.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:legacy -- tests/advisory-assistant-widget.test.mjs passes.</criterion>
    <criterion>npm run phase12:gate passes end-to-end.</criterion>
    <criterion>Widget test fails if client history reintroduced to POST body.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run phase12:gate</automated>
  </verify>
  <done>Phase 12 automated gate complete (DASH-10e).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser widget → API | Session cookie auth; no access tokens in widget |
| Officer UI → report_id attach | User-selected ID sent to server for RLS verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | Client history in widget | mitigate | Widget POST omits history; contract test asserts |
| T-12-08 | Spoofing | Coach API misuse | mitigate | Widget only calls /api/officer/assistant/messages |
| T-12-09 | Repudiation | Advisory disclaimer | mitigate | Persistent assistantDisclaimer / degraded warning per UI-SPEC |
| T-12-SC | Tampering | npm installs | accept | No new packages; shadcn Alert already in project |
</threat_model>

<verification>
Phase gate: npm run phase12:gate
Manual (optional): officer login → send message → refresh → thread persists; AI down disables send; degraded shows amber banner
</verification>

<success_criteria>
1. Widget loads server history on mount
2. Health UX matches 12-UI-SPEC (down blocks, degraded warns, unknown allows)
3. Report attach works with chip + POST report_id
4. EN/VI new keys present
5. phase12:gate green including widget contract test
</success_criteria>

<output>
Create `.planning/phases/12-dashboard-advisory-assistant-conversational-officer-chat-wid/12-03-SUMMARY.md` when done
</output>
