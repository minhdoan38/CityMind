---
phase: 11-triage-evaluator-spec-conformance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260722160001_evaluator_analysis_columns.sql
  - supabase/migrations/20260722160002_complete_triage_report_v2.sql
  - supabase/tests/11_phase11_contract.sql
  - src/server/domain/evaluator-analysis.ts
  - src/server/domain/evaluator-analysis.test.ts
  - src/server/domain/analysis-projection.ts
  - src/server/domain/analysis-projection.test.ts
  - src/server/validation/evaluator-policy.ts
  - src/server/validation/evaluator-policy.test.ts
  - src/server/ai/evaluator.ts
  - src/server/ai/openai-compatible.ts
  - src/server/triage/service.ts
  - src/server/triage/service.test.ts
  - src/server/triage/config.ts
  - src/server/triage/audit.ts
  - prompt/citymind_ai_triage_structured_output_evaluator.json
autonomous: true
requirements: [TRIAGE-09, TRIAGE-10, TRIAGE-11]
must_haves:
  truths:
    - Triage completion persists 11-key evaluator fields at the RPC boundary (observed_facts, inferences, unknowns, severity_reason, priority_reason, recommended_action, requires_human_review)
    - Legacy summary/recommendation/evidence/uncertainty columns receive dual-write projections for backward compatibility per D-11
    - System prompt and Zod category enum match evaluator JSON 10 categories per D-12
    - critical priority requires severity == 5 enforced by validateEvaluatorPolicy per D-13 (not legacy MIN_SEVERITY_BY_PRIORITY.critical 4)
    - Pre-migration rows still readable through dual-read adapter without empty citizen summaries
  artifacts:
    - path: src/server/domain/evaluator-analysis.ts
      provides: EvaluatorAnalysisSchema, EvaluatorCategorySchema (10 enums)
      exports: [EvaluatorAnalysisSchema, EvaluatorAnalysis]
    - path: src/server/domain/analysis-projection.ts
      provides: projectLegacyCitizenFields, projectEvaluatorAnalysis
      exports: [projectEvaluatorAnalysis, projectLegacyCitizenFields]
    - path: src/server/validation/evaluator-policy.ts
      provides: validateEvaluatorPolicy from evaluator policy_evaluator + cross_field_rules
      exports: [validateEvaluatorPolicy]
    - path: src/server/ai/evaluator.ts
      provides: loadEvaluatorConfig, buildEvaluatorSystemPrompt
      exports: [loadEvaluatorConfig]
    - path: supabase/migrations/20260722160002_complete_triage_report_v2.sql
      provides: complete_triage_report v2 mapping 11-key p_analysis
  key_links:
    - from: src/server/triage/service.ts
      to: src/server/validation/evaluator-policy.ts
      via: validateEvaluatorPolicy before complete_triage_report
      pattern: validateEvaluatorPolicy
    - from: src/server/triage/service.ts
      to: src/server/ai/evaluator.ts
      via: evaluator system_prompt replaces legacy SYSTEM_INSTRUCTION
      pattern: buildEvaluatorSystemPrompt
    - from: src/server/triage/audit.ts
      to: complete_triage_report
      via: RPC v2 with 11-key JSON payload
      pattern: complete_triage_report
---

## Phase Goal

**As an** operator, **I want** triage persistence and validation aligned with the evaluator 11-key contract, **so that** production runtime matches the authoritative spec without breaking existing citizen/officer read paths.

<objective>
Wave 1 foundation: additive DB columns, complete_triage_report v2, EvaluatorAnalysisSchema + dual-read adapter, evaluator-driven policy validation, and triage service wired to evaluator prompt. No dispatch routes, coach, or dashboard UI in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-CONTEXT.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-PATTERNS.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-GAP-ANALYSIS.md
@prompt/citymind_ai_triage_structured_output_evaluator.json
@src/server/domain/report-analysis.ts
@src/server/validation/analysis-policy.ts
@src/server/ai/openai-compatible.ts
@src/server/triage/service.ts
@src/server/triage/audit.ts
@supabase/migrations/20260722120002_async_triage_audit.sql

<interfaces>
From src/server/domain/report-analysis.ts:
- ReportAnalysisSchema — legacy 9-field shape (dual-write target only)
- CategorySchema — 7 values today; extend via separate EvaluatorCategorySchema

From src/server/validation/analysis-policy.ts:
- validateAnalysisPolicy — keep for Phase 10 eval until 11-06; triage path switches to validateEvaluatorPolicy
- MIN_SEVERITY_BY_PRIORITY.critical: 4 — MUST NOT be used on evaluator path (D-13)

From src/server/triage/service.ts:
- runTriageForReport(reportId, deps?) — replace ReportAnalysisSchema parse + validateAnalysisPolicy with EvaluatorAnalysis
- buildValidationRetryInstruction(violations) — reuse violation code strings

From prompt/citymind_ai_triage_structured_output_evaluator.json:
- output_schema.properties — 11 keys
- category.enum — 10 values (pothole, flooding, waste, streetlight, traffic_signal, obstruction, utility_hazard, structural_damage, graffiti, other)
- policy_evaluator + consistency_evaluator.cross_field_rules — source for validateEvaluatorPolicy
- config_version: "1.0.0" — PROMPT_VERSION target

Locked decisions:
- D-11 dual-read adapter during migration
- D-12 evaluator JSON as prompt + Zod source of truth
- D-13 critical requires severity == 5 + evaluator policy_assertions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evaluator DB columns and complete_triage_report v2</name>
  <files>supabase/migrations/20260722160001_evaluator_analysis_columns.sql, supabase/migrations/20260722160002_complete_triage_report_v2.sql, supabase/tests/11_phase11_contract.sql, src/server/triage/audit.ts</files>
  <read_first>
    <file>supabase/migrations/20260722120002_async_triage_audit.sql</file>
    <file>supabase/tests/09_routing_contract.sql</file>
    <file>.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Additive columns on reports: observed_facts JSONB NOT NULL DEFAULT '[]', inferences JSONB, unknowns JSONB, severity_reason TEXT, priority_reason TEXT, recommended_action TEXT, requires_human_review BOOLEAN NOT NULL DEFAULT true
    - Legacy columns (summary, recommendation, estimated_impact, evidence, uncertainty) remain; RPC dual-writes legacy from 11-key payload
    - complete_triage_report v2 accepts p_analysis JSONB with 11-key fields; COALESCE legacy keys for one-release backward compat
    - SQL contract asserts: intake → complete with 11-key → observed_facts non-empty; critical+severity5 constraint via application policy (not DB CHECK on priority)
    - audit.ts updateTriageTerminalState routes all completes through v2 RPC only
  </behavior>
  <action>
    Create migration 20260722160001 per RESEARCH Pattern 1 column table. Create migration 20260722160002 replacing or versioning complete_triage_report to map 11-key fields and dual-write legacy columns (summary ← severity_reason or joined observed_facts; recommendation ← recommended_action; evidence ← observed_facts; uncertainty ← unknowns; estimated_impact ← priority_reason or static placeholder). Scaffold supabase/tests/11_phase11_contract.sql with _test_assert helper (mirror 09_routing_contract.sql) covering v2 RPC round-trip — expand in 11-06. Update audit.ts RPC call signature to pass 11-key analysis object. Do not drop legacy columns.
  </action>
  <acceptance_criteria>
    <criterion>Migration applies cleanly via node scripts/run-supabase-sql.mjs.</criterion>
    <criterion>11_phase11_contract.sql v2 RPC section passes after migrations.</criterion>
    <criterion>No direct reports.update() bypass for terminal triage state.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-supabase-sql.mjs -f supabase/migrations/20260722160001_evaluator_analysis_columns.sql &amp;&amp; node scripts/run-supabase-sql.mjs -f supabase/migrations/20260722160002_complete_triage_report_v2.sql &amp;&amp; node scripts/run-supabase-sql.mjs -f supabase/tests/11_phase11_contract.sql</automated>
  </verify>
  <done>DB persists 11-key evaluator shape at RPC boundary with legacy dual-write (TRIAGE-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EvaluatorAnalysisSchema and dual-read projection adapter</name>
  <files>src/server/domain/evaluator-analysis.ts, src/server/domain/evaluator-analysis.test.ts, src/server/domain/analysis-projection.ts, src/server/domain/analysis-projection.test.ts</files>
  <read_first>
    <file>prompt/citymind_ai_triage_structured_output_evaluator.json</file>
    <file>src/server/domain/report-analysis.ts</file>
    <file>tests/migration/evidence-reconciliation.test.ts</file>
  </read_first>
  <behavior>
    - EvaluatorCategorySchema: exactly 10 enums from evaluator JSON (includes traffic_signal, utility_hazard, structural_damage; graffiti retained)
    - EvaluatorAnalysisSchema: 11 keys strict; requires_human_review literal true
    - projectEvaluatorAnalysis(row): prefer 11-key columns when observed_facts length &gt; 0 OR severity_reason IS NOT NULL; else map legacy ReportAnalysis fields
    - projectLegacyCitizenFields: maps 11-key → summary/recommendation for citizen status consumers
    - Tests: pre-migration row (legacy only), post-migration row (11-key only), hybrid row
  </behavior>
  <action>
    Implement evaluator-analysis.ts loading category enum from evaluator JSON at module init (read JSON file once). Implement analysis-projection.ts per RESEARCH dual-read rules (D-11). Export types for downstream plans. Add unit tests covering all three row shapes. Do not modify citizen-status.ts in this task — adapter is consumed in 11-04/11-06.
  </action>
  <acceptance_criteria>
    <criterion>EvaluatorCategorySchema rejects legacy-only 7-value categories not in evaluator enum when used on 11-key path.</criterion>
    <criterion>npm run test:unit -- src/server/domain/evaluator-analysis.test.ts passes.</criterion>
    <criterion>npm run test:unit -- src/server/domain/analysis-projection.test.ts passes dual-read cases.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/domain/evaluator-analysis.test.ts &amp;&amp; npm run test:unit -- src/server/domain/analysis-projection.test.ts</automated>
  </verify>
  <done>11-key Zod + dual-read adapter exist with green tests (TRIAGE-09, TRIAGE-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Evaluator policy, prompt loader, triage service wire-up</name>
  <files>src/server/validation/evaluator-policy.ts, src/server/validation/evaluator-policy.test.ts, src/server/ai/evaluator.ts, src/server/ai/openai-compatible.ts, src/server/triage/service.ts, src/server/triage/service.test.ts, src/server/triage/config.ts</files>
  <read_first>
    <file>src/server/validation/analysis-policy.ts</file>
    <file>src/server/triage/service.ts</file>
    <file>prompt/citymind_ai_triage_structured_output_evaluator.json</file>
  </read_first>
  <behavior>
    - validateEvaluatorPolicy implements policy_evaluator sections + cross_field_rules including critical_requires_severity_5 on observed_facts (not evidence array)
    - Retry only on json_parse_error / schema_validation_error; NOT on policy_violation (per evaluator validation_pipeline.retry)
    - loadEvaluatorConfig() returns system_prompt, output_schema, config_version from JSON file
    - openai-compatible analyzeStructured parses with EvaluatorAnalysisSchema; system instruction from evaluator.ts
    - PROMPT_VERSION in config.ts → "1.0.0" (evaluator config_version)
    - service.test.ts: critical+severity4 fails policy; critical+severity5 passes; severity5 without danger evidence in observed_facts fails cross_field rule
  </behavior>
  <action>
    Create evaluator-policy.ts loading phrase lists and rules from evaluator JSON (single source per D-12/D-13). Create evaluator.ts for prompt loading. Update openai-compatible.ts to use EvaluatorAnalysisSchema on triage path while keeping StructuredAnalysisResult compatible via projection. Wire runTriageForReport to validateEvaluatorPolicy + dual-write projection before complete_triage_report v2. Update service tests; keep shadow compare on legacy ReportAnalysis projection until 11-06 migrates eval snapshots. Remove reliance on MIN_SEVERITY_BY_PRIORITY for evaluator path.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:unit -- src/server/validation/evaluator-policy.test.ts passes critical/severity and grounding fixtures.</criterion>
    <criterion>npm run test:unit -- src/server/triage/service.test.ts passes with EvaluatorAnalysis mocks.</criterion>
    <criterion>Triage audit records prompt_version 1.0.0 on new runs.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/validation/evaluator-policy.test.ts &amp;&amp; npm run test:unit -- src/server/triage/service.test.ts</automated>
  </verify>
  <done>Evaluator prompt + policy wired into triage runtime (TRIAGE-10, TRIAGE-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI provider → triage service | Untrusted model output parsed through Zod + policy before persist |
| DB RPC → reports table | Service-role only; 11-key fields must not bypass validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | AI triage output | mitigate | EvaluatorAnalysisSchema.strict() + validateEvaluatorPolicy before RPC |
| T-11-02 | Information Disclosure | observed_facts in logs | mitigate | Do not log raw AI output in production paths; audit stores controlled snapshot |
| T-11-03 | Tampering | Legacy dual-write drift | mitigate | Single RPC v2 path; SQL contract asserts 11-key + legacy populated |
| T-11-SC | Tampering | npm installs | accept | Zero new packages per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
Wave 1 gate: npm run test:unit -- src/server/domain src/server/validation/evaluator-policy.test.ts src/server/triage/service.test.ts
SQL: node scripts/run-supabase-sql.mjs -f supabase/tests/11_phase11_contract.sql (v2 section)
</verification>

<success_criteria>
1. complete_triage_report v2 persists all 11 evaluator keys
2. Dual-read adapter serves legacy consumers without empty fields on new rows
3. 10-category enum parity with evaluator JSON
4. critical priority rejected unless severity === 5
5. Triage uses evaluator system_prompt; PROMPT_VERSION is 1.0.0
</success_criteria>

<output>
Create `.planning/phases/11-triage-evaluator-spec-conformance/11-01-SUMMARY.md` when done
</output>
