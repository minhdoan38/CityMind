---
phase: 11-triage-evaluator-spec-conformance
plan: "03"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/server/health/ai-readiness.ts
  - src/server/health/ai-readiness.test.ts
  - src/app/api/health/ai/route.ts
  - src/server/ai/openai-compatible.ts
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - GET /api/health/ai returns { status, model, latency_ms, checked_at } with no secrets per D-08
    - Status thresholds: up &lt; 5s latency, degraded 5–15s, down on error/timeout/missing env
    - Module-level TTL cache ~45s prevents probe stampede per RESEARCH Pattern 4
    - Response includes X-Cache HIT on cached reads; Cache-Control private max-age=30
    - /api/ready remains Supabase-only (unchanged)
  artifacts:
    - path: src/server/health/ai-readiness.ts
      provides: checkAiHealth(env)
      exports: [checkAiHealth, AiHealthResponse]
    - path: src/app/api/health/ai/route.ts
      provides: GET handler
      exports: [GET]
  key_links:
    - from: src/app/api/health/ai/route.ts
      to: checkAiHealth
      via: thin route delegate
      pattern: checkAiHealth
    - from: checkAiHealth
      to: openai-compatible chat completion
      via: minimal ping completion max_tokens 5
      pattern: buildChatCompletionsUrl
---

## Phase Goal

**As an** operator, **I want** a lightweight AI readiness probe separate from Supabase health, **so that** dashboard and citizen UI can gate coach/triage features when the model is unavailable.

<objective>
Wave 2 parallel track: AI health probe service with TTL cache and GET /api/health/ai route. Independent of dispatch (11-02) and coach UI (11-04/11-05) but required before those consumers wire up.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-triage-evaluator-spec-conformance/11-CONTEXT.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-PATTERNS.md
@src/server/health/readiness.ts
@src/app/api/ready/route.ts
@src/server/ai/openai-compatible.ts
@src/server/config/env.ts
@scripts/smoke-ai.mjs

<interfaces>
From src/server/health/readiness.ts:
- withTimeout(promise, ms) — reuse or mirror for probe
- checkReadiness() — Supabase only; do not extend

From src/server/ai/openai-compatible.ts:
- buildChatCompletionsUrl(env)
- fetch with Authorization Bearer THIRD_PARTY_API_KEY

From src/server/config/env.ts:
- AI_MODEL, AI_BASE_URL, THIRD_PARTY_API_KEY, AI_TIMEOUT_MS

Locked decisions:
- D-08 separate /api/health/ai lightweight probe
- D-10 dashboard chip consumes this endpoint (wired in 11-05)
- D-05 coach disabled when down (wired in 11-04)

Planner discretion:
- AI_HEALTH_TTL_MS = 45_000
- Probe timeout min(AI_TIMEOUT_MS, 10_000)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkAiHealth service with TTL cache</name>
  <files>src/server/health/ai-readiness.ts, src/server/health/ai-readiness.test.ts, src/server/ai/openai-compatible.ts</files>
  <read_first>
    <file>src/server/health/readiness.ts</file>
    <file>scripts/smoke-ai.mjs</file>
    <file>.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Missing AI env → status down, latency_ms 0, model from env or "unknown"
    - Successful probe: system "Reply with OK", user "ping", max_tokens 5, temperature 0
    - latency &lt; 5000 → up; 5000–15000 → degraded; else down
    - Second call within 45s returns cached body (test via injectable clock or module reset)
    - Never include API key, base URL, or raw provider error in response
    - Export AiHealthResponse type
  </behavior>
  <action>
    Implement ai-readiness.ts mirroring readiness.ts structure. Extract minimal probe helper in openai-compatible.ts (e.g. probeChatCompletion) to avoid duplicating URL/auth logic. Add ai-readiness.test.ts with mocked fetch: up/degraded/down paths, cache hit, missing env. Do not invoke full smoke-ai.mjs per request.
  </action>
  <acceptance_criteria>
    <criterion>ai-readiness.test.ts covers all three status thresholds.</criterion>
    <criterion>Cache prevents second fetch within TTL in test.</criterion>
    <criterion>Response shape matches RESEARCH AiHealthResponse.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/health/ai-readiness.test.ts</automated>
  </verify>
  <done>AI health probe service with cache and unit tests exists.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/health/ai route</name>
  <files>src/app/api/health/ai/route.ts</files>
  <read_first>
    <file>src/app/api/ready/route.ts</file>
    <file>src/app/api/health/route.ts</file>
  </read_first>
  <behavior>
    - GET returns JSON from checkAiHealth()
    - HTTP status: 200 for up and degraded; 503 for down
    - Headers: Cache-Control private, max-age=30; X-Cache HIT|MISS
    - No authentication required (public readiness-style endpoint)
    - Route is read-only; no side effects beyond cached probe
  </behavior>
  <action>
    Thin route per PATTERNS — delegate to checkAiHealth(getServerEnv()). Map status to HTTP codes. Add X-Cache header based on cache hit flag from service (return { body, cacheHit } from checkAiHealth or separate peekCache). Do not modify /api/ready.
  </action>
  <acceptance_criteria>
    <criterion>curl http://127.0.0.1:3000/api/health/ai returns JSON with status field.</criterion>
    <criterion>down status returns HTTP 503.</criterion>
    <criterion>No secrets in response body.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/health/ai-readiness.test.ts</automated>
  </verify>
  <done>GET /api/health/ai exposed for dashboard and citizen consumers (OPS-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → /api/health/ai | Unauthenticated; must not leak config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | health response | mitigate | Return model id string only; map errors to generic down |
| T-11-08 | Denial of Service | health probe stampede | mitigate | 45s TTL module cache; Cache-Control max-age=30 |
| T-11-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/health/ai-readiness.test.ts
With dev server: curl -s http://127.0.0.1:3000/api/health/ai | jq .status
</verification>

<success_criteria>
1. /api/health/ai returns up/degraded/down with latency and model id
2. TTL cache reduces provider calls
3. /api/ready unchanged
4. No secrets in response
</success_criteria>

<output>
Create `.planning/phases/11-triage-evaluator-spec-conformance/11-03-SUMMARY.md` when done
</output>
