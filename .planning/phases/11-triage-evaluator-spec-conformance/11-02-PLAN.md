---
phase: 11-triage-evaluator-spec-conformance
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/server/triage/dispatch.ts
  - src/server/triage/dispatch.test.ts
  - src/server/security/internal-auth.ts
  - src/server/security/internal-auth.test.ts
  - src/app/api/internal/triage/[reportId]/route.ts
  - src/app/api/officer/reports/[reportId]/triage/route.ts
  - src/app/api/officer/reports/triage/bulk/route.ts
  - src/server/services/officer-triage-dispatch.ts
  - src/server/services/officer-triage-dispatch.test.ts
  - src/server/services/report-service.ts
  - src/server/services/report-service.test.ts
  - src/server/config/env.ts
  - .env.example
autonomous: true
requirements: [TRIAGE-12]
user_setup:
  - service: citymind-internal
    why: Push-primary triage dispatch requires shared secret between intake and internal route
    env_vars:
      - name: INTERNAL_TRIAGE_SECRET
        source: Generate random 32+ byte secret; add to .env.local
must_haves:
  truths:
    - POST /api/internal/triage/{report_id} authenticates via X-CityMind-Internal-Key with timingSafeEqual per D-09
    - Intake submitReport fire-and-forget dispatches triage without blocking citizen response
    - Officer POST /api/officer/reports/{id}/triage triggers dispatch for pending/failed/retry only (D-15 backend)
    - Officer POST /api/officer/reports/triage/bulk accepts up to 25 report_ids serial dispatch (D-16 backend)
    - Dispatch is idempotent — returns 202 when triage_status is processing or completed
    - Poll worker (scripts/triage-worker.mjs) remains unchanged as fallback safety net
  artifacts:
    - path: src/server/triage/dispatch.ts
      provides: dispatchTriage(reportId) shared entry
      exports: [dispatchTriage]
    - path: src/app/api/internal/triage/[reportId]/route.ts
      provides: internal push route
      exports: [POST]
    - path: src/app/api/officer/reports/[reportId]/triage/route.ts
      provides: officer per-row dispatch
      exports: [POST]
    - path: src/server/config/env.ts
      provides: INTERNAL_TRIAGE_SECRET validation
  key_links:
    - from: src/server/services/report-service.ts
      to: /api/internal/triage/[reportId]
      via: non-blocking fetch after createIntakeReportWithAccessToken
      pattern: internal/triage
    - from: src/server/triage/dispatch.ts
      to: runTriageForReport
      via: claim guard + status check before invoke
      pattern: runTriageForReport
    - from: src/app/api/officer/reports/[reportId]/triage/route.ts
      to: dispatchTriage
      via: requireOfficerContext then dispatch
      pattern: dispatchTriage
---

## Phase Goal

**As a** citizen, **I want** triage to start immediately after I submit a report, **so that** I see results faster without waiting for the poll worker.

<objective>
Wave 2 dispatch backbone: shared dispatchTriage core, authenticated internal push route, intake enqueue, and officer API hooks for per-row and bulk retry. UI wiring deferred to 11-05. Requires 11-01 evaluator triage path.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-01-SUMMARY.md
</execution_context>

<context>
@.planning/phases/11-triage-evaluator-spec-conformance/11-CONTEXT.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-PATTERNS.md
@src/server/triage/service.ts
@src/server/services/report-service.ts
@src/server/security/access-tokens.ts
@src/app/api/officer/reports/[reportId]/status/route.ts
@scripts/triage-worker.mjs

<interfaces>
From 11-01 (must exist):
- runTriageForReport — evaluator-wired triage orchestration
- complete_triage_report v2 — persistence after dispatch

From src/server/security/access-tokens.ts:
- timingSafeEqual pattern for secret comparison

From src/server/services/report-service.ts:
- submitReport returns { report_id, access_token, triage_status: "pending" } — must not await triage

Locked decisions:
- D-09 push-primary via POST /internal/triage/{report_id}; worker fallback retained
- D-15/D-16 officer dispatch APIs (UI in 11-05)

Planner discretion (RESEARCH):
- INTERNAL_TRIAGE_SECRET env var name
- Bulk dispatch serial (max 25 ids) to avoid provider thundering herd
- Optional loopback guard on /api/internal/* for non-loopback IPs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dispatchTriage core and internal auth guard</name>
  <files>src/server/triage/dispatch.ts, src/server/triage/dispatch.test.ts, src/server/security/internal-auth.ts, src/server/security/internal-auth.test.ts, src/server/config/env.ts, .env.example</files>
  <read_first>
    <file>src/server/security/access-tokens.ts</file>
    <file>src/server/triage/service.ts</file>
    <file>.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md</file>
  </read_first>
  <behavior>
    - dispatchTriage(reportId): load row; if triage_status in (processing, completed) return { accepted: false, reason: "already_running_or_done" }
    - If pending, failed, or retry (and retry due if applicable) → invoke runTriageForReport
    - verifyInternalTriageRequest uses timingSafeEqual on X-CityMind-Internal-Key header
    - Missing INTERNAL_TRIAGE_SECRET → internal route returns 503
    - Env schema adds INTERNAL_TRIAGE_SECRET: z.string().min(32)
  </behavior>
  <action>
    Implement dispatch.ts as idempotent entry shared by internal and officer paths. Implement internal-auth.ts per RESEARCH Code Example (verifyInternalTriageRequest). Add env.ts field + .env.example documentation. Unit tests: wrong secret rejected; correct secret accepted; idempotent guard on processing/completed; dispatches on pending.
  </action>
  <acceptance_criteria>
    <criterion>dispatch.test.ts covers idempotency and eligible status sets.</criterion>
    <criterion>internal-auth.test.ts covers length mismatch and timing-safe compare.</criterion>
    <criterion>.env.example documents INTERNAL_TRIAGE_SECRET generation.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/dispatch.test.ts &amp;&amp; npm run test:unit -- src/server/security/internal-auth.test.ts</automated>
  </verify>
  <done>Shared dispatch core and internal auth guard exist with tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Internal push route and intake enqueue</name>
  <files>src/app/api/internal/triage/[reportId]/route.ts, src/server/services/report-service.ts, src/server/services/report-service.test.ts</files>
  <read_first>
    <file>src/server/services/report-service.ts</file>
    <file>src/app/api/officer/reports/[reportId]/routing/route.ts</file>
  </read_first>
  <behavior>
    - POST /api/internal/triage/[reportId] returns 202 { report_id, disposition: "accepted" } on dispatch start
    - Returns 401 without valid internal key; 404 unknown report_id; 202 already processing
    - submitReport after successful intake: void fetch loopback URL with internal key, AbortSignal.timeout(5000), catch swallow (worker fallback)
    - Citizen response latency unchanged — triage not awaited
    - report-service.test.ts asserts fetch called with correct URL/headers; outage of internal route does not fail intake
  </behavior>
  <action>
    Thin route delegates to handleInternalTriageDispatch(request, reportId). Add getInternalBaseUrl() helper (reuse existing pattern or env APP_URL default http://127.0.0.1:3000). Wire submitReport fire-and-forget per RESEARCH Pattern 2. Extend report-service.test.ts. Do not remove triage-worker.mjs.
  </action>
  <acceptance_criteria>
    <criterion>Internal route returns 401 on bad/missing secret.</criterion>
    <criterion>submitReport returns token while dispatch is async.</criterion>
    <criterion>npm run test:unit -- src/server/services/report-service.test.ts passes enqueue test.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/report-service.test.ts &amp;&amp; npm run test:unit -- src/server/triage/dispatch.test.ts</automated>
  </verify>
  <done>Push-primary intake enqueue wired (TRIAGE-12 intake path).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Officer triage dispatch API hooks</name>
  <files>src/app/api/officer/reports/[reportId]/triage/route.ts, src/app/api/officer/reports/triage/bulk/route.ts, src/server/services/officer-triage-dispatch.ts, src/server/services/officer-triage-dispatch.test.ts</files>
  <read_first>
    <file>src/server/officer/guard.ts</file>
    <file>src/app/api/officer/reports/[reportId]/status/route.ts</file>
  </read_first>
  <behavior>
    - POST officer single triage: requireOfficerContext; validate triage_status in (pending, failed, retry); call dispatchTriage; return 202
    - POST bulk: body { report_ids: string[] } max 25; serial dispatch; return { accepted: string[], skipped: { id, reason }[] }
    - Unauthorized → 401; invalid status → 409 with detail
    - Does not expose INTERNAL_TRIAGE_SECRET to browser — officer route calls dispatchTriage in-process
  </behavior>
  <action>
    Mirror thin route pattern from status/route.ts. Implement officer-triage-dispatch.ts with auth + validation. Bulk route caps selection at 25 per RESEARCH security table. Unit tests mock requireOfficerContext and dispatchTriage. UI buttons added in 11-05.
  </action>
  <acceptance_criteria>
    <criterion>Officer route rejects unauthenticated requests.</criterion>
    <criterion>Completed reports return 409 on manual dispatch.</criterion>
    <criterion>Bulk route processes serially and reports skipped ids.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/officer-triage-dispatch.test.ts</automated>
  </verify>
  <done>Officer dispatch API hooks ready for dashboard UI (TRIAGE-12, D-15/D-16 backend).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/internal/triage | Must reject unauthenticated dispatch |
| Browser → officer triage routes | Officer session required; no internal secret exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Elevation | internal triage route | mitigate | timingSafeEqual secret; optional loopback-only middleware |
| T-11-05 | Denial of Service | bulk triage | mitigate | Cap 25 ids; serial dispatch; 202 acceptance |
| T-11-06 | Spoofing | forged internal header | mitigate | Secret min 32 chars; never log header value |
| T-11-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/triage/dispatch.test.ts src/server/services/report-service.test.ts src/server/services/officer-triage-dispatch.test.ts
Manual: submit report → observe triage_status moves to processing without waiting 5s worker tick
</verification>

<success_criteria>
1. Internal route authenticated and idempotent
2. Intake enqueues triage without blocking response
3. Officer single + bulk dispatch APIs functional
4. Poll worker still runnable as fallback
</success_criteria>

<output>
Create `.planning/phases/11-triage-evaluator-spec-conformance/11-02-SUMMARY.md` when done
</output>
