---
phase: 11-triage-evaluator-spec-conformance
plan: "04"
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - supabase/migrations/20260722160003_chat_messages.sql
  - supabase/tests/11_phase11_contract.sql
  - src/server/repositories/chat-messages.ts
  - src/server/services/citizen-coach.ts
  - src/server/services/citizen-coach.test.ts
  - src/server/ai/coach.ts
  - src/server/ai/coach.test.ts
  - src/server/security/rate-limit.ts
  - src/app/api/public/reports/coach/messages/route.ts
  - src/components/coach/CoachPanel.tsx
  - src/app/[locale]/report/success/page.tsx
  - src/app/[locale]/status/page.tsx
  - messages/en.json
  - messages/vi.json
autonomous: true
requirements: [SHELP-01, SHELP-02, SHELP-03, SHELP-04, SHELP-05]
must_haves:
  truths:
    - Success page polls triage until terminal state then branches coach vs government per D-02/D-04
    - Coach chat on success AND status pages with same token-scoped thread per D-01
    - Messages persist in Postgres chat_messages per D-03; survive refresh and status resume
    - Coach uses same AI_MODEL/AI_BASE_URL/THIRD_PARTY_API_KEY with separate system prompt per D-06
    - Coach cannot change routing or report status per D-07
    - Coach UI disabled with calm warning when /api/health/ai is down per D-05
    - Escalate CTA visible in coach panel per D-04/SHELP-04
    - Bilingual EN/VI coach copy per D-01 discretion
  artifacts:
    - path: supabase/migrations/20260722160003_chat_messages.sql
      provides: chat_messages table RLS service_role only
    - path: src/server/services/citizen-coach.ts
      provides: handleCitizenCoachRequest, listCoachMessages, sendCoachMessage
      exports: [handleCitizenCoachRequest]
    - path: src/server/ai/coach.ts
      provides: generateCoachReply with playbook + triage context
      exports: [generateCoachReply]
    - path: src/components/coach/CoachPanel.tsx
      provides: reusable coach UI with escalate link
    - path: src/app/[locale]/report/success/page.tsx
      provides: triage polling + branch UI
  key_links:
    - from: src/app/[locale]/report/success/page.tsx
      to: POST /api/public/reports/status
      via: poll until terminal triage_status
      pattern: api/public/reports/status
    - from: CoachPanel
      to: /api/public/reports/coach/messages
      via: GET list + POST send
      pattern: coach/messages
    - from: citizen-coach.ts
      to: chat_messages
      via: service-role repository
      pattern: chat_messages
    - from: CoachPanel
      to: /api/health/ai
      via: mount fetch gates send button
      pattern: health/ai
---

## Phase Goal

**As a** citizen with an easy self-help issue, **I want** to chat with an AI coach after triage completes, **so that** I can resolve simple problems while hard cases are clearly routed to government.

<objective>
Wave 3 citizen UX vertical slice: chat_messages persistence, token-scoped coach API, coach AI provider, success-page triage polling with branch UI, status-page coach resume, bilingual copy. Depends on dispatch (11-02) for triage completion and health probe (11-03) for gating.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-02-SUMMARY.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-03-SUMMARY.md
</execution_context>

<context>
@.planning/phases/11-triage-evaluator-spec-conformance/11-CONTEXT.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-PATTERNS.md
@src/server/services/citizen-escalate.ts
@src/server/services/citizen-status.ts
@src/app/[locale]/report/success/page.tsx
@src/app/[locale]/status/page.tsx
@src/server/routing/playbooks.ts
@src/server/health/ai-readiness.ts

<interfaces>
From 11-03:
- GET /api/health/ai → { status: up|degraded|down, model, latency_ms, checked_at }

From src/server/services/citizen-escalate.ts:
- hashAccessToken, tokenBindsReport, enforceStatusRateLimit pattern
- POST escalate body { report_id, token }

From src/server/services/citizen-status.ts:
- projectCitizenTriageView → service_step, routing_destination, triage_status

From src/server/routing/playbooks.ts:
- resolvePlaybookId(category) → playbook steps for coach context

Locked decisions:
- D-01 coach on success + status
- D-02 wait on success page (poll, no immediate redirect)
- D-03 Postgres chat_messages
- D-04 government path no coach-first
- D-05 disable coach when AI down
- D-06 same provider, separate prompt (CONTEXT overrides SHELP-03 optional coach model)
- D-07 advisory only

Planner discretion:
- Poll 2s first 30s then 5s backoff, max 120s
- COACH_RATE_LIMIT 10/min per report_id+IP
- Max 50 messages per report per 24h
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chat_messages migration, repository, and coach API</name>
  <files>supabase/migrations/20260722160003_chat_messages.sql, supabase/tests/11_phase11_contract.sql, src/server/repositories/chat-messages.ts, src/server/services/citizen-coach.ts, src/server/services/citizen-coach.test.ts, src/server/security/rate-limit.ts, src/app/api/public/reports/coach/messages/route.ts</files>
  <read_first>
    <file>src/server/services/citizen-escalate.ts</file>
    <file>supabase/migrations/20260722140001_triage_shadow.sql</file>
    <file>.planning/phases/11-triage-evaluator-spec-conformance/11-RESEARCH.md</file>
  </read_first>
  <behavior>
    - chat_messages schema per RESEARCH: message_id UUID, report_id FK, role user|assistant|system, content 1–4000 chars, created_at, model, latency_ms
    - RLS enabled; service_role only grants
    - GET coach/messages: query report_id+token; returns ordered messages[]
    - POST: body { report_id, token, message }; gates triage_status=completed AND routing_destination=self_help; returns { assistant_message, messages }
    - Wrong token → 401 generic; government routing → 403; AI down → 503
    - enforceCoachRateLimit 10/min; 50 messages/24h per report_id count check
    - SQL contract: anon cannot SELECT chat_messages
  </behavior>
  <action>
    Create migration 20260722160003. Implement chat-messages repository (insert, listByReportId, countLast24h). Implement citizen-coach.ts mirroring citizen-escalate auth pattern. Add enforceCoachRateLimit to rate-limit.ts. Thin route supports GET and POST. Extend 11_phase11_contract.sql with chat_messages RLS assertions. Unit tests: token gate, routing gate, rate limit, message length.
  </action>
  <acceptance_criteria>
    <criterion>citizen-coach.test.ts passes auth and routing gate cases.</criterion>
    <criterion>SQL contract rejects anon read on chat_messages.</criterion>
    <criterion>POST returns 403 when routing_destination is government.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-coach.test.ts &amp;&amp; node scripts/run-supabase-sql.mjs -f supabase/tests/11_phase11_contract.sql</automated>
  </verify>
  <done>Token-scoped coach API with Postgres persistence (SHELP-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coach AI provider (distinct prompt, same env)</name>
  <files>src/server/ai/coach.ts, src/server/ai/coach.test.ts, src/server/services/citizen-coach.ts</files>
  <read_first>
    <file>src/server/ai/openai-compatible.ts</file>
    <file>src/server/routing/playbooks.ts</file>
    <file>src/server/domain/analysis-projection.ts</file>
  </read_first>
  <behavior>
    - COACH_SYSTEM_PROMPT: conversational self-help; includes routing_destination, playbook steps, observed_facts, recommended_action
    - Guardrail text: do not contradict routing; cannot change status; advisory only (D-07)
    - Uses AI_MODEL, AI_BASE_URL, THIRD_PARTY_API_KEY — no separate coach model env (D-06)
    - Multi-turn: prior chat_messages + new user message sent to provider (no response_format json_object)
    - generateCoachReply returns { content, model, latency_ms }
    - Test: mock provider; assert system prompt contains playbook id and guardrail
  </behavior>
  <action>
    Implement coach.ts using openai-compatible fetch helpers without structured JSON mode. Wire citizen-coach POST to persist user message, call generateCoachReply, persist assistant message. Keep prompts in coach.ts or messages/coach-prompt.ts — not in evaluator JSON.
  </action>
  <acceptance_criteria>
    <criterion>coach.test.ts verifies guardrail and playbook injection.</criterion>
    <criterion>No AI_COACH_MODEL added to env.ts.</criterion>
    <criterion>Assistant message persisted with model and latency_ms.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/ai/coach.test.ts &amp;&amp; npm run test:unit -- src/server/services/citizen-coach.test.ts</automated>
  </verify>
  <done>Distinct coach role with same provider config (SHELP-03).</done>
</task>

<task type="auto">
  <name>Task 3: Success/status UI branching, CoachPanel, bilingual copy</name>
  <files>src/components/coach/CoachPanel.tsx, src/app/[locale]/report/success/page.tsx, src/app/[locale]/status/page.tsx, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>src/app/[locale]/status/page.tsx</file>
    <file>src/app/[locale]/report/success/page.tsx</file>
    <file>messages/en.json</file>
  </read_first>
  <behavior>
    - Success page: after flash load, poll POST /api/public/reports/status every 2s (5s after 30s) until triage_status in (completed, failed, manual_review) or timeout 120s
    - completed + service_step self_help_guidance → render CoachPanel
    - completed + government → government queue messaging + status link (no coach)
    - failed/manual_review → calm automated_review_unavailable copy (no provider names)
    - pending/processing → spinner with statusWorkflow.stepAiPending
    - Status page: when self_help + completed, show CoachPanel below playbook (resume same thread via GET coach/messages)
    - CoachPanel: message list, input, send disabled when health/ai down with public.coach.aiDownWarning
    - Escalate link/button reuses existing escalate flow (SHELP-04)
    - Add public.coach.* and extend public.routing keys in en.json + vi.json (SHELP-05)
  </behavior>
  <action>
    Extract CoachPanel client component with props { reportId, accessToken, disabled?, onEscalate? }. Update success page with poll useEffect per RESEARCH Pattern 6. Extend status page to embed CoachPanel when eligible. Fetch /api/health/ai on CoachPanel mount. Add i18n keys mirroring public.routing tone. Consider per-report_id status rate limit exemption or document STATUS_RATE_LIMIT_PER_MINUTE raise if 429 observed (RESEARCH open question).
  </action>
  <acceptance_criteria>
    <criterion>Success page does not redirect immediately to status after submit.</criterion>
    <criterion>Government path never shows coach-first panel.</criterion>
    <criterion>Coach send disabled when health status is down.</criterion>
    <criterion>EN and VI keys exist for all new coach strings.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-coach.test.ts</automated>
  </verify>
  <done>Citizen coach UX on success and status pages with polling and branches (SHELP-01, SHELP-04, SHELP-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen → coach API | Untrusted message content; token auth |
| Coach AI → citizen | Advisory output; must not leak other reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Spoofing | coach token brute force | mitigate | Rate limit; hashAccessToken; generic 401 |
| T-11-10 | Tampering | prompt injection in user message | mitigate | System prompt isolation; 4000 char cap; server routing gate |
| T-11-11 | Information Disclosure | cross-report chat | mitigate | tokenBindsReport before list/insert |
| T-11-12 | Elevation | coach mutates routing | mitigate | No write paths to reports in coach service |
| T-11-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/services/citizen-coach.test.ts src/server/ai/coach.test.ts
SQL: chat_messages section in 11_phase11_contract.sql
Manual: submit self_help report → success page poll → coach appears; government report → no coach
</verification>

<success_criteria>
1. Coach API token-scoped and persisted
2. Success page polls and branches correctly
3. Status page resumes coach thread
4. AI down disables coach with warning
5. Escalate CTA present; EN/VI copy complete
</success_criteria>

<output>
Create `.planning/phases/11-triage-evaluator-spec-conformance/11-04-SUMMARY.md` when done
</output>
