---
phase: 11-triage-evaluator-spec-conformance
plan: "05"
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - src/components/dashboard/AiHealthChip.tsx
  - src/components/reports/TriageDispatchActions.tsx
  - src/components/reports/ReportsTable.tsx
  - src/app/dashboard/page.tsx
  - messages/en.json
  - messages/vi.json
  - tests/dashboard-table.test.mjs
autonomous: true
requirements: [DASH-09]
must_haves:
  truths:
    - Dashboard header shows AI status chip (green/amber/red) from GET /api/health/ai per D-10
    - Per-row "Run triage now" visible for pending/failed/retry; calls officer triage API per D-15
    - Bulk retry for selected pending/failed/retry rows per D-16
    - Row action does not navigate to detail (stopPropagation)
    - Chip polls health on interval without hammering provider (relies on server cache)
  artifacts:
    - path: src/components/dashboard/AiHealthChip.tsx
      provides: health status badge in dashboard header
    - path: src/components/reports/TriageDispatchActions.tsx
      provides: per-row dispatch button
    - path: src/components/reports/ReportsTable.tsx
      provides: row selection + bulk retry toolbar
  key_links:
    - from: AiHealthChip
      to: /api/health/ai
      via: fetch on mount + 60s interval
      pattern: health/ai
    - from: TriageDispatchActions
      to: POST /api/officer/reports/{id}/triage
      via: officerFetch
      pattern: officer/reports/.*/triage
    - from: ReportsTable bulk action
      to: POST /api/officer/reports/triage/bulk
      via: officerFetch with report_ids[]
      pattern: triage/bulk
---

## Phase Goal

**As an** officer, **I want** AI health visibility and one-click triage retry on the dashboard, **so that** I can act on stuck reports without waiting for the poll worker.

<objective>
Wave 3 parallel officer UX: AI health chip in dashboard header, per-row Run triage now, bulk retry selection. Wires to APIs from 11-02 and health from 11-03. No coach or schema changes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-02-SUMMARY.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-03-SUMMARY.md
</execution_context>

<context>
@.planning/phases/11-triage-evaluator-spec-conformance/11-CONTEXT.md
@.planning/phases/11-triage-evaluator-spec-conformance/11-PATTERNS.md
@src/components/reports/TriageStatusBadge.tsx
@src/components/reports/ReportsTable.tsx
@src/components/StatusActions.tsx
@src/app/dashboard/page.tsx
@src/lib/officer-fetch.ts

<interfaces>
From 11-02 (must exist):
- POST /api/officer/reports/[reportId]/triage → 202
- POST /api/officer/reports/triage/bulk → { accepted, skipped }

From 11-03 (must exist):
- GET /api/health/ai → { status, model, latency_ms, checked_at }

From src/components/reports/TriageStatusBadge.tsx:
- Badge variant pattern for elevated/pending states

From src/components/StatusActions.tsx:
- officer fetch + router.refresh() after mutation

Locked decisions:
- D-10 dashboard AI chip from /api/health/ai
- D-15 per-row Run triage now
- D-16 bulk retry

Planner discretion:
- Chip refresh interval 60s (server caches 45s)
- Bulk max 25 selection aligned with API cap
- Optional filter chip "AI unavailable" — implement only if low cost; else defer within table toolbar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AiHealthChip in dashboard header</name>
  <files>src/components/dashboard/AiHealthChip.tsx, src/app/dashboard/page.tsx, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>src/components/reports/TriageStatusBadge.tsx</file>
    <file>src/app/dashboard/page.tsx</file>
  </read_first>
  <behavior>
    - Fetch GET /api/health/ai on mount; refresh every 60s
    - Visual: up → green, degraded → amber, down → red
    - Show model id as subtle subtitle or tooltip (dashboard.aiHealth.modelLabel)
    - Loading state skeleton on first fetch
    - Placed in dashboard header beside ReportsDateRangeFilter
    - i18n keys dashboard.aiHealth.up, .degraded, .down, .checking
  </behavior>
  <action>
    Create AiHealthChip client component mirroring TriageStatusBadge styling patterns. Import into dashboard/page.tsx header row. Add EN/VI strings. Do not call provider directly — only /api/health/ai.
  </action>
  <acceptance_criteria>
    <criterion>Chip renders in dashboard header.</criterion>
    <criterion>Status colors match up/degraded/down.</criterion>
    <criterion>EN and VI labels present.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:legacy -- tests/dashboard-table.test.mjs</automated>
  </verify>
  <done>Dashboard AI status chip live (DASH-09 health portion).</done>
</task>

<task type="auto">
  <name>Task 2: Per-row Run triage now action</name>
  <files>src/components/reports/TriageDispatchActions.tsx, src/components/reports/ReportsTable.tsx, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>src/components/StatusActions.tsx</file>
    <file>src/components/RoutingOverrideActions.tsx</file>
  </read_first>
  <behavior>
    - Button visible only when triage_status in (pending, failed, retry)
    - Click POST /api/officer/reports/{reportId}/triage via officerFetch
    - Loading state on button; toast or inline success on 202; error on 409/401
    - e.stopPropagation() on click to prevent row navigation
    - router.refresh() after success
    - i18n dashboard.triage.runNow, runNowLoading, runNowFailed
  </behavior>
  <action>
    Create TriageDispatchActions.tsx client component with reportId + triageStatus props. Add column or actions cell in ReportsTable (mirror RoutingOverrideActions placement). Wire officerFetch POST. Add translations.
  </action>
  <acceptance_criteria>
    <criterion>Button hidden for completed/processing rows.</criterion>
    <criterion>Successful dispatch triggers table refresh.</criterion>
    <criterion>Row click not triggered when pressing button.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:legacy -- tests/dashboard-table.test.mjs</automated>
  </verify>
  <done>Per-row triage dispatch on dashboard (DASH-09, D-15).</done>
</task>

<task type="auto">
  <name>Task 3: Bulk retry selection and dispatch</name>
  <files>src/components/reports/ReportsTable.tsx, messages/en.json, messages/vi.json, tests/dashboard-table.test.mjs</files>
  <read_first>
    <file>src/components/reports/ReportsTable.tsx</file>
    <file>src/app/api/officer/reports/export/route.ts</file>
  </read_first>
  <behavior>
    - Add checkbox column with TanStack rowSelection state
    - Toolbar button "Retry triage" enabled when 1–25 eligible rows selected (pending/failed/retry only)
    - POST /api/officer/reports/triage/bulk with { report_ids }
    - Show toast with accepted count and skipped reasons
    - Clear selection after success
    - Extend dashboard-table.test.mjs if it asserts column count or actions
  </behavior>
  <action>
    Add rowSelection to ReportsTable following TanStack Table docs already in project. Filter selected ids to eligible triage_status before bulk POST. Cap at 25 with disabled state + dashboard.triage.bulkMaxHint copy. Serial dispatch handled server-side.
  </action>
  <acceptance_criteria>
    <criterion>Bulk button disabled when no eligible selection.</criterion>
    <criterion>Selection capped at 25 with user feedback.</criterion>
    <criterion>Bulk POST called with correct report_ids array.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:legacy -- tests/dashboard-table.test.mjs</automated>
  </verify>
  <done>Bulk triage retry on dashboard (DASH-09, D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer browser → triage APIs | requireOfficerContext on server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Elevation | bulk triage without auth | mitigate | Officer guard on API; UI uses officerFetch only |
| T-11-14 | Denial of Service | bulk selection abuse | mitigate | 25 cap enforced client + server |
| T-11-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<verification>
npm run test:legacy -- tests/dashboard-table.test.mjs
Manual: dashboard shows chip; run triage on pending row; bulk select failed rows
</verification>

<success_criteria>
1. AI health chip visible with correct colors
2. Per-row Run triage now works for eligible statuses
3. Bulk retry up to 25 reports
4. Officer session required (server-side)
</success_criteria>

<output>
Create `.planning/phases/11-triage-evaluator-spec-conformance/11-05-SUMMARY.md` when done
</output>
