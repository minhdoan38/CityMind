---
phase: 13-immediate-citizen-triage-on-submit-with-evaluator-prompt-and
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/server/triage/dispatch.test.ts
  - src/server/services/report-service.test.ts
  - supabase/tests/13_phase13_contract.sql
  - package.json
autonomous: true
requirements: [TRIAGE-12, TRIAGE-13, SHELP-01, CIT-02]
must_haves:
  truths:
    - dispatchTriageAndWait calls dispatchTriage with force:true and wait:true
    - dispatchTriage with wait:true awaits runTriage before returning (not fire-and-forget)
    - submitReport government routing projects officer_review service_step in response
    - phase13:gate script runs unit + legacy + SQL contract
    - retry status eligible for claim_triage_report per migration 20260722170002
  artifacts:
    - path: src/server/triage/dispatch.test.ts
      provides: dispatchTriageAndWait and wait-mode coverage
    - path: src/server/services/report-service.test.ts
      provides: government-path submit outcome test
    - path: supabase/tests/13_phase13_contract.sql
      provides: retry claim eligibility SQL gate
    - path: package.json
      provides: phase13:gate npm script
  key_links:
    - from: src/server/services/report-service.ts
      to: dispatchTriageAndWait
      via: submitReport try/catch + buildIntakeTriageOutcome
      pattern: dispatchTriageAndWait
    - from: src/server/triage/dispatch.ts
      to: runTriageForReport
      via: deps.wait synchronous await
      pattern: if \(deps\.wait\)
---

<objective>
Wave 2 service hardening: unit tests for sync dispatch wait mode and government-path intake outcome, optional SQL contract for retry claim eligibility, and composite phase13:gate script. Minimal production code changes only if tests expose regressions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-immediate-citizen-triage-on-submit-with-evaluator-prompt-and/13-01-SUMMARY.md
@.planning/phases/13-immediate-citizen-triage-on-submit-with-evaluator-prompt-and/13-RESEARCH.md
@.planning/phases/13-immediate-citizen-triage-on-submit-with-evaluator-prompt-and/13-PATTERNS.md
@src/server/triage/dispatch.ts
@src/server/triage/dispatch.test.ts
@src/server/services/report-service.test.ts
@supabase/migrations/20260722170002_reports_triage_status_retry.sql
@supabase/tests/11_phase11_contract.sql
@package.json

<interfaces>
From dispatch.ts:
export async function dispatchTriageAndWait(reportId, deps): Promise<void> {
  await dispatchTriage(reportId, { ...deps, force: true, wait: true });
}

dispatchTriage with deps.wait: await runTriage (sync) vs void runTriage (async)

From report-service.test.ts existing:
- self_help path test with dispatchTriageAndWait mock
- enqueueTriageDispatch on sync throw

From citizen-status.ts projection:
- routing_destination government → service_step officer_review (when not resolved/rejected)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dispatchTriageAndWait and wait-mode unit tests</name>
  <files>src/server/triage/dispatch.test.ts</files>
  <behavior>
    - dispatchTriageAndWait invokes dispatchTriage with force:true and wait:true
    - When wait:true, runTriage resolves before dispatchTriage returns accepted
    - When wait:true and runTriage throws, dispatchTriage still returns accepted (errors logged, not rethrown from dispatch layer)
  </behavior>
  <action>
    Extend dispatch.test.ts: import dispatchTriageAndWait. Add test that mocks runTriage with a deferred promise — with wait:true, dispatchTriage must not return until runTriage completes; without wait, returns before runTriage resolves (existing async test pattern). Add dispatchTriageAndWait test verifying it passes force and wait flags (spy on dispatchTriage or integration-style mock). Follow existing createClient mock pattern for pending report row. Per D-13-01.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/triage/dispatch.test.ts</automated>
  </verify>
  <done>dispatchTriageAndWait wait/force behavior covered (TRIAGE-12 citizen sync).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Government-path submitReport outcome test</name>
  <files>src/server/services/report-service.test.ts</files>
  <behavior>
    - When getCitizenStatus returns routing_destination government, severity 5, submitReport response has service_step officer_review (or equivalent government projection)
    - Response includes routing_destination government and hides self_help coach fields (can_escalate per projection rules)
    - dispatchTriageAndWait still called; provider.analyze not called
  </behavior>
  <action>
    Add describe block or it() in existing submitReport tests: mock getCitizenStatus with routing_destination government, triage_status completed, high severity category. Assert result.service_step is officer_review, routing_destination government, and not self_help_guidance. Optionally extend citizen-status.test.ts if projection edge needs coverage for CIT-02. Do not change submitReport implementation unless assertion fails — fix projection only if bug found.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/services/report-service.test.ts -t submit</automated>
  </verify>
  <done>Government vs self_help intake outcome differentiation tested (SHELP-01, CIT-02, TRIAGE-13).</done>
</task>

<task type="auto">
  <name>Task 3: 13_phase13_contract.sql and phase13:gate script</name>
  <files>supabase/tests/13_phase13_contract.sql, package.json</files>
  <action>
    Create supabase/tests/13_phase13_contract.sql with _test_assert helper (copy from 11_phase11_contract.sql). Assert: reports_triage_status_chk includes retry; claim_triage_report function exists; optional fixture — insert report with triage_status retry and triage_next_attempt_at in past, call claim_triage_report, assert transitions to processing. Cleanup fixture rows. Add package.json script phase13:gate mirroring phase12:gate structure: npm run test:unit -- report-service.test.ts dispatch.test.ts AND npm run test:legacy -- citizen-success-triage.test.mjs report-form.test.mjs AND node scripts/run-supabase-sql.mjs -f supabase/tests/13_phase13_contract.sql. Do not duplicate phase11:gate full suite — scope to Phase 13 paths only per 13-VALIDATION.md.
  </action>
  <verify>
    <automated>npm run phase13:gate</automated>
  </verify>
  <done>phase13:gate passes; SQL contract validates retry claim eligibility (TRIAGE-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → claim_triage_report | SQL contract validates RPC eligibility only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | claim_triage_report | mitigate | SQL contract asserts retry row claim transitions to processing |
| T-13-02-SC | Tampering | npm scripts | accept | No new npm dependencies |
</threat_model>

<verification>
npm run phase13:gate
</verification>

<success_criteria>
- dispatch.test.ts includes dispatchTriageAndWait coverage
- report-service.test.ts includes government-path outcome
- phase13:gate script exists and passes
- 13_phase13_contract.sql passes when SUPABASE_DB_URL set
</success_criteria>

<output>
Create `.planning/phases/13-immediate-citizen-triage-on-submit-with-evaluator-prompt-and/13-02-SUMMARY.md` when done
</output>
