---
phase: 03-dashboard-polish
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - frontend/src/components/reports/ExportButton.tsx
  - frontend/src/app/api/officer/reports/export/route.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/DashboardSidebar.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/dashboard-export.test.mjs
autonomous: true
requirements:
  - DASH-06
  - DASH-07
must_haves:
  truths:
    - "Export CSV/Excel button applies current filters via BFF streaming proxy (D-15 / D-16 / DASH-06)."
    - "Sidebar Export routes to /dashboard?focus=export and focuses the export control (D-17)."
    - "Export preparing/disabled and failure Alert states use UI-SPEC EN/VI copy (D-22 / DASH-07)."
  artifacts:
    - path: "frontend/src/components/reports/ExportButton.tsx"
      provides: "CSV/XLSX download trigger with loading/error"
    - path: "frontend/src/app/api/officer/reports/export/route.ts"
      provides: "Authenticated stream proxy to FastAPI /export"
  key_links:
    - from: "frontend/src/components/reports/ExportButton.tsx"
      to: "frontend/src/app/api/officer/reports/export/route.ts"
      via: "current filter query string + format=csv|xlsx"
      pattern: "format=|Preparing export|focus=export"
    - from: "frontend/src/components/DashboardSidebar.tsx"
      to: "frontend/src/app/dashboard/page.tsx"
      via: "/dashboard?focus=export focuses Export control"
      pattern: "focus=export"
---

## Phase Goal

**As an** officer, **I want to** filter, paginate, export, and resolve reports with audited notes, **so that** I can operate the queue with evidence and accountability.

<objective>
Deliver Track B export UI/BFF (split from Plan 03-02): filtered CSV/Excel download control, authenticated streaming proxy, and sidebar Export focus (DASH-06 / DASH-07 export states).

Purpose: Officers download the current filtered set using Plan 03-01 `/export` and Plan 03-02 URL filter params.
Output: ExportButton, export BFF route, sidebar focus wiring, EN/VI export strings, smoke tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-polish/03-RESEARCH.md
@.planning/phases/03-dashboard-polish/03-PATTERNS.md
@.planning/phases/03-dashboard-polish/03-UI-SPEC.md
@.planning/phases/03-dashboard-polish/03-01-PLAN.md
@.planning/phases/03-dashboard-polish/03-02-PLAN.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/components/DashboardSidebar.tsx
@frontend/src/lib/backend.ts
@frontend/src/app/api/officer/reports/[reportId]/image/route.ts
@frontend/messages/en.json
@frontend/messages/vi.json
@frontend/tests/dashboard-table.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire filtered CSV/Excel export button and sidebar Export focus</name>
  <files>frontend/src/components/reports/ExportButton.tsx, frontend/src/app/api/officer/reports/export/route.ts, frontend/src/app/dashboard/page.tsx, frontend/src/components/DashboardSidebar.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/dashboard-export.test.mjs</files>
  <behavior>
    - Export control on Reports toolbar offers Download CSV and Download Excel; request includes current filter query string from Plan 03-02 URL sync (D-15 / DASH-06).
    - BFF GET /api/officer/reports/export proxies to FastAPI /api/v1/reports/export, passing through Content-Type and Content-Disposition; getClaims fail-closed 401; body not parsed as JSON (D-16 / D-18 / PATTERNS image-route analog).
    - While preparing: disabled control + Preparing export…; on failure Alert with UI-SPEC export failed copy (D-22 / DASH-07).
    - DashboardSidebar Export url is /dashboard?focus=export; page focuses export control when focus=export (D-17).
  </behavior>
  <action>Write failing frontend/tests/dashboard-export.test.mjs asserting export route presence, format=csv|xlsx markers, and focus=export sidebar URL. Add ExportButton (split/dropdown) next to filters per UI-SPEC on the Plan 03-02 dashboard page. Create export BFF route mirroring officer image proxy pattern — forward query string including format and filters; never buffer full set client-side (no SheetJS). Point sidebar Export from dead hash to /dashboard?focus=export (D-17). On mount with focus=export, focus Export control. Add EN/VI export strings from UI-SPEC. Do not build a separate Export orphan page; do not change table/filter core (owned by 03-02) or detail resolve (owned by 03-03).</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/dashboard-export.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - export/route.ts uses officerFetch and Content-Disposition passthrough
    - DashboardSidebar Export url contains focus=export
    - ExportButton.tsx references format=csv or format=xlsx
  </acceptance_criteria>
  <done>Officers can stream filtered CSV/XLSX downloads from the Reports view; sidebar Export focuses the same control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next BFF export | Must require officer session before proxying stream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | export BFF | high | mitigate | getClaims gate + forward to officer-only FastAPI export (D-18). |
</threat_model>

<verification>
Run `cd frontend &amp;&amp; node --test tests/dashboard-export.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build`. Manually smoke: apply status filter → Export CSV downloads → sidebar Export focuses control.
</verification>

<success_criteria>
- DASH-06 and export DASH-07 satisfied.
- No orphan Export page; no SheetJS; no table/filter rewrite (Plan 03-02 owns that).
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-polish/03-04-SUMMARY.md` when done.
</output>
