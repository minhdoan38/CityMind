---
phase: 03-dashboard-polish
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
gap_closure: true
files_modified:
  - frontend/src/app/dashboard/loading.tsx
  - frontend/src/app/dashboard/reports/[reportId]/loading.tsx
  - frontend/tests/dashboard-loading-list.test.mjs
  - frontend/tests/dashboard-loading-detail.test.mjs
autonomous: true
requirements:
  - DASH-07
must_haves:
  truths:
    - "Navigating to /dashboard shows a table skeleton (header + 8–10 rows) while the RSC page loads (D-22 / DASH-07)."
    - "Navigating to /dashboard/reports/[reportId] shows a detail skeleton (header + section blocks) while the RSC page loads (D-22 / DASH-07)."
    - "Both loading surfaces import Skeleton from @/components/ui/skeleton per UI-SPEC."
  artifacts:
    - path: "frontend/src/app/dashboard/loading.tsx"
      provides: "Reports list route loading UI with table skeleton"
      contains: "Skeleton"
    - path: "frontend/src/app/dashboard/reports/[reportId]/loading.tsx"
      provides: "Report detail route loading UI with section-block skeletons"
      contains: "Skeleton"
    - path: "frontend/tests/dashboard-loading-list.test.mjs"
      provides: "Static smoke assertions for list loading.tsx"
    - path: "frontend/tests/dashboard-loading-detail.test.mjs"
      provides: "Static smoke assertions for detail loading.tsx"
  key_links:
    - from: "frontend/src/app/dashboard/loading.tsx"
      to: "frontend/src/components/ui/skeleton.tsx"
      via: "import { Skeleton } from @/components/ui/skeleton"
      pattern: "@/components/ui/skeleton"
    - from: "frontend/src/app/dashboard/reports/[reportId]/loading.tsx"
      to: "frontend/src/components/ui/skeleton.tsx"
      via: "import { Skeleton } from @/components/ui/skeleton"
      pattern: "@/components/ui/skeleton"
---

## Phase Goal

**As an** officer, **I want to** filter, paginate, export, and resolve reports with audited notes, **so that** I can operate the queue with evidence and accountability.

## Gap Closure

**Source:** `03-VERIFICATION.md` — score 12/13; single failed truth: list and detail loading skeletons missing (D-22 / DASH-07).

**Closes:** Empty/error/export-loading already shipped in 03-02/03/04; this plan adds the remaining UI-SPEC Skeleton loading surfaces via Next.js `loading.tsx` route boundaries (no changes to `page.tsx` data fetching).

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | Officer queue operates with polished dashboard UX including loading feedback | Task 2–3 |
| REQ DASH-07 | Loading, empty, and error states on all dashboard views | Tasks 2–3 (loading gap only) |
| UI-SPEC | Table: header + 8–10 row skeletons; Detail: header + section blocks | Tasks 2–3 |
| CONTEXT D-22 | Every dashboard surface implements loading states | Tasks 2–3 |

<objective>
Add Next.js `loading.tsx` route skeletons for the Reports list and report detail pages so DASH-07 loading contract is fully satisfied.

Purpose: Close the sole verification gap without replanning 03-01–03-04; officers see structured placeholders instead of blank chrome during RSC fetch.
Output: `dashboard/loading.tsx`, `reports/[reportId]/loading.tsx`, `dashboard-loading-list.test.mjs`, `dashboard-loading-detail.test.mjs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-polish/03-VERIFICATION.md
@.planning/phases/03-dashboard-polish/03-UI-SPEC.md
@.planning/phases/03-dashboard-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-polish/03-02-SUMMARY.md
@.planning/phases/03-dashboard-polish/03-03-SUMMARY.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/app/dashboard/reports/[reportId]/page.tsx
@frontend/src/app/dashboard/analytics/loading.tsx
@frontend/src/components/ui/skeleton.tsx

<interfaces>
From frontend/src/app/dashboard/analytics/loading.tsx (pattern analog):
- Default export function returning a div with `space-y-*` layout and `Skeleton` placeholders
- Import: `import { Skeleton } from "@/components/ui/skeleton";`

From frontend/src/app/dashboard/page.tsx (list layout to mirror):
- Outer: `w-full max-w-none space-y-6`
- Header row: title/subtitle left, export button right (`flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between`)
- Then: filters chrome, metrics strip (4 cells), bordered table region

From frontend/src/app/dashboard/reports/[reportId]/page.tsx (detail layout to mirror per D-19):
- Outer: `space-y-6 max-w-4xl mx-auto`
- Back link placeholder, header meta (title + id + status badge), 4-column meta grid
- Section blocks: citizen, evidence, AI advisory, urban, timeline, actions — each `rounded-lg border border-border p-6`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashboard loading smoke tests (RED)</name>
  <files>frontend/tests/dashboard-loading-list.test.mjs, frontend/tests/dashboard-loading-detail.test.mjs</files>
  <read_first>
    - frontend/tests/dashboard-table.test.mjs
    - frontend/tests/dashboard-detail.test.mjs
    - frontend/src/app/dashboard/analytics/loading.tsx
  </read_first>
  <behavior>
    - List test file: `dashboard/loading.tsx` exists, default-exports loading component, imports `@/components/ui/skeleton`, has table header + ≥8 row Skeleton placeholders.
    - Detail test file: `reports/[reportId]/loading.tsx` exists, default-exports loading component, imports `@/components/ui/skeleton`, has ≥5 section-block skeleton regions.
  </behavior>
  <action>
    Create two smoke files following the node:test + fs pattern in `dashboard-table.test.mjs`:
    1. `frontend/tests/dashboard-loading-list.test.mjs` — list loading assertions only
    2. `frontend/tests/dashboard-loading-detail.test.mjs` — detail loading assertions only
    Tests MUST fail before Tasks 2–3 create the respective loading files (RED gate per DASH-07 gap).
  </action>
  <acceptance_criteria>
    - `dashboard-loading-list.test.mjs` and `dashboard-loading-detail.test.mjs` exist under `frontend/tests/`
    - `cd frontend && node --test tests/dashboard-loading-list.test.mjs tests/dashboard-loading-detail.test.mjs` exits non-zero before Tasks 2–3
    - Each file imports `node:test` and reads source via `fs.readFileSync`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node --test tests/dashboard-loading-list.test.mjs tests/dashboard-loading-detail.test.mjs</automated>
  </verify>
  <done>Both smoke test files exist; fail until respective loading.tsx files land in Tasks 2–3.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Reports list loading.tsx (per D-22)</name>
  <files>frontend/src/app/dashboard/loading.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/page.tsx
    - frontend/src/app/dashboard/analytics/loading.tsx
    - frontend/src/components/ui/skeleton.tsx
    - frontend/tests/dashboard-loading-list.test.mjs
  </read_first>
  <action>
    Add `frontend/src/app/dashboard/loading.tsx` as a Server Component (no `"use client"`). Mirror `dashboard/page.tsx` chrome layout and `analytics/loading.tsx` Skeleton style.

    Structure (per UI-SPEC table skeleton contract):
    1. Page header skeletons: title bar (`h-8 w-48`), subtitle (`h-5 max-w-2xl`), export button placeholder (`h-11 w-36`) in the same flex header row as the live page.
    2. Filters toolbar skeleton: single bar ~`h-11 w-full` or compact row of 2–3 Skeleton chips.
    3. Metrics strip: horizontal row of 4 metric wells (caption + value Skeleton pairs) matching `ReportsMetrics` density.
    4. Table region inside `rounded-lg border border-border`: header row with 6 column Skeleton cells (lean ops columns); body with exactly **8–10** row skeletons (use `Array.from({ length: 9 })` or similar — UI-SPEC says 8–10; pick 9). Each row ~`h-12` with cell Skeletons in a grid/flex matching table row height (~48px per UI-SPEC).

    Import only `Skeleton` from `@/components/ui/skeleton`. No i18n strings needed (skeletons are non-text). Do not modify `page.tsx` or `ReportsTable.tsx` — Next.js App Router automatically shows this file while the async page segment loads.
  </action>
  <acceptance_criteria>
    - `frontend/src/app/dashboard/loading.tsx` exists with default export
    - Source contains `import { Skeleton } from "@/components/ui/skeleton"`
    - Source maps 8–10 row skeletons (e.g. `Array.from({ length: 9 })`)
    - `cd frontend && node --test tests/dashboard-loading-list.test.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node --test tests/dashboard-loading-list.test.mjs</automated>
  </verify>
  <done>List route loading file exists; list smoke tests pass; table skeleton shows header + 8–10 rows.</done>
</task>

<task type="auto">
  <name>Task 3: Implement report detail loading.tsx (per D-22)</name>
  <files>frontend/src/app/dashboard/reports/[reportId]/loading.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/reports/[reportId]/page.tsx
    - frontend/src/app/dashboard/analytics/loading.tsx
    - frontend/src/components/ui/skeleton.tsx
    - frontend/tests/dashboard-loading-detail.test.mjs
  </read_first>
  <action>
    Add `frontend/src/app/dashboard/reports/[reportId]/loading.tsx` as a Server Component. Mirror detail page `space-y-6 max-w-4xl mx-auto` wrapper and D-19 section grouping.

    Structure (per UI-SPEC detail skeleton contract):
    1. Back link skeleton (`h-4 w-40`).
    2. Header meta block: large title (`h-9 w-2/3 max-w-md`), id line, timestamp line, status badge placeholder on the right.
    3. Four-column meta grid skeleton (`grid gap-4 grid-cols-2 md:grid-cols-4` with bordered secondary fill) matching priority/severity/confidence/impact strip.
    4. Section block skeletons — each `rounded-lg border border-border p-6 space-y-3` with a heading Skeleton + 2–3 body lines:
       - Citizen report block
       - Evidence block (include taller image placeholder `min-h-48`)
       - AI analysis block (soft-accent optional: `bg-[#EFF6FF]` to match live page)
       - Urban context block
       - Status timeline block (2 timeline item Skeletons)
       - Officer decision / actions block (button-row Skeletons)

    Import `Skeleton` from `@/components/ui/skeleton` only. No fetch, no translations. Re-run full dashboard smoke suite.
  </action>
  <acceptance_criteria>
    - `frontend/src/app/dashboard/reports/[reportId]/loading.tsx` exists with default export
    - Source contains ≥5 `rounded-lg border border-border p-6` section-block skeleton regions
    - `cd frontend && node --test tests/dashboard-loading-detail.test.mjs` exits 0
    - `cd frontend && node --test tests/dashboard-table.test.mjs tests/dashboard-detail.test.mjs tests/dashboard-export.test.mjs` exits 0 (no regressions)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node --test tests/dashboard-loading-list.test.mjs tests/dashboard-loading-detail.test.mjs && node --test tests/dashboard-table.test.mjs tests/dashboard-detail.test.mjs tests/dashboard-export.test.mjs</automated>
  </verify>
  <done>Detail route loading file exists; all dashboard smoke tests pass; DASH-07 loading gap closed for list + detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer browser → Next.js RSC | Loading UI is static placeholder only; no user input or secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | loading.tsx | accept | Skeletons show no report data; placeholders only during fetch |
| T-03-05-02 | Tampering | npm installs | accept | No new packages; reuses existing shadcn Skeleton |
| T-03-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
After execution, re-run phase verification truth #11:
- `frontend/src/app/dashboard/loading.tsx` exists with Skeleton table header + 8–10 rows
- `frontend/src/app/dashboard/reports/[reportId]/loading.tsx` exists with header + section blocks
- `cd frontend && node --test tests/dashboard-loading-list.test.mjs tests/dashboard-loading-detail.test.mjs` passes
- Existing dashboard smoke suite still passes (table, detail, export)
</verification>

<success_criteria>
- DASH-07 loading requirement fully satisfied (list + detail skeletons; empty/error/export already done)
- Phase 3 verification score reaches 13/13 on loading truth
- No regressions in 03-02/03-03/03-04 behavior
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-polish/03-05-SUMMARY.md` when done
</output>
