---
phase: 03-dashboard-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260720_000002_dashboard_polish.sql
  - backend/requirements.txt
  - backend/app/security.py
  - backend/app/services/supabase.py
  - backend/app/api/reports.py
  - backend/tests/test_reports.py
  - backend/tests/test_export.py
  - backend/tests/test_security.py
autonomous: false
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
must_haves:
  truths:
    - "GET /api/v1/reports/recent accepts cursor+limit with opaque next_cursor and stable (sort_key, report_id) keyset ordering (DATA-04 / D-04)."
    - "GET /api/v1/reports/summary applies the same filter params as list and never returns global-only counts while filters are active (DATA-05 / D-08)."
    - "GET /api/v1/reports/export streams CSV or XLSX of the filtered set without buffering the full result in memory (DATA-06 / D-15 / D-16)."
    - "Status PATCH persists actor_id from JWT sub and returns 422 when resolved/rejected note is blank (DATA-07 / D-11 / D-13)."
  artifacts:
    - path: "supabase/migrations/20260720_000002_dashboard_polish.sql"
      provides: "reports.current_status denorm + status_events.actor_id + indexes/backfill"
    - path: "backend/app/security.py"
      provides: "OfficerPrincipal with actor_id=JWT sub"
    - path: "backend/app/api/reports.py"
      provides: "cursor list, filtered summary, streaming export, note gate"
    - path: "backend/tests/test_export.py"
      provides: "export auth + content-type coverage"
  key_links:
    - from: "backend/app/security.py"
      to: "backend/app/services/supabase.py"
      via: "update_status inserts actor_id from Principal; never body-supplied actor"
      pattern: "OfficerPrincipal|actor_id|payload\\[\"sub\"\\]"
    - from: "backend/app/api/reports.py"
      to: "backend/app/services/supabase.py"
      via: "keyset list_recent + filtered summary + iter_filtered export"
      pattern: "next_cursor|current_status|StreamingResponse|/export"
---

## Phase Goal

**As an** officer, **I want to** filter, paginate, export, and resolve reports with audited notes, **so that** I can operate the queue with evidence and accountability.

<objective>
Deliver Track A backend contract: additive schema (`current_status`, `actor_id`), keyset cursor list (DATA-04 / D-04), filtered summary (DATA-05 / D-08), streaming CSV/XLSX export (DATA-06 / D-15–D-18), and JWT-derived actor + note gate on status (DATA-07 / D-11 / D-13).

Purpose: Close RESEARCH Wave 0 gaps so Track B table UI and Track C resolve/detail can consume a correct, memory-safe officer API.
Output: Migration pushed to linked Supabase, Principal-aware security, sink keyset/export iterators, FastAPI routes registered with `/export` before `/{report_id}`, and pytest coverage.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
@/Users/minhmice/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dashboard-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-polish/03-RESEARCH.md
@.planning/phases/03-dashboard-polish/03-PATTERNS.md
@.planning/phases/03-dashboard-polish/03-UI-SPEC.md
@backend/app/api/reports.py
@backend/app/security.py
@backend/app/services/supabase.py
@backend/tests/test_reports.py
@supabase/migrations/20260720_000001_foundation.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve XlsxWriter==3.2.9 package identity</name>
  <files>.planning/phases/03-dashboard-polish/03-RESEARCH.md</files>
  <what-built>Phase 3 Package Legitimacy Audit flags XlsxWriter as SUS (unknown-downloads seam) with disposition Keep after checkpoint. No pip install of XlsxWriter may run until approval.</what-built>
  <action>Review XlsxWriter==3.2.9 on PyPI and the official repository jmcnamara/XlsxWriter against 03-RESEARCH.md Package Legitimacy Audit and constant_memory docs. Confirm preference over openpyxl for streaming workbook writes. Record approval before Task 2/3 install the pin.</action>
  <how-to-verify>
    1. Open https://pypi.org/project/XlsxWriter/ and confirm version 3.2.9.
    2. Confirm repository github.com/jmcnamara/XlsxWriter matches RESEARCH.
    3. Confirm RESEARCH rejects client-side SheetJS and openpyxl for this phase.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize XlsxWriter==3.2.9, or name the package that must be re-researched.</resume-signal>
  <verify>
    <automated>node /Users/minhmice/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.get .planning/phases/03-dashboard-polish/03-RESEARCH.md</automated>
  </verify>
  <done>Human approval recorded for XlsxWriter==3.2.9 before any Phase 3 pip install of that package.</done>
</task>

<task type="auto" tdd="true" gate="[BLOCKING]">
  <name>Task 2: [BLOCKING] Schema current_status + actor_id, Principal, note gate, and remote push</name>
  <files>supabase/migrations/20260720_000002_dashboard_polish.sql, backend/app/security.py, backend/app/services/supabase.py, backend/app/api/reports.py, backend/tests/test_reports.py, backend/tests/test_security.py</files>
  <behavior>
    - Migration adds reports.current_status NOT NULL DEFAULT new, backfills from latest status_events, adds status_events.actor_id TEXT, and indexes supporting status+created_at filter/sort (D-13 / DATA-07).
    - require_officer returns frozen OfficerPrincipal(token, actor_id=JWT sub, role); client-supplied actor_id is ignored (D-13).
    - update_status inserts actor_id and updates reports.current_status atomically; resolved/rejected with blank/whitespace note returns 422 (D-11 / DASH-05 server).
    - status_history select includes actor_id for timeline consumers (D-21).
  </behavior>
  <action>Write failing pytest for actor_id-from-sub and resolve-requires-note first. Add additive migration `supabase/migrations/20260720_000002_dashboard_polish.sql` per RESEARCH Runtime State Inventory (current_status denorm + actor_id + backfill + indexes) — do not rewrite Phase 1 RLS. Refactor require_officer to OfficerPrincipal with actor_id from payload sub (PATTERNS security.py analog; divergence: return Principal not raw JWT string). Update all Depends(require_officer) call sites and test overrides to Principal. Extend SupabaseReportSink.update_status to persist actor_id and set reports.current_status; enforce note gate in FastAPI before sink call (D-11 / D-13 / DATA-07). After SQL is written and local tests for this task pass, run the Phase 1 schema push path: require non-TTY SUPABASE_ACCESS_TOKEN + SUPABASE_PROJECT_REF + SUPABASE_DB_PASSWORD, then `supabase link --project-ref "$SUPABASE_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"` and `supabase db push --linked --password "$SUPABASE_DB_PASSWORD"`. Stop on push failure. Alternative if CLI auth unavailable: document and use MCP apply_migration with the same SQL — still [BLOCKING] before Task 3 verification against live schema. No Track B/C UI work in this plan.</action>
  <verify>
    <automated>cd backend &amp;&amp; pytest -q tests/test_reports.py::test_status_records_actor_id tests/test_reports.py::test_resolve_requires_note tests/test_security.py -x &amp;&amp; cd .. &amp;&amp; test -n "$SUPABASE_ACCESS_TOKEN" &amp;&amp; test -n "$SUPABASE_PROJECT_REF" &amp;&amp; test -n "$SUPABASE_DB_PASSWORD" &amp;&amp; SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase link --project-ref "$SUPABASE_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD" &amp;&amp; SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase db push --linked --password "$SUPABASE_DB_PASSWORD"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260720_000002_dashboard_polish.sql contains current_status and actor_id
    - backend/app/security.py defines OfficerPrincipal with actor_id
    - Status resolved/rejected without note returns HTTP 422
  </acceptance_criteria>
  <done>Remote schema includes current_status and actor_id; status events record JWT sub; note gate enforced server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keyset list, filtered summary, and streaming CSV/XLSX export</name>
  <files>backend/requirements.txt, backend/app/services/supabase.py, backend/app/api/reports.py, backend/tests/test_reports.py, backend/tests/test_export.py</files>
  <behavior>
    - /recent accepts cursor, limit (default 25, max 100), sort/order on created_at|priority|status|category, and filter params; returns items, count, next_cursor, sort, order — no OFFSET (DATA-04 / D-04).
    - /summary accepts the same filters and computes total_reports, critical_reports, avg_severity, top_category on the filtered set (DATA-05 / D-08).
    - /export?format=csv|xlsx streams filtered rows with columns: lean ops + severity + recommendation + latest status note; officer JWT required (DATA-06 / D-15 / D-16 / D-18).
    - Status filter uses SQL current_status — remove Python post-filter of full selects (RESEARCH Pitfall 1).
  </behavior>
  <action>After Task 1 approval, pin XlsxWriter==3.2.9 in backend/requirements.txt and install. Write failing tests for cursor pagination, filtered summary, and export auth/content-type. Implement sink keyset list_recent with opaque base64url cursor encoding `{sort}:{dir}:{value}:{report_id}` and report_id tie-breaker (D-04 / RESEARCH Pattern 1). Implement filtered summary using the same filter predicates. Add GET /export registered before `/{report_id}` (RESEARCH Pitfall 6): CSV via stdlib csv + StreamingResponse; XLSX via XlsxWriter constant_memory then stream file — never list(all_rows) in memory (D-16). Date-range params on list/summary/export. Soft cap ~10k rows documented in export handler comments if needed (RESEARCH A3). Keep enum allow-lists for status/category/priority. Do not build frontend UI here.</action>
  <verify>
    <automated>cd backend &amp;&amp; pytest -q tests/test_reports.py::test_list_cursor_pagination tests/test_reports.py::test_summary_respects_filters tests/test_export.py -x</automated>
  </verify>
  <acceptance_criteria>
    - reports.py registers /export before /{report_id} path
    - list response includes next_cursor field
    - summary endpoint accepts status query filter
    - export supports format=csv and format=xlsx
  </acceptance_criteria>
  <done>Officer list/summary/export APIs satisfy DATA-04/05/06 with SQL-side filters and streaming export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer client → FastAPI | JWT Bearer must be JWKS-validated; filters/export are authenticated only. |
| FastAPI → Supabase (caller JWT) | RLS applies; actor_id must come from token sub, never request body. |
| Export download | Large filtered datasets — memory and auth boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | status actor_id | high | mitigate | Derive actor_id only from JWT sub on Principal; ignore client actor fields (D-13 / DATA-07). |
| T-03-02 | Tampering | resolve without note | high | mitigate | FastAPI 422 when resolved/rejected note blank (D-11). |
| T-03-03 | Information Disclosure | /export | high | mitigate | Same require_officer + RLS path as list (D-18); no anon export. |
| T-03-04 | Denial of Service | unbounded export | medium | mitigate | Stream only; document soft row cap (~10k); auth-only. |
| T-03-05 | Tampering | filter injection | medium | mitigate | Enum allow-lists + parameterized PostgREST filters. |
| T-03-SC | Tampering | pip install XlsxWriter | high | mitigate | Blocking human checkpoint for SUS XlsxWriter==3.2.9 before install. |
</threat_model>

<verification>
Run `cd backend &amp;&amp; pytest -q tests/test_reports.py tests/test_export.py tests/test_security.py`. Confirm supabase db push succeeded for 20260720_000002_dashboard_polish.sql. Confirm /recent returns next_cursor, /summary changes under status= filter, and /export returns CSV/XLSX with officer auth.
</verification>

<success_criteria>
- DATA-04, DATA-05, DATA-06, and DATA-07 are demonstrably satisfied by tests and live schema.
- Schema push completed before treating list/export as verified against remote.
- Tracks B/C can consume cursor list, filtered summary, export, and actor-aware status without further backend contract work.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-polish/03-01-SUMMARY.md` when done.
</output>
