---
phase: 03-dashboard-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/popover.tsx
  - frontend/src/components/ui/collapsible.tsx
  - frontend/src/components/reports/ReportsTable.tsx
  - frontend/src/components/reports/ReportsFilters.tsx
  - frontend/src/components/reports/ReportsMetrics.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/dashboard-table.test.mjs
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-07
must_haves:
  truths:
    - "Officers see a compact TanStack/shadcn data table on /dashboard replacing Phase 2 cards, with lean default columns and severity hidden until toggled (D-01 / D-02 / D-05 / DASH-02)."
    - "Collapsible filter panel supports status, category, priority, severity range, date range, Clear filters, and URL searchParams sync; metrics strip uses the same filters (D-06 / D-07 / D-08 / D-09 / DASH-03)."
    - "List view shows loading, empty (filtered vs none), and error states with UI-SPEC EN/VI copy (D-22 / DASH-07)."
  artifacts:
    - path: "frontend/src/components/reports/ReportsTable.tsx"
      provides: "TanStack manualPagination/manualSorting table island"
    - path: "frontend/src/components/reports/ReportsFilters.tsx"
      provides: "Collapsible advanced filters + URL sync"
    - path: "frontend/src/components/reports/ReportsMetrics.tsx"
      provides: "Four filtered summary metrics strip"
  key_links:
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "GET /api/v1/reports/recent + /summary"
      via: "officerFetch with searchParams cursor/limit/filters/sort"
      pattern: "cursor|next_cursor|officerFetch|/summary"
---

## Phase Goal

**As an** officer, **I want to** filter, paginate, export, and resolve reports with audited notes, **so that** I can operate the queue with evidence and accountability.

<objective>
Deliver Track B dashboard list/filter/table UI (REALIGNED: not public Home SEO/a11y polish — ROADMAP Track B Home polish is out of Phase 3 scope). Replace Phase 2 cards with TanStack table + filter chrome + filtered metrics (DASH-02 / DASH-03 / DASH-07 list). Export button + BFF are Plan 03-04.

Purpose: Officers can operate the filtered queue using the Plan 03-01 API contract.
Output: ReportsTable/Filters/Metrics, dashboard page rewrite, EN/VI catalog keys, smoke tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-polish/03-RESEARCH.md
@.planning/phases/03-dashboard-polish/03-PATTERNS.md
@.planning/phases/03-dashboard-polish/03-UI-SPEC.md
@.planning/phases/03-dashboard-polish/03-01-PLAN.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/lib/backend.ts
@frontend/messages/en.json
@frontend/messages/vi.json
@frontend/tests/dashboard-shell.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install table stack and ship filterable cursor table on /dashboard</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/components/ui/table.tsx, frontend/src/components/ui/checkbox.tsx, frontend/src/components/ui/select.tsx, frontend/src/components/ui/popover.tsx, frontend/src/components/ui/collapsible.tsx, frontend/src/components/reports/ReportsTable.tsx, frontend/src/components/reports/ReportsFilters.tsx, frontend/src/components/reports/ReportsMetrics.tsx, frontend/src/app/dashboard/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/dashboard-table.test.mjs</files>
  <behavior>
    - /dashboard renders compact data table (not ReportCard grid) with default columns report_id, created_at, category, priority, status, truncated summary; severity hidden by default and toggleable; visibility persisted under localStorage key citymind.dashboard.columnVisibility (D-01 / D-02 / D-05 / DASH-02).
    - Row click / keyboard activate navigates to /dashboard/reports/[reportId]; no bulk-select (D-03 / D-10 list constraint).
    - Collapsible Filters panel above table: status, category, priority, severity range, date range, Clear filters; filters+sort+cursor sync to URL searchParams (D-06 / D-07 / D-09 / DASH-03).
    - Metrics strip above table shows filtered total_reports, critical_reports, avg_severity, top_category from /summary with same filters (D-08 / DATA-05 consumer) — four fields only, no status-count charts.
    - Loading skeletons, empty filtered/none copy, and destructive Alert error states per UI-SPEC (D-22 / DASH-07).
    - Server default limit=25; cursor Next uses next_cursor; no OFFSET page numbers (D-04 consumer).
  </behavior>
  <action>Write failing frontend/tests/dashboard-table.test.mjs asserting table/filter/metrics markers and absence of Phase 2 card-list-only chrome. Install @tanstack/react-table@8.21.3 (legitimacy OK — no SUS checkpoint). Add missing shadcn primitives via official CLI only: table, checkbox, select, popover, collapsible (dialog owned by Plan 03-03; export UI owned by Plan 03-04). Build ReportsTable with useReactTable manualPagination + manualSorting; initialState.columnVisibility.severity=false (D-01 / RESEARCH Pattern 2). Build ReportsFilters collapsible panel and ReportsMetrics strip. Rewrite dashboard/page.tsx as RSC reading searchParams, fetching /recent and /summary together via officerFetch, rendering the new components — remove ReportCard map (D-02). Extend en.json/vi.json with UI-SPEC Shared/list copy keys (identical trees). Clinic Blue product register; flat table (no ghost-card shadow). Do not implement ExportButton, export BFF, StatusActions/detail reorder, or public Home changes.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/dashboard-table.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json lists @tanstack/react-table
    - ReportsTable.tsx contains manualPagination and citymind.dashboard.columnVisibility
    - dashboard/page.tsx does not import ReportCard
    - en.json and vi.json contain Clear filters / Xóa bộ lọc equivalents under dashboard namespace
  </acceptance_criteria>
  <done>Officers can filter, sort, paginate, and toggle columns on /dashboard with filtered metrics and list empty/error states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC dashboard → FastAPI | Bearer via officerFetch; filters are untrusted query input validated server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | column visibility | low | accept | Client localStorage only (D-05) — no elevated data exposure beyond existing list fields. |
| T-03-08 | Tampering | URL filter params | medium | mitigate | Backend enum/range validation remains authoritative (Plan 03-01). |
</threat_model>

<verification>
Run `cd frontend &amp;&amp; node --test tests/dashboard-table.test.mjs &amp;&amp; npm run lint`. Manually smoke: login → /dashboard table replaces cards → apply status filter → metrics change → Next cursor.
</verification>

<success_criteria>
- DASH-02, DASH-03, and list DASH-07 satisfied.
- Phase 2 card list removed from /dashboard (D-02).
- No export UI (Plan 03-04); no public Home SEO/a11y work; no DASH-08 status-link copy.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-polish/03-02-SUMMARY.md` when done.
</output>
