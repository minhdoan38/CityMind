---
phase: 03-dashboard-polish
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/StatusActions.tsx
  - frontend/src/app/dashboard/reports/[reportId]/page.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/dashboard-detail.test.mjs
autonomous: true
requirements:
  - DASH-04
  - DASH-05
  - DASH-07
must_haves:
  truths:
    - "Detail page section order is header meta → citizen description → evidence → AI advisory → urban context → status timeline → resolve actions (D-19 / DASH-04)."
    - "AI block is labeled advisory with disclaimer; never styled as final authority (D-20)."
    - "Status timeline is newest-first with status, note, actor (truncated actor_id or Officer), and timestamp (D-21)."
    - "reviewing is immediate; resolved/rejected open Dialog with required note; cancel makes no change; server 422 surfaces validation (D-10–D-14 / DASH-05)."
    - "Detail loading/empty/error and resolve error states use UI-SPEC EN/VI copy (D-22 / DASH-07)."
  artifacts:
    - path: "frontend/src/app/dashboard/reports/[reportId]/page.tsx"
      provides: "D-19 section hierarchy + advisory AI + timeline actor"
    - path: "frontend/src/components/StatusActions.tsx"
      provides: "Dialog note-gated resolve/reject; reviewing immediate"
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive for confirm UI"
  key_links:
    - from: "frontend/src/components/StatusActions.tsx"
      to: "frontend/src/app/api/officer/reports/[reportId]/status/route.ts"
      via: "PATCH with status + note query params; router.refresh on success"
      pattern: "searchParams.set\\(\"note\"|Confirm resolve|Keep editing"
    - from: "frontend/src/app/dashboard/reports/[reportId]/page.tsx"
      to: "status_events.actor_id"
      via: "history fetch displays actor from Plan 03-01 API"
      pattern: "actor_id|Status history|AI analysis \\(advisory\\)"
---

## Phase Goal

**As an** officer, **I want to** filter, paginate, export, and resolve reports with audited notes, **so that** I can operate the queue with evidence and accountability.

<objective>
Deliver Track C detail polish and note-gated resolve workflow: reorder detail sections (DASH-04 / D-19–D-21), replace note-less StatusActions with Dialog confirm (DASH-05 / D-10–D-14), and ship detail loading/empty/error states (DASH-07 / D-22).

Purpose: Officers decide with full evidence context and an auditable note trail; AI remains advisory.
Output: Reordered detail page, Dialog-based StatusActions, EN/VI resolve/detail catalog keys, smoke tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-polish/03-RESEARCH.md
@.planning/phases/03-dashboard-polish/03-PATTERNS.md
@.planning/phases/03-dashboard-polish/03-UI-SPEC.md
@.planning/phases/03-dashboard-polish/03-01-PLAN.md
@.planning/phases/03-dashboard-polish/03-02-PLAN.md
@frontend/src/app/dashboard/reports/[reportId]/page.tsx
@frontend/src/components/StatusActions.tsx
@frontend/src/app/api/officer/reports/[reportId]/status/route.ts
@frontend/messages/en.json
@frontend/messages/vi.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder detail hierarchy with advisory AI and actor timeline</name>
  <files>frontend/src/app/dashboard/reports/[reportId]/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/dashboard-detail.test.mjs</files>
  <behavior>
    - Section order locked: header meta (id, status, timestamps, category/priority/severity) → citizen description → evidence (image + signals) → AI analysis (advisory) → urban context → status timeline → StatusActions (D-19 / DASH-04).
    - AI section title and disclaimer match UI-SPEC advisory language; soft-accent panel — not primary authority chrome (D-20).
    - Timeline newest-first; each event shows status, note, actor (truncated actor_id or Officer label), timestamp (D-21).
    - Not-found, load error, and empty timeline copy from UI-SPEC EN/VI (D-22 / DASH-07).
  </behavior>
  <action>Write failing frontend/tests/dashboard-detail.test.mjs asserting D-19 section heading order markers and advisory disclaimer presence. Reshuffle dashboard/reports/[reportId]/page.tsx sections strictly per D-19 — reuse existing fetch helpers; do not invent parallel APIs. Relabel AI block per UI-SPEC Detail copy; keep advisory Alert. Extend history event rendering to show actor_id from Plan 03-01 status_history payload (truncated UUID or Officer). Add identical EN/VI keys for detail sections. No DASH-08 citizen status-link copy. No table/filter work (owned by 03-02).</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/dashboard-detail.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - detail page source contains AI analysis (advisory) or matching message key usage
    - timeline rendering references actor_id or Officer label
    - section order places StatusActions after timeline in JSX
  </acceptance_criteria>
  <done>Detail page presents full evidence hierarchy with advisory AI and actor-aware timeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dialog note-gated resolve/reject on StatusActions</name>
  <files>frontend/src/components/ui/dialog.tsx, frontend/src/components/StatusActions.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/dashboard-detail.test.mjs</files>
  <behavior>
    - Status transitions only from detail StatusActions — no table-row resolve (D-10 / D-14).
    - Mark as reviewing PATCHes immediately without dialog; note optional (D-11).
    - Mark as resolved / rejected opens shadcn Dialog with required Decision note textarea; Keep editing dismisses with no PATCH (D-12).
    - Confirm sends note.trim(); client blocks empty note; 422 surfaces UI-SPEC validation; success router.refresh() (D-11 / DASH-05).
    - Reject confirm uses destructive styling; actor never collected in UI (D-13 client constraint).
  </behavior>
  <action>Add shadcn dialog if missing from Plan 03-02. Replace one-click StatusActions map with reviewing immediate + Dialog gate for resolved/rejected (D-10–D-14 / UI-SPEC Resolve copy). Analog: PATTERNS StatusActions fetch + status BFF note query param (already forwards note); divergence: Dialog + required note instead of one-click. Use next-intl catalogs for all new strings (migrate hardcoded EN). Extend dashboard-detail tests for Dialog note gate markers and reviewing-without-dialog. Do not add bulk actions or Phase 7 triage UX.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/dashboard-detail.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - StatusActions.tsx imports Dialog components
    - resolved/rejected path sets note on the PATCH URL
    - reviewing path does not require Dialog open for PATCH
    - en.json/vi.json contain Confirm resolve / Xác nhận đã xử lý keys
  </acceptance_criteria>
  <done>Officers can review immediately and resolve/reject only with a required note via Dialog; cancel is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer UI → status BFF → FastAPI | Note and status are client-controlled; server note gate + JWT actor remain authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | StatusActions note | high | mitigate | Client validation + server 422 from Plan 03-01; never trust UI alone (D-11). |
| T-03-10 | Spoofing | actor display | medium | mitigate | Display API actor_id only; do not accept actor from form fields (D-13). |
| T-03-11 | Repudiation | resolve without audit note | high | mitigate | Dialog + required note for resolved/rejected (D-12 / DASH-05). |
</threat_model>

<verification>
Run `cd frontend &amp;&amp; node --test tests/dashboard-detail.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build`. Smoke: open detail → confirm section order → Mark reviewing updates without dialog → Resolve opens Dialog → empty note blocked → confirm with note refreshes timeline with actor → Reject Keep editing leaves status unchanged.
</verification>

<success_criteria>
- DASH-04, DASH-05, and detail DASH-07 satisfied.
- D-10–D-14 and D-19–D-22 implemented; DASH-08 and Phase 7 triage absent.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-polish/03-03-SUMMARY.md` when done.
</output>
