---
phase: 05-analytics-pipeline
plan: "02"
type: execute
wave: 4
depends_on:
  - "05-04"
  - "05-03"
files_modified:
  - backend/app/api/analytics.py
  - backend/app/services/analytics.py
  - backend/app/schemas.py
  - backend/app/security.py
  - backend/app/config.py
  - backend/app/main.py
  - backend/.env.example
  - backend/tests/test_public_stats.py
  - frontend/src/app/api/public/stats/route.ts
  - frontend/src/components/analytics/PublicStatsStrip.tsx
  - frontend/src/app/[locale]/page.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/public-stats.test.mjs
autonomous: true
requirements:
  - ANLY-02
must_haves:
  truths:
    - "Public Home shows a thin Community snapshot strip with total reports (30d) and top 1–2 categories only — no lat/lng, descriptions, evidence, tokens, or notes (D-11 / D-16)."
    - "Categories with count under 3 are omitted server-side before response (k≥3) (D-17)."
    - "If public stats API/warehouse fails, Home still renders and the strip is hidden or shows unavailable copy — never blocks the page (D-12)."
    - "Public read path is rate-limited aggregate API/BFF only — no BigQuery credentials in the browser (D-13)."
  artifacts:
    - path: "backend/app/api/analytics.py"
      provides: "GET /api/v1/public/stats (or equivalent public mount) with k-anonymity"
    - path: "frontend/src/components/analytics/PublicStatsStrip.tsx"
      provides: "Optional Home stats strip"
    - path: "frontend/src/app/api/public/stats/route.ts"
      provides: "BFF proxy with XFF forward"
    - path: "backend/tests/test_public_stats.py"
      provides: "k-anonymity + rate limit coverage"
  key_links:
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "frontend/src/components/analytics/PublicStatsStrip.tsx"
      via: "SSR/section insert after Instructions / before Contact; hide on failure"
      pattern: "PublicStatsStrip|Community snapshot|public.stats"
    - from: "frontend/src/app/api/public/stats/route.ts"
      to: "GET /api/v1/public/stats"
      via: "backendEndpoint + X-Forwarded-For passthrough"
      pattern: "public/stats|X-Forwarded-For"
    - from: "backend/app/security.py"
      to: "public stats route"
      via: "public_stats_limiter key stats:{ip}"
      pattern: "public_stats_limiter|stats:"
---

## Phase Goal

**As a** resident, **I want to** see non-sensitive community totals on the Home page, **so that** I understand recent reporting activity without exposing personal or officer data.

<objective>
Deliver Track B thin public stats vertical slice: rate-limited aggregate API with k≥3, Next BFF, and bilingual Home strip that degrades closed (D-11 / D-12 / D-13 / D-17).

Purpose: Optional civic transparency on locale Home without warehouse credentials or small-cell leakage.
Output: public stats endpoint + limiter, BFF route, PublicStatsStrip, EN/VI public.stats catalogs, pytest + smoke tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-analytics-pipeline/05-CONTEXT.md
@.planning/phases/05-analytics-pipeline/05-RESEARCH.md
@.planning/phases/05-analytics-pipeline/05-PATTERNS.md
@.planning/phases/05-analytics-pipeline/05-UI-SPEC.md
@.planning/phases/05-analytics-pipeline/05-VALIDATION.md
@.planning/phases/05-analytics-pipeline/05-04-PLAN.md
@.planning/phases/05-analytics-pipeline/05-03-PLAN.md
@frontend/src/app/[locale]/page.tsx
@frontend/src/app/api/public/reports/status/route.ts
@backend/app/security.py
@backend/app/api/analytics.py
@backend/tests/test_citizen_status.py
@frontend/tests/dashboard-shell.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for public stats k-anonymity and rate limit</name>
  <files>backend/tests/test_public_stats.py, frontend/tests/public-stats.test.mjs</files>
  <behavior>
    - Categories with count under 3 omitted (or not returned as named small cells); top categories capped at 1–2; total_last_30d present (D-11 / D-17).
    - Over public_stats_rate_limit_per_minute → 429 with Retry-After; limiter key uses stats: prefix separate from analyze/status (D-13 / Phase 4 pattern).
    - Frontend smoke documents degrade/hide contract when stats fetch fails (D-12).
  </behavior>
  <action>
Create Wave 0-style failing stubs for Track B per 05-VALIDATION. Mirror test_citizen_status limiter fixtures and status BFF patterns. Do not implement handlers yet — suite must be red. Cite D-11, D-12, D-13, D-17. No chart/recharts work in this plan.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_public_stats.py -q --tb=no 2>&1 | tee /tmp/05-02-red.txt; grep -E "failed|ERROR" /tmp/05-02-red.txt; cd ../frontend && node --test tests/public-stats.test.mjs 2>&1 | tee /tmp/05-02-fe-red.txt</automated>
  </verify>
  <done>test_public_stats.py and public-stats.test.mjs exist; backend public-stats cases are red before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public aggregate API + BFF with k≥3 and dedicated rate limiter</name>
  <files>backend/app/api/analytics.py, backend/app/services/analytics.py, backend/app/schemas.py, backend/app/security.py, backend/app/config.py, backend/app/main.py, backend/.env.example, frontend/src/app/api/public/stats/route.ts, backend/tests/test_public_stats.py</files>
  <behavior>
    - GET /api/v1/public/stats returns { total_last_30d, top_categories: [{category, count}] } from warehouse aggregates (ANLY-02 read for public window) (D-11).
    - Server-side filter: drop category cells with count under 3 before response (D-17); prefer omit over inventing identifiable tiny labels.
    - public_stats_limiter SlidingWindowLimiter with key stats:{client_ip}; Depends enforce; 429 Retry-After 60 (D-13).
    - Next BFF GET /api/public/stats proxies to FastAPI, forwards X-Forwarded-For / X-Real-Ip, propagates Retry-After (PATTERNS status BFF analog).
  </behavior>
  <action>
Implement public read path per RESEARCH Pattern 4 and 05-PATTERNS (status_limiter + public status BFF). Extends analytics router/service from 05-04.

1. Add public_stats_rate_limit_per_minute to Settings + .env.example; public_stats_limiter + enforce_public_stats_rate_limit in security.py (separate from report_limiter and status_limiter).
2. Extend analytics service/API with unauthenticated GET public stats (mount under /api/v1/public or analytics router without require_officer). Query last-30d aggregates from BQ views/tables — count-only / category-only (D-11). Enforce k=3 in Python or SQL before return (D-17) — never UI-only.
3. Generic errors on failure (no warehouse internals). Wire main.py if new prefix needed.
4. frontend/src/app/api/public/stats/route.ts — GET proxy like reports/status BFF.
5. Green test_public_stats.py. Do not put BQ credentials in frontend (D-13).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_public_stats.py -q</automated>
  </verify>
  <done>Public stats API enforces k≥3 and dedicated rate limit; BFF proxies safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Home PublicStatsStrip SSR with EN/VI and graceful degrade</name>
  <files>frontend/src/components/analytics/PublicStatsStrip.tsx, frontend/src/app/[locale]/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/public-stats.test.mjs</files>
  <behavior>
    - Strip placed after Instructions / before Contact (Phase 2 order / UI-SPEC) (D-11).
    - Shows total + common issues from public.stats catalogs EN/VI; metric values Heading weight — not hero Display theater (UI-SPEC).
    - On fetch/API failure: render nothing (preferred) or muted unavailable line — Home must still render (D-12).
    - Optional short revalidate/cache (~300–3600s) on SSR fetch (RESEARCH discretion).
  </behavior>
  <action>
Ship Track B Home UI per 05-UI-SPEC public copy table and 05-PATTERNS Home section cadence. Runs after 05-03 so dashboard.analytics message keys already exist — merge public.stats carefully.

1. PublicStatsStrip — informational only; no Submit/dashboard CTAs; no lat/lng or tokens (D-11 / D-16).
2. Insert into [locale]/page.tsx after #instructions / before Contact cadence.
3. Add public.stats keys EN+VI (Community snapshot, Reports 30 days, Common issues, unavailable). Avoid conflicting with dashboard.analytics keys from 05-03 — merge carefully into same message files.
4. SSR fetch BFF/public API with try/catch; hide section on error (D-12).
5. Green public-stats.test.mjs degrade/hide contract.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_public_stats.py -q; cd ../frontend && node --test tests/public-stats.test.mjs</automated>
    <human-check>Visit /en and /vi Home — strip shows or hides cleanly; with API down, Home still loads.</human-check>
  </verify>
  <done>Thin public Home stats strip ships with k-anonymity upstream and graceful degrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser → public stats API/BFF | Unauthenticated; must return aggregates only. |
| Public API → BigQuery | Server-side queries only; browser never holds BQ credentials. |
| Scraper → category cells | Small counts enable re-identification if returned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Information disclosure | GET /api/v1/public/stats | high | mitigate | Count/category-only payload; omit cells with count under 3 server-side (D-11 / D-17); no lat/lng, notes, tokens, evidence |
| T-05-13 | Information disclosure | Browser | high | mitigate | BFF/SSR calls FastAPI only — never expose BigQuery SA or service role to client (D-13) |
| T-05-14 | Denial of service | Public stats endpoint | medium | mitigate | Dedicated public_stats_limiter stats:{ip} + 429 Retry-After (separate from analyze/status) |
| T-05-15 | Information disclosure | Home strip failure path | medium | mitigate | Catch errors and hide/unavailable — do not leak stack traces or warehouse errors to citizens (D-12) |
| T-05-16 | Elevation of privilege | Public route | low | accept | Intentionally unauthenticated aggregate read; mitigated by k-anonymity + rate limit + field allowlist |
| T-05-SC | Tampering | Package installs | low | accept | No new npm/pip packages in this plan |
</threat_model>

<verification>
- pytest tests/test_public_stats.py green (k-anonymity + rate limit)
- node --test frontend/tests/public-stats.test.mjs green
- Home EN/VI strip degrades without blocking render
</verification>

<success_criteria>
- D-11..D-13, D-17 satisfied for Track B
- Public path uses warehouse aggregates (ANLY-02) without officer JWT
- Optional Home stats do not regress Home availability (D-12)
</success_criteria>

<output>
Create `.planning/phases/05-analytics-pipeline/05-02-SUMMARY.md` when done
</output>
