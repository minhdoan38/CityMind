---
phase: 05-analytics-pipeline
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ui/chart.tsx
  - frontend/src/app/dashboard/analytics/page.tsx
  - frontend/src/components/analytics/DateRangeToolbar.tsx
  - frontend/src/components/analytics/VolumeChart.tsx
  - frontend/src/components/analytics/CategoryChart.tsx
  - frontend/src/components/analytics/SlaChart.tsx
  - frontend/src/components/analytics/HotspotTable.tsx
  - frontend/src/components/DashboardSidebar.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/analytics-shell.test.mjs
autonomous: false
requirements:
  - ANLY-03
must_haves:
  truths:
    - "Officer opens /dashboard/analytics behind existing dashboard auth and sees Volume, Category, SLA charts plus ranked hotspot list for the selected range (ANLY-03 / D-05 / D-06 / D-07)."
    - "Date presets Last 7/30/90 + custom from→to persist in URL searchParams (range, from, to); default range=30 (D-08 / D-09)."
    - "Empty warehouse and empty range show calm empty states without painting misleading zero series; loading skeletons and error Alert+Retry exist (D-10)."
    - "Charts use shadcn chart + recharts with Clinic Blue single accent series; no Chart.js; no maps (D-14 / D-15 / 05-UI-SPEC)."
  artifacts:
    - path: "frontend/src/app/dashboard/analytics/page.tsx"
      provides: "Officer Analytics tab route"
    - path: "frontend/src/components/ui/chart.tsx"
      provides: "shadcn chart primitives wrapping recharts"
    - path: "frontend/src/components/analytics/DateRangeToolbar.tsx"
      provides: "Preset + custom URL-synced range controls"
    - path: "frontend/tests/analytics-shell.test.mjs"
      provides: "Nav + URL key smoke coverage"
  key_links:
    - from: "frontend/src/app/dashboard/analytics/page.tsx"
      to: "backend GET /api/v1/analytics"
      via: "officerFetch with from/to query string"
      pattern: "officerFetch|/api/v1/analytics"
    - from: "frontend/src/components/analytics/DateRangeToolbar.tsx"
      to: "URL searchParams"
      via: "range|from|to keys; invalid from>to blocks fetch"
      pattern: "range=|searchParams"
    - from: "frontend/src/components/DashboardSidebar.tsx"
      to: "/dashboard/analytics"
      via: "Analytics nav item after Reports"
      pattern: "analytics|/dashboard/analytics"
---

## Phase Goal

**As an** officer, **I want to** open an Analytics tab with date-filtered warehouse charts, **so that** I can see volume, category mix, SLA, and category hotspots without leaving the dashboard.

<objective>
Deliver Track C: blocking recharts legitimacy checkpoint, shadcn `chart` install, `/dashboard/analytics` UI with three chart blocks + hotspot list + URL date chrome (ANLY-03 / D-05..D-10 / D-14 / D-15).

Purpose: Complete the officer-facing success criterion for Phase 5 using the Track A analytics API.
Output: chart.tsx, analytics components/page, sidebar nav, EN/VI dashboard.analytics catalogs, smoke tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-analytics-pipeline/05-CONTEXT.md
@.planning/phases/05-analytics-pipeline/05-RESEARCH.md
@.planning/phases/05-analytics-pipeline/05-PATTERNS.md
@.planning/phases/05-analytics-pipeline/05-UI-SPEC.md
@.planning/phases/05-analytics-pipeline/05-VALIDATION.md
@.planning/phases/05-analytics-pipeline/05-01-PLAN.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/app/dashboard/layout.tsx
@frontend/src/components/DashboardSidebar.tsx
@frontend/src/lib/backend.ts
@frontend/components.json
@frontend/tests/dashboard-shell.test.mjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify recharts package legitimacy (SUS)</name>
  <files>.planning/phases/05-analytics-pipeline/05-RESEARCH.md</files>
  <what-built>Research flagged recharts as [SUS] solely due to a recent npm publish age seam, while locking charts to official shadcn `chart` → recharts (05-RESEARCH Package Legitimacy Audit; 05-UI-SPEC). No install has run yet.</what-built>
  <action>Review recharts on npmjs.com and the official repository recharts/recharts against 05-RESEARCH.md Package Legitimacy Audit and https://ui.shadcn.com/docs/components/chart. Confirm Chart.js is rejected. Authorize `npx shadcn@latest add @shadcn/chart` (or `add chart` from frontend/) before Task 2 installs. Do not install until resume-signal.</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/recharts and confirm the package is the established Recharts project (github.com/recharts/recharts), not a typosquat.
    2. Confirm shadcn docs still list Recharts for the Chart component: https://ui.shadcn.com/docs/components/chart
    3. Confirm intended install path is `npx shadcn@latest add @shadcn/chart` (or `npx shadcn@latest add chart` from frontend/) — not Chart.js or an alternate registry.
    4. Reply **approved** to proceed with install, or describe blockers.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize shadcn chart / recharts install, or identify the package that must be re-researched.</resume-signal>
  <verify>
    <automated>node /Users/minhmice/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.get .planning/phases/05-analytics-pipeline/05-RESEARCH.md</automated>
  </verify>
  <done>Human approval recorded for recharts via official shadcn chart before any Phase 5 npm/shadcn chart install.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install shadcn chart + Analytics shell with URL date range and API fetch</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/components/ui/chart.tsx, frontend/src/app/dashboard/analytics/page.tsx, frontend/src/components/analytics/DateRangeToolbar.tsx, frontend/src/components/DashboardSidebar.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/analytics-shell.test.mjs</files>
  <behavior>
    - After approval: chart.tsx exists via official shadcn add; recharts present as dependency (D-05 UI stack / UI-SPEC).
    - Sidebar shows Analytics after Reports linking to /dashboard/analytics (D-07).
    - Default URL range=30; presets 7/30/90/custom write searchParams; invalid from&gt;to shows inline error and does not fetch (D-08 / D-09).
    - Page calls officerFetch `/api/v1/analytics?from=&amp;to=` (depends on 05-01).
  </behavior>
  <action>
Only after Task 1 approval:

1. From frontend/: run `npx shadcn@latest add @shadcn/chart` (or `add chart` per components.json). Install react-is peer only if required. Do not install Chart.js or nuqs (D-05 / RESEARCH).
2. Add frontend/tests/analytics-shell.test.mjs smoke asserting Analytics nav string/path and documenting URL keys range|from|to (VALIDATION).
3. DateRangeToolbar — presets + custom date inputs + Apply; native searchParams / router.replace; default Last 30 days (D-08 / D-09). Clinic Blue active preset per UI-SPEC.
4. dashboard/analytics/page.tsx — inherit layout requireOfficerSession; page header + freshness note copy from UI-SPEC; fetch via officerFetch; Skeleton loading; Alert error + Retry (D-07 / D-10).
5. DashboardSidebar — Analytics item (BarChart3 or similar lucide) after Reports; active accent (UI-SPEC).
6. Add dashboard.analytics EN/VI keys from 05-UI-SPEC copy table (no hardcoded English in new chrome). Do not touch public.stats keys (Track B).

Thin slice: chart plot components may be placeholders until Task 3, but fetch + toolbar + empty/error slots must work.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/analytics-shell.test.mjs</automated>
    <human-check>Confirm chart.tsx exists and package.json lists recharts after shadcn add.</human-check>
  </verify>
  <done>Analytics route + sidebar + URL date chrome + officerFetch wiring exist; chart primitive installed post-approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Three chart blocks + hotspot list with empty/loading/error states</name>
  <files>frontend/src/components/analytics/VolumeChart.tsx, frontend/src/components/analytics/CategoryChart.tsx, frontend/src/components/analytics/SlaChart.tsx, frontend/src/components/analytics/HotspotTable.tsx, frontend/src/app/dashboard/analytics/page.tsx, frontend/tests/analytics-shell.test.mjs</files>
  <behavior>
    - Volume (line/bar), Category (horizontal bar preferred), SLA (distribution or median summary) render from API payload (D-05).
    - HotspotTable ranked category concentrations top N — not a map (D-06 / D-14 / D-15).
    - Empty warehouse / empty range: UI-SPEC empty copy; do not draw flat zero series when API signals empty (D-10).
    - Text summary/caption per chart for a11y; prefers-reduced-motion: no entrance animations; Clinic Blue single accent series + neutral category ramp (UI-SPEC).
  </behavior>
  <action>
Complete Track C charts per 05-UI-SPEC component inventory and RESEARCH shadcn/recharts import shape.

1. VolumeChart, CategoryChart, SlaChart using ChartContainer / ChartTooltip from ui/chart + recharts primitives.
2. HotspotTable — rank, label, count; flat border; Clinic Blue rank numerals; no map widgets (D-06 / D-14).
3. Layout: 1-col mobile; Volume+Category 2-col ≥lg; SLA full width; hotspot bottom (UI-SPEC).
4. Wire empty/error/loading slots; captions from message catalogs; freshness note “Data through previous UTC day after the daily sync.”
5. Extend analytics-shell smoke if helpful; no public Home work (05-02).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/analytics-shell.test.mjs</automated>
    <human-check>Login as officer → /dashboard/analytics → Last 30 days → confirm three charts + hotspot list, URL share persistence, empty-state copy when warehouse empty.</human-check>
  </verify>
  <done>ANLY-03 UI complete: three charts + hotspot list + date selector per D-05..D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next dashboard → FastAPI analytics | Officer session cookie → Bearer JWT; date params untrusted. |
| Browser package install | recharts via shadcn must be the legitimate package. |
| Browser → BigQuery | Must never occur; charts consume JSON from FastAPI only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Elevation of privilege | /dashboard/analytics | high | mitigate | Route under existing dashboard layout requireOfficerSession + proxy matcher; data via officerFetch only (D-07) |
| T-05-09 | Information disclosure | Chart payloads | high | mitigate | Render only API aggregates; never request evidence URIs or tokens in UI (D-16 / D-18) |
| T-05-10 | Information disclosure | Client env | high | mitigate | No BigQuery SA keys or NEXT_PUBLIC warehouse credentials (D-13) |
| T-05-11 | Tampering | Date searchParams | medium | mitigate | Client blocks from&gt;to before fetch; server still validates 422 (defense in depth) |
| T-05-SC | Tampering | recharts / shadcn chart install | high | mitigate | Blocking human-verify on npmjs + official shadcn chart docs before `npx shadcn add chart` (RESEARCH SUS) |
</threat_model>

<verification>
- node --test frontend/tests/analytics-shell.test.mjs green
- chart.tsx present; recharts in package.json after approved install
- Manual: /dashboard/analytics date presets + three charts + hotspot list
</verification>

<success_criteria>
- ANLY-03 dashboard Analytics tab with date range selector
- D-05..D-10, D-14, D-15 honored in UI
- recharts installed only after SUS human-verify
</success_criteria>

<output>
Create `.planning/phases/05-analytics-pipeline/05-03-SUMMARY.md` when done
</output>
