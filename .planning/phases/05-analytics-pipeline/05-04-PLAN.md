---
phase: 05-analytics-pipeline
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/app/api/analytics.py
  - backend/app/services/analytics.py
  - backend/app/schemas.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/.env.example
  - backend/tests/test_analytics_api.py
autonomous: true
requirements:
  - ANLY-03
must_haves:
  truths:
    - "GET /api/v1/analytics requires officer JWT, validates from/to, and returns chart-ready aggregates via parameterized BigQuery queries — no browser BQ credentials (ANLY-03 / D-07 / D-13 / D-18)."
    - "Response includes volume series, category mix, SLA summary/histogram, and hotspot list for the selected range (D-05 / D-06)."
    - "When warehouse empty / no points, response signals empty so UI does not invent zero series (D-10)."
  artifacts:
    - path: "backend/app/api/analytics.py"
      provides: "Officer GET /api/v1/analytics with require_officer"
    - path: "backend/app/services/analytics.py"
      provides: "Parameterized queries against analytics views"
    - path: "backend/tests/test_analytics_api.py"
      provides: "Auth 401 + invalid range 422 coverage"
  key_links:
    - from: "backend/app/api/analytics.py"
      to: "backend/app/services/analytics.py"
      via: "require_officer → view queries with ScalarQueryParameter from/to"
      pattern: "require_officer|ScalarQueryParameter|v_volume_daily"
    - from: "backend/app/services/analytics.py"
      to: "infra/bigquery/analytics_views.sql"
      via: "Reads v_volume_daily, v_category_mix, v_sla_closed, v_hotspot_category"
      pattern: "v_volume_daily|v_category_mix|v_sla_closed|v_hotspot"
---

## Phase Goal

**As an** officer, **I want to** query BigQuery-backed volume, category, SLA, and hotspot aggregates for a date range via authenticated API, **so that** the Analytics tab can show warehouse trends without using BigQuery for ops CRUD.

<objective>
Deliver Track A API seam: Wave 0 Nyquist stubs + officer GET /api/v1/analytics with schemas, service, and router wiring (ANLY-03). Depends on warehouse DDL/views/ETL from 05-01.

Purpose: Authenticated, parameterized read path so Track C (05-03) and Track B (05-02) can consume aggregates without browser BQ credentials.
Output: FastAPI `/api/v1/analytics`, Pydantic DTOs, analytics service, pytest coverage.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-analytics-pipeline/05-CONTEXT.md
@.planning/phases/05-analytics-pipeline/05-RESEARCH.md
@.planning/phases/05-analytics-pipeline/05-PATTERNS.md
@.planning/phases/05-analytics-pipeline/05-VALIDATION.md
@.planning/phases/05-analytics-pipeline/05-01-PLAN.md
@backend/app/security.py
@backend/app/api/reports.py
@backend/app/main.py
@backend/app/schemas.py
@infra/bigquery/analytics_views.sql
@backend/tests/test_citizen_status.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 failing stubs for officer analytics API</name>
  <files>backend/tests/test_analytics_api.py</files>
  <behavior>
    - GET /api/v1/analytics without officer auth → 401 (ANLY-03 / D-07).
    - from after to → 422 (ANLY-03).
  </behavior>
  <action>
Create Wave 0 Nyquist stub from 05-VALIDATION.md before production code. Mirror TestClient + monkeypatch patterns from test_citizen_status.py (05-PATTERNS).

1. test_analytics_api.py — TestClient cases for require_officer 401 and invalid range 422 (wire lands in Task 2).
2. Do not implement the officer router yet — suite must be red. Cite D-07, D-13, D-18. No dashboard UI (05-03). No public /stats (05-02).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_analytics_api.py -q --tb=no 2>&1 | tee /tmp/05-04-w0-red.txt; grep -E "failed|ERROR" /tmp/05-04-w0-red.txt</automated>
  </verify>
  <done>Wave 0 test_analytics_api.py exists; suite is red before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Officer GET /api/v1/analytics read API</name>
  <files>backend/app/api/analytics.py, backend/app/services/analytics.py, backend/app/schemas.py, backend/app/main.py, backend/app/config.py, backend/.env.example, backend/tests/test_analytics_api.py</files>
  <behavior>
    - GET /api/v1/analytics?from=&to= with Depends(require_officer) returns volume series, category mix, SLA summary/histogram, hotspot list for closed-in-range semantics (D-05 / D-06 / D-07 / D-18).
    - from > to → 422; optional max span clamp ≤366 days; missing auth → 401 (ANLY-03).
    - All BQ queries use ScalarQueryParameter — never string-interpolate dates (SQLi mitigation).
    - When warehouse empty / no points, response signals empty so UI does not invent zero series (D-10) — API may set empty:true or zero totals with empty arrays.
  </behavior>
  <action>
Wire officer analytics read path per 05-PATTERNS (reports.py /summary + require_officer) and RESEARCH Pattern 4. Views/DDL must already exist from 05-01.

1. Pydantic response DTOs in schemas.py for volume/category/sla/hotspots — no evidence URIs, tokens, notes (D-16 / D-18).
2. services/analytics.py queries the four views with parameterized from/to (UTC inclusive dates).
3. api/analytics.py GET handler; include_router in main.py under prefix /api/v1/analytics.
4. Document any ENABLE_BIGQUERY / dataset settings already used in config.py + .env.example; do not put BQ keys in frontend env (D-13).
5. Green test_analytics_api.py auth + range cases; mock analytics service if live BQ unavailable in CI.
6. Do not implement public /stats here (Track B / 05-02). Do not add shadcn chart or dashboard UI (Track C / 05-03).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_analytics_api.py -q</automated>
  </verify>
  <done>Officer analytics API authenticated and range-validated; ANLY-03 API seam ready for Track C UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer client → FastAPI GET /api/v1/analytics | Untrusted query params (from/to); JWT required. |
| FastAPI → BigQuery | Server-side only; credentials must never reach the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Elevation of privilege | GET /api/v1/analytics | high | mitigate | Depends(require_officer) — reuse Supabase JWT officer/admin gate (D-07) |
| T-05-03 | Information disclosure | Browser / Next env | high | mitigate | BQ queries only in FastAPI; no NEXT_PUBLIC BigQuery keys (D-13) |
| T-05-04 | Tampering | Analytics date filters | high | mitigate | bigquery.ScalarQueryParameter for from/to; Pydantic/HTTP 422 on invalid range |
| T-05-07 | Information disclosure | Officer analytics DTO | medium | mitigate | Finer than public but still no evidence/token material (D-18) |
| T-05-SC | Tampering | npm/pip installs | low | accept | No new packages in this plan — reuse google-cloud-bigquery pins |
</threat_model>

<verification>
- pytest tests/test_analytics_api.py green
- GET /api/v1/analytics requires officer JWT and validates from/to
</verification>

<success_criteria>
- ANLY-03 API seam: officer GET /api/v1/analytics with auth + date validation
- D-05..D-07, D-10, D-13, D-16, D-18 covered for API read path
</success_criteria>

<output>
Create `.planning/phases/05-analytics-pipeline/05-04-SUMMARY.md` when done
</output>
