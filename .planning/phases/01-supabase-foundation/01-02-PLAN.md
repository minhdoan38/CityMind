---
phase: 01-supabase-foundation
plan: "02"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/migrate_bigquery_to_supabase.py
  - scripts/seed_reports.py
  - backend/tests/test_migrate_bigquery_to_supabase.py
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "Existing BigQuery demo reports and status events appear in Supabase without lost fields, IDs, or history."
    - "Repeating migration/seed operations creates no duplicate report or status rows."
  artifacts:
    - path: "scripts/migrate_bigquery_to_supabase.py"
      provides: "Dry-run, apply, idempotency, and reconciliation workflow"
    - path: "backend/tests/test_migrate_bigquery_to_supabase.py"
      provides: "Transformation, no-loss, and repeat-run coverage"
  key_links:
    - from: "scripts/migrate_bigquery_to_supabase.py"
      to: "public.reports and public.status_events"
      via: "BigQuery source reads followed by Supabase upserts/inserts and count/ID reconciliation"
      pattern: "reports|status_events|reconcil"
---

<objective>
**As an** officer, **I want to** find the existing demo reports and their history after cutover, **so that** the Supabase-backed workspace starts with complete operational continuity.

Purpose: Isolate DATA-02 migration/reconciliation from the schema/API slice while preserving D-06, D-08, and D-15.
Output: Tested, idempotent BigQuery-to-Supabase migration and adapted Supabase demo seed.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-RESEARCH.md
@.planning/phases/01-supabase-foundation/01-PATTERNS.md
@.planning/phases/01-supabase-foundation/01-01-SUMMARY.md
@scripts/seed_reports.py
@backend/app/demo_data.py
@infra/bigquery/schema.sql
@infra/bigquery/create_status_events.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate and reconcile every demo report and status event</name>
  <files>scripts/migrate_bigquery_to_supabase.py, backend/tests/test_migrate_bigquery_to_supabase.py</files>
  <behavior>
    - Dry run reports source report/status counts and planned target changes without mutation.
    - JSON strings become native JSONB values; stable IDs, timestamps, nullable fields, evidence, uncertainty, urban context, and full status history are preserved.
    - Applied migration fails non-zero if source/target counts, stable report IDs, or per-report history counts do not reconcile.
    - A second apply writes no duplicate reports or historical events.
  </behavior>
  <action>Write failing transformation/idempotency/reconciliation tests first, then implement the migration with the BigQuery client as source and the audited `supabase==2.31.0` privileged client as target. Read in bounded batches, normalize serialized JSON fields for D-06, upsert reports on stable `report_id`, and use a deterministic event identity or equivalent existence check so append-only history is not duplicated. Preserve legacy GCS image URIs as explicitly legacy storage references rather than attempting an unverified byte migration; D-15 still requires all new evidence to use Supabase Storage. Provide `--dry-run`, `--apply`, and `--verify`; redact credentials/errors and stop on partial write or reconciliation mismatch. BigQuery exists only in this one-time source utility per D-08, never in application fallback/dual-write code.</action>
  <verify>
    <automated>cd backend &amp;&amp; pytest -q tests/test_migrate_bigquery_to_supabase.py -x &amp;&amp; cd .. &amp;&amp; python3 scripts/migrate_bigquery_to_supabase.py --dry-run</automated>
  </verify>
  <done>Dry-run output is deterministic, conversions preserve all source fields/history, reconciliation catches loss, and repeat apply behavior is idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed Supabase deterministically and prove live no-loss migration</name>
  <files>scripts/seed_reports.py, backend/tests/test_migrate_bigquery_to_supabase.py</files>
  <behavior>
    - Seed dry run is non-mutating and applied seed is idempotent.
    - Applied migration reports equal source/target report IDs and status-event totals.
    - Re-running both tools reports zero new rows.
  </behavior>
  <action>Adapt `scripts/seed_reports.py` from BigQuery/GCS writes to the Supabase privileged client and private bucket while retaining deterministic demo IDs/data. Require Plan 01's successful blocking schema push before any apply. Run migration dry-run, apply with verification, rerun with verification, then seed twice; capture reconciled counts and zero-write second-run results without printing secrets. Do not add schema SQL here, so the Plan 01 push remains after all schema changes.</action>
  <verify>
    <automated>cd backend &amp;&amp; pytest -q tests/test_migrate_bigquery_to_supabase.py -x &amp;&amp; cd .. &amp;&amp; python3 scripts/migrate_bigquery_to_supabase.py --apply --verify &amp;&amp; python3 scripts/migrate_bigquery_to_supabase.py --apply --verify &amp;&amp; python3 scripts/seed_reports.py --apply &amp;&amp; python3 scripts/seed_reports.py --apply</automated>
  </verify>
  <done>Live source/target IDs and counts reconcile, all status history is present, and second migration/seed runs write zero rows.</done>
</task>

</tasks>

<must_haves>
- No demo report field or status event is lost.
- Apply is idempotent and reconciliation is blocking.
- BigQuery remains migration input only.
</must_haves>

## Artifacts this phase produces

- One-time BigQuery-to-Supabase migration utility.
- Supabase-native deterministic demo seed.
- Migration transformation and reconciliation tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BigQuery to migration process | Historical rows are external input to transformation. |
| Migration process to Supabase | Privileged writes bypass RLS and require strict target/reconciliation controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Tampering | migration transforms | high | mitigate | Stable IDs, deterministic transforms, idempotent writes, and source-target reconciliation. |
| T-01-14 | Information Disclosure | migration credentials/output | high | mitigate | Server-only environment credentials and redacted output/errors. |
| T-01-15 | Denial of Service | batch migration | medium | mitigate | Bounded reads/writes and fail-fast partial-write handling. |
</threat_model>

<verification>
Run the targeted tests, dry run, applied migration twice, and seed twice; verify equal source/target IDs/counts and zero second-run writes.
</verification>

<success_criteria>
- Existing demo data is present in Supabase without loss or duplication.
- Migration/seed can be rerun safely.
- Application runtime has no BigQuery operational fallback.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-02-SUMMARY.md` when done.
</output>
