---
phase: 01-supabase-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/components.json
  - frontend/src/lib/utils.ts
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/i18n/request.ts
  - frontend/src/i18n/routing.ts
  - frontend/src/proxy.ts
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/src/components/LocaleSwitcher.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/separator.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/sidebar.tsx
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/alert.tsx
  - frontend/src/hooks/use-mobile.ts
  - frontend/tests/public-shell.test.mjs
autonomous: true
requirements:
  - PUB-05
must_haves:
  truths:
    - "A citizen can open `/`, see the CityMind public shell, and use the primary Report an issue action without officer authentication."
    - "A citizen can switch the scaffold between English and Vietnamese without losing the current route."
    - "The public shell consistently uses the approved shadcn Radix/new-york/zinc theme, Source Sans 3, accessible focus states, and restrained civic palette."
  artifacts:
    - path: "frontend/components.json"
      provides: "Official shadcn Radix/new-york/zinc CSS-variable configuration"
    - path: "frontend/src/app/page.tsx"
      provides: "Unauthenticated public Home shell at `/`"
    - path: "frontend/src/components/LocaleSwitcher.tsx"
      provides: "Accessible EN/VI locale interaction that preserves route"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/messages/en.json and frontend/messages/vi.json"
      via: "next-intl request configuration and provider"
      pattern: "NextIntlClientProvider|getMessages"
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/app/report/page.tsx"
      via: "primary Report an issue link"
      pattern: "href=.*/report"
---

<objective>
**As a** citizen, **I want to** recognize CityMind, switch language, and reach reporting from a polished public entry point, **so that** I can begin the reporting journey without encountering the officer dashboard.

Purpose: Deliver the public walking-skeleton UI on top of the audited dependency gate, implementing D-14, D-16, and the public portion of D-17 while respecting the Phase 1 UI contract.
Output: Exact-pinned shadcn/next-intl setup, shared civic theme, bilingual message scaffold, locale interaction, and public Home shell.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-RESEARCH.md
@.planning/phases/01-supabase-foundation/01-PATTERNS.md
@.planning/phases/01-supabase-foundation/01-01-SUMMARY.md
@frontend/package.json
@frontend/src/app/layout.tsx
@frontend/src/app/globals.css
@frontend/src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the approved UI stack and render the public civic shell</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/components.json, frontend/src/lib/utils.ts, frontend/src/app/globals.css, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/src/components/ui/button.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/separator.tsx, frontend/src/components/ui/dropdown-menu.tsx, frontend/src/components/ui/sidebar.tsx, frontend/src/components/ui/sheet.tsx, frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/skeleton.tsx, frontend/src/components/ui/alert.tsx, frontend/src/hooks/use-mobile.ts, frontend/tests/public-shell.test.mjs</files>
  <action>Task 1 of Plan 01 has already approved all SUS packages; do not add another package checkpoint. From `frontend/`, install the audited exact pins `@supabase/supabase-js@2.110.7`, `@supabase/ssr@0.12.3`, and `next-intl@4.13.2`; initialize `shadcn@4.13.1` with `--base radix --css-variables`; inspect `components.json` and require new-york style, zinc base color, Radix, CSS variables, aliases under `@/`, and official registry only before adding exactly `button input label card separator dropdown-menu sidebar alert` plus its official sheet/tooltip/skeleton/use-mobile closure. Install exact closure pins `class-variance-authority@0.7.1`, `clsx@2.1.1`, `tailwind-merge@3.6.0`, `lucide-react@1.25.0`, `radix-ui@1.6.4`, dev pins `shadcn@4.13.1` and `tw-animate-css@1.4.0`; do not use `shadcn-ui`, `@radix-ui/react-*`, `tailwindcss-animate`, the deprecated toast component, Base UI, third-party registries, or unpinned versions. Review vendored generated UI source, run `npm ls` for the complete audited set, verify `npm view PACKAGE scripts.postinstall` is empty for each npm package, and run `npm audit --omit=dev`. First add a failing Node contract test for public route accessibility, exact CTA, semantic shell regions, approved theme/font tokens, and absence of an officer-session guard at `/`; then make `/` an unauthenticated semantic header/main/footer shell: CityMind wordmark is the first focal point, the shadcn Button link “Report an issue” reaches `/report`, placeholder body copy remains deliberately limited to Phase 1, and the old dashboard content/auth fetch is removed from this route per D-14/D-17. Apply the approved light-only CSS variables: white background, `#F0F4F3` secondary, `#0F766E` reserved accent, `#14201D` ink, `#5C6B66` muted text, `#D5DEDB` borders, `#B42318` destructive; Source Sans 3; exactly 14/16/20/28px roles and weights 400/600; 8-point spacing; 44px primary target; visible accent focus rings; reduced-motion instant state changes; no purple gradients, neon, dark cyber shell, or cream/sand body.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm ls @supabase/supabase-js@2.110.7 @supabase/ssr@0.12.3 next-intl@4.13.2 shadcn@4.13.1 class-variance-authority@0.7.1 clsx@2.1.1 tailwind-merge@3.6.0 lucide-react@1.25.0 radix-ui@1.6.4 tw-animate-css@1.4.0 &amp;&amp; npm audit --omit=dev &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>`/` builds as the unauthenticated CityMind public shell with the approved visual system and a working `/report` CTA; shadcn is official/Radix/new-york/zinc; exact package pins and lockfile pass dependency/audit checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a route-preserving bilingual locale interaction</name>
  <files>frontend/src/i18n/request.ts, frontend/src/i18n/routing.ts, frontend/src/proxy.ts, frontend/messages/en.json, frontend/messages/vi.json, frontend/src/components/LocaleSwitcher.tsx, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/tests/public-shell.test.mjs</files>
  <behavior>
    - English and Vietnamese message catalogs have identical keys for the Phase 1 public, login, dashboard, navigation, empty/error, locale, and logout strings.
    - Switching locale updates the locale cookie/no-prefix configuration while preserving the active pathname.
    - The root `<html lang>` and rendered shell copy reflect the active locale.
    - The control exposes accessible names `Language` and `Ngôn ngữ` and remains keyboard operable.
  </behavior>
  <action>Wire `next-intl@4.13.2` using App Router request configuration and no locale prefix, with `frontend/src/proxy.ts` serving the Next.js 16 refresh/i18n boundary. Create matching EN/VI namespaces for the exact UI-SPEC strings: public CTA, officer login heading/support/error, dashboard empty/load error, Reports/Export/Settings/Logout labels, locale name, and sign-out confirmation; do not pull full marketing content from Phase 2. Wrap the root layout in the provider, set `<html lang>` from the active locale, and build a small client `LocaleSwitcher` using the official dropdown-menu primitive that persists locale and preserves the current route. Render it in the public header. Keep focus-visible, contrast, and 44px touch-target rules; text plus color communicates errors/status.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; node -e "const fs=require('fs');const en=JSON.parse(fs.readFileSync('messages/en.json'));const vi=JSON.parse(fs.readFileSync('messages/vi.json'));const walk=o=&gt;Object.entries(o).flatMap(([k,v])=&gt;v&amp;&amp;typeof v==='object'?walk(v).map(x=&gt;k+'.'+x):[k]).sort();if(JSON.stringify(walk(en))!==JSON.stringify(walk(vi)))process.exit(1)" &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Citizens can toggle EN/VI from `/` without route loss, catalogs remain structurally identical, document language changes correctly, and the public shell satisfies the UI-SPEC accessibility/copy contract.</done>
</task>

</tasks>

<must_haves>
- `/` is public and visually distinct from the officer dashboard.
- The official shadcn stack is exact-pinned, audited, themed, and committed with its lockfile/generated source.
- EN/VI switching is interactive, accessible, and route-preserving.
</must_haves>

## Artifacts this phase produces

- shadcn configuration and core official UI primitives.
- CityMind civic design tokens and Source Sans 3 typography.
- EN/VI next-intl message/request routing scaffold.
- Public Home shell with report CTA and locale control.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to locale routing | User-controlled locale/cookie values select message resources and must be constrained to EN/VI. |
| Package registry to source tree | Generated shadcn source and npm dependencies enter the trusted build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | shadcn generated source | high | mitigate | Exact CLI/dependency pins, Radix base assertion, official registry only, generated diff review, lockfile, npm audit. |
| T-01-07 | Elevation of Privilege | public root route | medium | mitigate | Remove officer fetch/session assumptions from `/`; expose no privileged data or secret env values. |
| T-01-08 | Tampering | locale input | low | mitigate | Allowlist only `en` and `vi`, use server-owned message files, and fall back safely. |
| T-01-SC | Tampering | npm installs | high | mitigate | Consume the single blocking package approval from Plan 01; exact pins plus postinstall/lockfile/audit checks. |
</threat_model>

<verification>
Run `cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build`; start the built server and verify `/` returns 200, the report CTA resolves to `/report`, EN/VI switching preserves `/`, and computed focus/contrast/touch behavior matches 01-UI-SPEC.md at mobile and desktop widths.
</verification>

<success_criteria>
- shadcn/ui is installed through the official audited toolchain and themed consistently.
- Public `/` renders without authentication and routes citizens toward reporting.
- EN/VI scaffolding works end-to-end with exact Phase 1 strings and accessibility behavior.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-02-SUMMARY.md` when done.
</output>
