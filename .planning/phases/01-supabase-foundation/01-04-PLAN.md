---
phase: 01-supabase-foundation
plan: "04"
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/i18n/request.ts
  - frontend/src/i18n/routing.ts
  - frontend/src/proxy.ts
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/src/components/LocaleSwitcher.tsx
  - frontend/tests/public-shell.test.mjs
autonomous: true
requirements: [PUB-05]
must_haves:
  truths:
    - "Citizens open an unauthenticated public Home at `/` and can reach `/report`."
    - "Citizens switch EN/VI without losing the active route; document language and copy update."
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Semantic public Home shell"
    - path: "frontend/src/components/LocaleSwitcher.tsx"
      provides: "Accessible route-preserving language control"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/messages/en.json and frontend/messages/vi.json"
      via: "next-intl provider/request config"
      pattern: "NextIntlClientProvider|getMessages"
---

<objective>
**As a** citizen, **I want to** recognize CityMind, switch language, and reach reporting from `/`, **so that** I can begin without encountering officer-only UI.

Purpose: Deliver D-16 and the public route portion of D-17 using the UI foundation from Plan 03.
Output: Public Home shell, Source Sans 3 root layout, matching EN/VI catalogs, locale proxy/request config, and locale control.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-PATTERNS.md
@.planning/phases/01-supabase-foundation/01-03-SUMMARY.md
@frontend/src/app/layout.tsx
@frontend/src/app/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the officer root with the public Home slice</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/tests/public-shell.test.mjs</files>
  <behavior>
    - `/` renders without an officer session and contains semantic header/main/footer regions.
    - CityMind wordmark is first focal point and “Report an issue” links to `/report` with a 44px target.
    - Root layout uses Source Sans 3 and retains metadata/full-height structure.
  </behavior>
  <action>Write the failing Node shell contract first, then remove dashboard fetching/session enforcement from `/` and render the restrained Phase 1 public shell per D-17: semantic header/main/footer, CityMind wordmark, shadcn primary CTA “Report an issue” to `/report`, placeholder body only, and footer. Replace Geist with Source Sans 3 in the root layout. Apply exact UI-SPEC hierarchy, copy, 14/16/20/28px types, 400/600 weights, body width, focus/touch/accessibility rules. Do not add deferred marketing sections, metrics, badge clusters, multi-card grids, or ReportForm RHF/Zod polish.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Unauthenticated `/` renders the approved public shell and its primary CTA reaches `/report`; former dashboard content is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the route-preserving EN/VI interaction</name>
  <files>frontend/src/i18n/request.ts, frontend/src/i18n/routing.ts, frontend/src/proxy.ts, frontend/messages/en.json, frontend/messages/vi.json, frontend/src/components/LocaleSwitcher.tsx, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/tests/public-shell.test.mjs</files>
  <behavior>
    - EN and VI catalogs have identical keys for public, login, dashboard, navigation, empty/error, locale, and logout strings.
    - Locale switching persists an allowlisted `en`/`vi` preference and preserves pathname without a URL prefix.
    - `<html lang>` and rendered copy match locale; the control is named `Language`/`Ngôn ngữ` and keyboard operable.
  </behavior>
  <action>Wire `next-intl@4.13.2` with App Router request config and no-prefix routing per D-16. Create matching catalogs containing only the UI-SPEC scaffold strings, not Phase 2 marketing copy. Wrap the root layout with the provider, set document language, and add a dropdown-menu locale control that preserves the current route and allowlists EN/VI. Start the Next.js 16 `proxy.ts` as the combined locale/session-refresh seam that Plan 05 will extend without replacing. Test catalog parity, locale persistence, accessible name, and route preservation.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; node -e "const fs=require('fs');const en=JSON.parse(fs.readFileSync('messages/en.json'));const vi=JSON.parse(fs.readFileSync('messages/vi.json'));const walk=o=&gt;Object.entries(o).flatMap(([k,v])=&gt;v&amp;&amp;typeof v==='object'?walk(v).map(x=&gt;k+'.'+x):[k]).sort();if(JSON.stringify(walk(en))!==JSON.stringify(walk(vi)))process.exit(1)" &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Citizens can toggle complete Phase 1 scaffold copy between EN/VI without route loss, and the document/accessibility contract follows the active locale.</done>
</task>

</tasks>

<must_haves>
- `/` is public and routes citizens to reporting.
- EN/VI catalogs and locale interaction work end-to-end.
- Deferred Phase 2 content is absent.
</must_haves>

## Artifacts this phase produces

- Public CityMind Home shell.
- next-intl request/routing/provider scaffold and EN/VI catalogs.
- Accessible locale switcher and public-shell tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to locale selection | User-controlled locale/cookie values select trusted message resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering | locale input | low | mitigate | Allowlist EN/VI and load only server-owned catalogs. |
| T-01-18 | Information Disclosure | public root | medium | mitigate | Remove officer session/data fetches and expose no privileged values. |
</threat_model>

<verification>
Run Node contract tests, catalog parity, lint, and build; verify `/`, `/report`, and locale switching at mobile/desktop widths.
</verification>

<success_criteria>
- Public `/` and CTA render accessibly without auth.
- EN/VI switching preserves route and satisfies UI-SPEC copy.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-04-SUMMARY.md` when done.
</output>
