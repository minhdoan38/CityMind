---
phase: 01-supabase-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/.env.example
  - backend/app/config.py
  - backend/app/security.py
  - backend/app/main.py
  - backend/app/api/reports.py
  - backend/app/services/supabase.py
  - backend/app/services/storage.py
  - backend/tests/test_supabase.py
  - backend/tests/test_storage.py
  - backend/tests/test_security.py
  - backend/tests/test_reports.py
  - supabase/migrations/20260720_000001_foundation.sql
autonomous: false
requirements:
  - DATA-01
  - AUTH-02
  - AUTH-03
user_setup:
  - service: supabase
    why: "Apply the production-like Cloud schema, exercise RLS/Auth, and migrate operational data"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API"
      - name: SUPABASE_SECRET_KEY
        source: "Supabase Dashboard -> Project Settings -> API; server-only secret/service-role credential"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase account access tokens; required for non-TTY CLI schema push"
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard project URL/reference"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Project Settings -> Database"
must_haves:
  truths:
    - "A citizen can submit a report through the existing public UI/API path and the report persists in Supabase Postgres without authentication."
    - "An authenticated officer/admin can read and update reports through FastAPI with RLS applied, while anonymous or wrong-role callers are denied."
    - "New evidence is stored in a private Supabase Storage bucket and operational report APIs no longer use BigQuery or GCS."
  artifacts:
    - path: "supabase/migrations/20260720_000001_foundation.sql"
      provides: "Reports, append-only status events, hashed access-token storage, private evidence bucket, indexes, constraints, and RLS policies"
    - path: "backend/app/services/supabase.py"
      provides: "SupabaseReportSink with privileged-ingest and caller-JWT CRUD clients"
  key_links:
    - from: "backend/app/api/reports.py"
      to: "backend/app/services/supabase.py"
      via: "public service-role ingest and per-request caller-JWT officer operations"
      pattern: "SupabaseReportSink|create_user_client"
    - from: "backend/app/security.py"
      to: "Supabase JWKS and app_metadata.role"
      via: "Bearer JWT signature, issuer, audience, expiry, and role validation"
      pattern: "Authorization|app_metadata|officer|admin"
---

<objective>
**As a** citizen or officer, **I want to** submit, read, and update reports through the existing application path backed by Supabase, **so that** operational work is durable, access-controlled, and no longer coupled to BigQuery.

Purpose: Establish the walking skeleton's real UI-to-API-to-database write/read path, the officer authorization boundary, and the hard operational cutover per D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, and D-15.
Output: Audited dependency approval, versioned Supabase schema/RLS, FastAPI Supabase persistence and private evidence storage, remote schema push, and tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-RESEARCH.md
@.planning/phases/01-supabase-foundation/01-PATTERNS.md
@infra/bigquery/schema.sql
@infra/bigquery/create_status_events.sql
@backend/app/api/reports.py
@backend/app/services/bigquery.py
@backend/app/services/storage.py
@backend/app/security.py
@backend/tests/test_bigquery.py
@backend/tests/test_reports.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the seven SUS package identities and shadcn configuration</name>
  <files>.planning/phases/01-supabase-foundation/01-RESEARCH.md</files>
  <what-built>The Package Legitimacy Audit in 01-RESEARCH.md verified compatibility and found no SLOP packages, but freshness/incomplete-registry signals require one human approval before any install.</what-built>
  <action>Review the seven flagged package identities, exact versions, official repositories, recorded empty postinstall-script results, and the Radix/new-york/zinc shadcn configuration. Record the approval signal before allowing Task 2 or any later plan to execute a pip/npm/shadcn install.</action>
  <how-to-verify>
    1. Verify `supabase==2.31.0` at https://pypi.org/project/supabase/ and its official repository `supabase/supabase-py`.
    2. Verify `@supabase/supabase-js@2.110.7`, `@supabase/ssr@0.12.3`, `next-intl@4.13.2`, `shadcn@4.13.1`, `lucide-react@1.25.0`, and `radix-ui@1.6.4` on npmjs.com and confirm their repositories match 01-RESEARCH.md.
    3. Confirm the shadcn generated configuration will be Radix base, new-york style, zinc base color, CSS variables, and official registry only.
    4. Confirm the postinstall audit in 01-RESEARCH.md reports no postinstall script for the audited npm packages.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize these exact package identities/versions, or identify the package that must be re-researched.</resume-signal>
  <verify>
    <automated>node /Users/minhmice/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.get .planning/phases/01-supabase-foundation/01-RESEARCH.md</automated>
  </verify>
  <done>The executor records explicit human approval for all seven SUS names/versions and the required shadcn configuration before the first install command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove and implement the real report submission-to-Supabase slice</name>
  <files>backend/requirements.txt, backend/.env.example, backend/app/config.py, backend/app/security.py, backend/app/main.py, backend/app/api/reports.py, backend/app/services/supabase.py, backend/app/services/storage.py, backend/tests/test_supabase.py, backend/tests/test_storage.py, backend/tests/test_security.py, backend/tests/test_reports.py, supabase/migrations/20260720_000001_foundation.sql</files>
  <behavior>
    - RED: an existing public `POST /api/v1/reports/analyze` request persists a report through a service-role Supabase client and returns its report ID without a bearer token.
    - RED: missing, invalid, expired, wrong-issuer/audience, or non-officer/admin JWTs receive 401/403; a valid officer/admin JWT reaches a fresh caller-scoped client and protected reads/updates remain RLS-scoped.
    - RED: reports/status events preserve the current API field contract, officer/admin caller-JWT `update_status` succeeds by INSERT into `status_events`, JSON-valued fields round-trip as JSONB, and access-token values can only be stored as hashes.
    - RED: RLS grants officer/admin INSERT on `status_events` but denies UPDATE and DELETE, keeping history append-only for caller-scoped clients.
    - RED: new evidence uploads/downloads use a private Supabase Storage bucket and never expose the secret/service-role key.
  </behavior>
  <action>After Task 1 approval, add only `supabase==2.31.0` to the pinned Python requirements and install that exact distribution; do not use the nonexistent/deprecated `supabase-py` package name. First replace the relevant BigQuery/GCS/auth expectations with failing endpoint/service tests, then implement the slice. Create the versioned migration per D-06, D-07, D-09, and D-13: near-mirror report columns, JSONB `evidence`/`uncertainty`/`urban_context`, constrained report status values, append-only `status_events`, hashed-token-only `access_tokens` with report FK/timestamps, required indexes, a private evidence bucket, and RLS. Policies must deny broad public reads; allow `officer`/`admin` from `auth.jwt()->'app_metadata'->>'role'` to read reports; allow officer/admin UPDATE on reports; and explicitly allow officer/admin INSERT on `status_events` while granting no UPDATE or DELETE policy on that history table. Add SQL/integration assertions proving a caller-JWT `update_status` insert succeeds under RLS and subsequent history mutation/deletion is denied. Implement `SupabaseReportSink` with the existing sink method surface (`insert`, `list_recent`, `summary`, `get_report`, `update_status`, storage-path lookup, `status_history`); use a cached service-role client only for public ingest/privileged server writes and a fresh caller-JWT client for every officer request per D-10/D-11. Replace shared-key auth with verified Supabase JWKS bearer validation and typed principal claims per D-01/D-02, with signature, issuer, audience, expiry, and role checks and no development fail-open. Keep `/analyze` unauthenticated per D-05/AUTH-03; require the principal on recent/summary/detail/status/history/image endpoints; pass its JWT into the caller-scoped sink; allow `Authorization` in CORS. Rework `EvidenceStorage` for the private Supabase bucket per D-15, saving a storage-neutral path on the report. Remove operational `BigQueryReportSink`/GCS factories and provider-specific API errors per D-08, while retaining Google dependencies solely for Plan 02's one-time migration source. Keep service-role secrets out of browser-visible fields, logs, URLs, and responses.</action>
  <verify>
    <automated>cd backend &amp;&amp; pytest -q tests/test_supabase.py tests/test_storage.py tests/test_security.py tests/test_reports.py -x</automated>
  </verify>
  <done>The existing citizen submission route performs a real Supabase write; valid officers can read/update through caller-scoped clients; RLS/auth tests reject unauthorized access; evidence uses private Supabase Storage; no operational endpoint constructs or calls BigQuery/GCS.</done>
</task>

<task type="auto" gate="[BLOCKING]">
  <name>Task 3: [BLOCKING] Push the complete Supabase schema before remote verification</name>
  <files>supabase/migrations/20260720_000001_foundation.sql</files>
  <action>After every Phase 1 schema/RLS/storage SQL change in Task 2 is complete and its local tests pass, execute the mandatory non-TTY Cloud schema gate: require `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, and `SUPABASE_DB_PASSWORD`; link the exact project with the access token in the environment; run `supabase db push --linked --password "$SUPABASE_DB_PASSWORD"`; and stop immediately on any push failure. Do not substitute a build, type check, diff, or local-only database test. After the push, execute remote policy probes proving public analyze can use the privileged server path, anonymous report reads fail, officer/admin report reads/updates succeed, officer/admin `status_events` INSERT succeeds, and status-event UPDATE/DELETE fail.</action>
  <verify>
    <automated>test -n "$SUPABASE_ACCESS_TOKEN" &amp;&amp; test -n "$SUPABASE_PROJECT_REF" &amp;&amp; test -n "$SUPABASE_DB_PASSWORD" &amp;&amp; SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase link --project-ref "$SUPABASE_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD" &amp;&amp; SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase db push --linked --password "$SUPABASE_DB_PASSWORD" &amp;&amp; cd backend &amp;&amp; pytest -q tests/test_supabase.py tests/test_security.py tests/test_reports.py -x</automated>
  </verify>
  <done>The complete schema is pushed to the linked Supabase Cloud project before remote verification, and live RLS probes confirm the required report access plus INSERT-only officer/admin status history.</done>
</task>

</tasks>

<must_haves>
- The public analyze path reaches FastAPI and commits one real report row to Supabase.
- Officer/admin authorization is enforced both by verified JWT claims and database RLS.
- The remote schema push succeeds before migration or remote e2e verification.
- Operational BigQuery usage is absent.
</must_haves>

## Artifacts this phase produces

- Versioned Supabase Postgres/RLS/storage schema.
- Supabase-backed FastAPI report and evidence services.
- JWT principal boundary and caller-scoped RLS data access.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen/Next.js to FastAPI | Untrusted report fields and evidence cross the public ingest boundary. |
| Officer/Next.js to FastAPI | A bearer token crosses into protected officer endpoints. |
| FastAPI to Supabase | Caller JWT clients remain RLS-scoped; privileged clients bypass RLS and are server-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | `backend/app/security.py` | high | mitigate | Verify JWKS signature, issuer, audience, expiry, and officer/admin claim; fail closed. |
| T-01-02 | Elevation of Privilege | Supabase clients/RLS | critical | mitigate | Separate caller-scoped and service-role clients; enforce role policies and never expose the privileged key. |
| T-01-03 | Information Disclosure | reports/evidence/access tokens | high | mitigate | Deny anonymous reads, keep bucket private, and constrain access-token persistence to hashes. |
| T-01-04 | Tampering | status history | high | mitigate | Officer/admin INSERT-only RLS policy with explicit UPDATE/DELETE denial tests. |
| T-01-05 | Denial of Service | public analyze | medium | mitigate | Preserve bounded image validation and rate limiting before AI/storage calls. |
| T-01-SC | Tampering | npm/pip installs | high | mitigate | Blocking human checkpoint for all seven SUS packages, exact pins, lockfile review, `npm ls`, and `npm audit --omit=dev`. |
</threat_model>

<verification>
Run `cd backend &amp;&amp; pytest -q`; confirm the mandatory `supabase db push` succeeded against the linked project; submit one report through the existing `/report` UI/API route; authenticate as a seeded officer and verify recent/detail/status operations; confirm anonymous reads and status-history UPDATE/DELETE are denied.
</verification>

<success_criteria>
- Public report submission performs a real Supabase write with optional private evidence storage.
- Officer CRUD succeeds with verified JWTs and RLS; unauthorized requests fail closed.
- BigQuery/GCS are absent from operational request paths after cutover.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-01-SUMMARY.md` when done.
</output>
