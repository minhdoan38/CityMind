---
phase: 01-supabase-foundation
plan: "05"
type: execute
wave: 4
depends_on: [01-01, 01-04]
files_modified:
  - frontend/.env.example
  - frontend/src/lib/supabase/client.ts
  - frontend/src/lib/supabase/server.ts
  - frontend/src/lib/auth.ts
  - frontend/src/lib/backend.ts
  - frontend/src/proxy.ts
  - frontend/src/app/api/session/login/route.ts
  - frontend/src/app/api/session/logout/route.ts
  - frontend/src/app/api/officer/reports/[reportId]/status/route.ts
  - frontend/src/app/api/officer/reports/[reportId]/image/route.ts
  - frontend/src/app/login/page.tsx
  - frontend/tests/officer-auth.test.mjs
autonomous: true
requirements: [AUTH-01, AUTH-03]
must_haves:
  truths:
    - "A manually seeded officer/admin signs in through Supabase Auth; invalid credentials show approved inline feedback; no signup exists."
    - "Next.js authorizes with verified claims and forwards only the live access token to protected FastAPI endpoints."
    - "The custom HMAC/shared-password/shared-key path is fully replaced and public analyze remains unauthenticated."
  artifacts:
    - path: "frontend/src/lib/supabase/server.ts"
      provides: "SSR cookie client, trusted claims, and session-token retrieval"
    - path: "frontend/src/app/login/page.tsx"
      provides: "Officer-only sign-in UI"
    - path: "frontend/src/lib/backend.ts"
      provides: "Bearer-token FastAPI request bridge"
  key_links:
    - from: "frontend/src/app/api/session/login/route.ts"
      to: "Supabase Auth"
      via: "signInWithPassword and SSR cookie adapter"
      pattern: "signInWithPassword"
    - from: "frontend/src/lib/backend.ts"
      to: "FastAPI"
      via: "Authorization Bearer access token"
      pattern: "Authorization.*Bearer"
---

<objective>
**As an** officer, **I want to** sign in securely and make authenticated application requests, **so that** FastAPI and Supabase RLS can enforce my officer/admin identity end-to-end.

Purpose: Implement D-01–D-05 and AUTH-01/AUTH-03 as a bounded SSR-auth/BFF bridge before dashboard presentation.
Output: Supabase browser/server helpers, login/logout, combined locale/auth proxy, and bearer-forwarding BFF.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-RESEARCH.md
@.planning/phases/01-supabase-foundation/01-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-01-SUMMARY.md
@.planning/phases/01-supabase-foundation/01-04-SUMMARY.md
@frontend/src/lib/auth.ts
@frontend/src/lib/backend.ts
@frontend/src/app/api/session/login/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sign in a seeded officer through Supabase SSR</name>
  <files>frontend/.env.example, frontend/src/lib/supabase/client.ts, frontend/src/lib/supabase/server.ts, frontend/src/lib/auth.ts, frontend/src/proxy.ts, frontend/src/app/api/session/login/route.ts, frontend/src/app/api/session/logout/route.ts, frontend/src/app/login/page.tsx, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - Valid seeded officer/admin credentials persist SSR cookies and return a 303 redirect to `/dashboard`.
    - Invalid credentials return `/login?error=1` and exact inline Alert feedback; signup/quick access are absent.
    - Authorization uses `getClaims()` and accepts only `app_metadata.role` officer/admin; `getSession()` is used only to retrieve an access token.
    - Logout signs out, clears SSR cookies, and returns `/login`.
  </behavior>
  <action>Write failing Node auth contract tests first. Create separate browser/server helpers with the audited exact Supabase packages. Replace the custom HMAC session/password/quick-access model with `@supabase/ssr` cookie adapters per D-03, and authorize server paths from verified `getClaims()` role data per D-02; use `getSession()` only for token forwarding. Extend Plan 04's Next.js 16 `proxy.ts` so locale behavior remains intact while auth cookies refresh and `/dashboard/:path*` fails closed. Rework login/logout handlers around `signInWithPassword`/`signOut` with 303 redirects. Build the UI-SPEC login Card with heading “Officer sign-in”, supporting advisory copy, primary “Sign in to dashboard”, exact error text, email/password labels, 44px target, visible focus, and no signup/decorative imagery per D-04. Document only public Supabase URL/publishable key and backend URL in frontend env examples; never expose service-role secrets.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Seeded officer/admin credentials create SSR cookies, invalid/wrong-role requests fail closed with approved feedback, logout clears the session, and no open signup exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward the live Supabase JWT through officer BFF routes</name>
  <files>frontend/src/lib/backend.ts, frontend/src/lib/supabase/server.ts, frontend/src/app/api/officer/reports/[reportId]/status/route.ts, frontend/src/app/api/officer/reports/[reportId]/image/route.ts, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - Officer proxy calls attach `Authorization: Bearer <current access token>` and pass backend status/body through.
    - Missing session/token is rejected before FastAPI; public analyze keeps its unauthenticated forwarding path.
    - No service-role key, legacy shared header, or access token is emitted to logs/body/URL.
  </behavior>
  <action>Update `officerFetch` and protected status/image BFF routes to retrieve the current SSR access token server-side and forward it only in the Authorization header per D-01/AUTH-03. Preserve response pass-through and error handling. Keep public analyze unchanged and unauthenticated per D-05. Remove the legacy officer-header plumbing and test missing-token rejection, bearer forwarding, and secret non-disclosure against the Plan 01 FastAPI contract.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; rg -n "getClaims|Authorization.*Bearer|signInWithPassword" src/lib src/app/api/session src/app/api/officer</automated>
  </verify>
  <done>Protected Next BFF requests carry only the live Supabase bearer token, missing sessions fail closed, and public analyze remains open.</done>
</task>

</tasks>

<must_haves>
- Supabase SSR cookies are the only officer session.
- Claims authorization and bearer forwarding are distinct and correctly scoped.
- No signup, quick access, shared password, or service-role browser exposure exists.
</must_haves>

## Artifacts this phase produces

- Supabase SSR client helpers and combined locale/auth proxy.
- Officer login/logout flow.
- Bearer-token BFF bridge and auth contract tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to Supabase Auth | Untrusted credentials create an SSR cookie session. |
| Next.js to FastAPI | Access tokens cross a server boundary for officer calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Spoofing | SSR auth | high | mitigate | `getClaims()` authorization, cookie refresh, role allowlist, fail closed. |
| T-01-20 | Information Disclosure | bearer bridge | high | mitigate | HttpOnly/server handling and header-only forwarding without logging. |
| T-01-21 | Elevation of Privilege | BFF routes | critical | mitigate | Session guard at BFF plus independent FastAPI JWT/RLS enforcement. |
</threat_model>

<verification>
Run auth contract tests, lint, and build; test valid/invalid/wrong-role login, logout, missing-token BFF rejection, and an accepted bearer-authenticated FastAPI request.
</verification>

<success_criteria>
- Supabase Auth replaces the shared-password/HMAC path.
- Officer BFF requests use current bearer JWTs and public analyze stays unauthenticated.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-05-SUMMARY.md` when done.
</output>
