---
phase: 01-supabase-foundation
plan: "03"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/components.json
  - frontend/src/lib/utils.ts
  - frontend/src/app/globals.css
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/separator.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/alert.tsx
autonomous: true
requirements: [PUB-05]
must_haves:
  truths:
    - "Existing frontend routes render with the approved light civic theme and accessible focus/touch behavior."
    - "Only audited, exact-pinned official packages and shadcn registry source enter the frontend."
  artifacts:
    - path: "frontend/components.json"
      provides: "Resolved Radix/new-york/zinc/CSS-variable shadcn contract"
    - path: "frontend/src/app/globals.css"
      provides: "Approved civic tokens, typography, focus, and reduced-motion rules"
  key_links:
    - from: "frontend/components.json"
      to: "frontend/src/components/ui/*.tsx"
      via: "official shadcn CLI generation with @ aliases"
      pattern: "new-york|radix|zinc"
---

<objective>
**As a** CityMind user, **I want to** encounter a consistent, accessible civic visual system on existing routes, **so that** later public and officer slices share trustworthy controls rather than ad-hoc styling.

Purpose: Resolve and establish D-14/PUB-05 as the bounded UI contract consumed by Plans 04–06.
Output: Exact-pinned audited UI dependencies, resolved shadcn configuration, core primitives, and global theme.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-RESEARCH.md
@.planning/phases/01-supabase-foundation/01-01-SUMMARY.md
@frontend/package.json
@frontend/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and verify the approved dependency/config boundary</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/components.json, frontend/src/lib/utils.ts, frontend/src/app/globals.css</files>
  <action>Consume the single package approval recorded by Plan 01; do not add a second checkpoint. Install exact pins `@supabase/supabase-js@2.110.7`, `@supabase/ssr@0.12.3`, `next-intl@4.13.2`, `class-variance-authority@0.7.1`, `clsx@2.1.1`, `tailwind-merge@3.6.0`, `lucide-react@1.25.0`, `radix-ui@1.6.4`, dev pins `shadcn@4.13.1` and `tw-animate-css@1.4.0`. Run only `shadcn@4.13.1 init --base radix --css-variables` in this task; do not generate components yet. Accept `components.json` only when review confirms Radix, new-york, zinc, CSS variables, `@/` aliases, and official registry output. Reject Base UI, third-party registries, deprecated/wrong alternatives, toast/sonner, and unpinned versions. Confirm postinstall fields remain empty, run the full `npm ls` pin check, audit, and commit the lockfile. Task 2 may begin only after this dependency/config verification passes.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm ls @supabase/supabase-js@2.110.7 @supabase/ssr@0.12.3 next-intl@4.13.2 shadcn@4.13.1 class-variance-authority@0.7.1 clsx@2.1.1 tailwind-merge@3.6.0 lucide-react@1.25.0 radix-ui@1.6.4 tw-animate-css@1.4.0 &amp;&amp; npm audit --omit=dev</automated>
  </verify>
  <done>The lockfile resolves every audited pin and `components.json` matches the resolved Radix/new-york/zinc contract before any component generation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the bounded primitives and apply the civic theme</name>
  <files>frontend/src/app/globals.css, frontend/src/components/ui/button.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/separator.tsx, frontend/src/components/ui/dropdown-menu.tsx, frontend/src/components/ui/alert.tsx</files>
  <action>Only after Task 1 verification, run the pinned CLI dry-run and add exactly `button input label card separator dropdown-menu alert`; no sidebar closure or other registry item belongs in this plan. Review every generated file as application source and reject imports/packages outside the audited closure. Then map shadcn variables to the approved light-only UI-SPEC: background `#FFFFFF`, secondary `#F0F4F3`, reserved primary `#0F766E`, ink `#14201D`, muted text `#5C6B66`, border `#D5DEDB`, destructive `#B42318`. Establish Source Sans 3-compatible 14/16/20/28px roles, weights 400/600, 8-point spacing, 44px primary targets, visible accent focus rings, WCAG 2.2 AA contrast, and reduced-motion instant state changes. Remove automatic dark palette and exclude purple gradients, neon glow, cyber treatment, and cream/sand body backgrounds. Verify existing `/report` and `/login` inherit legible surfaces without implementing later-phase polish.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Existing pages compile and visibly inherit the approved civic tokens, typography, focus, contrast, touch-target, and reduced-motion behavior.</done>
</task>

</tasks>

<must_haves>
- The shadcn configuration resolves the research question exactly.
- Dependency pins and generated source pass audit/review.
- Global visual tokens match UI-SPEC without deferred page polish.
</must_haves>

## Artifacts this phase produces

- Audited frontend dependency lock.
- Official shadcn core primitives.
- Shared CityMind civic theme.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package registry to source tree | Dependencies and generated registry source enter the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Tampering | dependencies/generated UI | high | mitigate | Prior blocking approval, exact pins, resolved config assertions, source diff review, lockfile, npm audit. |
| T-01-SC | Tampering | npm installs | high | mitigate | Consume the one Plan 01 SUS approval and rerun postinstall/package-tree checks. |
</threat_model>

<verification>
Run dependency/audit checks, lint, and production build; inspect `components.json` and representative existing routes against UI-SPEC tokens.
</verification>

<success_criteria>
- shadcn/ui is installed and themed consistently with the resolved approved configuration.
- No unaudited/deprecated package or third-party registry is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-03-SUMMARY.md` when done.
</output>
