---
phase: 01-supabase-foundation
plan: "06"
type: execute
wave: 5
depends_on: [01-05]
files_modified:
  - frontend/src/components/ui/sidebar.tsx
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/hooks/use-mobile.ts
  - frontend/src/app/dashboard/layout.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/app/dashboard/reports/[reportId]/page.tsx
  - frontend/tests/dashboard-shell.test.mjs
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "Authenticated officers see a responsive dashboard with Reports, Export, Settings, and Logout; Reports is active."
    - "Anonymous/wrong-role users cannot render dashboard content."
    - "The shell shows approved placeholder/empty/error states without deferred Phase 3 data-table/export/resolve functionality."
  artifacts:
    - path: "frontend/src/app/dashboard/layout.tsx"
      provides: "Protected responsive dashboard/sidebar chrome"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Reports-active placeholder and empty/error states"
  key_links:
    - from: "frontend/src/app/dashboard/layout.tsx"
      to: "frontend/src/lib/auth.ts"
      via: "server-side officer/admin guard"
      pattern: "requireOfficer|dashboard"
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "backend/app/api/reports.py"
      via: "minimal bearer-authenticated report read through officerFetch"
      pattern: "officerFetch|reports"
---

<objective>
**As an** authenticated officer, **I want to** enter a responsive, navigable dashboard shell, **so that** I have a protected workspace ready for later report-triage features.

Purpose: Complete D-17/DASH-01 presentation on top of the verified auth bridge without mixing Phase 3 functionality into Phase 1.
Output: Official sidebar closure, protected dashboard layout, four-item navigation, placeholder/empty/error content, and dashboard-scoped detail route.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-supabase-foundation/01-CONTEXT.md
@.planning/phases/01-supabase-foundation/01-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-PATTERNS.md
@.planning/phases/01-supabase-foundation/01-05-SUMMARY.md
@frontend/src/app/page.tsx
@frontend/src/components/StatusActions.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the protected responsive officer navigation shell</name>
  <files>frontend/src/components/ui/sidebar.tsx, frontend/src/components/ui/sheet.tsx, frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/skeleton.tsx, frontend/src/hooks/use-mobile.ts, frontend/src/app/dashboard/layout.tsx, frontend/tests/dashboard-shell.test.mjs</files>
  <behavior>
    - Only an officer/admin principal can render `/dashboard`; missing/wrong-role sessions redirect to `/login`.
    - Desktop sidebar and mobile sheet show Reports, Export, Settings, and Logout with Reports active.
    - Logout displays exact confirm/dismiss copy before submitting sign-out.
  </behavior>
  <action>Write the failing dashboard shell contract first. Using the already installed pinned `shadcn@4.13.1`, add the official sidebar closure (`sidebar`, `sheet`, `tooltip`, `skeleton`, `use-mobile`) without another install/checkpoint or third-party registry. Build a server-guarded `/dashboard` layout consuming Plan 05 auth. Navigation is exactly Reports, Export, Settings, Logout; Reports is active, the collapsed rail uses 48px rows, mobile uses the official sheet, and Logout asks “Sign out of the officer dashboard?” with “Sign out”/“Stay signed in”. Apply UI-SPEC colors, 14px nav labels, focus, keyboard, reduced-motion, and restrained accent use. Export/Settings are visibly shell destinations, not implemented Phase 3 features.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/dashboard-shell.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Authorized users render the responsive four-item shell with exact logout behavior; unauthorized users redirect; official sidebar source is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Show the real report-read placeholder and dashboard-scoped detail route</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/app/dashboard/reports/[reportId]/page.tsx, frontend/tests/dashboard-shell.test.mjs</files>
  <behavior>
    - Dashboard performs one minimal authenticated Supabase-backed report read through FastAPI/BFF.
    - Empty result shows exact UI-SPEC heading/body; load failure shows exact shell error.
    - Report links resolve under `/dashboard/reports/[reportId]` and remain protected.
  </behavior>
  <action>Move only the smallest useful portion of the former officer root into `/dashboard`: a bearer-authenticated minimal report read proving the walking-skeleton DB read, exact “No reports to show yet” empty state/body, and exact shell-load error. Move report detail routing under `/dashboard/reports/[reportId]` per D-17 and update internal links. Preserve advisory-AI language and officer final authority. Do not implement the deferred table, filter panel, pagination, export, resolve notes, charts, or polished detail workflow.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/dashboard-shell.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; test -f src/app/dashboard/reports/'[reportId]'/page.tsx</automated>
  </verify>
  <done>A seeded officer reaches the protected shell, sees a real read/empty/error state, and opens dashboard-scoped detail; deferred dashboard features remain absent.</done>
</task>

</tasks>

<must_haves>
- Dashboard layout is role-protected and responsive.
- All four required navigation items and exact UI-SPEC states exist.
- One real report read completes the walking skeleton without Phase 3 scope.
</must_haves>

## Artifacts this phase produces

- Official responsive sidebar primitives.
- Protected dashboard layout and Reports placeholder.
- Dashboard-scoped report detail route and shell contract tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public routes to dashboard | Authenticated role state gates protected UI/data. |
| Dashboard to BFF/FastAPI | Officer report reads cross bearer-authenticated boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Elevation of Privilege | dashboard layout | critical | mitigate | Server role guard plus BFF/FastAPI/RLS defense in depth. |
| T-01-23 | Information Disclosure | report placeholder/detail | high | mitigate | Protected routes, bearer bridge, and RLS-scoped reads only. |
| T-01-24 | Spoofing | logout/navigation | low | mitigate | Server sign-out action and explicit confirmation; no client-trusted role state. |
</threat_model>

<verification>
Run dashboard contract tests, lint, and build; verify desktop/mobile shell, anonymous redirect, seeded officer render, exact empty/error/logout copy, minimal report read, and protected detail route.
</verification>

<success_criteria>
- The protected dashboard shell renders for officer/admin users only.
- Navigation, responsive behavior, placeholder states, and detail routing meet UI-SPEC/DASH-01.
- No deferred Phase 3 capability is implemented.
</success_criteria>

<output>
Create `.planning/phases/01-supabase-foundation/01-06-SUMMARY.md` when done.
</output>
