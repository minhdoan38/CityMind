---
phase: 09-self-help-vs-government-routing
plan: "03"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/server/routing/playbooks.ts
  - src/server/services/citizen-status.ts
  - src/server/services/citizen-status.test.ts
  - src/server/services/citizen-escalate.ts
  - src/server/repositories/reports.ts
  - src/app/api/public/reports/escalate/route.ts
  - messages/en.json
  - messages/vi.json
autonomous: true
requirements: [ROUT-04, ROUT-05, ROUT-08]
must_haves:
  truths:
    - D-05/D-06: Self-help shows 3–5 actionable playbook steps with optional links — static not AI-generated
    - D-07: Playbook content in-repo via playbooks.ts + messages/en.json + messages/vi.json
    - D-08: Citizen API hides category/severity/priority/summary/recommendation when routing_destination is self_help
    - D-09: Guidance appears on existing /[locale]/status page only
    - D-10: Self-help service_step uses adapted workflow (received → self_help_guidance → resolved/rejected)
    - D-04: Citizen escalate re-routes self_help → government after initial routing decision
    - D-11: can_escalate true on self_help path; escalate flips to government queue
    - D-12: Escalate keeps same access token — no re-issue
  artifacts:
    - path: src/server/routing/playbooks.ts
      provides: PLAYBOOK_BY_CATEGORY, resolvePlaybookId
    - path: src/app/api/public/reports/escalate/route.ts
      provides: POST escalate handler
      exports: [POST]
    - path: src/server/services/citizen-status.ts
      provides: extended projectCitizenTriageView with playbook_id and can_escalate
  key_links:
    - from: src/app/api/public/reports/escalate/route.ts
      to: escalate_report_to_government RPC
      via: hashAccessToken + service call
      pattern: escalate_report_to_government
    - from: src/server/services/citizen-status.ts
      to: projectCitizenTriageView
      via: routing_destination === self_help branch
      pattern: self_help_guidance
    - from: src/server/repositories/reports.ts
      to: getCitizenStatus
      via: SELECT routing_destination
      pattern: routing_destination
---

<objective>
Deliver citizen-facing routing slice: static bilingual playbooks, self-help status projection (AI fields hidden), and token-bound escalate API — parallel to worker hook in 09-02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md
@.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md
@src/server/services/citizen-status.ts
@src/app/api/public/reports/status/route.ts
@PRODUCT.md

<interfaces>
From src/server/services/citizen-status.ts (extend):
- CitizenServiceStep add "self_help_guidance"
- CitizenStatusResponse add playbook_id?: string | null, can_escalate?: boolean
- Never expose routing_reason or routing_policy_version in public response (RESEARCH Pitfall 4)

From src/server/repositories/reports.ts:
- CitizenStatusRawPayload — add routing_destination: string | null
- getCitizenStatus select must include routing_destination

From src/app/api/public/reports/status/route.ts (pattern):
- handleCitizenStatusRequest: rate limit + Zod body + uniform 401

New escalate route:
- POST { report_id, token } — same schema as status
- enforceStatusRateLimit (CIT-04 pattern)
- citizenStatusUnauthorized on token mismatch
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playbook catalog and bilingual message keys</name>
  <files>src/server/routing/playbooks.ts, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>messages/en.json</file>
    <file>messages/vi.json</file>
    <file>PRODUCT.md</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
    <file>src/server/routing/policy.ts</file>
  </read_first>
  <behavior>
    - PLAYBOOK_BY_CATEGORY maps pothole, waste, streetlight, graffiti → playbook id per D-22.
    - resolvePlaybookId(category) returns id or null for unknown categories.
    - messages/en.json and messages/vi.json under public.routing.playbooks.{id}: title, steps array (3–5 strings), optional links array with label+url per D-06.
    - Copy is calm civic FAQ tone — no AI theater, no confidence language per PRODUCT.md.
    - Escalate CTA keys: public.routing.escalateTitle, public.routing.escalateCta ("Still need city help?" direction per D-11).
  </behavior>
  <action>
    Create src/server/routing/playbooks.ts with PLAYBOOK_BY_CATEGORY and resolvePlaybookId(category: string | null): string | null. Add EN/VI message entries for all four eligible categories with 3–5 step bullets each plus at least one optional external link where sensible (e.g. city waste schedule). Add escalate CTA strings in both locales. Do not fetch playbooks from database. IDs in TS; all citizen-facing strings in message catalogs per D-07 discretion.
  </action>
  <acceptance_criteria>
    <criterion>playbooks.ts exports PLAYBOOK_BY_CATEGORY with graffiti, waste, pothole, streetlight keys.</criterion>
    <criterion>messages/en.json and messages/vi.json contain public.routing.playbooks.pothole.title and steps arrays with length 3–5.</criterion>
    <criterion>Escalate CTA keys exist in both locale files.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$p=Get-Content -Raw src/server/routing/playbooks.ts; if($p -notmatch 'graffiti'){exit 1}; $e=Get-Content -Raw messages/en.json; if($e -notmatch 'public.routing.playbooks'){exit 1}"</automated>
  </verify>
  <done>Static playbook catalog and bilingual copy ready for status projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Citizen status projection for self-help path</name>
  <files>src/server/repositories/reports.ts, src/server/services/citizen-status.ts, src/server/services/citizen-status.test.ts</files>
  <read_first>
    <file>src/server/services/citizen-status.ts</file>
    <file>src/server/repositories/reports.ts</file>
    <file>src/server/routing/playbooks.ts</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-05-SUMMARY.md</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - getCitizenStatus selects routing_destination from reports.
    - When triage_status completed AND routing_destination self_help AND status not resolved/rejected: service_step self_help_guidance, playbook_id from resolvePlaybookId(category), can_escalate true per D-10/D-11.
    - On self_help path: category, severity, priority, summary, recommendation all null in API response per D-08 — even though DB has triage values.
    - Government path unchanged from Phase 8 — AI fields revealed when triage completed.
    - Escalated self_help (routing_destination government after citizen escalate): revert to Phase 8 officer_review flow with AI reveal when completed.
    - Tests cover self_help hides AI fields, government shows AI when completed, can_escalate true only on active self_help.
  </behavior>
  <action>
    Extend CitizenStatusRawPayload with routing_destination. Update getCitizenStatus SELECT. Extend projectCitizenTriageView: after pending/processing and failed/manual_review branches, before government completed branch, insert self_help branch when routing_destination === self_help and status not terminal. Set playbook_id via resolvePlaybookId(row.category) — use DB category for playbook lookup only; do not expose category in response. Add citizen-status.test.ts cases for self_help projection and AI suppression. Preserve CIT-03 behavior on lookup — no routing fields leak on 401 paths.
  </action>
  <acceptance_criteria>
    <criterion>citizen-status.test.ts asserts self_help response has null category/summary and service_step self_help_guidance.</criterion>
    <criterion>citizen-status.test.ts asserts government completed response still includes category and summary.</criterion>
    <criterion>Response JSON shape never includes routing_reason or routing_policy_version.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-status.test.ts</automated>
  </verify>
  <done>Citizen status API returns self-help playbook metadata without AI triage narrative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Citizen escalate API with token binding and rate limit</name>
  <files>src/server/services/citizen-escalate.ts, src/server/services/citizen-escalate.test.ts, src/app/api/public/reports/escalate/route.ts, src/server/repositories/reports.ts</files>
  <read_first>
    <file>src/app/api/public/reports/status/route.ts</file>
    <file>src/server/services/citizen-status.ts</file>
    <file>supabase/migrations/20260722130001_routing_columns.sql</file>
    <file>src/server/security/rate-limit.ts</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md</file>
  </read_first>
  <behavior>
    - POST /api/public/reports/escalate body { report_id, token } validated with Zod.
    - hashAccessToken + tokenBindsReport — uniform 401 on failure per CIT-03.
    - enforceStatusRateLimit on request per CIT-04 (same limiter as status or dedicated escalate:{ip} key).
    - Only escalates when routing_destination is self_help — return 409 or 422 if already government (idempotent-safe: if already government, return 200 with current state).
    - Calls escalate_report_to_government RPC or repository wrapper; sets routing_reason citizen_escalated; appends status_events note; does NOT create new access_token per D-12.
    - Returns minimal json { ok: true, routing_destination: "government" } — no token in response.
  </behavior>
  <action>
    Create citizen-escalate.ts with handleCitizenEscalateRequest mirroring handleCitizenStatusRequest structure. Add escalateReportToGovernment in repositories/reports.ts calling escalate_report_to_government RPC with report_id and token hash. Create src/app/api/public/reports/escalate/route.ts exporting POST delegating to handler. Create src/server/services/citizen-escalate.test.ts with cases: valid token on self_help report returns 200 and government destination; invalid token returns 401; rate limit returns 429. Do not expose internal reason codes in response body.
  </action>
  <acceptance_criteria>
    <criterion>Escalate handler test: valid token on self_help report returns 200 and government destination.</criterion>
    <criterion>Escalate handler test: invalid token returns 401 with same shape as status unauthorized.</criterion>
    <criterion>No code path issues new access_token on escalate.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-escalate.test.ts</automated>
  </verify>
  <done>Citizens can escalate self-help reports to government queue with existing token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen → POST /escalate | Untrusted body; token hash validation |
| Citizen API response | Must not leak routing audit internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Spoofing | Escalate without valid token | mitigate | hashAccessToken + tokenBindsReport; uniform 401 |
| T-09-08 | Denial of Service | Escalate brute force | mitigate | enforceStatusRateLimit per CIT-04 |
| T-09-09 | Information Disclosure | routing_reason in JSON | mitigate | Omit audit fields from citizen response |
| T-09-10 | Tampering | Escalate government report | mitigate | Guard mutation to self_help only; idempotent if already government |
| T-09-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/services/citizen-status.test.ts
npm run test:unit -- src/server/services/citizen-escalate.test.ts
Wave 2 partial: npm run test:unit -- src/server/routing/ src/server/services/citizen-
</verification>

<success_criteria>
1. Self-help citizen API returns playbook_id and can_escalate with AI fields null
2. POST /api/public/reports/escalate flips routing_destination to government with same token
3. Bilingual playbook copy exists for all four eligible categories
</success_criteria>

<output>
Create `.planning/phases/09-self-help-vs-government-routing/09-03-SUMMARY.md` when done
</output>
