---
phase: 09-self-help-vs-government-routing
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/server/routing/apply-routing.ts
  - src/server/routing/apply-routing.test.ts
  - src/server/triage/service.ts
  - src/server/triage/service.test.ts
autonomous: true
requirements: [ROUT-01, ROUT-02, ROUT-08]
must_haves:
  truths:
    - D-01: Routing runs only after terminal triage — never at intake or during pending/processing
    - D-20: applyRoutingForReport executes in same worker pass as finishTriageRun
    - D-23: manual_review and failed dispositions force government routing with non-null routing_destination
    - D-02: Routing uses triage output fields only — no second AI call
    - Completed triage with eligible analysis can set routing_destination self_help
  artifacts:
    - path: src/server/routing/apply-routing.ts
      provides: applyRoutingForReport
      exports: [applyRoutingForReport]
    - path: src/server/triage/service.ts
      provides: runTriageForReport calls applyRoutingForReport on all terminal paths
      contains: applyRoutingForReport
  key_links:
    - from: src/server/triage/service.ts
      to: applyRoutingForReport
      via: await after finishTriageRun on completed|manual_review|failed returns
      pattern: applyRoutingForReport
    - from: src/server/routing/apply-routing.ts
      to: evaluateRoutingPolicy
      via: import from policy.ts
      pattern: evaluateRoutingPolicy
    - from: src/server/routing/apply-routing.ts
      to: reports table
      via: UPDATE routing_destination, routing_reason, routing_policy_version, routed_at
      pattern: routing_destination
---

<objective>
Wire deterministic routing into the triage worker: after every terminal disposition, persist routing decision on the report row using evaluateRoutingPolicy from 09-01.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md
@.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md
@src/server/triage/service.ts
@src/server/routing/policy.ts

<interfaces>
From src/server/triage/service.ts:
- runTriageForReport(reportId, deps) → { reportId, disposition: "completed" | "manual_review" | "failed" | "retry" }
- Terminal return paths: lines ~252-253 (failed), ~298-299 (failed), ~315-316 (completed), ~342-343 (manual_review), ~346-347 (manual_review fallback)
- TriageServiceDeps allows injecting finishTriageRun for tests

From src/server/routing/policy.ts (09-01):
- evaluateRoutingPolicy({ triageStatus, category, severity, priority, confidence })
- ROUTING_POLICY_VERSION

New apply-routing.ts:
- applyRoutingForReport(client, reportId, context: { disposition, analysis?: ReportAnalysis | null })
- On disposition completed: pass triage_status completed + analysis fields to evaluateRoutingPolicy
- On disposition manual_review|failed: force government with reasonCode triage_manual_or_failed (skip eligibility per D-23)
- UPDATE reports SET routing_destination, routing_reason, routing_policy_version, routed_at = now()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyRoutingForReport service module</name>
  <files>src/server/routing/apply-routing.ts, src/server/routing/apply-routing.test.ts</files>
  <read_first>
    <file>src/server/routing/policy.ts</file>
    <file>src/server/triage/service.ts</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md</file>
    <file>supabase/migrations/20260722130001_routing_columns.sql</file>
  </read_first>
  <behavior>
    - applyRoutingForReport loads nothing if report missing — no throw on missing row (log-safe no-op or early return).
    - completed + eligible analysis → may set self_help per policy.
    - manual_review|failed disposition → always government regardless of analysis fields per D-23.
    - Persists routing_reason as machine reasonCode, routing_policy_version from decision, routed_at UTC now per D-18/D-19.
    - Does not mutate triage_status or access_tokens.
    - Unit tests mock Supabase client .update chain; assert correct payload per disposition.
  </behavior>
  <action>
    Create src/server/routing/apply-routing.ts exporting applyRoutingForReport(client, reportId, context). Import evaluateRoutingPolicy from ./policy. For completed disposition, map analysis category/severity/priority/confidence into policy input with triageStatus completed. For manual_review and failed, call policy with triageStatus set accordingly (policy returns government). Single UPDATE on reports by report_id. Add apply-routing.test.ts with cases: completed pothole severity 2 → self_help; completed severity 4 → government; manual_review → government with triage_manual_or_failed; failed empty description path → government.
  </action>
  <acceptance_criteria>
    <criterion>apply-routing.test.ts passes for completed self_help, completed government, and forced government on manual_review/failed.</criterion>
    <criterion>UPDATE payload includes routing_policy_version matching ROUTING_POLICY_VERSION.</criterion>
    <criterion>No provider.analyze or HTTP calls in apply-routing module.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/routing/apply-routing.test.ts</automated>
  </verify>
  <done>Routing persistence module tested in isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook applyRoutingForReport into runTriageForReport</name>
  <files>src/server/triage/service.ts, src/server/triage/service.test.ts</files>
  <read_first>
    <file>src/server/triage/service.ts</file>
    <file>src/server/routing/apply-routing.ts</file>
    <file>src/server/triage/service.test.ts</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - After finishTriageRun on every terminal path (completed, manual_review, failed), await applyRoutingForReport before return per D-20.
    - Retry disposition must NOT call applyRoutingForReport per D-01.
    - handleInfraFailure paths that finish with failed or manual_review also invoke routing.
    - service.test.ts mocks applyRoutingForReport via deps injection or vi.mock; asserts called once with correct disposition on completed and manual_review paths.
    - Pending/processing code paths unchanged — routing_destination stays NULL until terminal triage.
  </behavior>
  <action>
    Import applyRoutingForReport in service.ts. Add optional applyRoutingForReport to TriageServiceDeps for test injection. After each await finishRun(...) that precedes a terminal return (not retry), call applyRoutingForReport with reportId, disposition, and analysis when available (null on failed paths without structured analysis). Extend service.test.ts: mock applyRoutingForReport; verify called on completed success with analysis object; verify called on manual_review after validation exhaustion; verify NOT called when disposition is retry. Do not route at intake or before finishTriageRun.
  </action>
  <acceptance_criteria>
    <criterion>service.test.ts asserts applyRoutingForReport invoked on completed and manual_review terminal returns.</criterion>
    <criterion>service.test.ts asserts applyRoutingForReport NOT invoked on retry disposition.</criterion>
    <criterion>All existing triage service tests still pass.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/service.test.ts</automated>
  </verify>
  <done>Worker applies routing automatically on every terminal triage outcome per D-01/D-20.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker service-role → reports UPDATE | Only applyRoutingForReport writes routing columns after triage |
| Citizen/client | Cannot invoke applyRoutingForReport — no public export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Client sets routing_destination | mitigate | Routing only in service-role worker path; no public API in this plan |
| T-09-05 | Denial of Service | Double routing on retry | mitigate | Skip applyRoutingForReport when disposition is retry |
| T-09-06 | Elevation | self_help on manual_review | mitigate | D-23 forced government in policy + apply-routing for non-completed dispositions |
| T-09-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/routing/apply-routing.test.ts
npm run test:unit -- src/server/triage/service.test.ts
Wave 2 partial: npm run test:unit -- src/server/triage/ src/server/routing/
</verification>

<success_criteria>
1. Every terminal triage disposition triggers applyRoutingForReport in runTriageForReport
2. Retry paths never set routing_destination
3. manual_review/failed always persist routing_destination government
</success_criteria>

<output>
Create `.planning/phases/09-self-help-vs-government-routing/09-02-SUMMARY.md` when done
</output>
