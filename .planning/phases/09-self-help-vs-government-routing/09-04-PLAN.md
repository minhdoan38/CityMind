---
phase: 09-self-help-vs-government-routing
plan: "04"
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - src/server/officer/filters.ts
  - src/server/repositories/reports.ts
  - src/server/repositories/reports.test.ts
  - src/server/services/officer-read.ts
  - src/server/services/officer-write.ts
  - src/app/api/officer/reports/[reportId]/routing/route.ts
  - src/components/reports/RoutingDestinationBadge.tsx
  - src/components/reports/ReportsTable.tsx
  - src/components/reports/ReportsFilters.tsx
  - src/components/reports/types.ts
  - src/app/dashboard/reports/[reportId]/page.tsx
  - src/app/[locale]/status/page.tsx
  - messages/en.json
  - messages/vi.json
  - tests/citizen-status.test.mjs
  - tests/dashboard-table.test.mjs
autonomous: true
requirements: [ROUT-03, ROUT-04, ROUT-05, ROUT-06, ROUT-07]
must_haves:
  truths:
    - D-03/D-13: Officer default list shows routing_destination IS NULL OR government; self_help excluded unless filter chip active
    - D-15: Reports table shows Self-help vs Government destination badge
    - D-16: Default triage_bucket sort preserved within government-default queue
    - D-04: Officer override re-routes self_help → government (or resolves) after initial routing decision
    - D-14: Officers can escalate self_help report to government and mark resolved from detail page
    - D-09/D-10/D-11: Status page renders playbook steps, adapted workflow stepper, and escalate CTA for self_help path
    - D-08: Status page does not render AI analysis panel on self_help path
  artifacts:
    - path: src/components/reports/RoutingDestinationBadge.tsx
      provides: destination badge column cell
    - path: src/components/reports/ReportsFilters.tsx
      provides: routing_destination filter chip (government default implicit)
    - path: src/app/[locale]/status/page.tsx
      provides: playbook panel + escalate button wired to POST /escalate
    - path: src/app/api/officer/reports/[reportId]/routing/route.ts
      provides: officer routing override (escalate to government, mark resolved)
  key_links:
    - from: src/server/repositories/reports.ts
      to: applyReportFilters
      via: .or('routing_destination.is.null,routing_destination.eq.government') default
      pattern: routing_destination\.is\.null
    - from: src/app/[locale]/status/page.tsx
      to: /api/public/reports/escalate
      via: fetch POST on CTA click
      pattern: /api/public/reports/escalate
    - from: src/components/reports/ReportsTable.tsx
      to: RoutingDestinationBadge
      via: routing_destination column
      pattern: RoutingDestinationBadge
---

<objective>
Complete Phase 9 UX: officer government-default queue with destination badges and overrides; citizen status page self-help playbook + escalate; preserve Phase 8 triage sort and bilingual copy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md
@.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md
@.planning/phases/08-async-triage-platform-refactor/08-05-PLAN.md
@src/components/reports/TriageStatusBadge.tsx
@src/components/reports/ReportsFilters.tsx
@src/app/[locale]/status/page.tsx

<interfaces>
From src/server/officer/filters.ts (extend ReportFilters):
- routing_destination?: "government_default" | "self_help" | "all" | null
- VALID_ROUTING_DESTINATIONS set for query param validation

From src/components/reports/types.ts (extend ReportRow):
- routing_destination: string | null

From src/components/reports/TriageStatusBadge.tsx (pattern for RoutingDestinationBadge):
- Badge variant by destination; text from t('dashboard.routing.badge_*')

Citizen status page (extend):
- When service_step === self_help_guidance: render playbook from t(`public.routing.playbooks.${playbook_id}`)
- Escalate button calls POST /api/public/reports/escalate with stored reportId + token from form state
- Hide AI analysis section when playbook_id present or service_step self_help_guidance
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Officer default government filter, repository tests, and destination badge</name>
  <files>src/server/officer/filters.ts, src/server/repositories/reports.ts, src/server/repositories/reports.test.ts, src/server/services/officer-read.ts, src/components/reports/RoutingDestinationBadge.tsx, src/components/reports/ReportsTable.tsx, src/components/reports/ReportsFilters.tsx, src/components/reports/types.ts, messages/en.json, messages/vi.json, tests/dashboard-table.test.mjs</files>
  <read_first>
    <file>src/server/officer/filters.ts</file>
    <file>src/server/repositories/reports.ts</file>
    <file>src/components/reports/ReportsTable.tsx</file>
    <file>src/components/reports/ReportsFilters.tsx</file>
    <file>src/components/reports/TriageStatusBadge.tsx</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Default routing filter (null or government_default): query.or('routing_destination.is.null,routing_destination.eq.government') per D-03/D-13.
    - routing_destination=self_help chip: .eq('routing_destination', 'self_help').
    - routing_destination=all: no routing filter applied.
    - mapOfficerReportRow includes routing_destination from DB.
    - ReportsTable adds Destination column with RoutingDestinationBadge — text labels Self-help / Government per D-15 (not color-only).
    - Default sort remains triage_bucket — no change to compareTriageBucket per D-16.
    - reports.test.ts asserts default filter OR clause applied; self_help chip uses eq filter.
    - dashboard-table.test.mjs asserts RoutingDestinationBadge import or destination column header string.
  </behavior>
  <action>
    Extend ReportFilters and parseReportFilters/validateReportFilters for routing_destination query param (default government_default when absent). Update applyReportFilters in reports.ts per RESEARCH Pattern 3. Extend OfficerReport type and mapOfficerReportRow. Create RoutingDestinationBadge.tsx mirroring TriageStatusBadge. Add column to ReportsTable. Add optional filter chip in ReportsFilters (e.g. "Include self-help") setting routing_destination=self_help; removing chip restores default. Add dashboard.routing.badge_self_help and badge_government keys to EN/VI messages. Extend reports.test.ts for filter behavior. Update dashboard-table.test.mjs.
  </action>
  <acceptance_criteria>
    <criterion>reports.test.ts passes default government filter including NULL routing_destination rows.</criterion>
    <criterion>ReportsTable renders RoutingDestinationBadge when routing_destination is set.</criterion>
    <criterion>Default list sort parameter triage_bucket unchanged from Phase 8.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/repositories/reports.test.ts &amp;&amp; npm run test:legacy -- tests/dashboard-table.test.mjs</automated>
  </verify>
  <done>Officer queue defaults to government-visible reports with destination badges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Officer routing override API and detail actions</name>
  <files>src/server/services/officer-write.ts, src/server/services/officer-write.test.ts, src/app/api/officer/reports/[reportId]/routing/route.ts, src/app/dashboard/reports/[reportId]/page.tsx, src/server/repositories/reports.ts</files>
  <read_first>
    <file>src/app/api/officer/reports/[reportId]/status/route.ts</file>
    <file>src/server/services/officer-write.ts</file>
    <file>src/app/dashboard/reports/[reportId]/page.tsx</file>
    <file>src/server/officer/guard.ts</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - POST /api/officer/reports/[reportId]/routing body { action: "escalate_to_government" | "mark_resolved", note?: string }.
    - requireOfficerContext() on all mutations per AUTH-04.
    - escalate_to_government: set routing_destination government, routing_reason officer_escalated, append status_events note per D-14.
    - mark_resolved: only when routing_destination self_help; uses updateReportStatus resolved with required note per D-14.
    - Detail page shows override buttons only when routing_destination is self_help and report not terminal.
    - RoutingDestinationBadge on detail header.
  </behavior>
  <action>
    Add handleOfficerRoutingOverrideRequest in officer-write.ts. Create src/server/services/officer-write.test.ts with cases: unauthenticated returns 401; escalate_to_government sets routing_destination government on self_help report; mark_resolved requires non-empty note and sets status resolved. Create routing/route.ts PATCH or POST delegating to handler. Add updateReportRouting in repositories/reports.ts for officer flip (or reuse update_report_with_status_event + routing UPDATE in transaction). On detail page: show "Move to government queue" and "Mark resolved" actions for self_help reports with Dialog + required note for resolved. Display RoutingDestinationBadge next to triage badge. Officer-only — no citizen access.
  </action>
  <acceptance_criteria>
    <criterion>Officer routing handler requires auth — unauthenticated returns 401.</criterion>
    <criterion>escalate_to_government sets routing_destination government on self_help report.</criterion>
    <criterion>mark_resolved requires non-empty note and sets status resolved.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/officer-write.test.ts</automated>
  </verify>
  <done>Officers can override self-help routing and resolve without citizen escalate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Citizen status page playbook UI, workflow steps, and escalate CTA</name>
  <files>src/app/[locale]/status/page.tsx, messages/en.json, messages/vi.json, tests/citizen-status.test.mjs</files>
  <read_first>
    <file>src/app/[locale]/status/page.tsx</file>
    <file>src/server/services/citizen-status.ts</file>
    <file>src/server/routing/playbooks.ts</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-05-PLAN.md</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - When API returns service_step self_help_guidance: render adapted 3-step workflow (received → guidance available → resolved/rejected) per D-10 — no officer-review step.
    - Render playbook title + steps from next-intl using playbook_id from API per D-09.
    - Show escalate CTA when can_escalate true; on click POST /api/public/reports/escalate with report_id + token from form; on success refetch status — same token per D-12.
    - Do NOT render AI category/severity/summary/recommendation blocks on self_help path per D-08.
    - Government path: preserve Phase 8 CitizenWorkflowStepper and AI reveal behavior.
    - citizen-status.test.mjs asserts EN catalog keys for self_help workflow and escalate CTA exist.
  </behavior>
  <action>
    Extend status/page.tsx: branch on service_step === self_help_guidance. Add SelfHelpWorkflowStepper or adapt existing stepper labels via new message keys public.statusWorkflow.selfHelp.*. Render playbook section from t.raw or steps array for playbook_id. Add escalate button with loading/error states; map 401/429 to existing catalog error strings (CIT-03 pattern). Hide AI analysis Card when on self_help path. Add any missing EN/VI keys for self-help workflow labels. Update tests/citizen-status.test.mjs for new catalog keys.
  </action>
  <acceptance_criteria>
    <criterion>status/page.tsx contains self_help_guidance branch and /api/public/reports/escalate fetch.</criterion>
    <criterion>citizen-status.test.mjs passes with self-help workflow and escalate catalog keys.</criterion>
    <criterion>AI analysis section not rendered when playbook_id is present in status response handling.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:legacy -- tests/citizen-status.test.mjs</automated>
  </verify>
  <done>End-to-end citizen self-help journey visible on status page with escalate to government.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer → routing override API | requireOfficerContext JWT |
| Officer default view | self_help hidden unless explicit filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Elevation | Citizen calls officer routing API | mitigate | requireOfficerContext on /api/officer/reports/*/routing |
| T-09-12 | Information Disclosure | Officer sees all self_help by default | mitigate | Default government_default filter per D-13 |
| T-09-13 | Tampering | Client-side routing filter bypass | mitigate | Server-side applyReportFilters enforces filter |
| T-09-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/repositories/reports.test.ts
npm run test:legacy -- tests/citizen-status.test.mjs tests/dashboard-table.test.mjs
Phase gate: npm run test
</verification>

<success_criteria>
1. Officer default queue excludes self_help unless filter chip enabled; pending reports still visible
2. Destination badge appears in table and detail for routed reports
3. Citizen status page shows playbook + escalate on self_help path without AI fields
4. Officer can escalate self_help to government and mark resolved with note
</success_criteria>

<output>
Create `.planning/phases/09-self-help-vs-government-routing/09-04-SUMMARY.md` when done
</output>
