---
phase: 09-self-help-vs-government-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260722130001_routing_columns.sql
  - supabase/tests/09_routing_contract.sql
  - src/server/routing/policy.ts
  - src/server/routing/policy.test.ts
  - src/server/domain/report-analysis.ts
  - src/server/domain/report-analysis.test.ts
  - src/server/officer/filters.ts
autonomous: false
requirements: [ROUT-01, ROUT-02, ROUT-03, ROUT-08]
user_setup:
  - service: supabase
    why: Push routing schema and run SQL contract gate
    env_vars: [SUPABASE_DB_URL]
must_haves:
  truths:
    - D-17/D-18: ROUTING_POLICY_VERSION constant and routing_* audit columns exist on reports
    - D-21: severity>=4 routes to government per policy unit tests
    - D-22: eligible category + severity<=2 routes to self_help; graffiti category accepted by CategorySchema
    - D-23: manual_review and failed triage_status always route to government
    - D-24: confidence<0.65 routes to government
    - D-03: routing_destination NULL allowed so pending/processing rows stay government-visible by default
  artifacts:
    - path: supabase/migrations/20260722130001_routing_columns.sql
      provides: routing_destination, routing_reason, routing_policy_version, routed_at + escalate RPC
    - path: src/server/routing/policy.ts
      provides: evaluateRoutingPolicy, ROUTING_POLICY_VERSION
      exports: [evaluateRoutingPolicy, ROUTING_POLICY_VERSION]
    - path: supabase/tests/09_routing_contract.sql
      provides: SQL contract for columns, CHECK constraint, escalate RPC
  key_links:
    - from: src/server/routing/policy.ts
      to: evaluateRoutingPolicy
      via: pure function consumed by apply-routing in 09-02
      pattern: export function evaluateRoutingPolicy
    - from: supabase/migrations/20260722130001_routing_columns.sql
      to: reports.routing_destination
      via: ALTER TABLE ADD COLUMN
      pattern: routing_destination
---

## Phase Goal

**As a** citizen or officer, **I want** reports routed to self-help guidance or the government queue after triage, **so that** low-risk issues get actionable playbooks and high-risk issues reach officers.

<objective>
Wave 1 foundation: routing schema + SQL contract + deterministic policy module with exhaustive unit tests. Unblocks worker hook (09-02) and citizen/officer surfaces (09-03/09-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md
@.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md
@.planning/phases/09-self-help-vs-government-routing/09-VALIDATION.md
@supabase/migrations/20260722120002_async_triage_audit.sql
@supabase/tests/08_async_triage_contract.sql
@src/server/validation/analysis-policy.ts
@src/server/domain/report-analysis.ts

<interfaces>
From src/server/validation/analysis-policy.ts (pattern to mirror):
- validateAnalysisPolicy(analysis, options) → PolicyResult
- CONFLICT_CONFIDENCE_CAP = 0.64 (routing uses 0.65 threshold per D-24)

From src/server/domain/report-analysis.ts (extend):
- CategorySchema — add "graffiti" for D-22 self-help eligibility

New exports (implement in src/server/routing/policy.ts):
- ROUTING_POLICY_VERSION = "1.0.0"
- evaluateRoutingPolicy(input) → { destination: "self_help" | "government", reasonCode: string, policyVersion: string }
- Evaluation order per RESEARCH: triage_not_completed → severity_or_priority → low_confidence → eligible_category_low_severity → default_government
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Routing schema migration and SQL contract scaffold</name>
  <files>supabase/migrations/20260722130001_routing_columns.sql, supabase/tests/09_routing_contract.sql</files>
  <read_first>
    <file>supabase/migrations/20260722120001_async_triage_intake.sql</file>
    <file>supabase/tests/08_async_triage_contract.sql</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-RESEARCH.md</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Migration adds routing_destination TEXT, routing_reason TEXT, routing_policy_version TEXT, routed_at TIMESTAMPTZ to public.reports per D-18.
    - CHECK constraint: routing_destination IS NULL OR IN ('self_help', 'government').
    - Partial index on routing_destination WHERE NOT NULL.
    - escalate_report_to_government RPC: token-validated path flips routing_destination to government, sets routing_reason to citizen_escalated, appends status_events note; SECURITY DEFINER; service_role grant only per Phase 8 RPC pattern.
    - SQL contract: seed completed report, assert NULL routing allowed on pending intake, assert CHECK rejects invalid destination, assert escalate RPC sets government + reason.
  </behavior>
  <action>
    Create immutable migration 20260722130001_routing_columns.sql per RESEARCH Pattern 6. Add escalate_report_to_government(p_report_id, p_token_hash) RPC mirroring create_intake_report_with_access_token grant/revoke pattern — validate token hash binds to report_id before UPDATE. Create supabase/tests/09_routing_contract.sql with _test_assert helper matching 08_async_triage_contract.sql style. Cover: (1) pending intake row has NULL routing_destination per D-03, (2) CHECK rejects invalid routing_destination value, (3) escalate RPC on self_help row sets government + routing_reason citizen_escalated + status_events row. Do not edit applied Phase 8 migrations.
  </action>
  <acceptance_criteria>
    <criterion>Migration contains routing_destination CHECK, routed_at column, and escalate_report_to_government RPC.</criterion>
    <criterion>09_routing_contract.sql asserts NULL routing on pending intake and escalate flips destination per D-11/D-12.</criterion>
    <criterion>No routing logic in SQL triggers — policy stays TypeScript per D-17.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260722130001_routing_columns.sql; if($s -notmatch 'routing_destination' -or $s -notmatch 'escalate_report_to_government'){exit 1}"</automated>
  </verify>
  <done>Schema and SQL contract files exist and are review-ready before push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push routing schema and run SQL contract</name>
  <files>supabase/migrations/20260722130001_routing_columns.sql, supabase/tests/09_routing_contract.sql</files>
  <read_first>
    <file>scripts/run-supabase-sql.mjs</file>
    <file>supabase/migrations/20260722130001_routing_columns.sql</file>
    <file>supabase/tests/09_routing_contract.sql</file>
    <file>.planning/STATE.md</file>
  </read_first>
  <action>
    [BLOCKING] Requires SUPABASE_DB_URL in .env.local. If missing, stop and ask operator to set direct Postgres URL — do not invent credentials. Apply migration: node scripts/run-supabase-sql.mjs supabase/migrations/20260722130001_routing_columns.sql. If Phase 8 migrations not yet applied, apply 20260722120001 and 20260722120002 first. Then run contract gate: node scripts/run-supabase-sql.mjs supabase/tests/09_routing_contract.sql. Stop on any migration or ASSERTION FAILED. Downstream plans 09-02/09-03 integration tests against live DB remain blocked until this passes.
  </action>
  <acceptance_criteria>
    <criterion>Live database has routing_* columns and escalate_report_to_government callable by service_role.</criterion>
    <criterion>09_routing_contract.sql exits 0 with no ASSERTION FAILED.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-supabase-sql.mjs supabase/tests/09_routing_contract.sql</automated>
  </verify>
  <done>Routing schema is live; worker and API plans unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Routing policy module, category schema, and unit tests</name>
  <files>src/server/routing/policy.ts, src/server/routing/policy.test.ts, src/server/domain/report-analysis.ts, src/server/domain/report-analysis.test.ts, src/server/officer/filters.ts</files>
  <read_first>
    <file>src/server/validation/analysis-policy.ts</file>
    <file>src/server/domain/report-analysis.ts</file>
    <file>src/server/officer/filters.ts</file>
    <file>prompt/citymind_ai_triage_structured_output_evaluator.json</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-CONTEXT.md</file>
  </read_first>
  <behavior>
    - ROUTING_POLICY_VERSION semver exported; every decision includes policyVersion per D-19.
    - reasonCode is machine string (e.g. severity_or_priority, eligible_category_low_severity) — not citizen-facing per RESEARCH Pitfall 4.
    - D-23: triage_status manual_review|failed → government / triage_manual_or_failed before eligibility checks.
    - D-21: severity>=4 OR priority high|critical → government.
    - D-24: confidence&lt;0.65 → government (threshold constant CONFIDENCE_GOV_THRESHOLD = 0.65).
    - D-22: categories graffiti|waste|pothole|streetlight with severity&lt;=2 and gates pass → self_help.
    - CategorySchema and VALID_CATEGORIES gain graffiti so triage can produce D-22-eligible category.
    - Policy test matrix: at least one case per reasonCode; graffiti eligible path; default_government fallback.
  </behavior>
  <action>
    Create src/server/routing/policy.ts with evaluateRoutingPolicy pure function per RESEARCH Pattern 1 — no Supabase imports. Add policy.test.ts covering D-21..D-24 matrix exhaustively. Extend CategorySchema enum with graffiti; add graffiti to VALID_CATEGORIES in filters.ts; update report-analysis.test.ts if category list asserted. Do NOT implement applyRoutingForReport here (09-02). Do NOT call policy from UI — server/worker only per D-02.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:unit -- src/server/routing/policy.test.ts passes all D-21..D-24 cases.</criterion>
    <criterion>CategorySchema.options includes graffiti and VALID_CATEGORIES includes graffiti.</criterion>
    <criterion>ROUTING_POLICY_VERSION is semver string exported from policy.ts.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/routing/policy.test.ts &amp;&amp; npm run test:unit -- src/server/domain/report-analysis.test.ts</automated>
  </verify>
  <done>Deterministic routing policy is tested and ready for worker hook in 09-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service role → routing columns | Only worker/escalate RPC may write routing_destination |
| Policy module → callers | Pure function; no I/O; version constant for audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | routing_destination CHECK | mitigate | DB CHECK constraint limits values to self_help/government |
| T-09-02 | Repudiation | routing_policy_version | mitigate | Semver persisted on every decision per D-19 |
| T-09-03 | Information Disclosure | routing_reason in public API | mitigate | Machine codes only in DB; citizen API must not expose in 09-03 |
| T-09-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
After Task 2: node scripts/run-supabase-sql.mjs supabase/tests/09_routing_contract.sql
Per plan: npm run test:unit -- src/server/routing/policy.test.ts
Wave 1 gate: npm run test:unit -- src/server/routing/
</verification>

<success_criteria>
1. routing_* columns and escalate RPC exist on live Supabase after blocking push
2. evaluateRoutingPolicy unit tests cover D-21..D-24 with ROUTING_POLICY_VERSION
3. graffiti category is valid in CategorySchema and officer VALID_CATEGORIES
</success_criteria>

<output>
Create `.planning/phases/09-self-help-vs-government-routing/09-01-SUMMARY.md` when done
</output>
