---
phase: 07-google-free-self-hosted-platform
plan: "07"
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - supabase/migrations/20260721130002_postgres_analytics.sql
  - supabase/tests/07_postgres_analytics.sql
  - frontend/src/server/repositories/analytics.ts
  - frontend/src/server/repositories/analytics.test.ts
  - frontend/src/app/api/v1/analytics/route.ts
  - frontend/src/app/api/v1/public/stats/route.ts
  - frontend/src/app/api/public/stats/route.ts
  - frontend/src/app/dashboard/analytics/page.tsx
  - frontend/src/components/PublicStatsStrip.tsx
  - frontend/tests/analytics-shell.test.mjs
autonomous: false
requirements: [SELFHOST-03]
requirements_addressed: [SELFHOST-03]
decisions_addressed: [D-08, D-09]
user_setup:
  - service: supabase
    why: Push and live-test analytics schema
    env_vars: [SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, SUPABASE_DB_URL]
    dashboard_config: [Link the existing self-hosted project]
must_haves:
  truths:
    - Officer analytics and privacy-thresholded public stats come only from self-hosted Supabase Postgres.
    - Existing 30-day defaults, 366-day maximum, empty signal, category/SLA/hotspot semantics, and k>=3 disclosure remain unchanged.
  artifacts:
    - path: supabase/migrations/20260721130002_postgres_analytics.sql
      provides: Postgres analytics views/RPCs/indexes
    - path: frontend/src/server/repositories/analytics.ts
      provides: Typed Postgres analytics mapping
  key_links:
    - from: frontend/src/app/dashboard/analytics/page.tsx
      to: frontend/src/server/repositories/analytics.ts
      via: direct user-scoped server call
---

## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Replace runtime BigQuery analytics with a complete Postgres-backed officer/public analytics slice.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/05-analytics-pipeline/05-04-SUMMARY.md @backend/app/services/analytics.py @supabase/migrations/20260721120002_geo_pins_rpc.sql</context>
<ownership_note>Intentional 10-file analytics vertical slice: SQL contract, repository/tests, officer/public routes, both UI consumers, and shell regression form one Postgres-backed privacy-preserving workflow. File-by-file review found no incidental path; removing any item breaks parity, k-anonymity, a required surface, or its executable proof.</ownership_note>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Postgres analytics views/RPCs and parity tests</name>
  <files>supabase/migrations/20260721130002_postgres_analytics.sql, supabase/tests/07_postgres_analytics.sql, frontend/src/server/repositories/analytics.ts, frontend/src/server/repositories/analytics.test.ts</files>
  <read_first><file>backend/app/services/analytics.py</file><file>backend/tests/test_analytics_views.py</file><file>backend/tests/test_public_stats.py</file><file>supabase/migrations/20260721120002_geo_pins_rpc.sql</file><file>frontend/tests/contracts/fastapi-golden/analytics.json</file></read_first>
  <behavior>- Volume/category/SLA/hotspot results match canonical operational rows. - SLA opens at report creation and closes at first resolved/rejected event. - Public category rows disclose only counts >=3. - Officer max span is 366 days and empty is explicit.</behavior>
  <action>Create additive Postgres views/RPCs and supporting indexes against canonical `reports`/`status_events`, not copied derived warehouse tables. Prefer SECURITY INVOKER and narrow grants. Add deterministic SQL fixture parity for day/category/status aggregates, filters, SLA, hotspots, public threshold, role denial, and empty data. Do not copy BigQuery ETL watermarks or create a second source of truth.</action>
  <acceptance_criteria><criterion>SQL fixture results match golden analytics values.</criterion><criterion>Anonymous cannot call officer analytics; public RPC returns only threshold-safe aggregates.</criterion></acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260721130002_postgres_analytics.sql; if($s -notmatch 'security invoker' -or $s -notmatch 'k' -or $s -notmatch 'sla'){exit 1}"</automated></verify>
  <done>Postgres owns the analytics semantics and privacy boundary.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push and live-test Postgres analytics schema</name>
  <files>supabase/migrations/20260721130002_postgres_analytics.sql, supabase/tests/07_postgres_analytics.sql</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>supabase/migrations/20260721130002_postgres_analytics.sql</file><file>supabase/tests/07_postgres_analytics.sql</file></read_first>
  <action>[BLOCKING] First verify the signed Plan 01 native-tooling decision; if native Supabase CLI or native `psql` is absent or incompatible, stop. Run `rtk supabase db push`, then `rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_postgres_analytics.sql`. Stop on credentials/linkage/push/privilege/parity failure. No analytics handler/page downstream verification may run first.</action>
  <acceptance_criteria><criterion>Live migration 20260721130002 exists and role/privacy/parity assertions pass.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker &amp;&amp; rtk supabase db push &amp;&amp; rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_postgres_analytics.sql</automated></verify>
  <done>Postgres analytics contract is live before application cutover.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch analytics handlers and pages to direct Postgres modules</name>
  <files>frontend/src/app/api/v1/analytics/route.ts, frontend/src/app/api/v1/public/stats/route.ts, frontend/src/app/api/public/stats/route.ts, frontend/src/app/dashboard/analytics/page.tsx, frontend/src/components/PublicStatsStrip.tsx, frontend/tests/analytics-shell.test.mjs</files>
  <read_first><file>frontend/src/app/dashboard/analytics/page.tsx</file><file>frontend/src/components/PublicStatsStrip.tsx</file><file>frontend/src/app/api/public/stats/route.ts</file><file>backend/app/api/analytics.py</file><file>supabase/tests/07_postgres_analytics.sql</file></read_first>
  <action>Only after Task 2 passes, implement typed repository mapping, officer/public handlers, direct Server Component calls, isolated public limiter, k>=3 disclosure, range/empty behavior, and remove active runtime BigQuery calls without deleting migration readers.</action>
  <acceptance_criteria><criterion>Golden 401/422/empty/happy and public privacy cases pass.</criterion><criterion>No runtime analytics module calls Google or FastAPI.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/repositories/analytics.test.ts tests/contracts/golden-contracts.test.ts &amp;&amp; rtk npm run test:legacy -- tests/analytics-shell.test.mjs &amp;&amp; rtk npm run lint</automated></verify>
  <done>Both analytics surfaces use the live Postgres contract.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-17 | Elevation | officer analytics | mitigate | claims + user-scoped client + RLS/function grants |
| T-07-18 | Information Disclosure | public stats | mitigate | server-side k>=3 threshold and allowlisted aggregates |
| T-07-19 | Tampering | analytics migration | mitigate | additive DDL and deterministic parity fixtures |
</threat_model>
<verification>Per-wave: live SQL parity, `cd frontend && rtk npm test && rtk npm run lint`.</verification>
<rollback_stop_conditions>- Push/parity/privilege failure blocks cutover. - Keep BigQuery readers/jobs read-only and runnable until Plan 08 reconciliation and Plan 12 deletion gate. - Never infer retained-data completeness from Postgres aggregates alone.</rollback_stop_conditions>
<success_criteria>All analytics user behavior runs against canonical self-hosted Postgres data.</success_criteria>
<output>Create `07-07-SUMMARY.md` when done.</output>
