---
phase: 07-google-free-self-hosted-platform
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - frontend/src/lib/supabase/admin.ts
  - frontend/src/server/http/errors.ts
  - frontend/src/server/security/access-tokens.ts
  - frontend/src/server/security/access-tokens.test.ts
  - frontend/src/server/security/rate-limit.ts
  - frontend/src/server/security/rate-limit.test.ts
  - frontend/src/server/repositories/reports.ts
  - frontend/src/server/repositories/reports.test.ts
  - frontend/src/app/api/v1/reports/status/route.ts
  - frontend/src/app/api/public/reports/status/route.ts
  - frontend/tests/citizen-status.test.mjs
autonomous: true
requirements: [SELFHOST-01]
requirements_addressed: [SELFHOST-01]
decisions_addressed: [D-01, D-03, D-08]
must_haves:
  truths:
    - A citizen can verify exactly one report with its access token through Next.js only.
    - Missing, wrong, expired, or cross-report tokens return the same 401 response.
    - Officer-only fields and service-role credentials never enter the citizen response or browser bundle.
  artifacts:
    - path: frontend/src/server/repositories/reports.ts
      provides: Explicit-client report repository and citizen-safe projection
    - path: frontend/src/app/api/v1/reports/status/route.ts
      provides: FastAPI-compatible citizen status endpoint
  key_links:
    - from: frontend/src/app/api/v1/reports/status/route.ts
      to: frontend/src/server/security/access-tokens.ts
      via: SHA-256 token verification and report binding
    - from: frontend/src/server/repositories/reports.ts
      to: frontend/src/lib/supabase/admin.ts
      via: explicitly injected trusted client only for token lookup
---

## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**

<objective>Port the complete citizen status behavior as the first public Next.js-only API slice, preserving D-03 privacy and the existing self-hosted Supabase boundary per D-08.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-01-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-02-SUMMARY.md @.planning/phases/04-citizen-status/04-01-SUMMARY.md</context>
<ownership_note>Intentional 11-file citizen-status vertical slice: explicit Supabase boundary, error mapping, token/rate-limit/repository logic with tests, compatibility route, browser BFF, and legacy contract test jointly enforce one token-scoped workflow. File-by-file review found no incidental path; removing any item loses privacy, uniform failure, route parity, or executable proof.</ownership_note>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Port token privacy and explicit Supabase client boundaries</name>
  <files>frontend/src/lib/supabase/admin.ts, frontend/src/server/security/access-tokens.ts, frontend/src/server/security/access-tokens.test.ts, frontend/src/server/security/rate-limit.ts, frontend/src/server/security/rate-limit.test.ts, frontend/src/server/repositories/reports.ts, frontend/src/server/repositories/reports.test.ts</files>
  <read_first><file>backend/app/services/tokens.py</file><file>backend/app/services/supabase.py</file><file>backend/app/security.py</file><file>frontend/src/lib/auth.ts</file><file>frontend/src/lib/supabase/server.ts</file><file>frontend/tests/contracts/fastapi-golden/citizen-status.json</file></read_first>
  <behavior>- Token hash uses SHA-256; plaintext is never persisted. - Token must bind report ID and be unexpired. - Citizen projection is report_id, summary, current_status, and history status/note/created_at only. - Analyze/status/stats limiter keyspaces remain separate and preserve Retry-After 60.</behavior>
  <action>Create a `server-only` admin client with disabled session persistence/refresh, but require repositories to receive clients explicitly. Port token hashing/verification with Node crypto and constant-time report binding. Port trusted-proxy client IP derivation and isolated process-local limiters. Add import-boundary tests that forbid admin imports from components/pages/officer handlers and forbid service-role names in browser output.</action>
  <acceptance_criteria><criterion>Cross-report, missing, wrong, and expired tokens are indistinguishable.</criterion><criterion>Citizen DTO omits actor_id, description, evidence, AI/provider fields, and token metadata.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/security/access-tokens.test.ts src/server/security/rate-limit.test.ts src/server/repositories/reports.test.ts</automated></verify>
  <done>Token-scoped repository behavior is executable without Python and service role remains a narrow trusted-server boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve citizen status through local Next.js handlers</name>
  <files>frontend/src/server/http/errors.ts, frontend/src/app/api/v1/reports/status/route.ts, frontend/src/app/api/public/reports/status/route.ts, frontend/tests/citizen-status.test.mjs</files>
  <read_first><file>frontend/src/app/api/public/reports/status/route.ts</file><file>backend/app/api/reports.py</file><file>frontend/src/server/repositories/reports.ts</file><file>frontend/src/app/api/officer/reports/[reportId]/status/route.ts</file></read_first>
  <behavior>- POST status preserves 422 input validation, uniform 401 verification failure, and 429 Retry-After. - Bilingual status page receives the unchanged JSON contract. - Internal failures return generic stable JSON.</behavior>
  <action>Create `/api/v1/reports/status` as the compatibility handler and change the existing browser BFF to delegate directly to the same server service instead of `backendEndpoint`/FastAPI. Validate `{report_id, token}` strictly, apply the status limiter before lookup, return the exact citizen-safe shape, and map only safe public errors. Keep client-side status fetching so tokens never enter Server Component logs or URLs beyond the already-established citizen deep link.</action>
  <acceptance_criteria><criterion>Golden status cases match exact status, JSON, and Retry-After headers.</criterion><criterion>No handler self-fetches or imports `backend.ts`.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/repositories/reports.test.ts tests/contracts/golden-contracts.test.ts &amp;&amp; rtk npm run test:legacy -- tests/citizen-status.test.mjs</automated></verify>
  <done>A citizen can use the existing EN/VI status UI against Next.js/Supabase with no FastAPI hop.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-06 | Elevation | admin client | mitigate | server-only explicit injection and import-boundary tests |
| T-07-07 | Information Disclosure | citizen status | mitigate | hash+binding+expiry, uniform 401, allowlisted projection |
| T-07-08 | Denial of Service | public status | mitigate | trusted-hop IP derivation, isolated limiter, Retry-After |
</threat_model>
<verification>Per-wave: `cd frontend && rtk npm test && rtk npm run lint`. Final: status UI/API golden cases plus browser-safe import scan.</verification>
<rollback_stop_conditions>- Do not remove FastAPI status code until golden parity passes. - Stop on any cross-report or actor/provider field disclosure. - Preserve the previous BFF behavior for rollback until the full phase gate.</rollback_stop_conditions>
<success_criteria>Citizen status is a complete Next.js -> Supabase vertical slice with identical privacy behavior.</success_criteria>
<output>Create `07-03-SUMMARY.md` when done.</output>
