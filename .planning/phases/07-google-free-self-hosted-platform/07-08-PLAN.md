---
phase: 07-google-free-self-hosted-platform
plan: "08"
type: execute
wave: 7
depends_on: ["07-05", "07-06", "07-07"]
files_modified:
  - supabase/migrations/20260721130004_evidence_path_additive.sql
  - supabase/tests/07_evidence_additive.sql
  - frontend/src/server/services/evidence-service.ts
  - frontend/src/server/repositories/reports.ts
  - frontend/scripts/inventory-google-sources.ps1
  - frontend/scripts/migrate-google-data.ps1
  - frontend/scripts/reconcile-migration.mjs
  - frontend/tests/migration/reconciliation.test.ts
  - frontend/tests/migration/evidence-reconciliation.test.ts
  - frontend/migration-manifests/source-access-and-backup-gate.json
  - frontend/migration-manifests/reconciliation.json
autonomous: false
requirements: [SELFHOST-03, SELFHOST-04]
requirements_addressed: [SELFHOST-03, SELFHOST-04]
decisions_addressed: [D-10, D-11]
user_setup:
  - service: supabase-and-google-source
    why: Additive schema push plus one-time read-only source inventory
    env_vars: [SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, SUPABASE_DB_URL]
    dashboard_config: [Link existing Supabase project, provide read-only Google inventory access or signed operator exports]
must_haves:
  truths:
    - Read-only Google inventory and signed backups exist before any migration write.
    - Additive evidence_path is live before objects/rows write it; image_gcs_uri and GCS reader remain for rollback.
    - Retained rows/objects reconcile with zero unexplained differences while all compatibility remains intact.
  artifacts:
    - path: supabase/migrations/20260721130004_evidence_path_additive.sql
      provides: Additive evidence_path only, with no destructive drop
    - path: frontend/scripts/reconcile-migration.mjs
      provides: Signed canonical row/object reconciliation
    - path: frontend/migration-manifests/reconciliation.json
      provides: Signed canonical reconciliation result consumed by restore and cleanup gates
  key_links:
    - from: frontend/scripts/migrate-google-data.ps1
      to: supabase/migrations/20260721130004_evidence_path_additive.sql
      via: writes only after live additive schema gate
---
## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Inventory, migrate, and reconcile retained Google data through an additive-only schema without deleting the legacy evidence column or Google compatibility reader.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md @.planning/phases/07-google-free-self-hosted-platform/07-06-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-07-SUMMARY.md</context>
<ownership_note>Intentional 11-file additive migration vertical slice: migration-time SQL, evidence/repository dual-read support, inventory/migration/reconciliation scripts, two reconciliation tests, and signed gates/manifests are inseparable evidence-preservation controls. File-by-file review found no incidental path; removing any item breaks additive ordering, source completeness, byte-level reconciliation, or signed proof.</ownership_note>
<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Establish signed read-only inventory and backup gate</name>
  <files>frontend/migration-manifests/source-access-and-backup-gate.json</files>
  <read_first><file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file><file>backend/tests/test_migrate_bigquery_to_supabase.py</file><file>backend/app/services/storage.py</file></read_first>
  <action>[BLOCKING] Preserve the last-known-good Python-capable revision and read-only Google readers. Obtain read-only ADC/credentials or operator exports and operator-supported Postgres plus separate Storage backups. Record source scope, credential scope without values, backup paths/hashes, revision, signer, timestamp, and PASS in the gate JSON. Unavailable inventory or backup is failure, not skip. Remote deletion is not authorized.</action>
  <acceptance_criteria><criterion>Signed gate includes BigQuery tables/rows and every GCS object/orphan scope plus DB and Storage backup hashes.</criterion></acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -Command "$g=ConvertFrom-Json (Get-Content -Raw frontend/migration-manifests/source-access-and-backup-gate.json); if(-not $g.signed -or $g.status -ne 'PASS' -or -not $g.read_only_inventory -or -not $g.db_backup_hash -or -not $g.storage_backup_hash){exit 1}"</automated></verify>
  <done>Authoritative source scope and rollback assets exist or execution stops before schema/data writes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Push additive evidence schema, then inventory, migrate, and reconcile</name>
  <files>supabase/migrations/20260721130004_evidence_path_additive.sql, supabase/tests/07_evidence_additive.sql, frontend/src/server/services/evidence-service.ts, frontend/src/server/repositories/reports.ts, frontend/scripts/inventory-google-sources.ps1, frontend/scripts/migrate-google-data.ps1, frontend/scripts/reconcile-migration.mjs, frontend/tests/migration/reconciliation.test.ts, frontend/tests/migration/evidence-reconciliation.test.ts, frontend/migration-manifests/reconciliation.json</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>frontend/migration-manifests/source-access-and-backup-gate.json</file><file>supabase/migrations/20260720120001_foundation.sql</file><file>backend/tests/test_migrate_bigquery_to_supabase.py</file><file>frontend/src/server/services/evidence-service.ts</file></read_first>
  <action>Verify the signed Plan 01 tooling decision. Create the additive migration first: nullable `evidence_path`, safe backfill of existing `supabase://`, indexes/constraints, no drop and no GCS-reader removal. Run `rtk supabase db push` then `rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_evidence_additive.sql`; stop if either fails. Implement/run read-only inventory before writes, then idempotent non-overwriting row/object migration. Reconcile IDs, hashes, timestamps/nulls, aggregates, object size/MIME/SHA-256, orphans/conflicts, signer and PASS. Keep image_gcs_uri, GCS reader, credentials, and Python revision through Plan 12.</action>
  <acceptance_criteria><criterion>Live evidence_path exists before first migrated write.</criterion><criterion>Signed reconciliation has zero unexplained differences.</criterion><criterion>Legacy column/reader remain usable.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker &amp;&amp; rtk supabase db push &amp;&amp; rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_evidence_additive.sql &amp;&amp; cd frontend &amp;&amp; rtk npm run test:unit -- tests/migration/reconciliation.test.ts tests/migration/evidence-reconciliation.test.ts &amp;&amp; rtk node scripts/reconcile-migration.mjs --require-pass --require-signed</automated></verify>
  <done>Retained data/evidence are proven in Supabase while rollback compatibility remains intact.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-20 | Tampering/Repudiation | migration | mitigate | read-only source and signed canonical manifests |
| T-07-21 | Information Disclosure | evidence | mitigate | private non-overwriting paths and MIME/hash verification |
| T-07-22 | Data loss | compatibility | mitigate | additive-only schema; exact restore/app rollback; no deletion in this plan |
</threat_model>
<verification>Per-wave: live additive SQL gate and signed canonical reconciliation.</verification>
<rollback_stop_conditions>- Inventory unavailable, backup missing, schema push failure, or reconciliation mismatch blocks Plan 09 restore proof and all cleanup. - This plan never drops image_gcs_uri or removes GCS/Python readers. - Remote Google deletion is not authorized.</rollback_stop_conditions>
<success_criteria>Retained BigQuery/GCS state is reconciled in Supabase while compatibility and rollback assets remain intact.</success_criteria>
<output>Create `07-08-SUMMARY.md` with manifest hashes and no secrets.</output>
