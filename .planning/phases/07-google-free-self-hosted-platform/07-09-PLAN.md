---
phase: 07-google-free-self-hosted-platform
plan: "09"
type: execute
wave: 8
depends_on: ["07-08"]
files_modified:
  - frontend/scripts/backup-citymind.ps1
  - frontend/scripts/restore-citymind.ps1
  - frontend/scripts/compare-migration-manifests.mjs
  - frontend/scripts/verify-gate-artifacts.mjs
  - frontend/migration-manifests/restore-and-rollback-gate.json
autonomous: false
requirements: [SELFHOST-03, SELFHOST-04, SELFHOST-05]
requirements_addressed: [SELFHOST-03, SELFHOST-04, SELFHOST-05]
decisions_addressed: [D-10, D-11, D-13]
must_haves:
  truths:
    - Separate database and Storage backups restore into an isolated target with exact manifest parity.
    - The preserved Python-capable revision operates against the isolated restored target.
    - Missing tooling, credentials, restore evidence, or application rollback evidence fails closed before cleanup.
  artifacts:
    - path: frontend/scripts/backup-citymind.ps1
      provides: Operator-approved database plus Storage backup wrapper
    - path: frontend/scripts/restore-citymind.ps1
      provides: Guarded isolated restore wrapper
    - path: frontend/migration-manifests/restore-and-rollback-gate.json
      provides: Signed exact restore and application rollback proof
  key_links:
    - from: frontend/migration-manifests/restore-and-rollback-gate.json
      to: frontend/migration-manifests/reconciliation.json
      via: exact canonical manifest comparison
---
## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Prove the additive migration is recoverable through an isolated DB+Storage restore and preserved-application rollback before any destructive approval.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-01-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-08-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Implement operator-approved DB and Storage recovery wrappers</name>
  <files>frontend/scripts/backup-citymind.ps1, frontend/scripts/restore-citymind.ps1, frontend/scripts/compare-migration-manifests.mjs, frontend/scripts/verify-gate-artifacts.mjs</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>frontend/migration-manifests/source-access-and-backup-gate.json</file><file>frontend/scripts/reconcile-migration.mjs</file><file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file></read_first>
  <action>Verify the signed Plan 01 native-tooling decision first. Implement typed `$PSScriptRoot`-relative wrappers around the installed native `pg_dump`/`psql` paths and the separately approved Storage-backend procedure. Back up Postgres and Storage separately, require explicit absolute paths and isolated targets, hash every artifact, redact credentials, and refuse production/current targets during restore. Implement deterministic manifest comparison for row IDs/hashes/aggregates and Storage keys/size/MIME/SHA-256. Do not use Docker or delete compatibility.</action>
  <acceptance_criteria><criterion>Wrappers reject ambiguous/current targets and missing DB or Storage artifacts.</criterion><criterion>Comparison fails on any row/object/hash difference.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk pg_dump --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --require-native-pg-dump --forbid-docker &amp;&amp; rtk powershell -NoProfile -Command "$b=Get-Content -Raw frontend/scripts/backup-citymind.ps1; $r=Get-Content -Raw frontend/scripts/restore-citymind.ps1; if($b -notmatch 'Storage' -or $r -notmatch 'isolated'){exit 1}"</automated></verify>
  <done>Recovery wrappers are bound to approved tooling and fail closed on unsafe targets or incomplete backups.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute isolated restore and preserved-application rollback proof</name>
  <files>frontend/migration-manifests/restore-and-rollback-gate.json</files>
  <read_first><file>frontend/scripts/backup-citymind.ps1</file><file>frontend/scripts/restore-citymind.ps1</file><file>frontend/scripts/compare-migration-manifests.mjs</file><file>frontend/migration-manifests/reconciliation.json</file></read_first>
  <action>[BLOCKING] Run `rtk powershell -NoProfile -File frontend/scripts/backup-citymind.ps1 -Output frontend/migration-backups/pre-clean`, `rtk powershell -NoProfile -File frontend/scripts/restore-citymind.ps1 -Input frontend/migration-backups/pre-clean -Target isolated`, and `rtk node frontend/scripts/compare-migration-manifests.mjs --source frontend/migration-manifests/reconciliation.json --target frontend/migration-manifests/restore.json --require-exact`. Run the preserved Python-capable revision in an isolated worktree against only that restored target. Record commands, versions, target identity, DB/Storage hashes, exact comparison, application smoke, signer, timestamp, and PASS. Pause for operator authentication if required; missing evidence is failure. Do not delete compatibility or remote resources.</action>
  <acceptance_criteria><criterion>Isolated DB+Storage restore matches the signed source exactly.</criterion><criterion>Preserved application rollback passes and signed gate is PASS.</criterion></acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -File frontend/scripts/backup-citymind.ps1 -Output frontend/migration-backups/pre-clean &amp;&amp; rtk powershell -NoProfile -File frontend/scripts/restore-citymind.ps1 -Input frontend/migration-backups/pre-clean -Target isolated &amp;&amp; rtk node frontend/scripts/compare-migration-manifests.mjs --source frontend/migration-manifests/reconciliation.json --target frontend/migration-manifests/restore.json --require-exact &amp;&amp; rtk node frontend/scripts/verify-gate-artifacts.mjs --gate restore-and-rollback --require-signed --require-db-restore --require-storage-restore --require-manifest-match --require-application-rollback</automated></verify>
  <done>Recoverability is proven or all destructive plans remain blocked.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-22 | Tampering/Data loss | backup and restore | mitigate | signed hashes, isolated target guard, exact manifest comparison |
| T-07-23 | Information Disclosure | recovery scripts | mitigate | no credential values in commands, logs, or manifests |
| T-07-24 | Repudiation | application rollback | mitigate | signed preserved-revision identity and smoke result |
</threat_model>
<verification>Exact backup, isolated restore, canonical comparison, signed gate verification, and preserved-application smoke all pass.</verification>
<rollback_stop_conditions>- Missing approved tooling, DB or Storage artifact, isolated target, exact match, or application rollback PASS blocks every cleanup plan. - Keep image_gcs_uri, GCS/Python readers, backups, and revision. - Remote Google deletion is unauthorized.</rollback_stop_conditions>
<success_criteria>The migration has executable, signed DB+Storage and application rollback proof.</success_criteria>
<output>Create `07-09-SUMMARY.md` with evidence hashes and no secrets.</output>
