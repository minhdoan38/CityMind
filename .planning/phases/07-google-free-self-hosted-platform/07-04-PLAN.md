---
phase: 07-google-free-self-hosted-platform
plan: "04"
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - supabase/migrations/20260721130001_next_backend_contract.sql
  - supabase/tests/07_next_backend_contract.sql
  - frontend/src/server/services/evidence-service.ts
  - frontend/src/server/services/evidence-service.test.ts
  - frontend/src/server/services/report-service.ts
  - frontend/src/server/services/report-service.test.ts
  - frontend/src/server/repositories/reports.ts
  - frontend/src/app/api/v1/reports/analyze/route.ts
  - frontend/src/app/api/public/reports/analyze/route.ts
  - frontend/src/components/ReportForm.tsx
autonomous: false
requirements: [SELFHOST-01, SELFHOST-02, SELFHOST-04]
requirements_addressed: [SELFHOST-01, SELFHOST-02, SELFHOST-04]
decisions_addressed: [D-01, D-03, D-06, D-08, D-11]
user_setup:
  - service: supabase
    why: Push and live-test schema
    env_vars: [SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, SUPABASE_DB_URL]
    dashboard_config: [Link the existing self-hosted project]
must_haves:
  truths:
    - A citizen submits text and optional evidence and receives report_id plus plaintext access token once.
    - Analysis remains synchronous in Phase 7 and failures do not create partial report/token state.
    - New evidence is validated before AI/Storage, stored privately in Supabase Storage, and compensated if DB persistence fails.
  artifacts:
    - path: supabase/migrations/20260721130001_next_backend_contract.sql
      provides: Atomic report plus hashed-token RPC and tightened private Storage policy
    - path: frontend/src/server/services/report-service.ts
      provides: Synchronous submission orchestration
    - path: frontend/src/app/api/v1/reports/analyze/route.ts
      provides: Compatibility multipart endpoint
  key_links:
    - from: frontend/src/server/services/report-service.ts
      to: frontend/src/server/ai/provider.ts
      via: structured synchronous analysis call
    - from: frontend/src/server/services/report-service.ts
      to: supabase/migrations/20260721130001_next_backend_contract.sql
      via: one atomic RPC for report and token hash
---

## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**

<objective>Deliver the citizen report-submission path end to end inside Next.js while preserving synchronous behavior, issue-once tokens, private evidence, and advisory analysis.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-02-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-03-SUMMARY.md @supabase/migrations/20260720120001_foundation.sql</context>
<ownership_note>Intentional 10-file citizen-submission vertical slice: atomic SQL contract, evidence/report services with tests, compatibility/local routes, repository connection, and ReportForm caller are one synchronous UI-to-AI-to-Storage-to-DB behavior. File-by-file review found no incidental path; removing any item breaks atomicity, evidence compensation, issue-once token parity, or the user flow.</ownership_note>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add atomic citizen persistence and private evidence invariants</name>
  <files>supabase/migrations/20260721130001_next_backend_contract.sql, supabase/tests/07_next_backend_contract.sql, frontend/src/server/services/evidence-service.ts, frontend/src/server/services/evidence-service.test.ts</files>
  <read_first><file>supabase/migrations/20260720120001_foundation.sql</file><file>backend/app/api/reports.py</file><file>backend/app/services/storage.py</file><file>backend/app/services/supabase.py</file><file>backend/tests/test_storage.py</file></read_first>
  <behavior>- Report and access-token hash commit together or neither commits. - Access token table remains service-role-only. - Evidence bucket stays private, size-limited, and JPEG/PNG/WebP only. - Database failure after upload deletes the just-created object.</behavior>
  <action>Add an immutable new migration; do not edit applied migrations. Create a transactional RPC for report+token creation with fixed search_path, explicit grants, RLS-aware privileges, and no plaintext token argument/storage. Tighten anonymous Storage insert if legacy policy is broad. Create evidence service using `file-type`, Content-Length/File.size/byte limit/magic MIME checks before upload or AI, deterministic report path, `upsert:false`, private download, and compensation delete. Add SQL and Vitest rollback/privilege/private-bucket tests.</action>
  <acceptance_criteria><criterion>Forced token insert failure leaves no report row.</criterion><criterion>Anonymous/non-officer cannot read evidence or access_tokens.</criterion><criterion>Malicious, spoofed, or oversized uploads reach neither Storage nor AI.</criterion></acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260721130001_next_backend_contract.sql; if($s -notmatch 'create_report' -or $s -notmatch 'access_tokens' -or $s -notmatch 'security invoker'){exit 1}"</automated></verify>
  <done>Atomic persistence and private evidence controls are ready for the submission service.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push and live-test the backend contract schema</name>
  <files>supabase/migrations/20260721130001_next_backend_contract.sql, supabase/tests/07_next_backend_contract.sql</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>supabase/migrations/20260721130001_next_backend_contract.sql</file><file>supabase/tests/07_next_backend_contract.sql</file></read_first>
  <action>[BLOCKING] First verify the signed Plan 01 native-tooling decision; if native Supabase CLI or native `psql` is absent or incompatible, stop. After all schema changes run `rtk supabase db push`, then `rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_next_backend_contract.sql`. Non-TTY execution requires access token, database URL/password, and linkage. Stop on credential, push, privilege, or rollback failure. No report-service downstream verification may run first.</action>
  <acceptance_criteria><criterion>Live migration history contains 20260721130001 and privilege/rollback SQL assertions pass.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker &amp;&amp; rtk supabase db push &amp;&amp; rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_next_backend_contract.sql</automated></verify>
  <done>Atomic contract is live before the application writes through it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port the synchronous analyze route and existing ReportForm caller</name>
  <files>frontend/src/server/services/report-service.ts, frontend/src/server/services/report-service.test.ts, frontend/src/server/repositories/reports.ts, frontend/src/app/api/v1/reports/analyze/route.ts, frontend/src/app/api/public/reports/analyze/route.ts, frontend/src/components/ReportForm.tsx</files>
  <read_first><file>frontend/src/components/ReportForm.tsx</file><file>frontend/src/app/api/public/reports/analyze/route.ts</file><file>backend/app/api/reports.py</file><file>frontend/tests/contracts/fastapi-golden/analyze.json</file><file>frontend/src/server/ai/provider.ts</file><file>supabase/tests/07_next_backend_contract.sql</file></read_first>
  <action>Only after Task 2 passes, implement synchronous report service orchestration, compensation, issue-once token response, compatibility handler, and local BFF delegation. Preserve EN/VI behavior and exclude Phase 8 queue/retry/worker scope.</action>
  <acceptance_criteria><criterion>Golden text/image/error cases match.</criterion><criterion>Secrets/provider bodies/evidence are redacted.</criterion><criterion>Plaintext token appears once.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/services/report-service.test.ts src/server/services/evidence-service.test.ts tests/contracts/golden-contracts.test.ts &amp;&amp; rtk npm run lint</automated></verify>
  <done>Bilingual submission runs through the already-live atomic schema.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-09 | Tampering/DoS | multipart upload | mitigate | size precheck, byte cap, magic-byte MIME, no user filename |
| T-07-10 | Information Disclosure | private evidence | mitigate | private bucket, server-only download, narrow grants |
| T-07-11 | Tampering | report/token persistence | mitigate | one transactional RPC plus rollback tests |
| T-07-12 | Information Disclosure | submission errors | mitigate | generic boundary errors and redacted safe logs |
</threat_model>
<verification>Per-wave: target tests, live SQL tests after push, then `cd frontend && rtk npm test && rtk npm run lint`.</verification>
<rollback_stop_conditions>- Schema push failure blocks completion. - Stop on any partial report/token write or public evidence read. - Keep Python analyze and legacy GCS read path until reconciliation/final cleanup; do not delete them here.</rollback_stop_conditions>
<success_criteria>Citizen submission is a complete Next.js-only synchronous slice with atomic persistence, private evidence, and issue-once tokens.</success_criteria>
<output>Create `07-04-SUMMARY.md` when done.</output>
