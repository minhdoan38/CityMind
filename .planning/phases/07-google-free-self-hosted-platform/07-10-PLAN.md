---
phase: 07-google-free-self-hosted-platform
plan: "10"
type: execute
wave: 9
depends_on: ["07-06", "07-07", "07-08", "07-09"]
files_modified:
  - frontend/src/app/api/health/route.ts
  - frontend/src/app/api/ready/route.ts
  - frontend/src/server/health/readiness.ts
  - frontend/src/server/health/readiness.test.ts
  - frontend/scripts/smoke-production.mjs
  - frontend/scripts/register-citymind-task.ps1
  - frontend/operations/operator-runtime-decision.json
  - frontend/package.json
  - frontend/.env.example
autonomous: false
requirements: [SELFHOST-05]
requirements_addressed: [SELFHOST-05]
decisions_addressed: [D-12, D-13, D-14]
must_haves:
  truths:
    - A clean Node 22 build starts directly on the laptop and binds loopback by default.
    - Liveness is cheap; readiness checks Supabase safely without spending AI tokens or leaking configuration.
    - Startup/restart and separate database/Storage backup+restore procedures are proven on the target laptop.
  artifacts:
    - path: frontend/scripts/smoke-production.mjs
      provides: Real next build/start production smoke
    - path: frontend/scripts/register-citymind-task.ps1
      provides: Windows Task Scheduler startup/restart registration
    - path: frontend/scripts/backup-citymind.ps1
      provides: Operator-supported DB plus separate Storage backup orchestration
  key_links:
    - from: frontend/scripts/smoke-production.mjs
      to: frontend/src/app/api/ready/route.ts
      via: loopback readiness polling
---

## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Make the complete application operable as one direct laptop Node process with safe health, startup, restart, backup, and restore procedures.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-09-SUMMARY.md @frontend/next.config.ts @scripts/seed_officer_role.ps1</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add safe liveness/readiness and real production smoke</name>
  <files>frontend/src/app/api/health/route.ts, frontend/src/app/api/ready/route.ts, frontend/src/server/health/readiness.ts, frontend/src/server/health/readiness.test.ts, frontend/scripts/smoke-production.mjs, frontend/package.json, frontend/.env.example</files>
  <read_first><file>backend/app/main.py</file><file>frontend/next.config.ts</file><file>frontend/package.json</file><file>frontend/src/server/config/env.ts</file></read_first>
  <behavior>- Liveness returns 200 without external calls. - Readiness returns 200/503, Cache-Control no-store, and only dependency name/status/latency. - Production smoke fails on wrong port/bind, early exit, 503 readiness, behavior regression, or secret in logs.</behavior>
  <action>Create `/api/health` and `/api/ready`; readiness uses bounded Supabase connectivity/config checks and no live AI inference. Add `smoke:production` that pins Node 22 expectation, performs clean build/start on explicit loopback host, polls both endpoints, exercises representative citizen/officer routes with synthetic fixtures, scans logs for secret names/values safely, and gracefully terminates. Never bind `0.0.0.0` by default.</action>
  <acceptance_criteria><criterion>Readiness never returns URL, SQL error, key, provider body, model, or endpoint.</criterion><criterion>Production smoke verifies loopback and clean shutdown.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/health/readiness.test.ts &amp;&amp; rtk npm run build &amp;&amp; rtk npm run smoke:production</automated></verify>
  <done>The complete app runs as a direct loopback Node production process.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm operator-supported backup mechanism and exposure boundary</name>
  <files>frontend/operations/operator-runtime-decision.json</files>
  <read_first><file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file><file>.planning/phases/07-google-free-self-hosted-platform/07-09-SUMMARY.md</file></read_first>
  <action>Discover and present the existing self-hosted Supabase operator's supported Postgres backup/restore mechanism and Storage backend procedure. Require an isolated restore target. Keep loopback as the authoritative default. If public/LAN exposure is requested, stop until an explicit TLS reverse-proxy owner/configuration and direct-HTTP blocking test are approved. Record the signed decision, exact backup/restore commands, isolated target, bind host, proxy approval state, signer, timestamp, and PASS in `frontend/operations/operator-runtime-decision.json`. Do not introduce CityMind Docker orchestration.</action>
  <acceptance_criteria><criterion>Operator confirms one supported DB mechanism, separate Storage backup, isolated restore target, and loopback or approved TLS proxy boundary.</criterion></acceptance_criteria>
  <verify><automated>rtk node frontend/scripts/verify-gate-artifacts.mjs --file frontend/operations/operator-runtime-decision.json --require-signed --require-backup-command --require-restore-command --require-isolated-target --require-loopback-or-tls-proxy</automated></verify>
  <done>Operational scripts have an authoritative backup target and safe network boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Prove startup, restart, backup, and isolated restore on the laptop</name>
  <files>frontend/scripts/register-citymind-task.ps1</files>
  <read_first><file>frontend/operations/operator-runtime-decision.json</file><file>frontend/scripts/backup-citymind.ps1</file><file>frontend/scripts/restore-citymind.ps1</file><file>frontend/scripts/smoke-production.mjs</file><file>scripts/seed_officer_role.ps1</file></read_first>
  <action>Implement the typed `$PSScriptRoot`-relative scheduled-task script using the Task 2 decision and reuse the restore-proven Plan 09 backup/restore scripts. Use guard clauses, an absolute working directory, pinned Node/npm path, non-secret logs, startup/logon trigger, restart-on-failure, and no secret printing. Register and query the task, confirm loopback readiness, force terminate, verify automatic restart, execute DB and Storage backup/isolated restore, and compare the Plan 09 manifests. Do not use PM2 or Docker. If reboot/logon or operator-only backup authentication cannot be completed autonomously, pause at a blocking human verification checkpoint and resume only after the same assertions pass.</action>
  <acceptance_criteria><criterion>Startup and forced-termination restart work.</criterion><criterion>Isolated DB+Storage restore matches signed reconciliation manifests.</criterion><criterion>Logs/task definition contain no secrets and bind is approved.</criterion></acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -File frontend/scripts/register-citymind-task.ps1 -Verify &amp;&amp; rtk powershell -NoProfile -File frontend/scripts/backup-citymind.ps1 -Output frontend/operations/backup &amp;&amp; rtk powershell -NoProfile -File frontend/scripts/restore-citymind.ps1 -Input frontend/operations/backup -Target isolated &amp;&amp; rtk node frontend/scripts/compare-migration-manifests.mjs --source frontend/migration-manifests/reconciliation.json --target frontend/migration-manifests/restore.json --require-exact &amp;&amp; cd frontend &amp;&amp; rtk npm run smoke:production</automated></verify>
  <done>Direct laptop operation and recovery are proven, not merely documented.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-23 | Information Disclosure | health/logs | mitigate | allowlisted public summary and secret scan |
| T-07-24 | Denial of Service | laptop exposure | mitigate | loopback default; public only behind approved TLS proxy |
| T-07-25 | Tampering/Data loss | backups | mitigate | separate DB/Storage artifacts and isolated restore manifest comparison |
| T-07-26 | Elevation | scheduled task | mitigate | explicit working directory/identity and no embedded secrets |
</threat_model>
<verification>Per-wave: unit+build+production smoke; human target-laptop restart and isolated restore drill.</verification>
<rollback_stop_conditions>- Public exposure without approved TLS proxy is a high-severity block. - Missing supported backup, failed restart, failed restore, or secret leakage blocks final cleanup. - Do not modify or remove Supabase-owned Docker infrastructure.</rollback_stop_conditions>
<success_criteria>CityMind operates and recovers as a single loopback-first Node application on the laptop.</success_criteria>
<output>Create `07-10-SUMMARY.md` with procedures/results but no secret values.</output>
