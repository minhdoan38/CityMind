---
phase: 07-google-free-self-hosted-platform
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/.env.example
  - frontend/src/server/config/env.ts
  - frontend/src/server/domain/report-analysis.ts
  - frontend/src/server/domain/report-analysis.test.ts
  - frontend/src/server/validation/analysis-policy.ts
  - frontend/src/server/validation/analysis-policy.test.ts
  - frontend/src/server/ai/provider.ts
  - frontend/src/server/ai/openai-compatible.ts
  - frontend/src/server/ai/openai-compatible.test.ts
  - frontend/scripts/smoke-ai.mjs
autonomous: false
requirements: [SELFHOST-02]
requirements_addressed: [SELFHOST-02]
decisions_addressed: [D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - A configured third-party API key can produce validated text and image report analysis.
    - Provider failures never expose keys, endpoint URLs, response bodies, or internal errors.
    - Actual provider label, response model, request ID, and latency are retained as lineage while officer authority remains final.
  artifacts:
    - path: frontend/src/server/ai/provider.ts
      provides: Provider-neutral analysis and lineage contract
    - path: frontend/src/server/ai/openai-compatible.ts
      provides: Configurable native-fetch adapter
    - path: frontend/scripts/smoke-ai.mjs
      provides: Live synthetic capability gate
  key_links:
    - from: frontend/src/server/ai/openai-compatible.ts
      to: frontend/src/server/domain/report-analysis.ts
      via: outbound JSON Schema and inbound Zod parsing
    - from: frontend/src/server/config/env.ts
      to: frontend/src/server/ai/openai-compatible.ts
      via: environment-only endpoint/model/key injection
---

## Phase Goal

**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**

<objective>
Deliver a provider-neutral synchronous AI analysis slice in TypeScript, per D-04 through D-07, without importing Phase 8 retries, workers, durable triage state, or manual-review workflow.
</objective>

<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/07-google-free-self-hosted-platform/07-CONTEXT.md
@.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md
@.planning/phases/07-google-free-self-hosted-platform/07-PATTERNS.md
@.planning/phases/07-google-free-self-hosted-platform/07-01-SUMMARY.md
</context>

<ownership_note>Intentional 10-file provider-neutral AI vertical slice: environment contract, domain schema/policy with tests, provider interface/adapter with tests, and live capability smoke form one security boundary. File-by-file review found no incidental path; removing any item breaks strict structured output, SSRF/privacy validation, lineage, or the live cutover gate.</ownership_note>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define failing structured-analysis and policy contracts</name>
  <files>frontend/src/server/domain/report-analysis.ts, frontend/src/server/domain/report-analysis.test.ts, frontend/src/server/validation/analysis-policy.ts, frontend/src/server/validation/analysis-policy.test.ts, frontend/src/server/ai/provider.ts</files>
  <read_first>
    <file>backend/app/schemas.py</file>
    <file>backend/app/services/gemini.py</file>
    <file>backend/tests/test_analyze.py</file>
    <file>frontend/tests/contracts/fastapi-golden/analyze.json</file>
  </read_first>
  <behavior>
    - Exact category/priority enums and numeric/string/list bounds match the legacy contract.
    - Unrecognized fields, invalid schema, unsupported image content, and policy violations fail closed.
    - Advisory wording and evidence/uncertainty separation are preserved.
  </behavior>
  <action>Create strict Zod `ReportAnalysisSchema`, inferred types, `reportAnalysisJsonSchema`, `AnalysisProvider`, `AnalysisInput`, `AnalysisLineage`, and pure `validateAnalysisPolicy`. Write RED tests before implementation. Policy checks only locked Phase 7 invariants; do not create triage states, retries, a worker, or a `manual_review` queue.</action>
  <acceptance_criteria><criterion>Schema snapshot matches the sanitized legacy fixture and rejects extra/out-of-range fields.</criterion><criterion>Policy output is deterministic and does not override officer decisions.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/domain/report-analysis.test.ts src/server/validation/analysis-policy.test.ts</automated></verify>
  <done>The provider-independent domain contract is executable and Phase 8 scope remains absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the hardened configurable AI adapter</name>
  <files>frontend/.env.example, frontend/src/server/config/env.ts, frontend/src/server/ai/openai-compatible.ts, frontend/src/server/ai/openai-compatible.test.ts, frontend/scripts/smoke-ai.mjs</files>
  <read_first>
    <file>frontend/src/server/domain/report-analysis.ts</file>
    <file>frontend/src/server/ai/provider.ts</file>
    <file>frontend/.env.example</file>
    <file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Text and optional validated image become OpenAI-compatible chat content with strict JSON Schema response format.
    - Timeout, refusal, non-2xx, redirect, oversized response, invalid JSON, and invalid schema map to generic typed failures.
    - Request-controlled URLs are impossible; actual response model/request ID/latency are returned.
  </behavior>
  <action>Create `server-only` Zod environment validation for `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `AI_BASE_URL`, `AI_MODEL`, `AI_PROVIDER_LABEL`, `THIRD_PARTY_API_KEY`, and bounded `AI_TIMEOUT_MS`. Reject non-HTTPS AI URLs except explicit loopback development, normalize once, append fixed `chat/completions`, set `redirect: "error"`, use native fetch/AbortSignal timeout, cap response bytes, and never log key, authorization, endpoint, prompts, evidence, or upstream body. Add generic names only to `.env.example`; do not name a gateway/provider. Implement mocked tests and a synthetic EN/VI text+image smoke script.</action>
  <acceptance_criteria><criterion>No request field controls the outbound host/path.</criterion><criterion>Tests prove redaction, byte cap, timeout, refusal/error mapping, strict validation, image part, and actual lineage.</criterion><criterion>No Gemini, Vertex, Google credential, or Google AI SDK import appears in new TypeScript.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- src/server/ai/openai-compatible.test.ts &amp;&amp; rtk npm run lint</automated></verify>
  <done>The adapter is swappable by configuration and safe for the synchronous Phase 7 report service.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prove the configured endpoint capability and privacy boundary</name>
  <files>frontend/scripts/smoke-ai.mjs</files>
  <read_first><file>frontend/scripts/smoke-ai.mjs</file><file>frontend/.env.example</file></read_first>
  <action>With operator-supplied generic endpoint/model/key configuration, run the synthetic smoke for EN text, VI text, and a non-sensitive image. Verify strict JSON Schema, image support, actual response lineage, generic refusal/error behavior, HTTPS, and acceptable provider retention/privacy terms. Never print the key or endpoint. If configuration is absent or any capability fails, stop AI cutover; do not substitute a named provider or relax parsing.</action>
  <acceptance_criteria><criterion>All required capabilities pass and operator approves the data-handling boundary.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk node scripts/smoke-ai.mjs --require-strict-schema --require-image --require-lineage --require-privacy-approval</automated></verify>
  <done>Configured endpoint is proven compatible, or the plan stops before report submission is switched.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-02 | Information Disclosure | env/client bundle | mitigate | server-only module, no NEXT_PUBLIC secrets, build scan tests |
| T-07-03 | SSRF | AI adapter | mitigate | environment-only HTTPS base URL, fixed path, redirects disabled |
| T-07-04 | Tampering | provider output | mitigate | strict schema plus deterministic policy validation and advisory-only contract |
| T-07-05 | Information Disclosure | provider errors/logs | mitigate | bounded reads and generic errors with safe correlation metadata only |
</threat_model>

<verification>Per-wave: `cd frontend && rtk npm test && rtk npm run lint`. Final: mocked suite plus operator-approved live synthetic capability smoke.</verification>
<rollback_stop_conditions>- Keep Gemini/FastAPI migration reader runnable until final deletion gate. - Stop if live endpoint cannot prove image and strict-schema support or acceptable privacy. - Never fall back to request-controlled endpoints or permissive JSON extraction.</rollback_stop_conditions>
<success_criteria>Provider-neutral structured multimodal analysis works with auditable lineage and no provider-specific Google dependency.</success_criteria>
<output>Create `07-02-SUMMARY.md` when done.</output>
