---
phase: 07-google-free-self-hosted-platform
plan: "06"
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - supabase/migrations/20260721130003_officer_operations.sql
  - supabase/tests/07_officer_operations.sql
  - frontend/src/server/repositories/reports.ts
  - frontend/src/server/repositories/reports.test.ts
  - frontend/src/server/exports/reports.ts
  - frontend/src/server/exports/reports.test.ts
  - frontend/src/app/api/v1/reports/export/route.ts
  - frontend/src/app/api/v1/reports/[reportId]/status/route.ts
autonomous: false
requirements: [SELFHOST-01]
requirements_addressed: [SELFHOST-01]
decisions_addressed: [D-01, D-03]
user_setup:
  - service: supabase
    why: Push and live-test officer schema
    env_vars: [SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, SUPABASE_DB_URL]
    dashboard_config: [Link the existing self-hosted project]
must_haves:
  truths:
    - Officer status changes atomically update the report and append an actor-attributed event.
    - CSV/XLSX exports preserve filters and neutralize formula-leading citizen content.
  artifacts:
    - path: supabase/migrations/20260721130003_officer_operations.sql
      provides: Transactional status RPC
    - path: frontend/src/server/exports/reports.ts
      provides: Safe filtered export
  key_links:
    - from: frontend/src/app/api/v1/reports/[reportId]/status/route.ts
      to: supabase/migrations/20260721130003_officer_operations.sql
      via: user-scoped RPC
---
## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Port officer decision writes and safe exports as a focused schema-backed increment.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-05-SUMMARY.md @supabase/migrations/20260720120002_dashboard_polish.sql</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement atomic status and formula-safe export</name>
  <files>supabase/migrations/20260721130003_officer_operations.sql, supabase/tests/07_officer_operations.sql, frontend/src/server/repositories/reports.ts, frontend/src/server/repositories/reports.test.ts, frontend/src/server/exports/reports.ts, frontend/src/server/exports/reports.test.ts, frontend/src/app/api/v1/reports/export/route.ts, frontend/src/app/api/v1/reports/[reportId]/status/route.ts</files>
  <read_first><file>backend/app/api/reports.py</file><file>backend/app/services/supabase.py</file><file>frontend/src/app/api/officer/reports/export/route.ts</file><file>supabase/migrations/20260720120002_dashboard_polish.sql</file></read_first>
  <behavior>- resolved/rejected require note; reviewing may omit it. - actor_id is JWT sub only. - report/event commit together. - CSV/XLSX preserve filters, cap at 10k, and neutralize =,+,-,@.</behavior>
  <action>Create the migration and SQL test first. Add fixed-search_path, narrow-grant transactional RPC and TypeScript handlers/repository. Implement streaming CSV and ExcelJS XLSX with identical filters, headers, cap, and formula neutralization. Write RED tests before implementation.</action>
  <acceptance_criteria><criterion>Forced event failure rolls back status.</criterion><criterion>Body actor is ignored and non-officer denied.</criterion><criterion>Formula fixtures are inert in both formats.</criterion></acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260721130003_officer_operations.sql; if($s -notmatch 'security invoker' -or $s -notmatch 'status_events'){exit 1}"</automated></verify>
  <done>Status/export implementation is ready for live schema application.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push and live-test officer schema before downstream verification</name>
  <files>supabase/migrations/20260721130003_officer_operations.sql, supabase/tests/07_officer_operations.sql</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>supabase/migrations/20260721130003_officer_operations.sql</file><file>supabase/tests/07_officer_operations.sql</file></read_first>
  <action>[BLOCKING] First verify the signed Plan 01 native-tooling decision; if native Supabase CLI or native `psql` is absent or incompatible, stop. Run `rtk supabase db push`, then the exact live privilege/atomicity gate `rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_officer_operations.sql`. If credentials, linkage, push, or SQL gate fail, stop. Only then run downstream TypeScript and legacy export/status verification.</action>
  <acceptance_criteria><criterion>Live migration exists and anonymous/non-officer/rollback/actor/note SQL assertions pass.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker &amp;&amp; rtk supabase db push &amp;&amp; rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_officer_operations.sql &amp;&amp; cd frontend &amp;&amp; rtk npm run test:unit -- src/server/repositories/reports.test.ts src/server/exports/reports.test.ts &amp;&amp; rtk npm run lint</automated></verify>
  <done>Live officer writes/exports are verified before any dependent migration work.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-14 | Tampering/Elevation | status RPC | mitigate | JWT actor, SECURITY INVOKER, RLS, atomic SQL tests |
| T-07-15 | Tampering | exports | mitigate | formula neutralization and bounded rows |
</threat_model>
<verification>Per-wave: live SQL gate then `cd frontend && rtk npm test && rtk npm run lint`.</verification>
<rollback_stop_conditions>- Push/privilege/atomicity failure blocks completion. - Keep Python write/export fallback until Plan 12 live schema gate passes.</rollback_stop_conditions>
<success_criteria>Officer writes and exports run in Next.js with atomic auditability and spreadsheet safety.</success_criteria>
<output>Create `07-06-SUMMARY.md` when done.</output>
