---
phase: 07-google-free-self-hosted-platform
plan: "11"
type: execute
wave: 10
depends_on: ["07-02", "07-06", "07-07", "07-08", "07-09", "07-10"]
files_modified:
  - frontend/scripts/google-exit-audit.mjs
  - frontend/tests/google-exit-audit.test.ts
  - frontend/package.json
  - frontend/operations/local-cleanup-approval.json
autonomous: false
requirements: [SELFHOST-06]
requirements_addressed: [SELFHOST-06]
decisions_addressed: [D-15, D-16, D-17, D-18]
must_haves:
  truths:
    - An executable audit detects active Google/FastAPI/Python/Docker dependencies without false-positive failure on Google Fonts or historical provenance.
    - Missing source inventory/reconciliation/restore evidence is a failed audit, never a skip.
  artifacts:
    - path: frontend/scripts/google-exit-audit.mjs
      provides: Source/dependency/build/runtime/schema/manifest audit
  key_links:
    - from: frontend/scripts/google-exit-audit.mjs
      to: frontend/src/lib/fonts.ts
      via: exact Google Fonts allowlist
    - from: frontend/scripts/google-exit-audit.mjs
      to: Plan 08 reconciliation and Plan 09 restore manifests
      via: mandatory pass evidence
---

## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Build the fail-closed audit that governs irreversible cleanup and proves Google Fonts is the sole active Google exception.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md @.planning/phases/07-google-free-self-hosted-platform/07-08-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-09-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-10-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement exact allowlist Google-exit audit</name>
  <files>frontend/scripts/google-exit-audit.mjs, frontend/tests/google-exit-audit.test.ts, frontend/package.json</files>
  <read_first><file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file><file>frontend/src/lib/fonts.ts</file><file>frontend/package.json</file><file>docker-compose.yml</file><file>README.md</file></read_first>
  <behavior>- Detect exact forbidden imports/packages/env names/URLs/URI schemes/files/commands. - Allow next/font/google only in frontend/src/lib/fonts.ts and designated design docs. - Exclude applied historical migrations and .planning/phases provenance while checking live schema. - Fail when signed inventory/reconciliation/restore evidence is absent.</behavior>
  <action>Create a manifest-aware Node audit and `audit:google-exit` script. Scan active tracked source/config/docs, ignored local env key names (never values), direct dependencies and `rtk npm ls --all --json`, clean `.next/server` output, live schema for legacy column, process command line, scheduled task, bind address, CityMind Docker images/containers, and signed manifests. Detect Google SDKs/services, FastAPI/uvicorn/Python entrypoints, backend URLs, BigQuery/GCS, deployment assets, CityMind Dockerfiles/compose/images. Avoid false positives for transitive Docker detection, Python license strings, immutable applied migrations, and historical phase docs.</action>
  <acceptance_criteria><criterion>Fixture tests prove every forbidden class fails and only exact font allowlist passes.</criterion><criterion>Unavailable inventory, unsigned reconciliation, or missing restore evidence returns nonzero.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- tests/google-exit-audit.test.ts</automated></verify>
  <done>The cleanup gate is executable, precise, and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Record the pre-clean audit baseline without deleting anything</name>
  <files>frontend/scripts/google-exit-audit.mjs</files>
  <read_first><file>frontend/scripts/google-exit-audit.mjs</file><file>.planning/phases/07-google-free-self-hosted-platform/07-08-SUMMARY.md</file><file>.planning/phases/07-google-free-self-hosted-platform/07-09-SUMMARY.md</file></read_first>
  <action>Run audit against source, dependencies, clean build, live runtime/schema, scheduled task, env key names, CityMind images, and signed manifests. Emit a deterministic removal inventory grouped by tracked source, ignored local files, OS/runtime state, and separately authorized remote Google resources. Do not delete or disable anything in this plan. Treat expected legacy findings as a nonzero pre-clean baseline that becomes the exact input to Plan 12.</action>
  <acceptance_criteria><criterion>Every active legacy item has an exact path/name and cleanup disposition.</criterion><criterion>Remote resources are inventory-only unless separately authorized.</criterion></acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run build &amp;&amp; rtk npm run audit:google-exit -- --mode pre-clean --write-manifest</automated></verify>
  <done>A complete, non-destructive cleanup manifest exists and all safety evidence is attached.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve local cleanup from complete pre-clean evidence</name>
  <files>frontend/operations/local-cleanup-approval.json</files>
  <read_first><file>frontend/scripts/google-exit-audit.mjs</file><file>.planning/phases/07-google-free-self-hosted-platform/07-08-SUMMARY.md</file><file>.planning/phases/07-google-free-self-hosted-platform/07-09-SUMMARY.md</file></read_first>
  <action>Present the signed inventory/reconciliation, DB+Storage isolated restore, Python-capable application rollback, AI privacy/capability, loopback/TLS decision, production smoke, and pre-clean manifest. Record explicit local-only cleanup approval with signer, timestamp, evidence hashes, and PASS. Remote Google deletion remains unauthorized.</action>
  <acceptance_criteria><criterion>Every high-severity gate is signed PASS and the operator explicitly approves local schema/runtime cleanup.</criterion></acceptance_criteria>
  <verify><automated>rtk node frontend/scripts/verify-gate-artifacts.mjs --file frontend/operations/local-cleanup-approval.json --require-signed --require-inventory --require-reconciliation --require-db-restore --require-storage-restore --require-application-rollback --require-explicit-local-cleanup-approval</automated></verify>
  <done>Plan 12 is authorized or cleanup remains blocked with compatibility intact.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-27 | Repudiation | exit proof | mitigate | deterministic signed audit manifest |
| T-07-28 | Information Disclosure | env/runtime scan | mitigate | inspect/report key names only; never values |
| T-07-29 | Tampering | allowlist | mitigate | exact path/import allowlist with false-positive tests |
| T-07-30 | Data loss | cleanup authorization | mitigate | signed inventory/reconciliation/restore/application-rollback evidence and explicit local approval; no deletion in this plan |
</threat_model>
<verification>Per-wave: audit test fixtures, clean build, and pre-clean runtime audit manifest.</verification>
<rollback_stop_conditions>- This plan authorizes no deletion. - Missing safety evidence blocks Plan 12. - Do not broaden the Google Fonts allowlist or exclude active docs/config to make the audit pass.</rollback_stop_conditions>
<success_criteria>The project has an objective, testable deletion gate and exact cleanup inventory.</success_criteria>
<output>Create `07-11-SUMMARY.md` with the pre-clean manifest path/hash.</output>
