---
phase: 07-google-free-self-hosted-platform
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/operations/tooling-decision.json
  - frontend/scripts/verify-tooling-decision.mjs
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vitest.config.mts
  - frontend/tests/contracts/fastapi-golden/analyze.json
  - frontend/tests/contracts/fastapi-golden/citizen-status.json
  - frontend/tests/contracts/fastapi-golden/officer-reports.json
  - frontend/tests/contracts/fastapi-golden/analytics.json
  - frontend/tests/contracts/golden-contracts.test.ts
  - frontend/test-approvals/vitest-4.1.10.json
autonomous: false
requirements: [SELFHOST-01]
requirements_addressed: [SELFHOST-01]
decisions_addressed: [D-03]
must_haves:
  truths:
    - Existing citizen and officer HTTP behavior is captured before any Python removal.
    - TypeScript behavioral tests run independently of the Python runtime.
  artifacts:
    - path: frontend/operations/tooling-decision.json
      provides: Signed fail-closed native Supabase CLI and native PostgreSQL client installation decision
    - path: frontend/vitest.config.mts
      provides: Non-watch TypeScript unit test configuration
    - path: frontend/tests/contracts/fastapi-golden/analyze.json
      provides: Sanitized report-analysis request/response/status/header fixtures
    - path: frontend/tests/contracts/golden-contracts.test.ts
      provides: Executable fixture schema and parity guard
  key_links:
    - from: frontend/tests/contracts/golden-contracts.test.ts
      to: frontend/tests/contracts/fastapi-golden
      via: fixture loading and strict contract assertions
---

## Phase Goal

**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**

<objective>
Establish the executable parity harness that makes the runtime migration safe. Per D-03, capture current API behavior before changing or deleting the Python implementation.

Purpose: Prevent contract drift and create the Wave 0 feedback loop used by every later vertical slice.
Output: Human-approved Vitest installation, sanitized golden fixtures, and non-watch test scripts.
</objective>

<execution_context>
@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/minhmice/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-google-free-self-hosted-platform/07-CONTEXT.md
@.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md
@.planning/phases/07-google-free-self-hosted-platform/07-PATTERNS.md
</context>

<ownership_note>Intentional 11-file cohesive phase-entry slice: the tooling decision/verifier, package approval, runner configuration, four endpoint fixtures, executable contract test, and lockfiles jointly create the only usable Wave 1 parity gate. File-by-file review found no incidental path; removing any one breaks native schema readiness, package reproducibility, or an explicitly required golden contract.</ownership_note>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Establish the Windows schema-tooling gate</name>
  <files>frontend/operations/tooling-decision.json, frontend/scripts/verify-tooling-decision.mjs</files>
  <read_first>
    <file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file>
    <file>frontend/package.json</file>
  </read_first>
  <action>[BLOCKING] Confirm the research finding that native Supabase CLI and native `psql` are absent. Present the exact non-Docker Windows installation routes `rtk scoop bucket add supabase https://github.com/supabase/scoop-bucket.git`, `rtk scoop install supabase`, and `rtk winget install --exact --id PostgreSQL.PostgreSQL.17`; execute installation only after explicit operator approval. Then require `rtk supabase --version` and `rtk psql --version` to pass from the laptop PATH. Accept Supabase CLI only at `&gt;=2.48.3 &lt;3.0.0`; accept native `psql` only at `&gt;=15 &lt;18` and not older than the live PostgreSQL server major. Also confirm the installed PostgreSQL package exposes native `pg_dump` for Plan 09. Create a verifier that executes the native version probes, rejects wrappers/containers/remote aliases, and records executable absolute paths, versions, server version, installer route, signer, timestamp, hashes, `approved:true`, and `status:"PASS"` in the decision JSON. There is no alternate/equivalent branch. Do not use `npx --yes` or Docker. Missing tools, refused installation, incompatible versions, or failed probes stop the phase.</action>
  <acceptance_criteria>
    <criterion>Native `rtk supabase --version` and `rtk psql --version` probes pass at accepted versions and are signed PASS.</criterion>
    <criterion>The decision contains no credential values and authorizes no Docker route.</criterion>
  </acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker</automated></verify>
  <done>Native Supabase CLI and native PostgreSQL clients are installed and approved, or phase execution stops.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify the official Vitest package before installation</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/test-approvals/vitest-4.1.10.json</files>
  <read_first>
    <file>.planning/phases/07-google-free-self-hosted-platform/07-RESEARCH.md</file>
    <file>frontend/package.json</file>
    <file>frontend/package-lock.json</file>
  </read_first>
  <action>Before any install, show the operator the exact candidate `vitest@4.1.10`, its npm page `npmjs.com/package/vitest`, official repository `github.com/vitest-dev/vitest`, and the official Next.js Vitest guide. Explain that slopcheck marked it SUS only because its name resembles `vite`. Do not install until the operator explicitly approves this exact identity/version. Record package, version, registry/repository URLs, signer, timestamp, and `approved:true` in `frontend/test-approvals/vitest-4.1.10.json`.</action>
  <acceptance_criteria>
    <criterion>Execution summary records explicit approval for `vitest@4.1.10`.</criterion>
    <criterion>No package files change before approval.</criterion>
  </acceptance_criteria>
  <verify><automated>rtk powershell -NoProfile -Command "$a=ConvertFrom-Json (Get-Content -Raw frontend/test-approvals/vitest-4.1.10.json); if($a.package -ne 'vitest' -or $a.version -ne '4.1.10' -or $a.approved -ne $true -or -not $a.signer){exit 1}"</automated></verify>
  <done>The exact official package/version is approved or execution stops without modifying dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Install the test runner and capture sanitized legacy contracts</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vitest.config.mts, frontend/tests/contracts/fastapi-golden/analyze.json, frontend/tests/contracts/fastapi-golden/citizen-status.json, frontend/tests/contracts/fastapi-golden/officer-reports.json, frontend/tests/contracts/fastapi-golden/analytics.json, frontend/tests/contracts/golden-contracts.test.ts</files>
  <read_first>
    <file>frontend/package.json</file>
    <file>backend/app/schemas.py</file>
    <file>backend/app/api/reports.py</file>
    <file>backend/app/api/analytics.py</file>
    <file>backend/tests/test_analyze.py</file>
    <file>backend/tests/test_citizen_status.py</file>
    <file>backend/tests/test_reports.py</file>
    <file>backend/tests/test_export.py</file>
    <file>backend/tests/test_analytics_api.py</file>
  </read_first>
  <action>After Task 2 approval, run `rtk npm install --save-dev vitest@4.1.10`. Add `test:unit = rtk vitest run`, `test:legacy = rtk node --test tests/*.test.mjs`, and `test = rtk npm run test:unit &amp;&amp; rtk npm run test:legacy`. Create `vitest.config.mts` for Node environment tests. Build sanitized JSON fixtures covering analyze text/image bounds and generic 502, issue-once token, uniform citizen 401, officer auth/filters/cursor/404, status note rules, CSV/XLSX headers, analytics range/empty/privacy behavior, and rate-limit `Retry-After`. Fixtures must contain no real token, report description, evidence bytes, credential, endpoint, or citizen PII.</action>
  <acceptance_criteria>
    <criterion>`rtk npm run test:unit -- tests/contracts/golden-contracts.test.ts` passes.</criterion>
    <criterion>Every legacy endpoint listed in 07-RESEARCH.md has at least one fixture case or an explicit reference to a later slice test.</criterion>
    <criterion>Fixture scan contains no access token, authorization header value, service-role key, endpoint URL, or evidence payload.</criterion>
  </acceptance_criteria>
  <verify><automated>cd frontend &amp;&amp; rtk npm run test:unit -- tests/contracts/golden-contracts.test.ts &amp;&amp; rtk npm run test:legacy</automated></verify>
  <done>The TypeScript parity harness is executable and the last-known Python contract is preserved without secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy runtime -> fixtures | Potentially sensitive production-shaped data is converted into committed test data |
| npm registry -> development toolchain | A flagged package enters the dependency graph only after human verification |
| Native Windows tooling -> live Supabase | Locally installed schema and SQL executables cross into the linked self-hosted database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-01 | Information Disclosure | golden fixtures | mitigate | synthetic/sanitized values plus exact secret/PII scan |
| T-07-02 | Tampering/Data loss | native schema tooling | mitigate | official non-Docker install routes, absolute executable paths, strict versions, signed probes, and fail-closed verification before schema work |
| T-07-SC | Tampering | vitest install | mitigate | blocking human verification of the official package and exact version |
</threat_model>

<verification>
Per-wave: `cd frontend && rtk npm test && rtk npm run lint`.
Final: fixtures cover the current FastAPI contract and remain runnable without Python.
</verification>

<rollback_stop_conditions>
- Stop before installation if package identity/version is not explicitly approved.
- Preserve all backend files and the last Python-capable revision; this plan authorizes no runtime deletion.
- Revert only this plan's dependency/test files if the fixture suite cannot run under Node 22.
</rollback_stop_conditions>

<success_criteria>
The migration has a trusted behavioral baseline and every later plan can prove parity with a targeted non-watch command.
</success_criteria>

<output>Create `.planning/phases/07-google-free-self-hosted-platform/07-01-SUMMARY.md` when done.</output>
