---
phase: 07-google-free-self-hosted-platform
plan: "12"
type: execute
wave: 11
depends_on: ["07-11"]
files_modified:
  - supabase/migrations/20260721130005_remove_legacy_evidence.sql
  - supabase/tests/07_remove_legacy_evidence.sql
  - supabase/tests/07_evidence_final.sql
autonomous: false
requirements: [SELFHOST-04, SELFHOST-06]
requirements_addressed: [SELFHOST-04, SELFHOST-06]
decisions_addressed: [D-10, D-11, D-15]
user_setup:
  - service: supabase
    why: Approval-gated legacy schema removal
    env_vars: [SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, SUPABASE_DB_URL]
    dashboard_config: [Link the existing self-hosted project]
must_haves:
  truths:
    - Signed inventory, reconciliation, DB+Storage restore, application rollback, and explicit local approval precede destructive SQL.
    - The legacy evidence column is dropped only after fail-closed live assertions prove complete private Storage parity.
  artifacts:
    - path: supabase/migrations/20260721130005_remove_legacy_evidence.sql
      provides: Approval-gated destructive legacy-column removal
    - path: supabase/tests/07_remove_legacy_evidence.sql
      provides: Live legacy-removal and private-evidence contract
    - path: supabase/tests/07_evidence_final.sql
      provides: Durable post-cutover evidence/RLS contract with no legacy-column dependency
  key_links:
    - from: frontend/operations/local-cleanup-approval.json
      to: supabase/migrations/20260721130005_remove_legacy_evidence.sql
      via: signed approval verification before migration creation and push
---
## Phase Goal
**As the CityMind operator, I want CityMind consolidated into a Next.js-only application running directly on my laptop with self-hosted Supabase and a configurable third-party AI API key, so that I can operate without FastAPI, Python, Docker, or Google Cloud services except Google Fonts.**
<objective>Remove the legacy evidence schema only after every inventory, reconciliation, restore, rollback, and operator-approval gate passes.</objective>
<execution_context>@C:/Users/minhmice/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/07-google-free-self-hosted-platform/07-01-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-09-SUMMARY.md @.planning/phases/07-google-free-self-hosted-platform/07-11-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Stage fail-closed legacy evidence removal SQL after approval</name>
  <files>supabase/migrations/20260721130005_remove_legacy_evidence.sql, supabase/tests/07_remove_legacy_evidence.sql, supabase/tests/07_evidence_final.sql</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>frontend/operations/local-cleanup-approval.json</file><file>frontend/migration-manifests/reconciliation.json</file><file>frontend/migration-manifests/restore-and-rollback-gate.json</file><file>supabase/migrations/20260721130004_evidence_path_additive.sql</file></read_first>
  <action>Verify the signed native-tooling and local-cleanup approvals first. Create immutable migration `20260721130005_remove_legacy_evidence.sql` and migration-stage `07_remove_legacy_evidence.sql` assertions for reconciliation PASS, exact DB+Storage restore, preserved-application rollback PASS, zero active `gs://`, complete `evidence_path`, private Storage read parity, and no legacy reader dependency before dropping `image_gcs_uri`. Separately create durable `07_evidence_final.sql` that assumes `image_gcs_uri` is absent and proves final `evidence_path` constraints, private bucket/RLS denial for anonymous and cross-role access, authorized officer reads, MIME/path integrity, and zero legacy function/view/policy dependencies. Do not push and do not remove GCS/Python readers in this task.</action>
  <acceptance_criteria><criterion>Migration raises before any drop when any signed or live evidence assertion is absent or false.</criterion><criterion>`07_evidence_final.sql` contains no `image_gcs_uri` acceptance dependency and validates durable post-cutover RLS/evidence behavior.</criterion><criterion>No compatibility reader is removed in this task.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker &amp;&amp; rtk powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260721130005_remove_legacy_evidence.sql; $f=Get-Content -Raw supabase/tests/07_evidence_final.sql; if($s -notmatch 'image_gcs_uri' -or $s -notmatch 'evidence_path' -or $s -notmatch 'raise exception' -or $f -notmatch 'evidence_path' -or $f -match 'select.+image_gcs_uri'){exit 1}"</automated></verify>
  <done>Destructive SQL is staged only after approval and cannot bypass recovery/evidence assertions.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push and live-test the approved destructive schema migration</name>
  <files>supabase/migrations/20260721130005_remove_legacy_evidence.sql, supabase/tests/07_remove_legacy_evidence.sql, supabase/tests/07_evidence_final.sql</files>
  <read_first><file>frontend/operations/tooling-decision.json</file><file>frontend/operations/local-cleanup-approval.json</file><file>supabase/migrations/20260721130005_remove_legacy_evidence.sql</file><file>supabase/tests/07_remove_legacy_evidence.sql</file><file>supabase/tests/07_evidence_final.sql</file></read_first>
  <action>[BLOCKING] Re-verify native Supabase CLI/`psql` and explicit Plan 11 cleanup approval. Run `rtk supabase db push`, then the migration-stage `rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_remove_legacy_evidence.sql`, then the durable post-cutover `rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_evidence_final.sql`. Stop on any tooling, credential, linkage, signature, push, assertion, or live SQL failure; do not remove readers before both live contracts pass.</action>
  <acceptance_criteria><criterion>Live schema has no image_gcs_uri and retained evidence resolves privately through evidence_path.</criterion><criterion>Durable final evidence/RLS contract passes after the drop.</criterion><criterion>Restore/rollback gate remains signed PASS.</criterion></acceptance_criteria>
  <verify><automated>rtk supabase --version &amp;&amp; rtk psql --version &amp;&amp; rtk node frontend/scripts/verify-tooling-decision.mjs --file frontend/operations/tooling-decision.json --require-signed --require-native-supabase --supabase-range 2.48.3:3.0.0 --require-native-psql --psql-range 15:18 --forbid-docker &amp;&amp; rtk supabase db push &amp;&amp; rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_remove_legacy_evidence.sql &amp;&amp; rtk psql $env:SUPABASE_DB_URL -v ON_ERROR_STOP=1 -f supabase/tests/07_evidence_final.sql &amp;&amp; rtk node frontend/scripts/verify-gate-artifacts.mjs --gate restore-and-rollback --require-signed --require-pass</automated></verify>
  <done>Legacy evidence schema is removed only through the approved live gate.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-30 | Data loss | destructive migration | mitigate | signed approval plus live fail-closed assertions before drop |
| T-07-31 | Tampering | migration execution | mitigate | immutable monotonic migration and exact live SQL contract |
| T-07-36 | Tampering | evidence contract lifecycle | mitigate | additive-stage SQL runs only in Plan 08; durable final SQL runs after legacy removal and in Plan 15 |
</threat_model>
<verification>Signed tooling/cleanup evidence, migration assertions, push, and exact live legacy-removal SQL all pass.</verification>
<rollback_stop_conditions>- Any missing/failed tooling decision, signature, reconciliation, restore, application rollback, approval, schema assertion, push, or SQL test blocks removal. - Keep backups/revision and readers until this live gate passes. - Remote deletion is unauthorized.</rollback_stop_conditions>
<success_criteria>The legacy evidence column is removed without bypassing D-10/D-11 recovery proof.</success_criteria>
<output>Create `07-12-SUMMARY.md` with approval and migration evidence hashes, never secrets.</output>
