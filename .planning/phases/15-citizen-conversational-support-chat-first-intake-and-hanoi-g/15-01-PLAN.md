---
phase: 15-citizen-conversational-support-chat-first-intake-and-hanoi-g
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/domain/hanoi-analysis.ts
  - src/server/domain/guidance-resolver.ts
  - src/server/domain/guidance-resolver.test.ts
  - src/server/ai/hanoi.ts
  - src/server/ai/openai-compatible.ts
  - src/server/ai/openai-compatible.test.ts
  - src/server/validation/hanoi-policy.ts
  - src/server/validation/hanoi-policy.test.ts
  - src/server/triage/service.ts
  - src/server/triage/config.ts
  - src/server/domain/analysis-projection.ts
  - supabase/migrations/20260723120001_hanoi_analysis_columns.sql
autonomous: true
requirements: [TRIAGE-15, TRIAGE-07]
must_haves:
  truths:
    - Triage loads Hanoi v5.2 system prompt and validates 16-key output
    - resolveGuidanceScript returns bilingual script for valid low/medium type-1 codes
    - high/critical and generate_later always resolve to generate_later
    - Empty provider content fails before Zod parse with invalid_response
    - Hanoi columns exist on reports table for persistence
  artifacts:
    - path: src/server/domain/hanoi-analysis.ts
      provides: HanoiAnalysisSchema with 16 keys
      exports: ["HanoiAnalysisSchema", "HANOI_SEVERITY_TO_INT"]
    - path: src/server/domain/guidance-resolver.ts
      provides: Deterministic script resolver
      exports: ["resolveGuidanceScript"]
    - path: src/server/ai/hanoi.ts
      provides: Hanoi config loader
      exports: ["buildHanoiSystemPrompt", "getHanoiConfigVersion"]
    - path: supabase/migrations/20260723120001_hanoi_analysis_columns.sql
      provides: Additive Hanoi columns on reports
  key_links:
    - from: src/server/ai/openai-compatible.ts
      to: src/server/domain/hanoi-analysis.ts
      via: HanoiAnalysisSchema.parse after provider response
      pattern: HanoiAnalysisSchema
    - from: src/server/triage/service.ts
      to: src/server/ai/hanoi.ts
      via: buildHanoiSystemPrompt for analyzeStructured
      pattern: buildHanoiSystemPrompt
    - from: src/server/domain/guidance-resolver.ts
      to: prompt/citymind_hanoi_guidance_scripts_v2_bilingual (1).json
      via: loadScriptCatalog at module init
      pattern: guidance_code
---

<objective>
Wave 1 — Hanoi v5.2 classifier foundation: 16-key Zod schema, config loader, guidance script resolver with unit tests, Hanoi policy validation, DB migration, and triage service wired to Hanoi prompt (replacing broken evaluator JSON path) per D-15-02 and D-15-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-CONTEXT.md
@.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-RESEARCH.md
@.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-PATTERNS.md
@prompt/citymind_ai_hanoi_triage_guidance_v5_2 (1).json
@prompt/citymind_hanoi_guidance_scripts_v2_bilingual (1).json
@src/server/ai/evaluator.ts
@src/server/domain/evaluator-analysis.ts
@src/server/ai/openai-compatible.ts
@src/server/triage/service.ts
@src/server/validation/evaluator-policy.ts

<interfaces>
From evaluator.ts (pattern to mirror in hanoi.ts):
- loadEvaluatorConfig(configPath?: string): EvaluatorConfig
- buildEvaluatorSystemPrompt(configPath?: string): string
- getEvaluatorConfigVersion(configPath?: string): string

From openai-compatible.ts:
- analyzeStructured(input, systemInstruction?): Promise<StructuredAnalysisResult>
- AnalysisProviderError codes include invalid_response, schema_invalid

Hanoi 16 required keys: category, matched_known_issue, observed_facts, inferences, unknowns, severity (string enum), severity_reason, confidence (0.9|0.75|0.55|0.3), handling_type (1|2|3), handling_label, allowed_actions, prohibited_actions, recommended_action, guidance_code, critical_alert, requires_human_review (true)

Severity int mapping: low→1, medium→2, high→4, critical→5

Locked: D-15-02 Hanoi v5.2 prompt; D-15-03 guidance_code resolver; D-15-06 requires_human_review always true
</interfaces>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | Hanoi v5.2 triage classifier | Tasks 1–3 |
| REQ | TRIAGE-15 Hanoi 16-key schema | Task 1 |
| REQ | TRIAGE-07 policy validation | Task 2 |
| RESEARCH | guidance resolver design | Task 1 |
| RESEARCH | empty provider content fix | Task 3 |
| RESEARCH | DB migration columns | Task 3 |
| CONTEXT | D-15-02 Hanoi prompt | Task 3 |
| CONTEXT | D-15-03 script resolver | Task 1 |
| CONTEXT | D-15-06 advisory only | Task 2 policy |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HanoiAnalysisSchema + guidance resolver</name>
  <files>src/server/domain/hanoi-analysis.ts, src/server/domain/guidance-resolver.ts, src/server/domain/guidance-resolver.test.ts, src/server/ai/hanoi.ts</files>
  <behavior>
    - HanoiAnalysisSchema.strict() validates all 16 keys; severity is z.enum(["low","medium","high","critical"]); confidence z.union of literal anchors; requires_human_review z.literal(true)
    - loadHanoiCategories and loadGuidanceCodeEnum read from Hanoi JSON at module init (mirror evaluator-analysis pattern but path prompt/citymind_ai_hanoi_triage_guidance_v5_2 (1).json)
    - HANOI_SEVERITY_TO_INT maps string severity to integer 1/2/4/5
    - resolveGuidanceScript: self_collect_safe_litter + low + type 1 → script_ready with vi-VN when report_text is Vietnamese
    - resolveGuidanceScript: report_road_damage + medium + type 2 → script_ready en-US for English text
    - resolveGuidanceScript: any high/critical → generate_later reason high_and_critical
    - resolveGuidanceScript: guidance_code generate_later → generate_later
    - resolveGuidanceScript: mismatched handling_type vs script required_handling_type → generate_later
    - hanoi.ts exports buildHanoiSystemPrompt, getHanoiConfigVersion (config_version 5.2.0)
  </behavior>
  <action>
    Create hanoi-analysis.ts with Zod schema loading enums from Hanoi JSON. Create hanoi.ts mirroring evaluator.ts with default path to Hanoi v5.2 JSON file (use path.resolve with exact filename including space and parentheses). Create guidance-resolver.ts loading script catalog from citymind_hanoi_guidance_scripts_v2_bilingual (1).json; implement resolveGuidanceScript per 15-RESEARCH algorithm and catalog behavior block. Export inferOutputLanguage(report_text) for vi-VN vs en-US using same rule as prompt (Vietnamese majority in report_text). Write guidance-resolver.test.ts covering at least 6 cases: script_ready vi, script_ready en, high→generate_later, generate_later code, handling mismatch, missing code. Per D-15-03.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/domain/guidance-resolver.test.ts</automated>
  </verify>
  <done>HanoiAnalysisSchema and resolveGuidanceScript pass unit tests; hanoi.ts loads v5.2 config (TRIAGE-15, D-15-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hanoi policy validation</name>
  <files>src/server/validation/hanoi-policy.ts, src/server/validation/hanoi-policy.test.ts</files>
  <behavior>
    - Reject handling_type 1 when severity is not low
    - Reject handling_type 3 when severity is low or medium
    - Reject critical_alert true when severity is not critical (and vice versa)
    - Reject matched_known_issue false when category is not other
    - Reject guidance_code self_* when handling_type is not 1
    - Pass valid micro-example payloads from Hanoi prompt
  </behavior>
  <action>
    Create hanoi-policy.ts with validateHanoiPolicy(analysis: HanoiAnalysis): PolicyResult mirroring evaluator-policy.ts structure. Encode handling/severity/critical_alert/matched_known_issue cross-field rules from Hanoi system_prompt FINAL CHECK section. Write hanoi-policy.test.ts with violations for each rule plus one passing LOW/MEDIUM/HIGH/CRITICAL micro-example. Per D-15-04 handling model and D-15-06 requires_human_review enforcement at schema level.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/validation/hanoi-policy.test.ts</automated>
  </verify>
  <done>Hanoi policy validation enforces handling_type routing invariants (TRIAGE-07, TRIAGE-15).</done>
</task>

<task type="auto">
  <name>Task 3: Wire triage to Hanoi + DB migration</name>
  <files>src/server/ai/openai-compatible.ts, src/server/ai/openai-compatible.test.ts, src/server/triage/service.ts, src/server/triage/config.ts, src/server/domain/analysis-projection.ts, supabase/migrations/20260723120001_hanoi_analysis_columns.sql</files>
  <action>
    Update openai-compatible.ts: replace EvaluatorAnalysisSchema with HanoiAnalysisSchema for analyzeStructured path; use buildHanoiSystemPrompt from hanoi.ts; after fetch, reject null/empty/refusal message content with AnalysisProviderError invalid_response before JSON.parse; run validateHanoiPolicy after Zod parse. Update triage/service.ts to persist Hanoi fields: severity_label, mapped severity int, handling_type, handling_label, guidance_code, critical_alert, matched_known_issue, allowed_actions, prohibited_actions; set PROMPT_VERSION or new HANOI_CONFIG_VERSION from getHanoiConfigVersion in triage audit metadata. Add migration 20260723120001_hanoi_analysis_columns.sql with additive columns (IF NOT EXISTS). Extend analysis-projection.ts with projectHanoiToLegacyRow helper for officer views. Update openai-compatible.test.ts for empty content rejection and valid Hanoi parse. Set triage/config.ts export HANOI_PROMPT_VERSION = "5.2.0". Per D-15-02.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/ai/openai-compatible.test.ts src/server/validation/hanoi-policy.test.ts</automated>
  </verify>
  <done>Triage uses Hanoi v5.2 end-to-end; empty provider responses gated; DB columns ready (TRIAGE-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI provider → server | Untrusted JSON; must pass Zod + policy before persistence |
| Script catalog → citizen | Only resolved text crosses boundary, never raw classifier output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | openai-compatible analyzeStructured | mitigate | Zod strict 16-key + hanoi-policy cross-field validation |
| T-15-02 | Information Disclosure | guidance resolver | mitigate | Return script text only; no internal script_id to citizen API |
| T-15-03 | Denial of Service | JSON parse on empty content | mitigate | invalid_response before parse; triage retry/manual_review |
| T-15-04 | Elevation | handling_type 1 misclassification | mitigate | Policy rejects type 1 unless severity low; high/critical → generate_later |
| T-15-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
```bash
npm run test:unit -- src/server/domain/guidance-resolver.test.ts src/server/validation/hanoi-policy.test.ts src/server/ai/openai-compatible.test.ts
```
</verification>

<success_criteria>
- Hanoi v5.2 is the active triage classifier (no import of deleted evaluator JSON on triage path)
- resolveGuidanceScript unit tests pass for vi/en script_ready and generate_later paths
- Hanoi policy rejects invalid handling/severity combinations
- Migration adds Hanoi columns without breaking existing reports
</success_criteria>

<output>
Create `.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-01-SUMMARY.md` when done
</output>
