---
phase: 15-citizen-conversational-support-chat-first-intake-and-hanoi-g
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/server/services/citizen-chat-intake.ts
  - src/server/services/citizen-chat-intake.test.ts
  - src/app/api/public/reports/intake/start/route.ts
  - src/app/api/public/reports/intake/messages/route.ts
  - src/app/api/public/reports/intake/submit/route.ts
  - src/server/security/rate-limit.ts
  - messages/en.json
  - messages/vi.json
autonomous: true
requirements: [PUB-07, TRIAGE-01, CIT-02, SHELP-02]
must_haves:
  truths:
    - POST intake/start creates report + access_token on first message boundary
    - Token-scoped intake messages persist to chat_messages
    - POST intake/submit finalizes description/location/evidence and runs sync triage
    - Wrong token returns 401 on intake APIs
    - Rate limits apply to intake message endpoints
  artifacts:
    - path: src/server/services/citizen-chat-intake.ts
      provides: Intake session orchestration
      exports: ["handleIntakeStartRequest", "handleIntakeMessagesRequest", "handleIntakeSubmitRequest"]
    - path: src/app/api/public/reports/intake/start/route.ts
      provides: Session start endpoint
    - path: src/app/api/public/reports/intake/messages/route.ts
      provides: Token-scoped chat CRUD
    - path: src/app/api/public/reports/intake/submit/route.ts
      provides: Finalize + triage dispatch
  key_links:
    - from: src/app/api/public/reports/intake/messages/route.ts
      to: src/server/services/citizen-chat-intake.ts
      via: handleIntakeMessagesRequest
      pattern: handleIntakeMessagesRequest
    - from: src/server/services/citizen-chat-intake.ts
      to: src/server/repositories/chat-messages.ts
      via: insertChatMessage / listChatMessagesByReportId
      pattern: insertChatMessage
    - from: src/server/services/citizen-chat-intake.ts
      to: src/server/triage/dispatch.ts
      via: dispatchTriageAndWait on submit
      pattern: dispatchTriageAndWait
---

<objective>
Wave 2 — Chat-first intake backend: token-scoped intake session (start/messages/submit APIs), facilitator conversation service extending coach patterns, chat_messages persistence, and sync triage on finalize per D-15-01, D-15-05, D-15-07, D-15-09.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-CONTEXT.md
@.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-PATTERNS.md
@src/server/services/citizen-coach.ts
@src/server/services/citizen-coach.test.ts
@src/server/repositories/chat-messages.ts
@src/server/services/report-service.ts
@src/server/triage/dispatch.ts
@src/app/api/public/reports/coach/messages/route.ts

<interfaces>
From citizen-coach.ts:
- authorizeCoachRequest(client, reportId, token) via hashAccessToken + tokenBindsReport
- CoachSendSchema / CoachAuthSchema Zod patterns
- insertChatMessage, listChatMessagesByReportId
- enforceCoachRateLimit, checkAiHealth

From report-service.ts:
- createIntakeReportWithAccessToken, issueAccessToken
- buildIntakeTriageOutcome, dispatchTriageAndWait fallback to enqueueTriageDispatch

Intake facilitator: distinct system prompt (collect description, location, evidence — NOT triage classification). Use triage provider env per 15-RESEARCH discretion.

Locked: D-15-05 token privacy; D-15-07 extend coach stack; D-15-09 report persisted with token
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: citizen-chat-intake service core</name>
  <files>src/server/services/citizen-chat-intake.ts, src/server/services/citizen-chat-intake.test.ts</files>
  <behavior>
    - handleIntakeStartRequest: creates report with triage_status pending, issues access token, returns report_id + access_token + welcome assistant message persisted
    - handleIntakeMessagesRequest GET: lists messages for authorized report
    - handleIntakeMessagesRequest POST: validates message length, rate limit, persists user message, calls facilitator AI, persists assistant reply
    - authorizeIntakeRequest rejects wrong token with 401 (same as coach)
    - AI down returns 503 with calm message when checkAiHealth fails
  </behavior>
  <action>
    Create citizen-chat-intake.ts mirroring citizen-coach.ts structure. Add INTAKE_FACILITATOR_SYSTEM_PROMPT constant (bilingual-aware: respond in user's language) instructing collection of incident description, optional location, optional photo — never classify or assign severity. Implement handleIntakeStartRequest, handleIntakeMessagesRequest (GET+POST), stub handleIntakeSubmitRequest signature for Task 2. Reuse chat-messages repository and access token auth from coach. Add enforceIntakeRateLimit alias or reuse enforceCoachRateLimit with intake namespace key. Write citizen-chat-intake.test.ts mocking AI and DB: 401 wrong token, 200 start returns ids, POST message persists two rows. Per D-15-05, D-15-07.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-chat-intake.test.ts</automated>
  </verify>
  <done>Intake service auth, start, and message exchange tested (PUB-07, CIT-02).</done>
</task>

<task type="auto">
  <name>Task 2: Intake API routes</name>
  <files>src/app/api/public/reports/intake/start/route.ts, src/app/api/public/reports/intake/messages/route.ts, src/server/security/rate-limit.ts</files>
  <action>
    Create route handlers delegating to citizen-chat-intake service. start/route.ts POST only. messages/route.ts GET+POST. Follow coach/messages/route.ts thin-delegation pattern. If rate-limit.ts needs intake-specific bucket, add enforceIntakeRateLimit with per-report+IP key distinct from coach (prevent shared exhaustion). Return generic JSON errors via jsonErrorResponse — no stack traces. Per D-15-05.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-chat-intake.test.ts</automated>
  </verify>
  <done>Intake start and messages HTTP endpoints wired (PUB-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Intake submit + i18n keys</name>
  <files>src/app/api/public/reports/intake/submit/route.ts, src/server/services/citizen-chat-intake.ts, src/server/services/citizen-chat-intake.test.ts, messages/en.json, messages/vi.json</files>
  <behavior>
    - handleIntakeSubmitRequest: requires description min 5 chars, optional lat/lng, optional evidence upload
    - Updates report row, runs dispatchTriageAndWait, returns ReportSubmissionResponse shape with outcome projection
    - On triage failure: enqueueTriageDispatch fallback (mirror submitReport)
    - Test: submit returns report_id, access_token, triage_status, service_step fields
  </behavior>
  <action>
    Implement handleIntakeSubmitRequest in citizen-chat-intake.ts: parse multipart or JSON body with report_id, token, description, optional coordinates, optional image (reuse report-service evidence upload helpers or delegate to shared parseReportFormData patterns). Call dispatchTriageAndWait then buildIntakeTriageOutcome from report-service. Create submit/route.ts POST handler. Add public.intake namespace to en.json and vi.json: welcome, placeholder, submitReport, loadError, sendError, aiDown, classicFormLink — parity required. Extend tests for submit happy path and description validation 400. Per D-15-09 and TRIAGE-01.
  </action>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-chat-intake.test.ts</automated>
  </verify>
  <done>Intake finalize persists report, runs sync triage, returns outcome (PUB-07, TRIAGE-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen → intake API | Untrusted messages; token required after start |
| Facilitator AI → DB | Advisory prompts only; no status changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Spoofing | intake/messages | mitigate | hashAccessToken + tokenBindsReport on every request |
| T-15-06 | Denial of Service | intake message flood | mitigate | enforceIntakeRateLimit per report+IP; MAX_MESSAGES_PER_DAY |
| T-15-07 | Information Disclosure | error responses | mitigate | jsonErrorResponse generic messages; no provider errors |
| T-15-08 | Tampering | cross-report token reuse | mitigate | 401 when token report_id mismatch |
| T-15-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
```bash
npm run test:unit -- src/server/services/citizen-chat-intake.test.ts
```
</verification>

<success_criteria>
- Intake start returns report_id + access_token immediately
- Messages API requires valid token; persists to chat_messages
- Submit triggers sync triage and returns citizen outcome projection
- EN/VI intake copy keys exist with parity
</success_criteria>

<output>
Create `.planning/phases/15-citizen-conversational-support-chat-first-intake-and-hanoi-g/15-02-SUMMARY.md` when done
</output>
