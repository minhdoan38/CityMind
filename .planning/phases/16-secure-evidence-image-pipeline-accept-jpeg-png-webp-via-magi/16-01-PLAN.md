---
phase: 16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/evidence-limits.ts
  - src/server/services/clamav-client.ts
  - src/server/services/clamav-client.test.ts
  - src/server/services/evidence-service.ts
autonomous: true
requirements: [SEC-IMG-02, SEC-IMG-05, SEC-IMG-08]
user_setup:
  - service: clamav
    why: "Malware scanning daemon for evidence uploads (production fail-closed)"
    env_vars:
      - name: CLAMAV_ENABLED
        source: "false in dev .env.example; true in production docs — explicit opt-out only for local dev without clamd"
      - name: CLAMAV_HOST
        source: "ClamAV clamd TCP host — default 127.0.0.1"
      - name: CLAMAV_PORT
        source: "ClamAV clamd TCP port — default 3310"
      - name: CLAMAV_TIMEOUT_MS
        source: "Scan/ping timeout ms — default 30000"
    dashboard_config:
      - task: "Install ClamAV Windows MSI; enable TCPSocket 3310; set StreamMaxLength >= 10M; start ClamD service"
        location: "OS service manager / clamd.conf"
must_haves:
  truths:
    - sharp and clamscan are installed dependencies with slopcheck-approved versions
    - Client-safe evidence-limits module exports 10MB default aligned with Supabase bucket
    - ClamAV client fails closed on scan errors when CLAMAV_ENABLED is true
    - Unit tests mock clamscan without requiring live clamd
  artifacts:
    - path: src/lib/evidence-limits.ts
      provides: Shared MAX_IMAGE_BYTES constant for client and server
      exports: [DEFAULT_MAX_EVIDENCE_BYTES, resolveMaxEvidenceBytesFromEnv]
    - path: src/server/services/clamav-client.ts
      provides: INSTREAM scan + ping wrapper
      exports: [assertCleanBuffer, pingClamav, isClamavEnabled, EvidenceScanError]
    - path: src/server/services/clamav-client.test.ts
      provides: Mocked infected/clean/unavailable scan cases
  key_links:
    - from: src/server/services/clamav-client.ts
      to: clamscan npm
      via: NodeClam.init with localFallback false
      pattern: localFallback:\s*false
    - from: src/server/services/evidence-service.ts
      to: src/lib/evidence-limits.ts
      via: import DEFAULT_MAX_EVIDENCE_BYTES
      pattern: evidence-limits
---

<objective>
Wave 1 foundation — install approved image/malware packages, centralize 10MB evidence limits in a client-safe module, and ship a fail-closed ClamAV TCP client with mocked unit tests. No upload pipeline or API wiring yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-VALIDATION.md
@package.json
@src/server/services/evidence-service.ts
@supabase/migrations/20260721130001_next_backend_contract.sql

<interfaces>
From evidence-service.ts (current):
- DEFAULT_MAX_EVIDENCE_BYTES = 8 * 1024 * 1024 (change source to evidence-limits.ts)
- resolveMaxEvidenceBytes(env): number — reads MAX_IMAGE_BYTES env
- ALLOWED_IMAGE_MIME_TYPES: jpeg, png, webp only

Locked stack (user decision):
- npm: sharp ^0.35.3, clamscan ^2.4.0, file-type already installed
- ClamAV INSTREAM via clamd TCP; localFallback: false; fail closed on scanner error (503 at API layer in 16-03)
- Max 10MB aligned client/server/bucket (10485760)
- CLAMAV_ENABLED: false default in dev docs, true in production docs (Open Q1 RESOLVED)
</interfaces>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | Secure evidence pipeline foundation | 16-01 deps + clamav client |
| REQ | SEC-IMG-02 ClamAV fail closed | Task 2–3 |
| REQ | SEC-IMG-05 10MB max | Task 1 evidence-limits |
| REQ | SEC-IMG-08 shared limits module | Task 1 |
| RESEARCH | Package Legitimacy Audit all OK | Task 1 install |
| RESEARCH | clamav-client.ts Pattern 2 | Task 3 |
| RESEARCH | evidence-limits.ts client-safe | Task 1 |
| PATTERNS | ai-readiness lazy singleton analog | Task 3 |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sharp/clamscan and create evidence-limits module</name>
  <files>package.json, package-lock.json, src/lib/evidence-limits.ts, src/server/services/evidence-service.ts</files>
  <read_first>
    <file>package.json</file>
    <file>src/server/services/evidence-service.ts</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md</file>
  </read_first>
  <action>
    Run npm install sharp@^0.35.3 clamscan@^2.4.0 per Package Legitimacy Audit (all OK). Create src/lib/evidence-limits.ts without server-only import: export DEFAULT_MAX_EVIDENCE_BYTES = 10 * 1024 * 1024; export resolveMaxEvidenceBytesFromEnv(env) parsing MAX_IMAGE_BYTES then NEXT_PUBLIC_MAX_IMAGE_BYTES with same validation as current resolveMaxEvidenceBytes. Update evidence-service.ts: import DEFAULT_MAX_EVIDENCE_BYTES and resolveMaxEvidenceBytesFromEnv from @/lib/evidence-limits; re-export DEFAULT_MAX_EVIDENCE_BYTES for backward compat; change resolveMaxEvidenceBytes to delegate to resolveMaxEvidenceBytesFromEnv(process.env). Do not change upload or validation logic beyond the constant source move.
  </action>
  <acceptance_criteria>
    <criterion>package.json dependencies include sharp and clamscan.</criterion>
    <criterion>evidence-limits.ts has no server-only import and exports 10MB default.</criterion>
    <criterion>evidence-service.ts imports limits from evidence-limits.ts.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/evidence-service.test.ts</automated>
  </verify>
  <done>Approved packages installed; 10MB limit centralized per SEC-IMG-05/08 (server side).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClamAV client unit tests (RED)</name>
  <files>src/server/services/clamav-client.test.ts</files>
  <read_first>
    <file>src/server/health/ai-readiness.test.ts</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md</file>
  </read_first>
  <behavior>
    - Mock clamscan module init returning { ping, scanStream }
    - isClamavEnabled(): true unless process.env.CLAMAV_ENABLED === 'false'
    - CLAMAV_ENABLED=false skips scanStream call
    - Clean scan: scanStream returns { isInfected: false }
    - Infected: scanStream returns { isInfected: true, viruses: ['Eicar-Test-Signature'] } → EvidenceScanError code infected
    - Scanner throw: scanStream rejects → EvidenceScanError code scanner_unavailable
    - pingClamav when enabled and ping throws → scanner_unavailable
    - NodeClam init must use localFallback false, clamscan active false (assert via mock init args in test)
  </behavior>
  <action>
    Create clamav-client.test.ts with vi.mock('clamscan') BEFORE clamav-client.ts exists. Import assertCleanBuffer, pingClamav, isClamavEnabled, EvidenceScanError from @/server/services/clamav-client (module stub or minimal export skeleton allowed only if Vitest requires it — prefer importing from not-yet-implemented module so tests fail RED). Cover all behavior bullets. No live clamd connection. Plan for resetClamavClientForTests helper to be added in Task 3 for singleton isolation.
  </action>
  <acceptance_criteria>
    <criterion>clamav-client.test.ts exists with at least four distinct cases: disabled skip, clean, infected, scanner error.</criterion>
    <criterion>Tests fail RED until Task 3 implements clamav-client.ts.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/clamav-client.test.ts</automated>
  </verify>
  <done>Failing ClamAV client tests written first per Nyquist (SEC-IMG-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ClamAV client implementation (GREEN)</name>
  <files>src/server/services/clamav-client.ts</files>
  <read_first>
    <file>src/server/services/clamav-client.test.ts</file>
    <file>src/server/health/ai-readiness.ts</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md</file>
  </read_first>
  <behavior>
    - When disabled: assertCleanBuffer resolves without calling scanner
    - When enabled: scanStream clean → resolve; isInfected true → EvidenceScanError code infected
    - When enabled: scanStream throws → EvidenceScanError code scanner_unavailable
    - pingClamav: when enabled and ping fails → throw scanner_unavailable
    - NodeClam init uses clamdscan host/port from env, localFallback false, clamscan active false
  </behavior>
  <action>
    Create src/server/services/clamav-client.ts with import server-only. Export EvidenceScanError with codes infected | scanner_unavailable. Lazy-init singleton NodeClam client on first use. Implement assertCleanBuffer(bytes) using Readable.from(Buffer.from(bytes)) and scanStream (in-memory INSTREAM per Open Q3 RESOLVED). Implement pingClamav() calling client.ping(). Implement isClamavEnabled() returning false only when CLAMAV_ENABLED === 'false' (dev default per Open Q1 RESOLVED). Export resetClamavClientForTests for test singleton reset. Never set localFallback true. Do not log virus names to console in production path.
  </action>
  <acceptance_criteria>
    <criterion>clamav-client.ts exports assertCleanBuffer, pingClamav, isClamavEnabled, EvidenceScanError.</criterion>
    <criterion>localFallback is false in clamdscan config.</criterion>
    <criterion>All Task 2 tests pass GREEN.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/clamav-client.test.ts</automated>
  </verify>
  <done>ClamAV TCP client ready for pipeline integration per SEC-IMG-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → clamd TCP :3310 | Malware scan of citizen-upload bytes; untrusted input crosses here |
| npm registry → package.json | New sharp/clamscan installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation | Malware in upload | mitigate | assertCleanBuffer INSTREAM before persist (16-02 wires) |
| T-16-02 | Elevation | Scanner bypass on clamd down | mitigate | localFallback false; scanner_unavailable error; fail closed |
| T-16-03 | Spoofing | Fake clamscan package | mitigate | Package Legitimacy Audit OK; pin ^2.4.0 |
| T-16-04 | Denial of service | Scan timeout | mitigate | CLAMAV_TIMEOUT_MS bounded default 30000 |
| T-16-SC | Tampering | npm install sharp/clamscan | mitigate | slopcheck OK table in RESEARCH; approved versions only |
</threat_model>

<verification_criteria>
- npm run test:unit -- src/server/services/clamav-client.test.ts
- npm run test:unit -- src/server/services/evidence-service.test.ts (limits refactor regression)
- grep -v '^#' src/lib/evidence-limits.ts | grep -c '10 \* 1024 \* 1024' returns >= 1
</verification_criteria>

<success_criteria>
- sharp and clamscan in package.json with lockfile updated
- evidence-limits.ts is client-importable with 10MB default
- ClamAV client fails closed when enabled and scan/ping errors
- All clamav-client tests pass without live daemon
</success_criteria>

<output>
Create `.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-01-SUMMARY.md` when done
</output>
