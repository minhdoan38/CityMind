---
phase: 16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi
plan: "02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/server/services/evidence-image-pipeline.ts
  - src/server/services/evidence-image-pipeline.test.ts
  - src/server/services/evidence-service.ts
  - src/server/services/evidence-service.test.ts
autonomous: true
requirements: [SEC-IMG-01, SEC-IMG-02, SEC-IMG-03, SEC-IMG-04, SEC-IMG-05]
must_haves:
  truths:
    - processAndStoreEvidence runs size → magic bytes → in-memory scan → Sharp WebP → upload
    - GIF and SVG magic bytes rejected before scan or Sharp
    - Final Storage object is always image/webp under reports/{reportId}/{uuid}.webp
    - No OS quarantine temp file for ≤10MB MVP path (in-memory bytes only)
    - Sharp uses limitInputPixels, rotate, webp quality 88, metadata stripped
  artifacts:
    - path: src/server/services/evidence-image-pipeline.ts
      provides: processAndStoreEvidence orchestrator
      exports: [processAndStoreEvidence]
    - path: src/server/services/evidence-service.ts
      provides: UUID WebP path builder, extended validation codes
      exports: [buildEvidenceObjectPath, validateEvidenceBytes]
  key_links:
    - from: src/server/services/evidence-image-pipeline.ts
      to: src/server/services/clamav-client.ts
      via: assertCleanBuffer on in-memory bytes before Sharp
      pattern: assertCleanBuffer
    - from: src/server/services/evidence-image-pipeline.ts
      to: evidence bucket upload
      via: contentType image/webp
      pattern: contentType:\s*['\"]image/webp['\"]
---

<objective>
Wave 2 core pipeline — implement evidence-image-pipeline.ts (in-memory ClamAV scan → Sharp WebP), extend evidence-service validation/path helpers, and lock behavior with test-first unit tests. No report-service or UI wiring yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-VALIDATION.md
@src/server/services/evidence-service.ts
@src/server/services/clamav-client.ts
@src/lib/evidence-limits.ts

<interfaces>
From clamav-client.ts (16-01):
- assertCleanBuffer(bytes: Uint8Array): Promise<void>
- EvidenceScanError with code infected | scanner_unavailable
- isClamavEnabled(): boolean

From evidence-service.ts:
- validateEvidenceBytes, uploadEvidence, buildSupabaseEvidenceUri, EVIDENCE_BUCKET pattern
- EvidenceServiceError codes — extend with infected, scanner_unavailable, transform_failed

Pipeline contract (new):
processAndStoreEvidence({ client, reportId, bytes, bucketName, declaredContentLength?, declaredMimeType? })
  → Promise<{ evidenceUri: string; webpBytes: Uint8Array }>

Locked decisions (Open Questions RESOLVED):
- Magic bytes only JPEG/PNG/WebP; reject SVG/GIF explicitly
- sharp: rotate(), limitInputPixels (default 16_777_216 or EVIDENCE_MAX_INPUT_PIXELS env), webp q88, animated false, pages 1, failOn warning
- In-memory scan + Sharp for ≤10MB — no OS temp quarantine file (Open Q3)
- Random server filenames via randomUUID() in buildEvidenceObjectPath
</interfaces>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | Scan + sanitize before Storage | Task 3 pipeline |
| REQ | SEC-IMG-01 magic bytes, reject SVG/GIF | Task 1 validation |
| REQ | SEC-IMG-02 ClamAV in pipeline | Task 2–3 |
| REQ | SEC-IMG-03 Sharp WebP q88 | Task 3 sanitizeToWebp |
| REQ | SEC-IMG-04 in-memory + private bucket | Task 3 |
| REQ | SEC-IMG-05 size gate | Task 3 uses validateEvidenceBytes |
| RESEARCH | Pattern 1 sanitizeToWebp | Task 3 |
| RESEARCH | Pattern 3 UUID paths | Task 1 |
| PATTERNS | processAndStoreEvidence contract | Task 3 |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend evidence-service validation and UUID WebP paths</name>
  <files>src/server/services/evidence-service.ts</files>
  <read_first>
    <file>src/server/services/evidence-service.ts</file>
    <file>src/server/services/evidence-service.test.ts</file>
  </read_first>
  <action>
    Extend detectImageMime / validateEvidenceBytes to explicitly reject when file-type returns image/gif or image/svg+xml (return invalid_type). Change buildEvidenceObjectPath(reportId, objectId?) to return reports/{reportId}/{objectId ?? randomUUID()}.webp — import randomUUID from node:crypto. Extend EvidenceServiceError code union with infected, scanner_unavailable, transform_failed. Keep uploadEvidence for backward compat but document as legacy; pipeline uses direct upload in processAndStoreEvidence. Add uploadWebpEvidence helper or inline upload in pipeline with upsert false. Do not remove parseEvidencePath or downloadEvidenceObject EXT_TO_MIME webp mapping.
  </action>
  <acceptance_criteria>
    <criterion>buildEvidenceObjectPath produces reports/{id}/{uuid}.webp shape.</criterion>
    <criterion>GIF and SVG bytes return invalid_type from validateEvidenceBytes.</criterion>
    <criterion>EvidenceServiceError includes pipeline error codes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/evidence-service.test.ts</automated>
  </verify>
  <done>Magic-byte gate and UUID WebP paths per SEC-IMG-01/04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pipeline unit tests (RED)</name>
  <files>src/server/services/evidence-image-pipeline.test.ts, src/server/services/evidence-service.test.ts</files>
  <read_first>
    <file>src/server/services/evidence-service.test.ts</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md</file>
  </read_first>
  <behavior>
    - evidence-service: GIF89a bytes → invalid_type; SVG snippet → invalid_type; buildEvidenceObjectPath matches /reports\/.+\/.+\.webp/
    - pipeline: mock assertCleanBuffer; mock storage upload; PNG fixture → contentType image/webp; webpBytes starts with RIFF WEBP or passes file-type webp check
    - pipeline: mock infected scan → throws infected code
    - pipeline: mock scanner throw → scanner_unavailable
    - pipeline: oversized bytes → oversized without upload call
    - pipeline: pixel bomb — tiny PNG or crafted buffer with dimensions exceeding EVIDENCE_MAX_INPUT_PIXELS / limitInputPixels → transform_failed without storage upload call
    - pipeline: assert no fs.writeFile to quarantine path (in-memory only per Open Q3)
  </behavior>
  <action>
    Create evidence-image-pipeline.test.ts with vi.mock on clamav-client BEFORE evidence-image-pipeline.ts exists. Extend evidence-service.test.ts for GIF/SVG rejection and new path format. Use existing PNG_BYTES/JPEG_BYTES fixtures. Mock Supabase storage client per PATTERNS.md createStorageClient pattern. For pixel-bomb case: use a minimal PNG with IHDR width/height exceeding limitInputPixels (or mock sharp to throw with limitInputPixels message) and assert EvidenceServiceError code transform_failed. Tests fail RED until Task 3 implements pipeline.
  </action>
  <acceptance_criteria>
    <criterion>evidence-image-pipeline.test.ts exists with infected, scanner error, pixel-bomb transform_failed, and WebP output cases.</criterion>
    <criterion>GIF and SVG cases in evidence-service.test.ts.</criterion>
    <criterion>Tests fail RED until Task 3.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/evidence-image-pipeline.test.ts src/server/services/evidence-service.test.ts</automated>
  </verify>
  <done>Failing pipeline tests written first per Nyquist (SEC-IMG-01–05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: evidence-image-pipeline implementation (GREEN)</name>
  <files>src/server/services/evidence-image-pipeline.ts</files>
  <read_first>
    <file>src/server/services/evidence-image-pipeline.test.ts</file>
    <file>src/server/services/evidence-service.ts</file>
    <file>src/server/services/clamav-client.ts</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md</file>
  </read_first>
  <behavior>
    - validateEvidenceBytes first; propagate validation failures as EvidenceServiceError
    - assertCleanBuffer on same in-memory bytes (no OS temp quarantine per Open Q3 RESOLVED)
    - Map EvidenceScanError infected → EvidenceServiceError infected; scanner_unavailable → scanner_unavailable
    - sanitizeToWebp: sharp with limitInputPixels, failOn warning, animated false, pages 1, rotate(), webp quality from EVIDENCE_WEBP_QUALITY env default 88
    - Upload via storage.from(bucket).upload with contentType image/webp, upsert false; object path from buildEvidenceObjectPath(reportId, randomUUID())
    - Return { evidenceUri: buildSupabaseEvidenceUri(...), webpBytes }
    - transform_failed on Sharp errors including limitInputPixels exceeded
  </behavior>
  <action>
    Create evidence-image-pipeline.ts with import server-only. Export processAndStoreEvidence and internal sanitizeToWebp if needed for tests. Process bytes entirely in-memory — do NOT write quarantine temp files for ≤10MB MVP. Import validateEvidenceBytes, buildEvidenceObjectPath, buildSupabaseEvidenceUri, EvidenceServiceError from evidence-service. Do not modify report-service yet. All Task 2 tests must pass GREEN.
  </action>
  <acceptance_criteria>
    <criterion>processAndStoreEvidence exported and returns evidenceUri + webpBytes.</criterion>
    <criterion>Upload contentType is image/webp.</criterion>
    <criterion>Pixel-bomb / limitInputPixels case throws transform_failed.</criterion>
    <criterion>All Task 2 tests pass GREEN.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/evidence-image-pipeline.test.ts src/server/services/evidence-service.test.ts</automated>
  </verify>
  <done>Full server-side sanitization pipeline implemented per SEC-IMG-02/03/04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen bytes → validateEvidenceBytes | Untrusted binary crosses validation |
| Validated bytes → Sharp/libvips | Decompression bomb risk |
| Bytes → clamd INSTREAM | Malware scan boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Spoofing | MIME/polyglot upload | mitigate | file-type + reject GIF/SVG; Sharp re-encode |
| T-16-06 | Elevation | Malware persistence | mitigate | ClamAV before Sharp; upload WebP only |
| T-16-07 | Denial of service | Pixel bomb | mitigate | limitInputPixels + MAX_IMAGE_BYTES + failOn warning + transform_failed test |
| T-16-08 | Tampering | Path traversal filename | mitigate | Server randomUUID paths; ignore client names |
| T-16-SC | Tampering | sharp binary install | mitigate | Approved npm package; no postinstall scripts concern |
</threat_model>

<verification_criteria>
- npm run test:unit -- src/server/services/evidence-image-pipeline.test.ts
- npm run test:unit -- src/server/services/evidence-service.test.ts
- grep -v '^#' src/server/services/evidence-image-pipeline.ts | grep -c 'image/webp' returns >= 1
</verification_criteria>

<success_criteria>
- processAndStoreEvidence orchestrates in-memory scan → Sharp → upload
- GIF/SVG rejected; JPEG/PNG/WebP accepted
- Storage upload always WebP with UUID key
- Unit tests cover scan failures, pixel bomb, and transform path
</success_criteria>

<output>
Create `.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-02-SUMMARY.md` when done
</output>
