---
phase: 16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi
plan: "03"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/server/services/report-service.ts
  - src/server/services/report-service.test.ts
  - src/server/services/citizen-chat-intake.ts
  - src/server/services/citizen-chat-intake.test.ts
  - src/server/http/errors.ts
  - src/components/ReportForm.tsx
  - tests/report-form.test.mjs
autonomous: true
requirements: [SEC-IMG-01, SEC-IMG-02, SEC-IMG-05, SEC-IMG-08, DATA-10]
must_haves:
  truths:
    - All citizen evidence uploads route through processAndStoreEvidence at report-service and citizen-chat-intake call sites
    - Synchronous triage receives sanitized WebP bytes not original upload bytes
    - Scanner unavailable returns 503 with generic citizen message
    - Infected or transform failures return generic 415 without virus names
    - ReportForm client Zod limit matches 10MB server default
  artifacts:
    - path: src/server/services/report-service.ts
      provides: Pipeline-wired submitReport and analyzeReport upload paths
    - path: src/server/services/citizen-chat-intake.ts
      provides: Pipeline-wired finalizeIntakeSubmit
    - path: src/components/ReportForm.tsx
      provides: 10MB Zod limit from evidence-limits
  key_links:
    - from: src/server/services/report-service.ts
      to: processAndStoreEvidence
      via: replace uploadEvidence calls
      pattern: processAndStoreEvidence
    - from: src/server/services/report-service.ts
      to: dispatchTriageAndWait / triage vision input
      via: webpBytes from pipeline result
      pattern: webpBytes
    - from: src/components/ReportForm.tsx
      to: src/lib/evidence-limits.ts
      via: DEFAULT_MAX_EVIDENCE_BYTES import
      pattern: evidence-limits
---

<objective>
Wire the secure evidence pipeline into all citizen upload paths — report-service, citizen-chat-intake, and ReportForm client limits — with generic HTTP error mapping per DATA-10.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md
@src/server/services/report-service.ts
@src/server/services/citizen-chat-intake.ts
@src/server/services/evidence-image-pipeline.ts
@src/server/http/errors.ts
@src/components/ReportForm.tsx
@tests/report-form.test.mjs

<interfaces>
From evidence-image-pipeline.ts (16-02):
- processAndStoreEvidence(...) → { evidenceUri, webpBytes }

From errors.ts:
- imageTooLarge(): HttpError 413
- unsupportedImageType(received): HttpError 415
- Add evidenceScanningUnavailable(): HttpError 503

Error mapping (locked):
- oversized → imageTooLarge()
- invalid_type, spoofed_mime, empty, infected, transform_failed → unsupportedImageType('unknown') — never echo virus names
- scanner_unavailable → evidenceScanningUnavailable() 503
</interfaces>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | End-to-end secure citizen upload | All tasks |
| REQ | SEC-IMG-08 client/server 10MB | Task 3 |
| REQ | DATA-10 generic errors | Task 1 error mapping |
| REQ | SEC-IMG-02 fail closed 503 | Task 1 |
| RESEARCH | Three call sites | Task 1–2 |
| PATTERNS | Compensation delete on failure | Task 1–2 |
| PATTERNS | report-service error mapping | Task 1 |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire report-service to pipeline + HTTP error mapping</name>
  <files>src/server/services/report-service.ts, src/server/services/report-service.test.ts, src/server/http/errors.ts</files>
  <read_first>
    <file>src/server/services/report-service.ts</file>
    <file>src/server/services/report-service.test.ts</file>
    <file>src/server/http/errors.ts</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md</file>
  </read_first>
  <behavior>
    - submitReport: processAndStoreEvidence replaces uploadEvidence; pass webpBytes to triage/AI path instead of original imageBytes
    - analyzeReport legacy path: same pipeline swap if still present
    - EvidenceServiceError scanner_unavailable → evidenceScanningUnavailable()
    - infected, transform_failed → unsupportedImageType('unknown')
    - Compensation deleteEvidenceByUri on DB/triage failure unchanged
    - Unit test: mock processAndStoreEvidence returns webpBytes; triage receives webp not original
    - Unit test: scanner_unavailable maps to 503 generic message
  </behavior>
  <action>
    Add evidenceScanningUnavailable() to errors.ts with message Evidence scanning is temporarily unavailable. Update report-service.ts imports: processAndStoreEvidence from evidence-image-pipeline; extend EvidenceServiceError catch blocks in submitReport and analyzeReport. Remove duplicate pre-upload validateEvidenceBytes where pipeline centralizes validation (keep parse-time size check if needed for early reject). Update report-service.test.ts with pipeline mock tests. Preserve existing oversized and invalid_type test expectations.
  </action>
  <acceptance_criteria>
    <criterion>report-service uses processAndStoreEvidence for image uploads.</criterion>
    <criterion>scanner_unavailable produces 503 HttpError.</criterion>
    <criterion>infected does not leak virus name in HTTP response.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/report-service.test.ts</automated>
  </verify>
  <done>Classic report submit path secured per SEC-IMG-02 and DATA-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire citizen-chat-intake to pipeline</name>
  <files>src/server/services/citizen-chat-intake.ts, src/server/services/citizen-chat-intake.test.ts</files>
  <read_first>
    <file>src/server/services/citizen-chat-intake.ts</file>
    <file>src/server/services/citizen-chat-intake.test.ts</file>
    <file>src/server/services/report-service.ts</file>
  </read_first>
  <action>
    In finalizeIntakeSubmit replace uploadEvidence with processAndStoreEvidence. Remove redundant validateEvidenceBytes block in parseIntakeSubmitForm when pipeline handles validation — keep lightweight size check on parse if it improves UX. Mirror report-service EvidenceServiceError → HttpError mapping including scanner_unavailable 503. Pass webpBytes to sync triage dispatch if image present. Extend citizen-chat-intake.test.ts with pipeline mock asserting processAndStoreEvidence called and scanner error maps to 503.
  </action>
  <acceptance_criteria>
    <criterion>finalizeIntakeSubmit uses processAndStoreEvidence.</criterion>
    <criterion>Error mapping matches report-service for all pipeline codes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-chat-intake.test.ts</automated>
  </verify>
  <done>Chat-first intake upload path secured (SEC-IMG-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align ReportForm client limits with server</name>
  <files>src/components/ReportForm.tsx, tests/report-form.test.mjs</files>
  <read_first>
    <file>src/components/ReportForm.tsx</file>
    <file>src/lib/evidence-limits.ts</file>
    <file>tests/report-form.test.mjs</file>
  </read_first>
  <action>
    Remove local MAX_FILE_SIZE = 8MB constant from ReportForm.tsx. Import DEFAULT_MAX_EVIDENCE_BYTES from @/lib/evidence-limits. Update Zod refine to use imported constant. Change user-facing message to Max image size is 10MB. Keep accept attribute JPEG/PNG/WebP only — no GIF/SVG. Update tests/report-form.test.mjs to assert evidence-limits import or 10 * 1024 * 1024 instead of 8MB literal.
  </action>
  <acceptance_criteria>
    <criterion>ReportForm imports DEFAULT_MAX_EVIDENCE_BYTES from evidence-limits.</criterion>
    <criterion>No 8 * 1024 * 1024 literal remains in ReportForm.tsx.</criterion>
    <criterion>report-form.test.mjs passes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:legacy -- tests/report-form.test.mjs</automated>
  </verify>
  <done>Client/server size alignment per SEC-IMG-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → report-service / citizen-chat-intake | Untrusted multipart image field |
| Service → citizen HTTP response | Must not leak scanner internals or virus names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Information disclosure | Error messages | mitigate | DATA-10 generic 415/503; no virus names |
| T-16-11 | Elevation | Bypass pipeline via legacy uploadEvidence | mitigate | Replace all call sites; tests assert processAndStoreEvidence |
| T-16-12 | Tampering | Client size spoof | mitigate | Server validateEvidenceBytes authoritative; client pre-check UX only |
| T-16-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification_criteria>
- npm run test:unit -- src/server/services/report-service.test.ts
- npm run test:unit -- src/server/services/citizen-chat-intake.test.ts
- npm run test:legacy -- tests/report-form.test.mjs
</verification_criteria>

<success_criteria>
- Both server upload paths use processAndStoreEvidence exclusively
- Triage receives sanitized WebP bytes
- Generic citizen errors for infected/scanner failures
- ReportForm enforces 10MB client limit
</success_criteria>

<output>
Create `.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-03-SUMMARY.md` when done
</output>
