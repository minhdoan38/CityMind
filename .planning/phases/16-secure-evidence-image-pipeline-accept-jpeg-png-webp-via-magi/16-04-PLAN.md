---
phase: 16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi
plan: "04"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/server/health/clamav-readiness.ts
  - src/server/health/clamav-readiness.test.ts
  - src/app/api/health/clamav/route.ts
  - src/server/health/readiness.ts
  - src/server/health/readiness.test.ts
  - scripts/migrate-evidence-to-webp.mjs
autonomous: true
requirements: [SEC-IMG-06, SEC-IMG-07]
must_haves:
  truths:
    - GET /api/health/clamav returns up/down with latency_ms and TTL cache
    - checkReadiness includes clamav dependency when CLAMAV_ENABLED is true
    - migrate-evidence-to-webp.mjs supports --dry-run without mutating Storage or DB
    - Batch script converts legacy evidence.{ext} objects to UUID .webp keys
  artifacts:
    - path: src/server/health/clamav-readiness.ts
      provides: checkClamavHealth with 45s TTL cache
      exports: [checkClamavHealth, resetClamavHealthCache]
    - path: src/app/api/health/clamav/route.ts
      provides: Ops health endpoint
    - path: scripts/migrate-evidence-to-webp.mjs
      provides: Legacy evidence batch conversion
  key_links:
    - from: src/app/api/health/clamav/route.ts
      to: checkClamavHealth
      via: GET handler
      pattern: checkClamavHealth
    - from: scripts/migrate-evidence-to-webp.mjs
      to: Supabase Storage evidence bucket
      via: list/download/sharp/upload/remove
      pattern: migrate-evidence-to-webp
---

<objective>
Wave 3 ops tooling — ClamAV health probe (mirroring AI health), readiness integration, and batch migration script for legacy evidence objects.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md
@.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-PATTERNS.md
@src/server/health/ai-readiness.ts
@src/app/api/health/ai/route.ts
@src/server/health/readiness.ts
@scripts/capture-migration-inventory.mjs
@scripts/load-project-env.mjs

<interfaces>
From ai-readiness.ts:
- TTL cache pattern AI_HEALTH_TTL_MS = 45_000
- resetAiHealthCache() for tests
- Returns { body, cacheHit }

ClamAV health response:
{ status: 'up' | 'down', latency_ms: number, checked_at: string }

Migration script CLI:
--dry-run, --limit N, --report-id ID
</interfaces>

## Multi-Source Coverage Audit

| Source | Item | Plan |
|--------|------|------|
| GOAL | Ops visibility + legacy migration | All tasks |
| REQ | SEC-IMG-06 batch script | Task 3 |
| REQ | SEC-IMG-07 health probe | Task 1–2 |
| RESEARCH | migrate-evidence-to-webp.mjs | Task 3 |
| RESEARCH | clamav-readiness mirror ai-readiness | Task 1 |
| PATTERNS | capture-migration-inventory.mjs | Task 3 |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClamAV readiness module and API route</name>
  <files>src/server/health/clamav-readiness.ts, src/server/health/clamav-readiness.test.ts, src/app/api/health/clamav/route.ts</files>
  <read_first>
    <file>src/server/health/ai-readiness.ts</file>
    <file>src/app/api/health/ai/route.ts</file>
    <file>src/server/services/clamav-client.ts</file>
  </read_first>
  <behavior>
    - When CLAMAV_ENABLED false: status down or skipped with reason in body (document: disabled counts as down for ops visibility)
    - When enabled and ping succeeds: status up, latency_ms recorded
    - When enabled and ping fails: status down
    - TTL cache 45s; second call within TTL returns cacheHit true
    - GET /api/health/clamav returns 200 for up, 503 for down; Cache-Control private max-age=30; X-Cache HIT/MISS
  </behavior>
  <action>
    Create clamav-readiness.ts with server-only import. Use pingClamav from clamav-client. Export resetClamavHealthCache for tests. Create route.ts mirroring ai/route.ts shape. Create clamav-readiness.test.ts with mocked pingClamav — success, failure, cache TTL, disabled env cases.
  </action>
  <acceptance_criteria>
    <criterion>checkClamavHealth exported with cache behavior.</criterion>
    <criterion>GET /api/health/clamav route exists.</criterion>
    <criterion>clamav-readiness.test.ts passes without live clamd.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/health/clamav-readiness.test.ts</automated>
  </verify>
  <done>ClamAV ops health endpoint per SEC-IMG-07.</done>
</task>

<task type="auto">
  <name>Task 2: Extend checkReadiness with clamav dependency</name>
  <files>src/server/health/readiness.ts, src/server/health/readiness.test.ts</files>
  <read_first>
    <file>src/server/health/readiness.ts</file>
    <file>src/server/health/readiness.test.ts</file>
    <file>src/server/health/clamav-readiness.ts</file>
  </read_first>
  <action>
    When isClamavEnabled() is true, call checkClamavHealth in checkReadiness and append { name: 'clamav', status, latency_ms } to dependencies array. If clamav status is down, set overall readiness status to not_ready (mirror fail-closed ops expectation per Open Q2 RESOLVED). When CLAMAV_ENABLED is false, omit clamav from dependencies (dev default per Open Q1). Do not break existing supabase probe behavior. Extend readiness.test.ts with vi.mock on clamav-readiness and isClamavEnabled: (1) when clamav enabled and up → dependencies includes clamav status up and overall ready; (2) when clamav enabled and down → overall not_ready; (3) when clamav disabled → dependencies omits clamav entry.
  </action>
  <acceptance_criteria>
    <criterion>checkReadiness dependencies includes clamav when enabled and up.</criterion>
    <criterion>not_ready when clamav enabled but down.</criterion>
    <criterion>readiness.test.ts covers clamav integration cases.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/health/readiness.test.ts src/server/health/clamav-readiness.test.ts</automated>
  </verify>
  <done>Readiness gate reflects ClamAV availability (SEC-IMG-07).</done>
</task>

<task type="auto">
  <name>Task 3: Batch migration script migrate-evidence-to-webp.mjs</name>
  <files>scripts/migrate-evidence-to-webp.mjs</files>
  <read_first>
    <file>scripts/capture-migration-inventory.mjs</file>
    <file>scripts/load-project-env.mjs</file>
    <file>.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-RESEARCH.md</file>
  </read_first>
  <action>
    Create scripts/migrate-evidence-to-webp.mjs using loadProjectEnv and Supabase service role client. Parse CLI flags --dry-run, --limit, --report-id. List reports with non-null evidence_path or enumerate storage under reports/. For each legacy object not already .webp under UUID path: download bytes, run same Sharp sanitize logic as pipeline (quality 88, rotate, limitInputPixels), upload new reports/{reportId}/{uuid}.webp, UPDATE reports.evidence_path, remove old storage object — skip mutations when --dry-run. Emit JSON summary { converted, skipped, failed, errors[] }. Use fail() helper and exit 1 on fatal config errors. Order: upload new → update DB → delete old per RESEARCH Pitfall 6.
  </action>
  <acceptance_criteria>
    <criterion>node scripts/migrate-evidence-to-webp.mjs --dry-run exits 0 with JSON stdout.</criterion>
    <criterion>Script does not mutate Storage or DB in dry-run mode.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/migrate-evidence-to-webp.mjs --dry-run</automated>
  </verify>
  <done>Legacy evidence batch conversion tooling per SEC-IMG-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ops → /api/health/clamav | Unauthenticated health — no secrets in response |
| Migration script → service role | Elevated Storage/DB access — local operator only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Information disclosure | Health endpoint | mitigate | Return status/latency only; no CLAMAV_HOST secrets |
| T-16-14 | Tampering | Migration script DB updates | mitigate | Dry-run default path tested; operator runs with credentials locally |
| T-16-15 | Denial of service | Migration full bucket scan | mitigate | --limit flag; paginated list |
| T-16-SC | Tampering | npm installs | mitigate | Script uses existing sharp dep from 16-01 |
</threat_model>

<verification_criteria>
- npm run test:unit -- src/server/health/clamav-readiness.test.ts
- npm run test:unit -- src/server/health/readiness.test.ts
- node scripts/migrate-evidence-to-webp.mjs --dry-run
</verification_criteria>

<success_criteria>
- ClamAV health endpoint and readiness integration shipped
- Batch migration script runs dry-run without side effects
- TTL cache prevents probe storms
</success_criteria>

<output>
Create `.planning/phases/16-secure-evidence-image-pipeline-accept-jpeg-png-webp-via-magi/16-04-SUMMARY.md` when done
</output>
