---
phase: 06-maps-geospatial
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ReportLocationMiniMap.tsx
  - frontend/src/components/ReportLocationMiniMapInner.tsx
  - frontend/src/components/ReportForm.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/.env.example
  - frontend/tests/report-minimap.test.mjs
autonomous: false
requirements:
  - MAP-02
must_haves:
  truths:
    - "Citizen report form shows optional mini map below lat/lng fields; click-to-pin updates RHF latitude/longitude (D-11 / D-12 / D-13)."
    - "Use my location and manual lat/lng still work; submit succeeds when map fails to load (06-UI-SPEC Track B)."
    - "Map uses MapLibre via react-map-gl/maplibre with next/dynamic ssr:false (D-15)."
    - "OSM raster tiles from NEXT_PUBLIC_MAP_TILE_URL with attribution (D-14)."
    - "EN/VI copy in public.map namespace per 06-UI-SPEC."
  artifacts:
    - path: "frontend/src/components/ReportLocationMiniMap.tsx"
      provides: "Dynamic import wrapper + degradation"
    - path: "frontend/src/components/ReportLocationMiniMapInner.tsx"
      provides: "Client MapLibre pin-drop map"
    - path: "frontend/tests/report-minimap.test.mjs"
      provides: "Degradation + import smoke"
  key_links:
    - from: "frontend/src/components/ReportForm.tsx"
      to: "ReportLocationMiniMap"
      via: "insert below lat/lng grid in location section"
      pattern: "ReportLocationMiniMap|latitude|longitude"
    - from: "ReportLocationMiniMapInner.tsx"
      to: "react-hook-form setValue"
      via: "onMapClick updates latitude/longitude to 6 decimals"
      pattern: "setValue\\('latitude'|setValue\\('longitude'"
---

## Phase Goal

**As a** citizen, **I want** an optional map to pin my report location, **so that** I can place incidents visually without losing manual coordinate entry.

<objective>
Deliver Track B: MapLibre mini-map on bilingual report form, env tile config, graceful degradation, smoke tests (MAP-02 partial — citizen surface).

Purpose: Improve location capture UX without changing analyze API contract.
Output: mini-map components, i18n strings, package deps, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-maps-geospatial/06-CONTEXT.md
@.planning/phases/06-maps-geospatial/06-RESEARCH.md
@.planning/phases/06-maps-geospatial/06-UI-SPEC.md
@frontend/src/components/ReportForm.tsx
@frontend/src/app/[locale]/report/page.tsx
@frontend/messages/en.json
@frontend/messages/vi.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify MapLibre package legitimacy (SUS)</name>
  <read_first>
    - .planning/phases/06-maps-geospatial/06-RESEARCH.md
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Registry Safety)
    - frontend/package.json
  </read_first>
  <acceptance_criteria>
    - Human confirms maplibre-gl and react-map-gl are official packages before npm install
    - Resume signal "approved" recorded before Task 2 runs
  </acceptance_criteria>
  <action>
Review on npmjs.com:
- maplibre-gl (MapLibre organization)
- react-map-gl (visgl / uber-react-map-gl lineage)

Confirm versions: maplibre-gl@^5.24.0, react-map-gl@^8.1.1 per 06-RESEARCH.md. Reject Leaflet/Google Maps per D-15.

Reply **approved** to authorize install, or describe blockers.
  </action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/maplibre-gl — confirm MapLibre GL JS official package.
    2. Open https://www.npmjs.com/package/react-map-gl — confirm vis.gl bindings.
    3. Confirm import path `react-map-gl/maplibre` per UI-SPEC.
    4. Reply **approved** to proceed.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize maplibre-gl + react-map-gl install.</resume-signal>
  <verify>
    <automated>echo awaiting_human_approval</automated>
  </verify>
  <done>SUS checkpoint passed; install authorized.</done>
</task>

<task type="auto">
  <name>Task 2: Install maplibre-gl + react-map-gl and env tile config</name>
  <read_first>
    - frontend/package.json
    - frontend/.env.example
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Tile style)
  </read_first>
  <acceptance_criteria>
    - frontend/package.json lists maplibre-gl and react-map-gl with locked versions in package-lock.json
    - frontend/.env.example contains NEXT_PUBLIC_MAP_TILE_URL and NEXT_PUBLIC_MAP_TILE_ATTRIBUTION with OSM dev defaults
    - `cd frontend && npm run build` does not fail due to missing maplibre CSS (CSS imported in client bundle only)
  </acceptance_criteria>
  <action>
After SUS approval:
```
cd frontend && npm install maplibre-gl@^5.24.0 react-map-gl@^8.1.1
```

Add env example entries:
```
NEXT_PUBLIC_MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
NEXT_PUBLIC_MAP_TILE_ATTRIBUTION=© OpenStreetMap contributors
```

Do not import maplibre-gl in server components.
  </action>
  <verify>
    <automated>cd frontend && npm ls maplibre-gl react-map-gl</automated>
  </verify>
  <done>Map dependencies installed; env documented.</done>
</task>

<task type="auto">
  <name>Task 3: ReportLocationMiniMap component + ReportForm integration</name>
  <read_first>
    - frontend/src/components/ReportForm.tsx
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Track B screen contract)
    - .planning/phases/06-maps-geospatial/06-CONTEXT.md D-11..D-13
  </read_first>
  <acceptance_criteria>
    - ReportLocationMiniMap.tsx uses next/dynamic with ssr:false loading Skeleton 240px/200px mobile
    - ReportLocationMiniMapInner.tsx renders Map from react-map-gl/maplibre with raster OSM style from env
    - Click map sets pin and calls onCoordsChange(lat,lng) with 6 decimal places
    - Changing lat/lng inputs from parent moves pin (debounced 300ms)
    - Default center 21.0285, 105.8542 zoom 12 when no coords
    - On tile load error: hide canvas, show degraded helper text — no Alert blocking form
    - ReportForm.tsx inserts mini map below lat/lng grid inside location section; does not remove manual fields or Use my location button
    - No clustering, no other reports on public map
    - prefers-reduced-motion: no flyTo; use jumpTo
  </acceptance_criteria>
  <action>
1. Create ReportLocationMiniMapInner.tsx (client): build raster style JSON from NEXT_PUBLIC_MAP_TILE_URL; Marker for pin; map click handler.
2. Create ReportLocationMiniMap.tsx: dynamic import wrapper; error boundary → degraded state.
3. Wire into ReportForm after longitude field block; pass form watch/setValue for sync.
4. Import maplibre-gl/dist/maplibre-gl.css only in Inner client file.

Container: rounded-lg border bg-secondary/30, height 240px desktop / 200px mobile per UI-SPEC.
  </action>
  <verify>
    <automated>cd frontend && npm run lint -- --max-warnings 0 src/components/ReportLocationMiniMap.tsx src/components/ReportLocationMiniMapInner.tsx src/components/ReportForm.tsx</automated>
  </verify>
  <done>Mini map integrated on report form with pin sync.</done>
</task>

<task type="auto">
  <name>Task 4: EN/VI public.map copy + smoke test</name>
  <read_first>
    - frontend/messages/en.json
    - frontend/messages/vi.json
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Copywriting Track B)
    - frontend/tests/public-stats.test.mjs (smoke pattern)
  </read_first>
  <acceptance_criteria>
    - en.json and vi.json contain public.map keys: label, helper, loading, degraded per UI-SPEC table
    - ReportLocationMiniMap uses useTranslations('public.map') — no hardcoded English chrome
    - frontend/tests/report-minimap.test.mjs asserts ReportForm imports ReportLocationMiniMap and degraded copy key exists in messages
    - `node --test frontend/tests/report-minimap.test.mjs` exits 0
  </acceptance_criteria>
  <action>
Add all Track B copy rows from 06-UI-SPEC Copywriting table to messages files.

Create report-minimap.test.mjs:
- Assert ReportForm.tsx contains ReportLocationMiniMap
- Assert en.json has public.map.degraded string
- Assert dynamic import pattern ssr:false in wrapper file

Follow existing frontend smoke test style (node:test).
  </action>
  <verify>
    <automated>node --test frontend/tests/report-minimap.test.mjs</automated>
  </verify>
  <done>Bilingual mini-map copy and smoke test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public report form | Mini map shows only user's pin — no officer data or other reports. |
| Tile provider | Third-party OSM tiles — attribution required; no API keys in client for dev. |
| Location optional | Submit must not require map load success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-B1 | Information Disclosure | public mini map | medium | mitigate | No officer incidents layer; single citizen pin only (D-13). |
| T-06-B2 | Denial of Service | tile CDN failure | low | mitigate | Degraded helper; submit unaffected (UI-SPEC). |
| T-06-B3 | Tampering | coords via map click | low | accept | Same trust as manual lat/lng fields; server validates range on analyze. |
| T-06-SC | Tampering | npm maplibre packages | high | mitigate | Blocking SUS human checkpoint before install (Task 1). |
</threat_model>

<verification>
Run `node --test frontend/tests/report-minimap.test.mjs`. Manually open /en/report — mini map loads or degrades without blocking submit. Confirm lat/lng fields still editable.
</verification>

<success_criteria>
- Track B citizen mini-map shipped per D-11..D-13 and 06-UI-SPEC.
- MapLibre deps installed with SUS approval.
- MAP-02 citizen surface portion complete (officer map is 06-03).
</success_criteria>

<output>
Create `.planning/phases/06-maps-geospatial/06-02-SUMMARY.md` when done.
</output>
