---
phase: 06-maps-geospatial
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - frontend/src/components/reports/types.ts
  - frontend/src/components/reports/ReportsViewToggle.tsx
  - frontend/src/components/reports/ReportsMapView.tsx
  - frontend/src/components/reports/GeoFilterBar.tsx
  - frontend/src/components/reports/GeoFilterPanel.tsx
  - frontend/src/components/reports/IncidentsListPanel.tsx
  - frontend/src/components/reports/MapCanvasSkeleton.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/DashboardSidebar.tsx
  - frontend/src/app/api/officer/reports/geo/pins/route.ts
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/dashboard-map.test.mjs
  - frontend/tests/dashboard-geo-params.test.mjs
autonomous: true
requirements:
  - MAP-02
  - MAP-03
must_haves:
  truths:
    - "Officer dashboard toggles Table view (default) and Map view via URL view=table|map on same /dashboard page (D-01 / D-02 / D-04 / MAP-02)."
    - "Map shows MapLibre client-side clustered pins colored by priority; cluster click zooms; marker click navigates directly to /dashboard/reports/[id] (D-03 / D-05 / D-06 / D-07)."
    - "BBox geo filter via map draw + panel numeric fallback syncs to URL bbox param; Clear area filter removes bbox only (D-08 / D-09 / MAP-03)."
    - "Unlocated reports hidden from map; {count} reports lack location shown when unlocated_count > 0 (D-16 / D-17)."
    - "Attribute filters from Phase 3 apply to map pin fetch, metrics, and export (shared buildReportsQuery)."
    - "Keyboard list panel List incidents in view provides SR path without map-only interaction (06-UI-SPEC a11y)."
  artifacts:
    - path: "frontend/src/components/reports/ReportsMapView.tsx"
      provides: "Officer incident map with clustering"
    - path: "frontend/src/components/reports/ReportsViewToggle.tsx"
      provides: "Table | Map URL toggle"
    - path: "frontend/src/app/api/officer/reports/geo/pins/route.ts"
      provides: "Client BFF for geo pins with JWT cookie"
    - path: "frontend/tests/dashboard-map.test.mjs"
      provides: "Map view shell smoke"
  key_links:
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "ReportsMapView"
      via: "view=map searchParam branch"
      pattern: "view.*map|ReportsMapView"
    - from: "ReportsMapView.tsx"
      to: "/api/officer/reports/geo/pins"
      via: "fetch on viewport moveend debounced 300ms"
      pattern: "geo/pins|unlocated_count"
    - from: "frontend/src/components/reports/types.ts"
      to: "buildReportsQuery"
      via: "view and bbox URL keys"
      pattern: "bbox|view"
---

## Phase Goal

**As an** officer, **I want** a full incident map on the dashboard with clustering and bbox filtering, **so that** I can see spatial patterns while keeping the table as my default workflow.

<objective>
Deliver Track C: dashboard map view, client clustering, bbox geo filter UI, URL sync, BFF geo pins route, EN/VI dashboard.map copy, smoke tests (MAP-02, MAP-03).

Purpose: Officer-facing map success criteria for Phase 6.
Depends on: 06-01 geo pins API.
Output: map components, dashboard integration, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-maps-geospatial/06-CONTEXT.md
@.planning/phases/06-maps-geospatial/06-RESEARCH.md
@.planning/phases/06-maps-geospatial/06-UI-SPEC.md
@.planning/phases/06-maps-geospatial/06-VALIDATION.md
@.planning/phases/06-maps-geospatial/06-01-PLAN.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/components/reports/types.ts
@frontend/src/components/reports/ReportsFilters.tsx
@frontend/src/components/reports/ExportButton.tsx
@frontend/src/components/DashboardSidebar.tsx
@frontend/src/app/api/officer/reports/export/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DashboardSearchParams + geo pins BFF route</name>
  <read_first>
    - frontend/src/components/reports/types.ts
    - frontend/src/app/api/officer/reports/export/route.ts
    - backend/app/api/reports.py (geo/pins contract from 06-01)
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (URL parameters)
  </read_first>
  <acceptance_criteria>
    - DashboardSearchParams includes optional view?: string and bbox?: string
    - buildReportsQuery serializes view and bbox when present
    - FILTER_PARAM_KEYS unchanged; bbox cleared when Clear filters runs (dashboard behavior)
    - frontend/src/app/api/officer/reports/geo/pins/route.ts proxies GET to backend /api/v1/reports/geo/pins with officer session JWT
    - frontend/tests/dashboard-geo-params.test.mjs asserts buildReportsQuery includes bbox=1,2,3,4 when param set
    - `node --test frontend/tests/dashboard-geo-params.test.mjs` exits 0
  </acceptance_criteria>
  <action>
1. Extend types.ts: add view, bbox to DashboardSearchParams; update buildReportsQuery to append them.
2. Create geo/pins/route.ts mirroring export route auth pattern (getSessionToken → Authorization Bearer).
3. Export a small pure function buildGeoPinsQuery(west,south,east,north, filters, filterBbox?) for client map use — can live in types.ts or reports/mapQuery.ts.
4. Add dashboard-geo-params.test.mjs importing buildReportsQuery logic (use node --experimental-vm-modules or duplicate minimal test of query string builder).

Default view absent = table per D-01.
  </action>
  <verify>
    <automated>node --test frontend/tests/dashboard-geo-params.test.mjs</automated>
  </verify>
  <done>URL param contract and BFF geo route ready for map client.</done>
</task>

<task type="auto">
  <name>Task 2: ReportsViewToggle + dashboard page view=map branch</name>
  <read_first>
    - frontend/src/app/dashboard/page.tsx
    - frontend/src/components/reports/ReportsTable.tsx
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Layout Track C)
    - .planning/phases/03-dashboard-polish/03-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - ReportsViewToggle.tsx renders Table view / Map view toggle (ToggleGroup or button pair) with 44px touch targets
    - Clicking Map sets view=map in URL via router.push preserving other searchParams
    - Clicking Table removes view param or sets view=table
    - dashboard/page.tsx: when view=map, render ReportsMapView instead of ReportsTable; ReportsFilters, ReportsMetrics, ExportButton remain
    - Map view uses max-w-none / wider main per UI-SPEC
    - Default load without view param shows table (D-01)
  </acceptance_criteria>
  <action>
1. Create ReportsViewToggle client component using useRouter + useSearchParams.
2. Insert toggle in dashboard page header row next to ExportButton.
3. Branch table vs map render; pass normalized searchParams to both paths.
4. Add MapCanvasSkeleton.tsx for map loading placeholder (480px desktop / 320px mobile).

Use shadcn toggle-group if not installed: npx shadcn@latest add toggle-group from frontend/.
  </action>
  <verify>
    <automated>cd frontend && npm run lint -- src/app/dashboard/page.tsx src/components/reports/ReportsViewToggle.tsx</automated>
  </verify>
  <done>Table/map toggle wired on dashboard with URL sync.</done>
</task>

<task type="auto">
  <name>Task 3: ReportsMapView with client clustering and marker navigation</name>
  <read_first>
    - frontend/src/components/reports/ReportsMapView.tsx (create)
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Track C, Color priority table)
    - .planning/phases/06-maps-geospatial/06-RESEARCH.md (clustering pattern)
    - frontend/src/components/ReportLocationMiniMapInner.tsx (reuse tile style helper if extracted)
  </read_first>
  <acceptance_criteria>
    - ReportsMapView.tsx is client-only via dynamic import from page or 'use client' with parent dynamic wrapper
    - Fetches pins from /api/officer/reports/geo/pins on mount and on moveend debounced 300ms
    - MapLibre Source cluster=true with cluster count labels; clusterMaxZoom ~14, clusterRadius ~50
    - Individual marker click calls router.push(`/dashboard/reports/${reportId}`) — no popup gate (D-03)
    - Pin colors reflect priority per UI-SPEC table (critical/high/medium/low) with text labels in list panel
    - Optional hover tooltip: category + priority text only — no description
    - Shows MapCanvasSkeleton on initial load; thin toolbar spinner on viewport refresh
    - Displays unlocated_count message below map when > 0 using dashboard.map.unlocatedCount i18n key
    - Zoom controls have aria-label Zoom in / Zoom out
    - prefers-reduced-motion: jumpTo not flyTo
  </acceptance_criteria>
  <action>
1. Implement ReportsMapView with react-map-gl/maplibre Map, Source, Layer for clusters + unclustered points.
2. Convert pins API response to GeoJSON FeatureCollection in client.
3. Cluster click: map.easeTo or zoom +2 per UI-SPEC.
4. Extract shared buildRasterStyleFromEnv() from mini-map if useful.
5. Empty states per UI-SPEC interaction states table.

Do NOT implement server-side clustering endpoint. Client clusters flat pin array per D-05.
  </action>
  <verify>
    <automated>cd frontend && npm run lint -- src/components/reports/ReportsMapView.tsx</automated>
  </verify>
  <done>Dashboard map renders clustered pins with direct marker navigation.</done>
</task>

<task type="auto">
  <name>Task 4: GeoFilterBar + GeoFilterPanel bbox draw and URL sync</name>
  <read_first>
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Geo filter flow, Interaction contract)
    - .planning/phases/06-maps-geospatial/06-CONTEXT.md D-08..D-10
    - frontend/src/components/reports/ReportsFilters.tsx (URL pattern reference)
  </read_first>
  <acceptance_criteria>
    - GeoFilterBar floats over map or sits below attribute filters with Draw area / Apply area filter / Clear area filter
    - Rectangle draw on map OR west/south/east/north numeric inputs in GeoFilterPanel (keyboard fallback required)
    - Apply sets URL bbox=west,south,east,north and triggers pin refetch + metrics/export query update
    - Clear area filter removes bbox param only; attribute filters remain (D-09)
    - Invalid bbox shows inline error from dashboard.map.invalidBbox copy — no fetch
    - No radius mode UI or URL params (deferred per CONTEXT)
    - Bbox preview rectangle: accent stroke 2px, soft-accent fill 25% opacity
    - aria-pressed on draw mode toggle; panel keyboard hint string present
  </acceptance_criteria>
  <action>
1. GeoFilterPanel: four inputs west,south,east,north + Apply/Clear ghost buttons.
2. GeoFilterBar: toggles draw mode; lightweight pointer handlers on map canvas for rectangle (store draft bbox until Apply).
3. Sync URL via useRouter; on Apply call router.push with updated searchParams.
4. Pass filter bbox to geo pins API as filter_bbox when URL bbox set.

Implement draw with map overlay div or MapLibre fill layer — avoid @maplibre/maplibre-gl-draw unless necessary per RESEARCH.
  </action>
  <verify>
    <automated>cd frontend && npm run lint -- src/components/reports/GeoFilterBar.tsx src/components/reports/GeoFilterPanel.tsx</automated>
  </verify>
  <done>Bbox-only geo filter synced to URL with panel fallback.</done>
</task>

<task type="auto">
  <name>Task 5: IncidentsListPanel a11y + EN/VI dashboard.map + smoke tests</name>
  <read_first>
    - .planning/phases/06-maps-geospatial/06-UI-SPEC.md (Accessibility, Copywriting Track C)
    - frontend/src/components/DashboardSidebar.tsx
    - frontend/messages/en.json
  </read_first>
  <acceptance_criteria>
    - IncidentsListPanel.tsx toggles list of pins in viewport with category, status, priority text; Enter navigates to detail
    - dashboard.map.* EN/VI keys from UI-SPEC copy table added to messages (view toggles, geo filter, empty, error, unlocatedCount, listPanelToggle)
    - DashboardSidebar highlights Reports when on map view; optional quick link or label sets view=map on /dashboard (D-04 — view=map on same route, not separate /dashboard/map)
    - frontend/tests/dashboard-map.test.mjs asserts page.tsx references ReportsMapView and view=map
    - `node --test frontend/tests/dashboard-map.test.mjs` exits 0
  </acceptance_criteria>
  <action>
1. Add IncidentsListPanel collapsible beside map for SR users.
2. Add all dashboard.map strings from 06-UI-SPEC to en.json and vi.json.
3. Create dashboard-map.test.mjs smoke tests.
4. Ensure ExportButton passes bbox in export query when geo filter active (extend ExportButton if needed to read bbox from URL).

No radius copy strings. Use "Table view" / "Map view" per UI-SPEC revision.
  </action>
  <verify>
    <automated>node --test frontend/tests/dashboard-map.test.mjs frontend/tests/dashboard-geo-params.test.mjs</automated>
  </verify>
  <done>Accessibility list panel, i18n, and smoke tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer map client | JWT via BFF; pins minimal fields only. |
| Map popup/hover | No description, tokens, or citizen PII on map chrome. |
| Public vs officer | Map shows officer-authenticated data only on /dashboard. |
| Geo filter URL | bbox param validated client + server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-C1 | Information Disclosure | map hover/list | high | mitigate | UI-SPEC allows category/status/priority text only; no summary field. |
| T-06-C2 | Spoofing | BFF geo/pins | high | mitigate | Session JWT required; mirror export route pattern. |
| T-06-C3 | Tampering | bbox URL param | medium | mitigate | Server 422 on invalid bbox; client inline validation. |
| T-06-C4 | Repudiation | officer map access | low | accept | Existing officer audit via JWT sub on API calls. |
| T-06-C5 | Denial of Service | rapid viewport refetch | medium | mitigate | 300ms debounce on moveend; optional pin cap from API. |
</threat_model>

<verification>
Run `node --test frontend/tests/dashboard-map.test.mjs frontend/tests/dashboard-geo-params.test.mjs`. Manual: /dashboard?view=map loads map; cluster click zooms; marker click opens detail; bbox draw updates URL; unlocated count appears when applicable. Confirm no radius UI.
</verification>

<success_criteria>
- MAP-02 and MAP-03 officer UI complete per CONTEXT and UI-SPEC.
- Table remains default; map is secondary toggle.
- Phase 6 officer map success criteria satisfied for demo/UAT.
</success_criteria>

<output>
Create `.planning/phases/06-maps-geospatial/06-03-SUMMARY.md` when done.
</output>
