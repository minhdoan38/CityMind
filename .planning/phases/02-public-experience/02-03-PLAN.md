---
phase: 02-public-experience
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - frontend/src/proxy.ts
  - frontend/src/lib/supabase/client.ts
  - frontend/src/lib/supabase/server.ts
  - frontend/src/lib/auth.ts
  - frontend/src/app/login/page.tsx
  - frontend/src/app/api/session/login/route.ts
  - frontend/src/app/api/session/logout/route.ts
  - frontend/tests/officer-auth.test.mjs
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Unauthenticated requests to /dashboard and /dashboard/reports/* redirect to /login with returnUrl preserving the requested path."
    - "Officers sign in with Supabase email/password and land on returnUrl (default /dashboard) only when the path is same-origin and starts with /."
  artifacts:
    - path: "frontend/src/proxy.ts"
      provides: "next-intl + Supabase getClaims() gate for /dashboard/:path*"
    - path: "frontend/src/app/login/page.tsx"
      provides: "Supabase Auth sign-in honoring returnUrl"
  key_links:
    - from: "frontend/src/proxy.ts"
      to: "frontend/src/lib/supabase/server.ts"
      via: "cookie SSR client getClaims authorization on dashboard matcher"
      pattern: "getClaims|returnUrl|/dashboard"
---

## Phase Goal

**As an** officer, **I want to** sign in with Supabase Auth and reach a protected dashboard path, **so that** only authenticated officers can open officer routes.

<objective>
Deliver Track C auth gate: complete Supabase Auth login with returnUrl (D-15), protect `/dashboard` via Next.js 16 `proxy.ts` using `getClaims()` (D-17 / AUTH-04 — not deprecated middleware.ts and not public `/`).

Purpose: Close the officer gate after Plan 02-02 established locale proxy composition.
Output: Supabase SSR clients (ensure present), login/logout without HMAC password cookie, composed proxy matcher with getClaims + returnUrl.

Dashboard cards, Bearer officerFetch, and detail route move are Plan 02-05. `/login` and `/dashboard` stay outside `[locale]` (RESEARCH Q2 RESOLVED).
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-experience/02-CONTEXT.md
@.planning/phases/02-public-experience/02-RESEARCH.md
@.planning/phases/02-public-experience/02-PATTERNS.md
@.planning/phases/02-public-experience/02-UI-SPEC.md
@.planning/phases/02-public-experience/02-VALIDATION.md
@.planning/phases/02-public-experience/02-02-PLAN.md
@frontend/src/proxy.ts
@frontend/src/app/login/page.tsx
@frontend/src/lib/auth.ts
@frontend/tests/officer-auth.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace HMAC login with Supabase Auth and returnUrl</name>
  <files>frontend/src/lib/supabase/client.ts, frontend/src/lib/supabase/server.ts, frontend/src/lib/auth.ts, frontend/src/app/login/page.tsx, frontend/src/app/api/session/login/route.ts, frontend/src/app/api/session/logout/route.ts, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - Login accepts email+password via signInWithPassword (or equivalent @supabase/ssr browser/server flow).
    - After success, redirect to returnUrl query when it is a same-origin path starting with /; otherwise default /dashboard (D-15).
    - HMAC citymind_officer_session password quick-access is retired from the production login path.
    - Logout clears Supabase cookies/session.
    - /login remains outside [locale] (RESEARCH Q2 RESOLVED).
  </behavior>
  <action>Write/extend officer-auth contract tests first. Ensure `@supabase/ssr` createBrowserClient/createServerClient helpers exist under frontend/src/lib/supabase (create if Phase 1 gap — verified present as of 2026-07-20). Analog: PATTERNS Track C supabase client + auth.ts cookie boundary; divergence: Supabase session/claims replace HMAC password cookie. Replace HMAC session helpers in auth.ts with Supabase session/claims-based requireOfficerSession. Rebuild login page for email/password and returnUrl handling per D-15; keep /login outside [locale]. Retire or no-op the shared-password session login route so it cannot mint the old cookie. Align logout with Supabase signOut. Use existing @supabase/ssr@0.12.3 pin — no divergent version install. Prefer EN login copy inline (outside locale catalogs) to avoid Wave 2 message-file conflicts with Plan 02-04.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Officers authenticate with Supabase email/password and honor safe returnUrl redirects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate /dashboard in proxy.ts with getClaims</name>
  <files>frontend/src/proxy.ts, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - Unauthenticated /dashboard and /dashboard/reports/* redirect to /login?returnUrl= encoded path+search (D-15 / D-17 / AUTH-04).
    - Authorization uses getClaims() (not getSession user object alone).
    - Public locale Home/report routes remain reachable without officer cookies (D-17).
    - File remains proxy.ts on Next.js 16 — do not add middleware.ts.
  </behavior>
  <action>Extend the Plan 02-02 next-intl proxy composition: for matcher /dashboard/:path* create a Supabase cookie client, call getClaims(), and redirect unauthenticated callers to /login with returnUrl (D-15/D-17). Analog: PATTERNS `proxy.ts` exact seam + RESEARCH Pattern 5 getClaims; divergence: compose auth after locale handler — never protect public /. Preserve locale middleware behavior for [locale] public routes. Update AUTH-04 path semantics in tests to protect dashboard — never protect public /. Document path correction vs legacy REQUIREMENTS wording that mentioned root protection.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Dashboard routes require claims; public routes stay open; gate lives in proxy.ts.</done>
</task>

</tasks>

<must_haves>
- AUTH-04 protects /dashboard via proxy.ts + getClaims + returnUrl.
- Supabase Auth replaces HMAC officer password cookie for login.
- Dashboard card UI deferred to Plan 02-05.
</must_haves>

## Artifacts this phase produces

- Composed locale + auth proxy.ts.
- Supabase Auth login/logout with returnUrl.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /login | Untrusted credentials; Supabase Auth is source of truth. |
| proxy.ts → /dashboard | Cookie session claims cross into officer UI; must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of Privilege | /dashboard gate | critical | mitigate | proxy.ts getClaims() + matcher /dashboard/:path*; never protect public Home (D-15/D-17 / AUTH-04). |
| T-02-09 | Information Disclosure | returnUrl | medium | mitigate | Allow only same-origin paths starting with /; default /dashboard. |
</threat_model>

<verification>
Run officer-auth node tests and lint. Confirm unauthenticated /dashboard redirects with returnUrl; public /en remains open.
</verification>

<success_criteria>
- AUTH-04 gate + login/returnUrl satisfied for /dashboard (path-corrected).
- D-15 and D-17 implemented; D-16 card list is Plan 02-05.
</success_criteria>

<output>
Create `.planning/phases/02-public-experience/02-03-SUMMARY.md` when done.
</output>
