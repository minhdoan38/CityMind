---
phase: 02-public-experience
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - frontend/src/proxy.ts
  - frontend/src/lib/supabase/client.ts
  - frontend/src/lib/supabase/server.ts
  - frontend/src/lib/auth.ts
  - frontend/src/lib/backend.ts
  - frontend/src/app/login/page.tsx
  - frontend/src/app/api/session/login/route.ts
  - frontend/src/app/api/session/logout/route.ts
  - frontend/src/app/dashboard/layout.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/app/dashboard/reports/[reportId]/page.tsx
  - frontend/src/components/dashboard/ReportCard.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/officer-auth.test.mjs
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Unauthenticated requests to /dashboard and /dashboard/reports/* redirect to /login with returnUrl preserving the requested path."
    - "Officers sign in with Supabase email/password and land on returnUrl (default /dashboard) only when the path is same-origin and starts with /."
    - "Authenticated dashboard shows a simple recent report card list (category, priority, status, summary) linking to /dashboard/reports/[reportId] — not a Phase 3 data table."
  artifacts:
    - path: "frontend/src/proxy.ts"
      provides: "next-intl + Supabase getClaims() gate for /dashboard/:path*"
    - path: "frontend/src/app/login/page.tsx"
      provides: "Supabase Auth sign-in honoring returnUrl"
    - path: "frontend/src/components/dashboard/ReportCard.tsx"
      provides: "Simple card view for recent reports"
  key_links:
    - from: "frontend/src/proxy.ts"
      to: "frontend/src/lib/supabase/server.ts"
      via: "cookie SSR client getClaims authorization on dashboard matcher"
      pattern: "getClaims|returnUrl|/dashboard"
    - from: "frontend/src/lib/backend.ts"
      to: "FastAPI /api/v1/reports/recent"
      via: "Authorization Bearer JWT from Supabase session"
      pattern: "Authorization|Bearer|officerFetch"
---

## Phase Goal

**As an** officer, **I want to** sign in with Supabase Auth and open a protected dashboard of recent report cards, **so that** only authenticated officers can review submissions.

<objective>
Deliver Track C: complete Supabase Auth login with returnUrl (D-15), protect `/dashboard` via Next.js 16 `proxy.ts` using `getClaims()` (D-17 / AUTH-04 — not deprecated middleware.ts and not public `/`), and ship a simple recent card list (D-16).

Purpose: Close the officer gate after Plan 02 established locale proxy composition.
Output: Supabase SSR clients (if missing), login/logout without HMAC password cookie, composed proxy matcher, Bearer JWT officerFetch, dashboard cards + relocated detail route.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-experience/02-CONTEXT.md
@.planning/phases/02-public-experience/02-RESEARCH.md
@.planning/phases/02-public-experience/02-PATTERNS.md
@.planning/phases/02-public-experience/02-UI-SPEC.md
@.planning/phases/02-public-experience/02-02-PLAN.md
@frontend/src/proxy.ts
@frontend/src/app/login/page.tsx
@frontend/src/lib/auth.ts
@frontend/src/lib/backend.ts
@frontend/src/app/dashboard/page.tsx
@frontend/tests/officer-auth.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace HMAC login with Supabase Auth and returnUrl</name>
  <files>frontend/src/lib/supabase/client.ts, frontend/src/lib/supabase/server.ts, frontend/src/lib/auth.ts, frontend/src/app/login/page.tsx, frontend/src/app/api/session/login/route.ts, frontend/src/app/api/session/logout/route.ts, frontend/tests/officer-auth.test.mjs, frontend/messages/en.json, frontend/messages/vi.json</files>
  <behavior>
    - Login accepts email+password via signInWithPassword (or equivalent @supabase/ssr browser/server flow).
    - After success, redirect to returnUrl query when it is a same-origin path starting with /; otherwise default /dashboard (D-15).
    - HMAC citymind_officer_session password quick-access is retired from the production login path.
    - Logout clears Supabase cookies/session.
  </behavior>
  <action>Write/extend officer-auth contract tests first. Ensure `@supabase/ssr` createBrowserClient/createServerClient helpers exist under frontend/src/lib/supabase (create if Phase 1 gap). Replace HMAC session helpers in auth.ts with Supabase session/claims-based requireOfficerSession. Rebuild login page for email/password and returnUrl handling per D-15; keep /login outside [locale]. Retire or no-op the shared-password session login route so it cannot mint the old cookie. Align logout with Supabase signOut. Use existing @supabase/ssr@0.12.3 pin — no divergent version install.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Officers authenticate with Supabase email/password and honor safe returnUrl redirects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate /dashboard in proxy.ts with getClaims</name>
  <files>frontend/src/proxy.ts, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - Unauthenticated /dashboard and /dashboard/reports/* redirect to /login?returnUrl= encoded path+search (D-15 / D-17 / AUTH-04).
    - Authorization uses getClaims() (not getSession user object alone).
    - Public locale Home/report routes remain reachable without officer cookies (D-17).
    - File remains proxy.ts on Next.js 16 — do not add middleware.ts.
  </behavior>
  <action>Extend the Plan 02 next-intl proxy composition: for matcher /dashboard/:path* create a Supabase cookie client, call getClaims(), and redirect unauthenticated callers to /login with returnUrl (D-15/D-17). Preserve locale middleware behavior for [locale] public routes. Update AUTH-04 path semantics in tests to protect dashboard — never protect public /. Document path correction vs legacy REQUIREMENTS wording that mentioned root protection.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Dashboard routes require claims; public routes stay open; gate lives in proxy.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship simple dashboard report cards with Bearer JWT</name>
  <files>frontend/src/lib/backend.ts, frontend/src/app/dashboard/layout.tsx, frontend/src/app/dashboard/page.tsx, frontend/src/app/dashboard/reports/[reportId]/page.tsx, frontend/src/components/dashboard/ReportCard.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - officerFetch sends Authorization Bearer from the Supabase server session (not X-CityMind-Officer-Key as the primary auth path).
    - /dashboard lists recent reports as cards showing category, priority, status, summary with badge text (not color-only) linking to /dashboard/reports/[reportId] (D-16).
    - Empty state uses UI-SPEC No reports yet copy.
    - Phase 3 data table, advanced filters, export, and resolve-note chrome are absent.
  </behavior>
  <action>Update officerFetch to forward the officer JWT. Simplify dashboard page to a card list via ReportCard (shadcn Card/Badge as needed) per D-16; drop KPI strip / advanced filter form from the Phase 2 definition of done (optional minimal query params only if already trivial). Move detail from /reports/[reportId] to /dashboard/reports/[reportId] so the proxy matcher covers it. Keep dashboard outside [locale].</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Authenticated officers see recent cards under /dashboard with JWT-backed fetches; Phase 3 table is not present.</done>
</task>

</tasks>

<must_haves>
- AUTH-04 protects /dashboard via proxy.ts + getClaims + returnUrl.
- Supabase Auth replaces HMAC officer password cookie for login.
- Simple card list only (D-16); no Phase 3 table.
</must_haves>

## Artifacts this phase produces

- Composed locale + auth proxy.ts.
- Supabase Auth login/logout with returnUrl.
- Dashboard card list and relocated detail route.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /login | Untrusted credentials; Supabase Auth is source of truth. |
| proxy.ts → /dashboard | Cookie session claims cross into officer UI; must fail closed. |
| Dashboard RSC → FastAPI | Bearer JWT must be verified server-side (Phase 1 JWKS); UI gate is not sufficient alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of Privilege | /dashboard gate | critical | mitigate | proxy.ts getClaims() + matcher /dashboard/:path*; never protect public Home (D-15/D-17 / AUTH-04). |
| T-02-08 | Spoofing | officerFetch | high | mitigate | Send Supabase access token as Bearer; FastAPI JWKS remains authoritative. |
| T-02-09 | Information Disclosure | returnUrl | medium | mitigate | Allow only same-origin paths starting with /; default /dashboard. |
</threat_model>

<verification>
Run officer-auth node tests, lint, and build. Confirm unauthenticated /dashboard redirects with returnUrl; after login, cards render; public /en remains open.
</verification>

<success_criteria>
- AUTH-04 satisfied for /dashboard (path-corrected).
- D-15, D-16, and D-17 implemented.
- No Phase 3 dashboard polish features shipped as Phase 2 done.
</success_criteria>

<output>
Create `.planning/phases/02-public-experience/02-03-SUMMARY.md` when done.
</output>
