---
phase: 02-public-experience
plan: "05"
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - frontend/src/lib/backend.ts
  - frontend/src/app/dashboard/layout.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/app/dashboard/reports/[reportId]/page.tsx
  - frontend/src/components/dashboard/ReportCard.tsx
  - frontend/tests/officer-auth.test.mjs
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Authenticated dashboard shows a simple recent report card list (category, priority, status, summary) linking to /dashboard/reports/[reportId] — not a Phase 3 data table."
    - "officerFetch sends Authorization Bearer from the Supabase server session to FastAPI recent/detail endpoints."
  artifacts:
    - path: "frontend/src/components/dashboard/ReportCard.tsx"
      provides: "Simple card view for recent reports"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Protected recent card list under /dashboard"
    - path: "frontend/src/lib/backend.ts"
      provides: "Bearer JWT officerFetch"
  key_links:
    - from: "frontend/src/lib/backend.ts"
      to: "FastAPI /api/v1/reports/recent"
      via: "Authorization Bearer JWT from Supabase session"
      pattern: "Authorization|Bearer|officerFetch"
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "frontend/src/app/dashboard/reports/[reportId]/page.tsx"
      via: "card link under /dashboard so proxy matcher covers detail"
      pattern: "/dashboard/reports/"
---

## Phase Goal

**As an** officer, **I want to** see recent report cards on a protected dashboard, **so that** I can open submissions after signing in.

<objective>
Deliver Track C dashboard UX: simple recent card list (D-16), Bearer JWT `officerFetch`, and relocate detail under `/dashboard/reports/[reportId]`.

Purpose: Complete AUTH-04 officer experience after Plan 02-03 auth gate.
Output: Dashboard layout/page, ReportCard, Bearer-backed fetches, relocated detail route.

Dashboard stays outside `[locale]` (RESEARCH Q2 RESOLVED) — no hard depends_on Plan 02-02. Depends on Plan 02-03 for proxy gate + session.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-experience/02-CONTEXT.md
@.planning/phases/02-public-experience/02-RESEARCH.md
@.planning/phases/02-public-experience/02-PATTERNS.md
@.planning/phases/02-public-experience/02-UI-SPEC.md
@.planning/phases/02-public-experience/02-VALIDATION.md
@.planning/phases/02-public-experience/02-03-PLAN.md
@frontend/src/lib/backend.ts
@frontend/src/app/dashboard/page.tsx
@frontend/tests/officer-auth.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bearer officerFetch and simple dashboard report cards</name>
  <files>frontend/src/lib/backend.ts, frontend/src/app/dashboard/layout.tsx, frontend/src/app/dashboard/page.tsx, frontend/src/components/dashboard/ReportCard.tsx, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - officerFetch sends Authorization Bearer from the Supabase server session (not X-CityMind-Officer-Key as the primary auth path).
    - /dashboard lists recent reports as cards showing category, priority, status, summary with badge text (not color-only) linking to /dashboard/reports/[reportId] (D-16).
    - Empty state uses UI-SPEC No reports yet copy (inline EN acceptable outside locale).
    - Phase 3 data table, advanced filters, export, and resolve-note chrome are absent.
  </behavior>
  <action>Update officerFetch to forward the officer JWT. Analog: PATTERNS `backend.ts` officerFetch boundary; divergence: Authorization Bearer from Supabase session replaces X-CityMind-Officer-Key primary path. Simplify dashboard page to a card list via ReportCard (shadcn Card/Badge as needed) per D-16; drop KPI strip / advanced filter form from the Phase 2 definition of done (optional minimal query params only if already trivial). Analog: PATTERNS dashboard list articles; divergence: D-16 cards only — not Phase 3 table. Keep dashboard outside [locale]. Extend officer-auth tests for Bearer header contract and card-list presence markers.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Authenticated officers see recent cards under /dashboard with JWT-backed fetches; Phase 3 table is not present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relocate report detail under /dashboard/reports</name>
  <files>frontend/src/app/dashboard/reports/[reportId]/page.tsx, frontend/src/app/dashboard/page.tsx, frontend/tests/officer-auth.test.mjs</files>
  <behavior>
    - Detail lives at /dashboard/reports/[reportId] so the Plan 02-03 proxy matcher covers it.
    - Cards link to the relocated detail path.
    - Legacy /reports/[reportId] is removed or redirects into /dashboard/reports/[reportId].
  </behavior>
  <action>Move detail from /reports/[reportId] to /dashboard/reports/[reportId] so the proxy matcher covers it. Analog: PATTERNS move-detail note under Track C dashboard; divergence: path prefix only — preserve existing detail fields/fetch. Update card links and tests; remove or redirect the old public-ish officer detail path. Do not add Phase 3 resolve/export chrome.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/officer-auth.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Detail route is under /dashboard/reports/[reportId] and covered by the auth gate.</done>
</task>

</tasks>

<must_haves>
- Simple card list only (D-16); no Phase 3 table.
- Bearer JWT officerFetch wired.
- Detail under /dashboard/reports so AUTH-04 matcher applies.
</must_haves>

## Artifacts this phase produces

- Dashboard card list and relocated detail route.
- Bearer-backed officerFetch.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard RSC → FastAPI | Bearer JWT must be verified server-side (Phase 1 JWKS); UI gate is not sufficient alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Spoofing | officerFetch | high | mitigate | Send Supabase access token as Bearer; FastAPI JWKS remains authoritative. |
</threat_model>

<verification>
Run officer-auth node tests, lint, and build. Confirm after login cards render and detail opens under /dashboard/reports.
</verification>

<success_criteria>
- D-16 implemented; AUTH-04 UX complete with Bearer fetches.
- No Phase 3 dashboard polish features shipped as Phase 2 done.
</success_criteria>

<output>
Create `.planning/phases/02-public-experience/02-05-SUMMARY.md` when done.
</output>
