---
phase: 02-public-experience
plan: "04"
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/ReportForm.tsx
  - frontend/src/app/[locale]/report/page.tsx
  - frontend/src/app/[locale]/report/success/page.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/report-form.test.mjs
autonomous: false
requirements:
  - PUB-03
  - PUB-04
  - PUB-06
must_haves:
  truths:
    - "Report form validates with React Hook Form + Zod, allows submit without location, disables during Analyzing your report…, and navigates to /[locale]/report/success."
    - "Success page shows report_id and access token once via sessionStorage flash (not query string) with copy actions and status-link prep."
  artifacts:
    - path: "frontend/src/components/ReportForm.tsx"
      provides: "RHF+Zod multipart submit with flash handoff"
    - path: "frontend/src/app/[locale]/report/success/page.tsx"
      provides: "One-time token display and copy UX"
  key_links:
    - from: "frontend/src/components/ReportForm.tsx"
      to: "frontend/src/app/[locale]/report/success/page.tsx"
      via: "sessionStorage citymind:report-success flash then locale router push"
      pattern: "sessionStorage|accessToken|report/success"
    - from: "Plan 02-01 AnalyzeResponse.access_token"
      to: "ReportForm success flash"
      via: "soft A→B contract — prefer 02-01 green before live success smoke"
      pattern: "access_token"
---

## Phase Goal

**As a** citizen, **I want to** submit a validated report and see my access token once, **so that** I can track status later without leaking the token in the URL.

<objective>
Deliver Track B report flow: rebuild ReportForm with RHF+Zod (D-09/D-10/D-12 / PUB-03) and dedicated success flash page (D-11/D-18 / PUB-04), including package checkpoint + exact pins for react-hook-form/zod/@hookform/resolvers.

Purpose: Complete the citizen submit path under Plan 02-02 locale routes.
Output: Locale report + success routes, RHF form, sessionStorage flash UX, Report/Success catalog strings.

**Soft depends_on Plan 02-01:** Success flash needs `access_token` from Track A. Hard dependency is only `02-02` (locale routes). Documented phase-gate: prefer 02-01 green before live success smoke; if absent, surface API errors without claiming PUB-04 done against a live analyze.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-experience/02-CONTEXT.md
@.planning/phases/02-public-experience/02-RESEARCH.md
@.planning/phases/02-public-experience/02-PATTERNS.md
@.planning/phases/02-public-experience/02-UI-SPEC.md
@.planning/phases/02-public-experience/02-VALIDATION.md
@.planning/phases/02-public-experience/02-02-PLAN.md
@frontend/src/components/ReportForm.tsx
@frontend/messages/en.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve React Hook Form stack package identities</name>
  <files>.planning/phases/02-public-experience/02-RESEARCH.md</files>
  <what-built>RESEARCH flags react-hook-form as SUS (too-new seam) with GO after checkpoint; zod and @hookform/resolvers are OK. next-intl@4.13.2 is already installed from Phase 1 (verified live package.json) — no next-intl reinstall.</what-built>
  <action>Review exact pins react-hook-form@7.82.0, zod@4.4.3, and @hookform/resolvers@5.4.0 on npmjs.com against 02-RESEARCH.md. Confirm empty/harmless postinstall posture from RESEARCH. Authorize install before Task 2 form work. Do not install until resume-signal.</action>
  <how-to-verify>
    1. Verify react-hook-form@7.82.0 and repository react-hook-form/react-hook-form.
    2. Verify zod@4.4.3 and @hookform/resolvers@5.4.0 match RESEARCH.
    3. Confirm next-intl@4.13.2 remains the Phase 1 pin (no divergent upgrade required for this plan).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize these exact npm pins, or identify the package that must be re-researched.</resume-signal>
  <verify>
    <automated>node /Users/minhmice/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.get .planning/phases/02-public-experience/02-RESEARCH.md</automated>
  </verify>
  <done>Human approval recorded for RHF/zod/resolvers pins before npm install.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild ReportForm and one-time success flash</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/components/ReportForm.tsx, frontend/src/app/[locale]/report/page.tsx, frontend/src/app/[locale]/report/success/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/report-form.test.mjs</files>
  <behavior>
    - Form uses React Hook Form + zodResolver; description max 3000; location optional with helper (D-09); image optional with client MIME/size refine and helper text (D-10); server remains authoritative.
    - While submitting: primary button disabled and Analyzing your report… shown; double-submit prevented (D-12).
    - On success: write sessionStorage flash key citymind:report-success with reportId + accessToken, then navigate to /[locale]/report/success without putting the token in the URL (D-11 / D-18 / PUB-04).
    - Success page reads and clears flash once; missing flash redirects to report; shows report_id, access token, copy buttons with live region, and Status link (coming soon) prep — no Phase 4 /status UI.
  </behavior>
  <action>After Task 1 approval, install exact pins react-hook-form@7.82.0 zod@4.4.3 @hookform/resolvers@5.4.0 and add needed official shadcn form/textarea/badge pieces if missing. Write report-form contract tests, then rebuild ReportForm with RHF+Zod Controller patterns, geolocation helper preserved, civic light chrome (not slate-950), FormData POST to /api/public/reports/analyze, and flash handoff expecting access_token from Plan 02-01 (soft A→B contract — gracefully surface API errors with UI-SPEC network copy if field absent). Analog: PATTERNS ReportForm.tsx exact rebuild + RESEARCH Pattern 3 success flash; divergence: success route is new (no prior analog). Implement success page per D-11/D-18/PUB-04/PUB-06. Extend message catalogs for Report/Success strings from UI-SPEC (preserve Home keys from Plan 02-02).</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/report-form.test.mjs tests/public-shell.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Citizens can validate, submit, and view a one-time token on the dedicated success route under locale prefixes.</done>
</task>

</tasks>

<must_haves>
- RHF+Zod report submit with analyzing state and sessionStorage success flash.
- No access token in query strings; no Phase 4 status page UI.
- Soft A→B contract documented; prefer 02-01 before live token smoke.
</must_haves>

## Artifacts this phase produces

- Locale `/[locale]/report` + `/success` surfaces.
- RHF+Zod ReportForm + one-time token flash UX.
- Report/Success EN/VI catalog strings.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → public BFF | Citizen text/coords/file; client Zod is UX-only. |
| Success flash (sessionStorage) | Plaintext token briefly resident in the origin; XSS could read it once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | success handoff | high | mitigate | sessionStorage flash only; clear on read; never put token in URL/query (D-11/D-18). |
| T-02-06 | Tampering | client image validation | low | accept | Client Zod is advisory; Track A magic bytes remain authoritative (D-10/D-20). |
| T-02-SC | Tampering | npm RHF/zod installs | high | mitigate | Blocking human checkpoint for SUS react-hook-form pin before install. |
</threat_model>

<verification>
Run report-form + public-shell tests, lint, build. Manually smoke report submit → success (token once, URL clean) after Plan 02-01 is green.
</verification>

<success_criteria>
- PUB-03, PUB-04, and PUB-06 (Report/Success copy) implemented against UI-SPEC and D-09–D-12, D-18.
- Soft A→B access_token contract honored; no Phase 4 /status UI.
</success_criteria>

<output>
Create `.planning/phases/02-public-experience/02-04-SUMMARY.md` when done.
</output>
