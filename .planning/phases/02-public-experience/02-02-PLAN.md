---
phase: 02-public-experience
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/i18n/routing.ts
  - frontend/src/i18n/navigation.ts
  - frontend/src/i18n/request.ts
  - frontend/src/proxy.ts
  - frontend/src/services/locale.ts
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/src/app/[locale]/layout.tsx
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/components/LocaleSwitcher.tsx
  - frontend/tests/public-shell.test.mjs
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-06
must_haves:
  truths:
    - "Citizens land on locale-prefixed Home (/en or /vi) with Accept-Language detection and can switch locales without losing the route."
    - "Home hero offers Report an issue CTA, AI-advisory supporting line, full-bleed civic visual, and section order How it works → Instructions → About → Contact → Footer."
    - "EN/VI message catalogs share identical key trees for Home/navigation namespaces (natural VI, not calques)."
  artifacts:
    - path: "frontend/src/app/[locale]/page.tsx"
      provides: "Bilingual Home per D-01–D-08 and UI-SPEC"
    - path: "frontend/src/i18n/routing.ts"
      provides: "defineRouting localePrefix always"
    - path: "frontend/src/proxy.ts"
      provides: "next-intl createMiddleware locale seam for Plan 02-03 to extend"
  key_links:
    - from: "frontend/src/i18n/routing.ts"
      to: "frontend/src/proxy.ts"
      via: "next-intl createMiddleware with localePrefix always"
      pattern: "localePrefix|createMiddleware|defineRouting"
---

## Phase Goal

**As a** citizen, **I want to** use a bilingual Home with always-prefix locales, **so that** I can browse CityMind in EN or VI before reporting.

<objective>
Deliver Track B i18n + Home shell: migrate next-intl to `localePrefix: 'always'` (D-13/D-14 / PUB-02), ship polished Home (D-01–D-08 / PUB-01/PUB-06), and establish the locale-only `proxy.ts` seam for Plan 02-03.

Purpose: Ship the public locale surface Plan 02-04 (Report/success) mounts under.
Output: `[locale]` App Router Home tree, EN/VI Home catalogs per UI-SPEC, locale-aware proxy (intl only — Auth gate is Plan 02-03). ReportForm / RHF / success flash are Plan 02-04.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-experience/02-CONTEXT.md
@.planning/phases/02-public-experience/02-RESEARCH.md
@.planning/phases/02-public-experience/02-PATTERNS.md
@.planning/phases/02-public-experience/02-UI-SPEC.md
@.planning/phases/02-public-experience/02-VALIDATION.md
@frontend/src/i18n/routing.ts
@frontend/src/proxy.ts
@frontend/src/app/page.tsx
@frontend/src/components/LocaleSwitcher.tsx
@frontend/messages/en.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate always-prefix routing and locale proxy seam</name>
  <files>frontend/src/i18n/routing.ts, frontend/src/i18n/navigation.ts, frontend/src/i18n/request.ts, frontend/src/proxy.ts, frontend/src/services/locale.ts, frontend/tests/public-shell.test.mjs</files>
  <behavior>
    - routing uses locales en/vi, defaultLocale en, localePrefix always (D-13); Accept-Language detection remains enabled (D-14).
    - createNavigation helpers support prefixed Link/redirect/usePathname between /en and /vi.
    - proxy.ts composes next-intl middleware for public locale negotiation only; it must not protect public Home or report routes (D-17). Dashboard auth composition is deferred to Plan 02-03.
    - /login and /dashboard stay outside [locale] (RESEARCH Q2 RESOLVED).
  </behavior>
  <action>Update failing public-shell tests for prefix routes. Set defineRouting localePrefix to always and add createNavigation helpers (D-13/D-14 / PUB-02). Analog: PATTERNS Pattern 1 / current `i18n/routing.ts` no-prefix scaffold; divergence: `localePrefix: 'always'` + `[locale]` tree (Phase 1 used never). Wire proxy.ts with next-intl createMiddleware matcher for locale routes — do not recreate deprecated middleware.ts; do not gate /dashboard here (Plan 02-03 owns that). Analog: PATTERNS `proxy.ts` exact seam; divergence: matcher/auth composition replaced in Plan 02-03. Keep login/dashboard outside [locale].</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Always-prefix routing and locale-only proxy seam are in place; public routes stay ungated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship bilingual Home and EN/VI catalogs</name>
  <files>frontend/messages/en.json, frontend/messages/vi.json, frontend/src/app/[locale]/layout.tsx, frontend/src/app/[locale]/page.tsx, frontend/src/components/LocaleSwitcher.tsx, frontend/tests/public-shell.test.mjs</files>
  <behavior>
    - Public Home lives under app/[locale]/page.tsx with hero CTA Report an issue → locale report route (D-01), AI advisory line in hero (D-02), section order How it works → Instructions (3–5 steps) → About → Contact static → Footer (D-03/D-05/D-06), civic light UI (D-04), subtle Officer sign-in link (D-07), full-bleed civic hero without overlay clutter (D-08).
    - LocaleSwitcher navigates via createNavigation between /en and /vi preserving path; EN/VI catalogs share identical key trees including UI-SPEC Home copy (PUB-02/PUB-06).
    - Unprefixed public pages that would bypass prefixes are removed or redirected.
  </behavior>
  <action>Move public Home under app/[locale]/; remove or redirect unprefixed public pages that would bypass prefixes. Expand en.json/vi.json with UI-SPEC Home namespaces (natural VI, not calques) — Report/Success strings may be stubbed empty or deferred to Plan 02-04. Analog: PATTERNS Home shell + LocaleSwitcher cookie dropdown; divergence: prefix navigation via createNavigation, not cookie-only setUserLocale. Implement Home per D-01–D-08 and UI-SPEC (44px CTA, focus-visible, Source Sans / Phase 1 tokens). Do not rebuild ReportForm here (Plan 02-04).</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; node -e "const fs=require('fs');const en=JSON.parse(fs.readFileSync('messages/en.json'));const vi=JSON.parse(fs.readFileSync('messages/vi.json'));const walk=o=&gt;Object.entries(o).flatMap(([k,v])=&gt;v&amp;&amp;typeof v==='object'?walk(v).map(x=&gt;k+'.'+x):[k]).sort();if(JSON.stringify(walk(en))!==JSON.stringify(walk(vi)))process.exit(1)" &amp;&amp; npm run lint</automated>
  </verify>
  <done>Prefixed bilingual Home matches locked decisions D-01–D-08 and PUB-01/02/06 shell contracts.</done>
</task>

</tasks>

<must_haves>
- /en and /vi public Home routes work with Accept-Language + switcher.
- Home delivers D-01–D-08 content contract.
- Locale-only proxy.ts seam ready for Plan 02-03 auth composition.
- No ReportForm RHF rewrite and no Phase 4 /status UI in this plan.
</must_haves>

## Artifacts this phase produces

- Always-prefix next-intl public App Router Home tree.
- Polished bilingual Home + message catalogs (Home namespaces).
- Locale-only proxy composition seam for Plan 02-03 to extend.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → locale Home | Public unauthenticated content; no secrets. |
| proxy.ts locale middleware | Untrusted Accept-Language / path; must not gate public routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Elevation of Privilege | proxy locale matcher | medium | mitigate | Locale middleware only; never protect public Home/report here (D-17); Plan 02-03 adds dashboard getClaims. |
</threat_model>

<verification>
Run frontend public-shell tests + en/vi key parity + lint. Manually smoke /en and /vi Home CTA and locale switch. Prefer end-of-phase human checklist over mid-plan UI checkpoint per workflow.human_verify_mode.
</verification>

<success_criteria>
- PUB-01, PUB-02, and PUB-06 (Home shell) are implemented against UI-SPEC and locked decisions D-01–D-08, D-13, D-14.
- Public routes never require officer auth.
- ReportForm / success flash deferred to Plan 02-04; deferred /status UI absent.
</success_criteria>

<output>
Create `.planning/phases/02-public-experience/02-02-SUMMARY.md` when done.
</output>
