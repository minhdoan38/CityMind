---
phase: 02-public-experience
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/i18n/routing.ts
  - frontend/src/i18n/navigation.ts
  - frontend/src/i18n/request.ts
  - frontend/src/proxy.ts
  - frontend/src/services/locale.ts
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/src/app/[locale]/layout.tsx
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/app/[locale]/report/page.tsx
  - frontend/src/app/[locale]/report/success/page.tsx
  - frontend/src/components/ReportForm.tsx
  - frontend/src/components/LocaleSwitcher.tsx
  - frontend/tests/public-shell.test.mjs
  - frontend/tests/report-form.test.mjs
autonomous: false
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-06
must_haves:
  truths:
    - "Citizens land on locale-prefixed Home (/en or /vi) with Accept-Language detection and can switch locales without losing the route."
    - "Home hero offers Report an issue CTA, AI-advisory supporting line, full-bleed civic visual, and section order How it works → Instructions → About → Contact → Footer."
    - "Report form validates with React Hook Form + Zod, allows submit without location, disables during Analyzing your report…, and navigates to /[locale]/report/success."
    - "Success page shows report_id and access token once via sessionStorage flash (not query string) with copy actions and status-link prep."
  artifacts:
    - path: "frontend/src/app/[locale]/page.tsx"
      provides: "Bilingual Home per D-01–D-08 and UI-SPEC"
    - path: "frontend/src/components/ReportForm.tsx"
      provides: "RHF+Zod multipart submit with flash handoff"
    - path: "frontend/src/app/[locale]/report/success/page.tsx"
      provides: "One-time token display and copy UX"
  key_links:
    - from: "frontend/src/i18n/routing.ts"
      to: "frontend/src/proxy.ts"
      via: "next-intl createMiddleware with localePrefix always"
      pattern: "localePrefix|createMiddleware|defineRouting"
    - from: "frontend/src/components/ReportForm.tsx"
      to: "frontend/src/app/[locale]/report/success/page.tsx"
      via: "sessionStorage citymind:report-success flash then locale router push"
      pattern: "sessionStorage|accessToken|report/success"
---

## Phase Goal

**As a** citizen, **I want to** use a bilingual Home and Report flow that validates my input and shows my access token once, **so that** I can submit issues confidently in EN or VI.

<objective>
Deliver Track B public experience: migrate next-intl to `localePrefix: 'always'` (D-13/D-14 / PUB-02), build polished Home (D-01–D-08 / PUB-01/PUB-06), rebuild ReportForm with RHF+Zod (D-09/D-10/D-12 / PUB-03), and dedicated success flash page (D-11/D-18 / PUB-04).

Purpose: Ship the citizen-facing product surface consuming Track A `access_token` when present.
Output: `[locale]` App Router tree, EN/VI catalogs per UI-SPEC, locale-aware proxy seam (intl only — Auth gate is Plan 03), Report + success UX.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-experience/02-CONTEXT.md
@.planning/phases/02-public-experience/02-RESEARCH.md
@.planning/phases/02-public-experience/02-PATTERNS.md
@.planning/phases/02-public-experience/02-UI-SPEC.md
@.planning/phases/01-supabase-foundation/01-UI-SPEC.md
@frontend/src/i18n/routing.ts
@frontend/src/proxy.ts
@frontend/src/app/page.tsx
@frontend/src/components/ReportForm.tsx
@frontend/messages/en.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve React Hook Form stack package identities</name>
  <files>.planning/phases/02-public-experience/02-RESEARCH.md</files>
  <what-built>RESEARCH flags react-hook-form as SUS (too-new seam) with GO after checkpoint; zod and @hookform/resolvers are OK. next-intl@4.13.2 is already installed from Phase 1.</what-built>
  <action>Review exact pins react-hook-form@7.82.0, zod@4.4.3, and @hookform/resolvers@5.4.0 on npmjs.com against 02-RESEARCH.md. Confirm empty/harmless postinstall posture from RESEARCH. Authorize install before Task 3 form work. Do not install until resume-signal.</action>
  <how-to-verify>
    1. Verify react-hook-form@7.82.0 and repository react-hook-form/react-hook-form.
    2. Verify zod@4.4.3 and @hookform/resolvers@5.4.0 match RESEARCH.
    3. Confirm next-intl@4.13.2 remains the Phase 1 pin (no divergent upgrade required for this plan).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize these exact npm pins, or identify the package that must be re-researched.</resume-signal>
  <verify>
    <automated>node /Users/minhmice/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.get .planning/phases/02-public-experience/02-RESEARCH.md</automated>
  </verify>
  <done>Human approval recorded for RHF/zod/resolvers pins before npm install.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate always-prefix locales and ship bilingual Home</name>
  <files>frontend/src/i18n/routing.ts, frontend/src/i18n/navigation.ts, frontend/src/i18n/request.ts, frontend/src/proxy.ts, frontend/src/services/locale.ts, frontend/messages/en.json, frontend/messages/vi.json, frontend/src/app/[locale]/layout.tsx, frontend/src/app/[locale]/page.tsx, frontend/src/components/LocaleSwitcher.tsx, frontend/tests/public-shell.test.mjs</files>
  <behavior>
    - routing uses locales en/vi, defaultLocale en, localePrefix always (D-13); Accept-Language detection remains enabled (D-14).
    - Public Home lives under app/[locale]/page.tsx with hero CTA Report an issue → locale report route (D-01), AI advisory line in hero (D-02), section order How it works → Instructions (3–5 steps) → About → Contact static → Footer (D-03/D-05/D-06), civic light UI (D-04), subtle Officer sign-in link (D-07), full-bleed civic hero without overlay clutter (D-08).
    - LocaleSwitcher navigates via createNavigation between /en and /vi preserving path; EN/VI catalogs share identical key trees including UI-SPEC Home copy (PUB-02/PUB-06).
    - proxy.ts composes next-intl middleware for public locale negotiation only; it must not protect public Home or report routes (D-17). Dashboard auth composition is deferred to Plan 03.
  </behavior>
  <action>Update failing public-shell tests for prefix routes. Set defineRouting localePrefix to always and add createNavigation helpers (D-13/D-14 / PUB-02). Move public Home/report entry under app/[locale]/; remove or redirect unprefixed public pages that would bypass prefixes. Expand en.json/vi.json with UI-SPEC Home namespaces (natural VI, not calques). Implement Home per D-01–D-08 and UI-SPEC (44px CTA, focus-visible, Source Sans / Phase 1 tokens). Wire proxy.ts with next-intl createMiddleware matcher for locale routes — do not recreate deprecated middleware.ts; do not gate /dashboard here (Plan 03 owns that). Keep login/dashboard outside [locale].</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/public-shell.test.mjs &amp;&amp; node -e "const fs=require('fs');const en=JSON.parse(fs.readFileSync('messages/en.json'));const vi=JSON.parse(fs.readFileSync('messages/vi.json'));const walk=o=&gt;Object.entries(o).flatMap(([k,v])=&gt;v&amp;&amp;typeof v==='object'?walk(v).map(x=&gt;k+'.'+x):[k]).sort();if(JSON.stringify(walk(en))!==JSON.stringify(walk(vi)))process.exit(1)" &amp;&amp; npm run lint</automated>
  </verify>
  <done>Prefixed bilingual Home matches locked decisions D-01–D-08 and PUB-01/02/06 shell contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild Report form and one-time success flash</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/components/ReportForm.tsx, frontend/src/app/[locale]/report/page.tsx, frontend/src/app/[locale]/report/success/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/report-form.test.mjs</files>
  <behavior>
    - Form uses React Hook Form + zodResolver; description max 3000; location optional with helper (D-09); image optional with client MIME/size refine and helper text (D-10); server remains authoritative.
    - While submitting: primary button disabled and Analyzing your report… shown; double-submit prevented (D-12).
    - On success: write sessionStorage flash key citymind:report-success with reportId + accessToken, then navigate to /[locale]/report/success without putting the token in the URL (D-11 / D-18 / PUB-04).
    - Success page reads and clears flash once; missing flash redirects to report; shows report_id, access token, copy buttons with live region, and Status link (coming soon) prep — no Phase 4 /status UI.
  </behavior>
  <action>After Task 1 approval, install exact pins react-hook-form@7.82.0 zod@4.4.3 @hookform/resolvers@5.4.0 and add needed official shadcn form/textarea/badge pieces if missing. Write report-form contract tests, then rebuild ReportForm with RHF+Zod Controller patterns, geolocation helper preserved, civic light chrome (not slate-950), FormData POST to /api/public/reports/analyze, and flash handoff expecting access_token from Track A (gracefully surface API errors with UI-SPEC network copy). Implement success page per D-11/D-18/PUB-04/PUB-06. Extend message catalogs for Report/Success strings from UI-SPEC.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/report-form.test.mjs tests/public-shell.test.mjs &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Citizens can validate, submit, and view a one-time token on the dedicated success route under locale prefixes.</done>
</task>

</tasks>

<must_haves>
- /en and /vi public routes work with Accept-Language + switcher.
- Home delivers D-01–D-08 content contract.
- RHF+Zod report submit with analyzing state and sessionStorage success flash.
- No access token in query strings; no Phase 4 status page UI.
</must_haves>

## Artifacts this phase produces

- Always-prefix next-intl public App Router tree.
- Polished bilingual Home + Report + success surfaces.
- Locale-only proxy composition seam for Plan 03 to extend.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → public BFF | Citizen text/coords/file; client Zod is UX-only. |
| Success flash (sessionStorage) | Plaintext token briefly resident in the origin; XSS could read it once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | success handoff | high | mitigate | sessionStorage flash only; clear on read; never put token in URL/query (D-11/D-18). |
| T-02-06 | Tampering | client image validation | low | accept | Client Zod is advisory; Track A magic bytes remain authoritative (D-10/D-20). |
| T-02-SC | Tampering | npm RHF/zod installs | high | mitigate | Blocking human checkpoint for SUS react-hook-form pin before install. |
</threat_model>

<verification>
Run frontend node tests + lint + build. Manually smoke /en and /vi Home CTA, locale switch, report submit happy path to success (token visible once, URL clean). Prefer end-of-phase human checklist over mid-plan UI checkpoint per workflow.human_verify_mode.
</verification>

<success_criteria>
- PUB-01, PUB-02, PUB-03, PUB-04, and PUB-06 are implemented against UI-SPEC and locked decisions D-01–D-14, D-18.
- Public routes never require officer auth.
- Deferred /status UI and Phase 3 table chrome are absent.
</success_criteria>

<output>
Create `.planning/phases/02-public-experience/02-02-SUMMARY.md` when done.
</output>
