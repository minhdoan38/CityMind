---
phase: 08-async-triage-platform-refactor
plan: "04"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - supabase/migrations/20260722120002_async_triage_audit.sql
  - supabase/tests/08_async_triage_contract.sql
  - src/server/triage/audit.ts
  - src/server/triage/audit.test.ts
  - src/server/triage/service.ts
autonomous: false
requirements: [TRIAGE-06]
user_setup:
  - service: supabase
    why: Push audit tables migration
    env_vars: [SUPABASE_DB_URL]
must_haves:
  truths:
    - D-24: Every provider call creates triage_attempts row with model, prompt version, raw output, latency, validation errors, disposition
    - D-24: triage_runs groups attempts per report triage cycle with complete lineage
  artifacts:
    - path: supabase/migrations/20260722120002_async_triage_audit.sql
      provides: triage_runs, triage_attempts tables, complete_triage_report RPC
    - path: src/server/triage/audit.ts
      provides: recordTriageRun, recordTriageAttempt writers
  key_links:
    - from: src/server/triage/service.ts
      to: src/server/triage/audit.ts
      via: record attempt after each provider call
    - from: src/server/triage/audit.ts
      to: complete_triage_report RPC
      via: atomic report update + audit insert
---

## Phase Goal

**As a** citizen, **I want to** submit a report and receive my reference ID and access token immediately, **so that** AI triage failure never blocks my intake or status access.

<objective>
Add durable audit tables and wire triage service to persist full lineage per D-24 for Phase 10 eval gate readiness.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md
@.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md
@.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md
@supabase/migrations/20260721130003_officer_operations.sql

<interfaces>
From 08-02 service.ts:
- runTriageForReport — wire audit calls at each attempt boundary

Audit row shape per D-24:
- triage_runs: run_id, report_id, started_at, finished_at, final_disposition, prompt_version
- triage_attempts: attempt_id, run_id, attempt_number, model, raw_output, latency_ms, validation_errors jsonb, disposition, created_at

From 08-02 config.ts:
- PROMPT_VERSION constant
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit tables migration and complete_triage_report RPC</name>
  <files>supabase/migrations/20260722120002_async_triage_audit.sql, supabase/tests/08_async_triage_contract.sql</files>
  <read_first>
    <file>supabase/migrations/20260721130003_officer_operations.sql</file>
    <file>supabase/migrations/20260722120001_async_triage_intake.sql</file>
    <file>supabase/tests/08_async_triage_contract.sql</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
  </read_first>
  <behavior>
    - triage_runs FK to reports(report_id) ON DELETE CASCADE
    - triage_attempts FK to triage_runs(run_id) ON DELETE CASCADE
    - RLS: service_role only — no anon/authenticated direct read
    - complete_triage_report RPC: in one transaction updates reports AI columns + triage_status + triaged_at, inserts/updates triage_runs and triage_attempts
    - Extend 08_async_triage_contract.sql: after mock triage complete, assert triage_attempts row count >= 1
  </behavior>
  <action>
    Create migration 20260722120002_async_triage_audit.sql with triage_runs and triage_attempts per D-24. Columns: model, prompt_version, raw_output (text/json), latency_ms, validation_errors (jsonb array), disposition per attempt. Implement complete_triage_report(p_report_id, p_analysis jsonb, p_disposition, p_attempt metadata) SECURITY DEFINER with grants matching intake RPC pattern. Append audit assertions to 08_async_triage_contract.sql — test helper inserts run+attempt via RPC or direct service_role insert then asserts FK integrity. Do not expose audit tables to citizen or officer dashboard UI (out of scope per UI-SPEC).
  </action>
  <acceptance_criteria>
    <criterion>Migration creates triage_runs and triage_attempts with FK to reports.</criterion>
    <criterion>complete_triage_report function exists with service_role grant only.</criterion>
    <criterion>SQL contract includes assertion that triage_attempts row links to valid run_id.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260722120002_async_triage_audit.sql; if($s -notmatch 'triage_runs' -or $s -notmatch 'triage_attempts' -or $s -notmatch 'complete_triage_report'){exit 1}"</automated>
  </verify>
  <done>Audit schema and atomic complete RPC defined.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push audit migration and re-run SQL contract</name>
  <files>supabase/migrations/20260722120002_async_triage_audit.sql, supabase/tests/08_async_triage_contract.sql</files>
  <read_first>
    <file>scripts/run-supabase-sql.mjs</file>
    <file>supabase/migrations/20260722120002_async_triage_audit.sql</file>
  </read_first>
  <action>
    [BLOCKING] Apply 20260722120002 migration via node scripts/run-supabase-sql.mjs or supabase db push. Re-run full contract: node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql including new audit assertions. Stop on failure before wiring application audit writer.
  </action>
  <acceptance_criteria>
    <criterion>Live DB has triage_runs and triage_attempts tables.</criterion>
    <criterion>08_async_triage_contract.sql exits 0 after audit section added.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql</automated>
  </verify>
  <done>Audit tables live on Supabase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Audit writer module and wire into triage service</name>
  <files>src/server/triage/audit.ts, src/server/triage/audit.test.ts, src/server/triage/service.ts</files>
  <read_first>
    <file>src/server/triage/service.ts</file>
    <file>src/server/triage/config.ts</file>
    <file>supabase/migrations/20260722120002_async_triage_audit.sql</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md</file>
  </read_first>
  <behavior>
    - startTriageRun(reportId) creates triage_runs row, returns runId
    - recordTriageAttempt stores each provider call with lineage + raw JSON + validation_errors + disposition
    - finishTriageRun sets final_disposition and finished_at
    - runTriageForReport uses complete_triage_report RPC on terminal completed path; audit rows for intermediate retries always written
    - Officer/citizen APIs never SELECT from audit tables
  </behavior>
  <action>
    Implement audit.ts with getAdminClient and RPC wrappers. Replace any direct report UPDATE in service.ts with complete_triage_report for completed disposition; use companion RPC or SQL for manual_review/failed terminal states that still write audit. Add audit.test.ts mocking Supabase client RPC/insert. Ensure raw_output stores provider JSON string redacted of API keys. Per D-24 persist validation retry output as separate attempt rows.
  </action>
  <acceptance_criteria>
    <criterion>Test: successful triage creates >=1 triage_attempts row via mocked client.</criterion>
    <criterion>Test: policy failure validation retry creates 2 attempt records before manual_review.</criterion>
    <criterion>service.ts imports audit module — no orphaned stub from 08-02.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/audit.test.ts src/server/triage/service.test.ts</automated>
  </verify>
  <done>TRIAGE-06 full audit trail persisted for every triage attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit tables → officer/citizen APIs | Audit data service-role only; no public exposure |
| raw_output storage | May contain citizen PII from report text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-8-13 | Information Disclosure | triage_attempts.raw_output | mitigate | RLS service_role only; no API routes read audit tables in Phase 8 |
| T-8-14 | Tampering | Audit row integrity | mitigate | FK constraints; complete_triage_report single transaction |
| T-8-15 | Repudiation | Missing attempt log | mitigate | recordTriageAttempt on every provider call including failures |
| T-8-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql
npm run test:unit -- src/server/triage/audit.test.ts src/server/triage/service.test.ts
</verification>

<success_criteria>
triage_runs and triage_attempts capture model, prompt version, raw output, latency, validation errors, and disposition for every attempt.
</success_criteria>

<output>
Create `.planning/phases/08-async-triage-platform-refactor/08-04-SUMMARY.md` when done
</output>
