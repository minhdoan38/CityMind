---
phase: 08-async-triage-platform-refactor
plan: "05"
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/server/services/citizen-status.ts
  - src/server/services/citizen-status.test.ts
  - src/server/repositories/reports.ts
  - src/server/repositories/reports.test.ts
  - src/server/officer/filters.ts
  - src/server/services/officer-dashboard.ts
  - src/app/[locale]/status/page.tsx
  - src/app/[locale]/report/success/page.tsx
  - src/components/reports/ReportsTable.tsx
  - src/components/reports/ReportsFilters.tsx
  - src/components/reports/types.ts
  - src/app/dashboard/reports/[reportId]/page.tsx
  - messages/en.json
  - messages/vi.json
autonomous: true
requirements: [TRIAGE-03, TRIAGE-04]
must_haves:
  truths:
    - D-13: Citizen status shows 4-step service progress labels (received → AI review pending → under officer review → resolved/rejected)
    - D-14: AI fields hidden while triage_status is pending or processing
    - D-15: Calm copy on manual_review/failed — no provider errors exposed
    - D-16: AI analysis fields revealed only when triage_status=completed
    - D-17: Officer table shows AI pending badge and elevated badge for manual_review/failed
    - D-18: Default sort elevates manual_review/failed first, then pending/processing, then completed
    - D-19: All triage statuses visible by default with optional triage_status filter chip
    - D-20: Detail page section order per architecture doc; NULL AI fields never backfilled
  artifacts:
    - path: src/app/[locale]/status/page.tsx
      provides: CitizenWorkflowStepper + conditional AI reveal
    - path: src/components/reports/ReportsTable.tsx
      provides: triage_status badge column
    - path: src/app/dashboard/reports/[reportId]/page.tsx
      provides: reordered detail sections with triage gating
  key_links:
    - from: src/server/services/citizen-status.ts
      to: projectCitizenTriageView
      via: strips summary until triage_status=completed
    - from: src/server/repositories/reports.ts
      to: listRecentReports
      via: triage_bucket default sort + triage_status filter
---

## Phase Goal

**As a** citizen, **I want to** submit a report and receive my reference ID and access token immediately, **so that** AI triage failure never blocks my intake or status access.

<objective>
Deliver citizen and officer UX contracts from 08-UI-SPEC: service-progress status, calm failure copy, triage badges, default elevated sort, filter chips, detail reorder.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-async-triage-platform-refactor/08-UI-SPEC.md
@.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md
@.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md
@src/app/[locale]/status/page.tsx
@src/components/reports/ReportsTable.tsx

<interfaces>
CitizenStatusResponse (extend in citizen-status.ts):
- report_id, received_at, triage_status, service_step, status, category?, severity?, priority?, summary?, recommendation?, history

ReportRow (extend in types.ts):
- triage_status: string

DashboardSearchParams:
- triage_status?: string (comma-separated)
- sort default triage_bucket
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Citizen status API projection and status page UX</name>
  <files>src/server/repositories/reports.ts, src/server/services/citizen-status.ts, src/server/services/citizen-status.test.ts, src/app/[locale]/status/page.tsx, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>src/server/services/citizen-status.ts</file>
    <file>src/server/repositories/reports.ts</file>
    <file>src/app/[locale]/status/page.tsx</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-UI-SPEC.md</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
  </read_first>
  <behavior>
    - getCitizenStatus selects triage_status, category, severity, priority, summary, recommendation, created_at
    - projectCitizenTriageView maps service_step per D-13: received, ai_review_pending, officer_review, resolved, rejected, automated_review_unavailable
    - pending/processing: summary, category, severity, priority, recommendation all null in API response (D-14)
    - manual_review/failed: calm notice fields only; never triage_error (D-15)
    - completed: reveal AI fields (D-16)
    - Status page: CitizenWorkflowStepper component (inline or extracted), calm Alert role=status, conditional AI block in soft accent well
    - EN/VI keys under public.statusWorkflow and public.triage per UI-SPEC copy table
  </behavior>
  <action>
    Extend repository getCitizenStatus select columns. Implement projectCitizenTriageView in citizen-status.ts preserving uniform 401 via citizenStatusUnauthorized. Update status page.tsx to render 4-step stepper from service_step; hide AI block unless triage_status=completed; show calm notice for automated_review_unavailable. Add message catalog keys (EN/VI). Extend citizen-status.test.ts: pending→summary null; manual_review→automated_review_unavailable step; completed→summary present. Never add triage_error to response type.
  </action>
  <acceptance_criteria>
    <criterion>Test: triage_status pending returns summary null and service_step ai_review_pending.</criterion>
    <criterion>Test: triage_status manual_review returns service_step automated_review_unavailable.</criterion>
    <criterion>Test: triage_status completed returns non-null summary when DB has summary.</criterion>
    <criterion>messages/en.json contains public.triage.calmNoticeBody exact D-15 English string.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/citizen-status.test.ts</automated>
  </verify>
  <done>TRIAGE-03 citizen status contract implemented server + UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Officer queue badges, triage sort, and filter chips</name>
  <files>src/server/repositories/reports.ts, src/server/repositories/reports.test.ts, src/server/officer/filters.ts, src/server/services/officer-dashboard.ts, src/components/reports/types.ts, src/components/reports/ReportsTable.tsx, src/components/reports/ReportsFilters.tsx, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>src/components/reports/ReportsTable.tsx</file>
    <file>src/components/reports/ReportsFilters.tsx</file>
    <file>src/server/officer/filters.ts</file>
    <file>src/server/repositories/reports.ts</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - Default sort triage_bucket: manual_review+failed first (oldest created_at), then pending+processing, then completed (D-18)
    - triage_status filter chip: All | AI pending (pending,processing) | Needs review (manual_review,failed) | AI complete (completed) (D-19)
    - ReportsTable: new AI triage column with TriageStatusBadge; category/priority/summary show em dash when not completed
    - Extend FILTER_PARAM_KEYS, parseReportFilters, hasActiveFilters for triage_status
    - All triage statuses visible by default — no tab hiding completed
  </behavior>
  <action>
    Extend listRecentReports (or RPC) with triage_status filter and bucket sort when sort absent or sort=triage_bucket. Add triage_status to ReportRow and officer-dashboard mapping. Implement triageVariant helper in ReportsTable per UI-SPEC amber elevated badge for manual_review/failed. Add TriageFilterChips row or extend ReportsFilters with chip UI updating URL triage_status param. Add dashboard.triage.* i18n keys. repositories.test.ts asserts bucket ordering: manual_review row before pending before completed. Per D-20 NULL cells use — not placeholder text.
  </action>
  <acceptance_criteria>
    <criterion>Test: list with mixed triage_status returns manual_review report before pending when sort=triage_bucket.</criterion>
    <criterion>ReportRow type includes triage_status field.</criterion>
    <criterion>ReportsTable renders badge column with dashboard.triage.badgePending label for pending rows.</criterion>
    <criterion>parseReportFilters parses triage_status=comma list from URL.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/repositories/reports.test.ts</automated>
  </verify>
  <done>TRIAGE-04 officer queue visibility and elevated sort complete.</done>
</task>

<task type="auto">
  <name>Task 3: Officer detail reorder, success page verify, confidence label</name>
  <files>src/app/dashboard/reports/[reportId]/page.tsx, src/app/[locale]/report/success/page.tsx, messages/en.json, messages/vi.json</files>
  <read_first>
    <file>src/app/dashboard/reports/[reportId]/page.tsx</file>
    <file>src/app/[locale]/report/success/page.tsx</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-UI-SPEC.md</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Detail section order D-20: citizen description → evidence image → triage badge → observed facts → unknowns → narrative → severity/priority → location → urban context → timeline → StatusActions
    - Pending triage: title "Report pending review" not category; meta grid hidden until completed
    - Confidence: label "Model confidence — uncalibrated" qualitative band only — remove Math.round(confidence*100)% (D-23)
    - Success page: verify flash schema { reportId, accessToken } only — no AI fields (D-08)
  </behavior>
  <action>
    Reorder [reportId]/page.tsx sections per UI-SPEC S5 table. Add TriageStatusBadge in detail with helper copy. Gate SignalList/bullet sections on triage_status===completed. Replace confidence percentage with uncalibrated label. Verify success page does not read analysis from flash or API — adjust only if regression found. Add any missing dashboard.triage detail keys to EN/VI.
  </action>
  <acceptance_criteria>
    <criterion>Detail page source shows citizen description block before AI summary block.</criterion>
    <criterion>No Math.round(confidence * 100) pattern in detail page after edit.</criterion>
    <criterion>Success page does not render category, severity, or summary elements.</criterion>
    <criterion>When triage_status pending, detail title uses dashboard.triage.detailPendingTitle not report.category.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$d=Get-Content -Raw src/app/dashboard/reports/[reportId]/page.tsx; if($d -match 'Math\.round\(.*confidence'){exit 1}; if($d -notmatch 'triage_status'){exit 1}"</automated>
  </verify>
  <done>Officer detail and citizen success surfaces match Phase 8 UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen status API → browser | Token-scoped; no triage_error or provider detail |
| Officer dashboard → browser | Shows triage_status; NULL AI until completed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-8-16 | Information Disclosure | Citizen status on failed triage | mitigate | Calm copy only; API omits triage_error and provider codes (D-15) |
| T-8-17 | Tampering | Officer sees fake AI defaults | mitigate | NULL/— display until completed; no placeholder severity (D-20) |
| T-8-18 | Information Disclosure | Confidence percentage | mitigate | Uncalibrated qualitative label only (D-23) |
| T-8-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
npm run test:unit -- src/server/services/citizen-status.test.ts src/server/repositories/reports.test.ts
npm run test
npm run lint
</verification>

<success_criteria>
Citizens see service-progress workflow without provider errors; officers see elevated manual_review/failed rows with triage badges and correct detail section order.
</success_criteria>

<output>
Create `.planning/phases/08-async-triage-platform-refactor/08-05-SUMMARY.md` when done
</output>
