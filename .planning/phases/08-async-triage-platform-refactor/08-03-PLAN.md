---
phase: 08-async-triage-platform-refactor
plan: "03"
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/server/triage/claim.ts
  - src/server/triage/claim.test.ts
  - src/server/triage/worker.ts
  - src/server/triage/worker.test.ts
  - scripts/triage-worker.mjs
  - scripts/load-project-env.mjs
  - scripts/register-citymind-task.ps1
  - package.json
  - package-lock.json
autonomous: false
requirements: [TRIAGE-02, TRIAGE-05]
user_setup:
  - service: postgres
    why: Worker polls via direct pg connection
    env_vars: [SUPABASE_DB_URL, THIRD_PARTY_API_KEY, AI_BASE_URL, AI_MODEL]
must_haves:
  truths:
    - D-01: Separate Node worker process registered via Windows Task Scheduler for production laptop ops
    - D-02: Worker polls Postgres and claims pending reports with SKIP LOCKED idempotency
    - D-03: Worker uses DB claim only — no loopback HTTP handler required
    - D-04: Local dev requires npm run dev + npm run triage:worker in second terminal
    - D-11: Stuck processing rows reclaim after 15 minutes
  artifacts:
    - path: scripts/triage-worker.mjs
      provides: production/dev worker entrypoint
    - path: src/server/triage/worker.ts
      provides: runWorkerLoop poll + tick
    - path: src/server/triage/claim.ts
      provides: claimNextTriageReport, reclaimStuckTriageReports
  key_links:
    - from: scripts/triage-worker.mjs
      to: src/server/triage/worker.ts
      via: import runWorkerLoop
    - from: src/server/triage/worker.ts
      to: claim_triage_report RPC
      via: pg Pool parameterized query
    - from: src/server/triage/worker.ts
      to: runTriageForReport
      via: import from service.ts after claim commit
---

## Phase Goal

**As a** citizen, **I want to** submit a report and receive my reference ID and access token immediately, **so that** AI triage failure never blocks my intake or status access.

<objective>
Deliver self-hosted background worker: pg poll loop, SKIP LOCKED claim, reconciliation reclaim, npm script, and Task Scheduler registration per D-01–D-04.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md
@.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md
@.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md
@scripts/register-citymind-task.ps1
@scripts/run-supabase-sql.mjs

<interfaces>
From 08-01 migration (must be live):
- public.claim_triage_report() RETURNS SETOF reports
- public.reclaim_stuck_triage_reports(stuck_interval interval)

From 08-02 (must be merged):
- runTriageForReport(reportId: string): Promise<void>

Package install per RESEARCH (legitimacy audit approved):
- pg@8.22.0, @types/pg (dev)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Approve pg package install</name>
  <read_first>
    <file>.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md</file>
  </read_first>
  <what-built>Package legitimacy audit marks pg [OK] on npm; required for FOR UPDATE SKIP LOCKED worker connection.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/pg — verify publisher node-postgres, ~6M weekly downloads.
    2. Visit https://github.com/brianc/node-postgres — confirm source repo matches.
    3. Reply "approved" to proceed with npm install pg @types/pg.
  </how-to-verify>
  <resume-signal>Type "approved" or describe concerns</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Install pg and implement claim/reclaim wrappers</name>
  <files>package.json, package-lock.json, src/server/triage/claim.ts, src/server/triage/claim.test.ts</files>
  <read_first>
    <file>supabase/migrations/20260722120001_async_triage_intake.sql</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md</file>
  </read_first>
  <behavior>
    - claimNextTriageReport(client) calls SELECT * FROM public.claim_triage_report() inside caller's transaction.
    - reclaimStuckTriageReports(client, '15 minutes') calls reclaim RPC before each claim tick.
    - Unit tests mock pg PoolClient query results for claim returning one row vs empty.
  </behavior>
  <action>
    After Task 0 approval: npm install pg && npm install -D @types/pg. Implement claim.ts with server-only import guard; parameterized queries only — no string interpolation of intervals in SQL text (use RPC default or pass as single param). Add claim.test.ts mocking PoolClient. Per D-11 use 15 minute reclaim default.
  </action>
  <acceptance_criteria>
    <criterion>package.json lists pg dependency.</criterion>
    <criterion>claim.test.ts passes with mocked claim returning report_id.</criterion>
    <criterion>claim.ts uses SELECT * FROM public.claim_triage_report() pattern.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/claim.test.ts</automated>
  </verify>
  <done>Claim layer test-covered and ready for worker loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Worker poll loop, entry script, and Task Scheduler registration</name>
  <files>src/server/triage/worker.ts, src/server/triage/worker.test.ts, scripts/triage-worker.mjs, scripts/load-project-env.mjs, scripts/register-citymind-task.ps1, package.json</files>
  <read_first>
    <file>scripts/run-supabase-sql.mjs</file>
    <file>scripts/smoke-ai.mjs</file>
    <file>src/server/triage/service.ts</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
  </read_first>
  <behavior>
    - tick(): BEGIN → reclaim → claim → COMMIT → if row, await runTriageForReport(report_id) outside claim transaction.
    - runWorkerLoop(): setInterval or sleep loop (~5s poll, planner discretion), graceful SIGINT shutdown.
    - triage-worker.mjs loads SUPABASE_DB_URL, THIRD_PARTY_API_KEY, AI_BASE_URL via load-project-env.mjs; fail fast if missing.
    - package.json script: "triage:worker": "node scripts/triage-worker.mjs"
    - Per D-04: no sync-in-request fallback in Next.js routes.
    - Per D-01: extend register-citymind-task.ps1 to register a CityMind-Triage scheduled task that runs npm run triage:worker at logon (mirror existing CityMind-Next task pattern).
  </behavior>
  <action>
    Implement worker.ts with pg Pool from SUPABASE_DB_URL. On runTriageForReport error, log server-side only — do not crash loop; ensure report row disposition handled inside service. Create triage-worker.mjs entry importing worker (use tsx or compile path consistent with project — if no tsx, use dynamic import of compiled module or .mts pattern from smoke scripts). Add triage:worker npm script. Document poll interval constant in worker.ts. Worker authenticates via DB only per D-03. Add worker.test.ts mocking pg PoolClient and runTriageForReport to assert tick order: reclaim → claim → runTriageForReport after COMMIT. Extend scripts/register-citymind-task.ps1 with a CityMind-Triage task action pointing at npm run triage:worker in the project root (same working-directory and env-loading pattern as the Next.js task).
  </action>
  <acceptance_criteria>
    <criterion>npm run triage:worker starts without error when SUPABASE_DB_URL is set.</criterion>
    <criterion>Worker tick calls reclaim before claim on each iteration.</criterion>
    <criterion>runTriageForReport invoked only after successful COMMIT of claim transaction.</criterion>
    <criterion>worker.test.ts passes with mocked reclaim/claim/runTriageForReport ordering.</criterion>
    <criterion>register-citymind-task.ps1 contains CityMind-Triage task registration (grep match).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/claim.test.ts src/server/triage/worker.test.ts &amp;&amp; powershell -Command "Select-String -Path scripts/register-citymind-task.ps1 -Pattern 'CityMind-Triage' -Quiet"</automated>
  </verify>
  <done>Runnable worker process and Task Scheduler registration exist for dev and production laptop ops.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Two-terminal triage smoke</name>
  <files>scripts/triage-worker.mjs</files>
  <read_first>
    <file>.planning/phases/08-async-triage-platform-refactor/08-VALIDATION.md</file>
    <file>scripts/register-citymind-task.ps1</file>
  </read_first>
  <what-built>Worker poll + claim + triage service wired; CityMind-Triage task registered in register-citymind-task.ps1.</what-built>
  <how-to-verify>
    1. Terminal A: npm run dev
    2. Terminal B: npm run triage:worker
    3. Submit report via /api/public/reports (or Report form)
    4. Confirm report triage_status transitions pending → processing → completed (or manual_review on policy/AI failure) within 2 minutes
    5. Run register-citymind-task.ps1 and verify CityMind-Triage task exists in Task Scheduler
  </how-to-verify>
  <resume-signal>Type "approved" after observing triage completion or describe stuck state</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker process → Postgres | Service-role DB URL; must never ship to client |
| Worker → AI provider | Same as 08-02; outbound only from worker host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-8-09 | Tampering | Double triage same report | mitigate | FOR UPDATE SKIP LOCKED claim RPC from 08-01 |
| T-8-10 | Information Disclosure | SUPABASE_DB_URL in client bundle | mitigate | Worker script server-side only; never import worker in client components |
| T-8-11 | Denial of Service | Runaway poll loop | mitigate | Single-row claim LIMIT 1; bounded concurrency |
| T-8-12 | Tampering | Stuck processing rows | mitigate | reclaim_stuck_triage_reports every tick (D-11) |
| T-8-SC | Tampering | pg npm install | mitigate | Human checkpoint Task 0 before install; slopcheck OK per RESEARCH |
</threat_model>

<verification>
Unit: npm run test:unit -- src/server/triage/claim.test.ts
SQL (from 08-01): node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql
Manual: two-terminal dev flow per Task 3 checkpoint
</verification>

<success_criteria>
Self-hosted worker claims pending reports, runs triage, and reclaims stuck processing rows; npm run triage:worker documented for D-04.
</success_criteria>

<output>
Create `.planning/phases/08-async-triage-platform-refactor/08-03-SUMMARY.md` when done
</output>
