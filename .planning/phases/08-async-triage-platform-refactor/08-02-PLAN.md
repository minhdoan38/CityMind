---
phase: 08-async-triage-platform-refactor
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/server/triage/config.ts
  - src/server/triage/disposition.ts
  - src/server/triage/service.ts
  - src/server/triage/service.test.ts
  - src/server/validation/analysis-policy.ts
  - src/server/validation/analysis-policy.test.ts
  - src/server/ai/openai-compatible.ts
  - src/server/ai/openai-compatible.test.ts
autonomous: true
requirements: [TRIAGE-07]
must_haves:
  truths:
    - D-21: Triage service validates AI output against full MVP policy set
    - D-22: Policy failure triggers one validation retry with errors in context, then manual_review disposition
    - D-09: Maximum 3 infrastructure triage attempts before terminal disposition
    - D-10: Terminal disposition defaults to manual_review after retries exhaust
    - D-12: Exponential backoff between infrastructure retries (30s → 2m → 10m)
    - D-23: Officer-facing confidence shows uncalibrated label only — no percentage
  artifacts:
    - path: src/server/triage/service.ts
      provides: runTriageForReport orchestration
      exports: [runTriageForReport]
    - path: src/server/validation/analysis-policy.ts
      provides: extended validateAnalysisPolicy with D-21 rules
    - path: src/server/triage/disposition.ts
      provides: resolveDisposition for retry vs manual_review vs failed
  key_links:
    - from: src/server/triage/service.ts
      to: src/server/ai/openai-compatible.ts
      via: analyzeStructured schema-only call
    - from: src/server/triage/service.ts
      to: validateAnalysisPolicy
      via: post-parse policy check before report update
---

## Phase Goal

**As a** citizen, **I want to** submit a report and receive my reference ID and access token immediately, **so that** AI triage failure never blocks my intake or status access.

<objective>
Implement the triage execution module: provider structured call, schema validation, semantic policy validation with validation-retry, and disposition logic. Worker (08-03) and audit writer (08-04) consume this module.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md
@.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md
@.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md
@prompt/citymind_ai_triage_structured_output_evaluator.json

<interfaces>
From src/server/domain/report-analysis.ts:
- ReportAnalysisSchema, ReportAnalysis type (11 fields — keep unchanged per RESEARCH A1)

From src/server/validation/analysis-policy.ts:
- validateAnalysisPolicy(analysis): PolicyResult
- PolicyViolation: { code, message }

From src/server/ai/openai-compatible.ts:
- createOpenAiCompatibleProvider — currently throws policy_invalid; triage path needs schema-only export

From src/server/ai/provider.ts:
- AnalysisProvider.analyze({ description, image? })

Disposition contract (create in disposition.ts):
- resolveDisposition({ infraAttempt, maxInfraAttempts, policyViolations, hadValidationRetry, unrecoverableError }): "completed" | "manual_review" | "failed" | "retry"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend semantic policy validation for MVP rules (D-21)</name>
  <files>src/server/validation/analysis-policy.ts, src/server/validation/analysis-policy.test.ts</files>
  <read_first>
    <file>src/server/validation/analysis-policy.ts</file>
    <file>src/server/validation/analysis-policy.test.ts</file>
    <file>prompt/citymind_ai_triage_structured_output_evaluator.json</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
  </read_first>
  <behavior>
    - critical priority requires severity 5 alignment.
    - severity 5 requires immediate-danger evidence in evidence[] text.
    - conflicting signals cap confidence at 0.64.
    - unsupported claims (evidence items not grounded in description) → violation leading to manual_review in service layer.
    - Existing Phase 7 checks preserved (empty evidence items, priority/severity floor, autonomous language).
  </behavior>
  <action>
    Extend validateAnalysisPolicy per D-21 using ReportAnalysis fields (evidence, uncertainty, confidence, priority, severity) — map evaluator cross_field_rules to deterministic helpers hasImmediateDangerEvidence and hasConflictingSignals. Add focused unit tests for each new violation code. Keep pure function — no DB or provider imports. Export violation codes as stable strings for triage_attempts validation_errors column in 08-04.
  </action>
  <acceptance_criteria>
    <criterion>Test: priority critical + severity 4 returns ok:false with critical_requires_severity_5.</criterion>
    <criterion>Test: severity 5 without danger evidence returns ok:false.</criterion>
    <criterion>Test: conflicting signals with confidence 0.9 returns ok:false with conflict_confidence_cap.</criterion>
    <criterion>All existing analysis-policy.test.ts cases still pass.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/validation/analysis-policy.test.ts</automated>
  </verify>
  <done>D-21 policy rules are test-covered and ready for triage service consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split provider schema-only path from policy enforcement</name>
  <files>src/server/ai/openai-compatible.ts, src/server/ai/openai-compatible.test.ts, src/server/triage/config.ts</files>
  <read_first>
    <file>src/server/ai/openai-compatible.ts</file>
    <file>src/server/ai/openai-compatible.test.ts</file>
    <file>src/server/config/env.ts</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md</file>
  </read_first>
  <behavior>
    - analyzeStructured (or equivalent) returns { analysis, lineage } after JSON + ReportAnalysisSchema parse only.
    - policy_invalid throw removed from triage code path; sync analyzeReport legacy path may still validate in provider until fully removed.
    - config.ts exports PROMPT_VERSION constant (e.g. "phase8-mvp-v1") and MAX_INFRA_ATTEMPTS=3 per D-09.
    - Lineage includes providerLabel, responseModel, requestId, latencyMs.
  </behavior>
  <action>
    Refactor openai-compatible.ts: extract parseAndValidateSchema helper used by both sync analyze (if retained) and new analyzeStructured export. analyzeStructured must NOT call validateAnalysisPolicy. Add triage/config.ts with prompt/config version constants for audit lineage (D-24 prep). Update openai-compatible tests to cover schema failure vs policy separation. Per RESEARCH Pitfall 2: triage service owns policy disposition.
  </action>
  <acceptance_criteria>
    <criterion>analyzeStructured returns parsed ReportAnalysis without throwing on policy violations.</criterion>
    <criterion>Invalid JSON or schema mismatch still throws typed error for infra retry handling.</criterion>
    <criterion>config.ts exports PROMPT_VERSION and MAX_INFRA_ATTEMPTS=3.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/ai/openai-compatible.test.ts</automated>
  </verify>
  <done>Provider adapter supports triage-owned policy flow with lineage metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement runTriageForReport with validation retry and disposition</name>
  <files>src/server/triage/disposition.ts, src/server/triage/service.ts, src/server/triage/service.test.ts</files>
  <read_first>
    <file>src/server/services/report-service.ts</file>
    <file>src/server/repositories/reports.ts</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
  </read_first>
  <behavior>
    - runTriageForReport(reportId) loads report + evidence from DB, calls analyzeStructured, runs validateAnalysisPolicy.
    - On first policy failure: one validation retry with violations appended to prompt context (D-22); does not count as separate infra attempt bucket (RESEARCH A2).
    - After validation retry still failing → disposition manual_review.
    - Infra failures (timeout, HTTP, schema parse): increment triage_attempt_count, schedule triage_next_attempt_at with backoff 30s→2m→10m (D-12), requeue pending until 3 attempts then manual_review (D-09/D-10).
    - Unrecoverable data errors → failed (D-10 reserved for system errors only).
    - On completed: update reports AI columns + triage_status=completed + triaged_at; call audit hook stub recordTriageAttempt if audit module not yet wired (08-04 completes).
    - Never update citizen-visible triage_error field on public API surfaces.
  </behavior>
  <action>
    Create disposition.ts with resolveDisposition per 08-PATTERNS. Implement service.ts runTriageForReport using getAdminClient, evidence download from evidence_path, analyzeStructured, validateAnalysisPolicy, disposition logic. Export types for worker import. Use repository helpers or direct RPC for complete_triage_report when available (08-04); until then use minimal update path with TODO-free implementation via Supabase update matching existing report column names. Add service.test.ts with mocked provider: policy fail→retry→manual_review, infra fail→retry scheduling, success→completed. Do NOT implement poll loop here (08-03).
  </action>
  <acceptance_criteria>
    <criterion>Test: policy violation on first call + second call success → triage_status completed.</criterion>
    <criterion>Test: policy violation on both validation attempts → manual_review disposition.</criterion>
    <criterion>Test: third infra failure → manual_review not failed (D-10).</criterion>
    <criterion>Test: missing report row → failed or early exit without throwing unhandled.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/service.test.ts</automated>
  </verify>
  <done>TRIAGE-07 triage module ready for worker and audit integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → AI provider | Report text/image leaves trust zone; advisory output only |
| Triage service → reports table | Service-role writes; citizens cannot invoke runTriageForReport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-8-05 | Spoofing | Citizen description in AI prompt | mitigate | Existing system instruction; policy checks; no widened trust on validation retry |
| T-8-06 | Tampering | AI output → report columns | mitigate | ReportAnalysisSchema + validateAnalysisPolicy before persist |
| T-8-07 | Information Disclosure | Policy violation messages | mitigate | Store in triage_attempts only; never expose on citizen status API |
| T-8-08 | Elevation | Fabricated AI defaults | mitigate | NULL AI fields until completed disposition (D-20) |
| T-8-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
npm run test:unit -- src/server/validation/analysis-policy.test.ts src/server/ai/openai-compatible.test.ts src/server/triage/service.test.ts
</verification>

<success_criteria>
Semantic validation routes invalid AI output to manual_review after one validation retry; provider path no longer blocks policy retry flow.
</success_criteria>

<output>
Create `.planning/phases/08-async-triage-platform-refactor/08-02-SUMMARY.md` when done
</output>
