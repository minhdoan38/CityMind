---
phase: 08-async-triage-platform-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260722120001_async_triage_intake.sql
  - supabase/tests/08_async_triage_contract.sql
  - src/server/services/report-service.ts
  - src/server/services/report-service.test.ts
  - src/server/repositories/reports.ts
  - src/app/api/public/reports/route.ts
  - src/app/api/v1/reports/route.ts
  - src/app/api/public/reports/analyze/route.ts
  - src/app/api/v1/reports/analyze/route.ts
  - src/components/ReportForm.tsx
  - tests/report-form.test.mjs
  - tests/contracts/golden-contracts.test.ts
autonomous: false
requirements: [TRIAGE-01, TRIAGE-02]
user_setup:
  - service: supabase
    why: Push async triage intake schema and run SQL contract gate
    env_vars: [SUPABASE_DB_URL, SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD]
must_haves:
  truths:
    - D-05: Citizen POST /api/public/reports persists report and returns report_id + access_token before any AI call
    - D-06: ReportSubmissionResponse contains only report_id, access_token, intake_status, triage_status
    - D-07: Legacy POST /analyze returns 410 Gone with documented migration message
    - D-08: ReportForm success flash stores reportId + accessToken only (no AI fields)
    - D-02: New reports start with triage_status=pending, claim RPC uses SKIP LOCKED pattern
  artifacts:
    - path: supabase/migrations/20260722120001_async_triage_intake.sql
      provides: triage lifecycle columns, intake RPC, claim/reclaim RPCs
    - path: src/app/api/public/reports/route.ts
      provides: POST intake handler
      exports: [POST]
    - path: src/server/services/report-service.ts
      provides: submitReport without provider.analyze
      contains: submitReport
  key_links:
    - from: src/components/ReportForm.tsx
      to: /api/public/reports
      via: fetch POST multipart FormData
      pattern: fetch.*\/api\/public\/reports
    - from: src/server/services/report-service.ts
      to: create_intake_report_with_access_token
      via: Supabase RPC
      pattern: create_intake_report_with_access_token
---

## Phase Goal

**As a** citizen, **I want to** submit a report and receive my reference ID and access token immediately, **so that** AI triage failure never blocks my intake or status access.

<objective>
Deliver Wave 1 vertical slice: schema + intake API + 410 on legacy /analyze. Citizen can submit and get token without waiting for AI; reports enter pending triage queue.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md
@.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md
@.planning/phases/08-async-triage-platform-refactor/08-PATTERNS.md
@.planning/phases/08-async-triage-platform-refactor/08-UI-SPEC.md
@supabase/migrations/20260721130004_evidence_path_additive.sql
@supabase/tests/07_next_backend_contract.sql

<interfaces>
From src/server/services/report-service.ts:
- ReportServiceDeps: { client, provider, evidenceBucket?, maxEvidenceBytes? }
- analyzeReport(formData, deps) — sync path to split; reuse validation lines 84-117
- handleAnalyzeReportRequest — pattern for handleSubmitReportRequest

From src/server/repositories/reports.ts:
- createReportWithAccessToken(client, params) — calls create_report_with_access_token RPC
- CreateReportWithTokenParams requires analysis: ReportAnalysis — intake RPC must accept NULL AI fields

New types (implement in report-service.ts):
- ReportSubmissionResponse: { report_id, access_token, intake_status: "received", triage_status: "pending" }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add intake schema, claim RPCs, and SQL contract scaffold</name>
  <files>supabase/migrations/20260722120001_async_triage_intake.sql, supabase/tests/08_async_triage_contract.sql</files>
  <read_first>
    <file>supabase/migrations/20260721130004_evidence_path_additive.sql</file>
    <file>supabase/tests/07_next_backend_contract.sql</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-CONTEXT.md</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Migration adds triage_status (pending|processing|completed|failed|manual_review), triage_error, triaged_at, triage_next_attempt_at, triage_attempt_count, triage_claimed_at on reports with CHECK constraint and partial index on pending rows.
    - create_intake_report_with_access_token RPC atomically inserts report (NULL AI columns, triage_status=pending) + access_tokens hash row per D-05/D-06.
    - claim_triage_report() uses CTE + FOR UPDATE SKIP LOCKED pattern from RESEARCH; reclaim_stuck_triage_reports(interval) resets processing rows older than 15 minutes per D-11.
    - All RPCs: SECURITY DEFINER, search_path=public, REVOKE PUBLIC, GRANT service_role only.
    - SQL contract tests: seed pending report, claim transitions to processing, reclaim resets stale processing, duplicate claim is idempotent-safe.
  </behavior>
  <action>
    Create immutable migration 20260722120001_async_triage_intake.sql per D-02/D-05/D-06/D-11. Do NOT add triage_runs/triage_attempts here (Plan 08-04). Add create_intake_report_with_access_token mirroring create_report_with_access_token but with NULL category/severity/summary/etc. and triage_status default pending. Implement claim_triage_report and reclaim_stuck_triage_reports as documented in 08-RESEARCH Pattern 1 and Pattern 4. Create supabase/tests/08_async_triage_contract.sql with _test_assert helper matching 07_next_backend_contract.sql style; cover intake RPC NULL AI columns, claim single-winner, reclaim after fake stale triage_claimed_at. Do not edit applied migrations.
  </action>
  <acceptance_criteria>
    <criterion>Migration file contains triage_status CHECK, claim_triage_report CTE pattern, and create_intake_report_with_access_token.</criterion>
    <criterion>08_async_triage_contract.sql asserts pending→processing claim and 15-minute reclaim per D-11.</criterion>
    <criterion>No triage_runs or triage_attempts tables in this migration.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$s=Get-Content -Raw supabase/migrations/20260722120001_async_triage_intake.sql; if($s -notmatch 'triage_status' -or $s -notmatch 'claim_triage_report' -or $s -notmatch 'create_intake_report_with_access_token'){exit 1}; if($s -match 'triage_runs'){exit 1}"</automated>
  </verify>
  <done>Schema and SQL contract files exist and are review-ready before push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema and run async triage SQL contract</name>
  <files>supabase/migrations/20260722120001_async_triage_intake.sql, supabase/tests/08_async_triage_contract.sql</files>
  <read_first>
    <file>scripts/run-supabase-sql.mjs</file>
    <file>supabase/migrations/20260722120001_async_triage_intake.sql</file>
    <file>supabase/tests/08_async_triage_contract.sql</file>
  </read_first>
  <action>
    [BLOCKING] After Task 1 files are committed locally, apply schema to live Supabase Postgres. Prefer: node scripts/run-supabase-sql.mjs supabase/migrations/20260722120001_async_triage_intake.sql OR supabase db push when SUPABASE_DB_URL is set. Then run contract gate: node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql. Stop on any migration or assertion failure. No application code in Task 3 may be verified against live DB until this passes.
  </action>
  <acceptance_criteria>
    <criterion>Live database has triage_status column and claim_triage_report RPC callable by service_role.</criterion>
    <criterion>08_async_triage_contract.sql exits 0 with no ASSERTION FAILED.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql</automated>
  </verify>
  <done>Intake and claim schema is live; downstream app tasks unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Intake API, 410 analyze removal, and ReportForm retarget</name>
  <files>src/server/services/report-service.ts, src/server/services/report-service.test.ts, src/server/repositories/reports.ts, src/app/api/public/reports/route.ts, src/app/api/v1/reports/route.ts, src/app/api/public/reports/analyze/route.ts, src/app/api/v1/reports/analyze/route.ts, src/components/ReportForm.tsx, tests/report-form.test.mjs, tests/contracts/golden-contracts.test.ts</files>
  <read_first>
    <file>src/server/services/report-service.ts</file>
    <file>src/server/repositories/reports.ts</file>
    <file>src/app/api/public/reports/analyze/route.ts</file>
    <file>src/components/ReportForm.tsx</file>
    <file>.planning/phases/08-async-triage-platform-refactor/08-UI-SPEC.md</file>
    <file>supabase/migrations/20260722120001_async_triage_intake.sql</file>
  </read_first>
  <behavior>
    - submitReport reuses description/coords/image validation from analyzeReport but never calls deps.provider.analyze.
    - Returns ReportSubmissionResponse only: report_id, access_token, intake_status received, triage_status pending per D-06.
    - POST /api/public/reports and POST /api/v1/reports delegate to handleSubmitReportRequest (rate limit + FormData + json).
    - POST /api/public/reports/analyze and v1 mirror return 410 Gone via HttpError per D-07.
    - ReportForm posts to /api/public/reports; sessionStorage flash keeps reportId + accessToken only per D-08.
    - submitReport test asserts provider.analyze never called.
  </behavior>
  <action>
    Add createIntakeReportWithAccessToken in repositories/reports.ts calling create_intake_report_with_access_token RPC. Implement submitReport and handleSubmitReportRequest in report-service.ts; keep analyzeReport exported only if tests still need it — do not wire analyze routes to it. Create thin route.ts files for POST intake. Replace analyze route handlers with 410 jsonErrorResponse per 08-PATTERNS. Update ReportForm fetch URL and submitting copy key (public.submitting per UI-SPEC). Add report-service tests for submitReport; update report-form.test.mjs to match /api/public/reports not /analyze; add golden fixture for intake response and analyze 410. Preserve evidence compensation delete on persistence failure. Per D-04: no sync-in-request triage shortcut.
  </action>
  <acceptance_criteria>
    <criterion>submitReport returns four-field ReportSubmissionResponse without analysis object.</criterion>
    <criterion>POST /api/public/reports/analyze returns HTTP 410 with migration detail string.</criterion>
    <criterion>ReportForm source contains /api/public/reports and does not POST to /analyze.</criterion>
    <criterion>Unit test mocks provider and verifies analyze was not invoked.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/services/report-service.test.ts &amp;&amp; npm run test:legacy -- tests/report-form.test.mjs</automated>
  </verify>
  <done>Citizen intake vertical slice complete: submit → token → pending triage row; legacy analyze gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citizen → POST /reports | Untrusted multipart; rate-limited public intake |
| Citizen → POST /analyze | Deprecated; 410 only — no body processing |
| Service role → intake RPC | Privileged persist; no plaintext token stored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T-8-01 | Tampering | POST /reports FormData | mitigate | Reuse magic-byte evidence validation; Zod/coordinate guards; 422 on malformed body |
| T-8-02 | Information Disclosure | 410 analyze response | mitigate | Generic migration message only; no stack traces |
| T-8-03 | Denial of Service | Public intake | mitigate | enforceReportRateLimit unchanged from sync path |
| T-8-04 | Tampering | claim_triage_report RPC | mitigate | FOR UPDATE SKIP LOCKED CTE; service_role grant only |
| T-8-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
After Task 2 push: node scripts/run-supabase-sql.mjs supabase/tests/08_async_triage_contract.sql
Per plan: npm run test:unit -- src/server/services/report-service.test.ts && npm run test:legacy -- tests/report-form.test.mjs
Wave 1 gate: npm run test
</verification>

<success_criteria>
1. POST /api/public/reports returns report_id + access_token with triage_status pending before any AI work
2. Legacy /analyze returns 410 on public and v1 routes
3. SQL contract passes on live Supabase after blocking push
</success_criteria>

<output>
Create `.planning/phases/08-async-triage-platform-refactor/08-01-SUMMARY.md` when done
</output>
