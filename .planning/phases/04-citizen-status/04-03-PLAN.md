---
phase: 04-citizen-status
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - frontend/src/components/CopyStatusLink.tsx
  - frontend/src/app/dashboard/reports/[reportId]/page.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/citizen-status.test.mjs
autonomous: true
requirements:
  - DASH-08
must_haves:
  truths:
    - "Officer detail header exposes Copy status link that writes an absolute /{locale}/status?reportId= URL with no token query param (DASH-08 / D-13 / D-14a)."
    - "Muted recovery hint explains the full tokenized link was shown once at submission and cannot be recovered from the dashboard (D-14b / D-14c)."
    - "Clipboard success uses Check icon swap + aria-live Link copied EN/VI (D-15)."
  artifacts:
    - path: "frontend/src/components/CopyStatusLink.tsx"
      provides: "ReportId-only clipboard control + recovery hint + live region"
    - path: "frontend/src/app/dashboard/reports/[reportId]/page.tsx"
      provides: "Header/meta placement for copy control"
  key_links:
    - from: "frontend/src/components/CopyStatusLink.tsx"
      to: "clipboard"
      via: "origin/{locale}/status?reportId= without token="
      pattern: "reportId=|statusLinkRecoveryHint|token"
    - from: "frontend/src/app/dashboard/reports/[reportId]/page.tsx"
      to: "frontend/src/components/CopyStatusLink.tsx"
      via: "header/meta near report id / status badge"
      pattern: "CopyStatusLink"
---

## Phase Goal

**As a** citizen, **I want to** look up my report with a report ID and access token, **so that** I can see current status and history without creating an account.

<objective>
Deliver Track C officer vertical slice: Copy status link on report detail (reportId-only absolute URL), recovery hint, and bilingual clipboard feedback (DASH-08). Realigns ROADMAP’s old “audit log display” wording — history UI already exists from Phase 3; this plan implements DASH-08 only.

Purpose: Let officers share a deep link that prefills report ID on `/status` while honestly disclosing tokens are unrecoverable (D-14).
Output: CopyStatusLink component, detail header wiring, dashboard catalog keys, smoke asserts.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-citizen-status/04-CONTEXT.md
@.planning/phases/04-citizen-status/04-RESEARCH.md
@.planning/phases/04-citizen-status/04-PATTERNS.md
@.planning/phases/04-citizen-status/04-UI-SPEC.md
@.planning/phases/04-citizen-status/04-VALIDATION.md
@.planning/phases/04-citizen-status/04-02-SUMMARY.md
@frontend/src/app/dashboard/reports/[reportId]/page.tsx
@frontend/src/app/[locale]/report/success/page.tsx
@frontend/messages/en.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing smoke for Copy status link reportId-only URL and catalog keys</name>
  <files>frontend/tests/citizen-status.test.mjs</files>
  <behavior>
    - Asserts CopyStatusLink component (or detail page) builds status URL with reportId and without token= (DASH-08 / D-14a).
    - Asserts dashboard.copyStatusLink, statusLinkCopied, statusLinkRecoveryHint keys exist in en.json and vi.json with matching trees (D-15 / UI-SPEC).
  </behavior>
  <action>Extend frontend/tests/citizen-status.test.mjs with asserts for CopyStatusLink source (or detail page import) containing reportId-only URL construction and explicitly asserting the clipboard URL builder does not append a token query param. Assert dashboard catalog keys from UI-SPEC. Tests red until Task 2. Cite D-13, D-14a, D-15. Do not implement token re-issue (D-14c deferred).</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/citizen-status.test.mjs 2&gt;&amp;1 | tee /tmp/cit-dash-red.txt; grep -E "not ok|fail|copyStatus|token" /tmp/cit-dash-red.txt</automated>
  </verify>
  <done>Smoke coverage for DASH-08 exists and fails before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CopyStatusLink in detail header + recovery hint + EN/VI feedback</name>
  <files>frontend/src/components/CopyStatusLink.tsx, frontend/src/app/dashboard/reports/[reportId]/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/citizen-status.test.mjs</files>
  <behavior>
    - Control placed in detail header/meta near report id / status — not only beside resolve actions (D-13).
    - Clipboard value: `${origin}/${locale}/status?reportId=${encodeURIComponent(reportId)}` with locale default en while dashboard is unlocalized (D-14a / RESEARCH A5); absolute URL preferred (UI-SPEC).
    - Always-visible muted recovery hint per UI-SPEC statusLinkRecoveryHint (D-14b); no re-issue UI (D-14c).
    - Copy feedback: lucide Copy→Check ~2s + role=status aria-live polite with statusLinkCopied (D-15); min 44×44 hit target.
  </behavior>
  <action>Create CopyStatusLink client component mirroring success-page clipboard + live-region pattern (04-PATTERNS / 04-UI-SPEC). Wire into dashboard reports/[reportId]/page.tsx header/meta area (D-13). Add dashboard.copyStatusLink, statusLinkCopied, statusLinkRecoveryHint to en.json and vi.json with identical keys; merge carefully with Track B public keys already present. Never reconstruct or request plaintext tokens. Make Task 1 asserts green. Cite D-13, D-14a, D-14b, D-14c, D-15.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/citizen-status.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <human-check>
    Open an officer detail page → Copy status link → paste shows /en/status?reportId=… with no token → confirm recovery hint visible and toast/live region announces Link copied.
  </human-check>
  <done>DASH-08 delivered with honest reportId-only share link and recovery hint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Officer dashboard → clipboard | Authenticated officer; must not invent tokens or expose hash material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Information disclosure | Copy status link | medium | mitigate | Copy reportId-only URL; never attach token (D-14a); hint documents unrecoverability (D-14b) |
| T-04-11 | Elevation of privilege | Token re-issue temptation | low | mitigate | Explicitly omit re-issue/rotation UI (D-14c deferred) |
| T-04-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
- node --test citizen-status.test.mjs asserts reportId-only URL + catalog keys
- Manual clipboard paste check on detail page
</verification>

<success_criteria>
- DASH-08 complete per D-13..D-15
- Officers cannot and do not get fake full tokenized links from the dashboard
</success_criteria>

<output>
Create `.planning/phases/04-citizen-status/04-03-SUMMARY.md` when done
</output>
