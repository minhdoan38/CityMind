---
phase: 04-citizen-status
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/src/app/api/public/reports/status/route.ts
  - frontend/src/app/[locale]/status/page.tsx
  - frontend/src/app/status/page.tsx
  - frontend/src/app/[locale]/report/success/page.tsx
  - frontend/messages/en.json
  - frontend/messages/vi.json
  - frontend/tests/citizen-status.test.mjs
autonomous: true
requirements:
  - CIT-01
  - CIT-02
  - CIT-03
  - CIT-04
must_haves:
  truths:
    - "Citizen can open /en/status or /vi/status, enter report ID + token (or land on a deep link with reportId+token), and see current status, summary, and newest-first history (CIT-01 / CIT-02 / D-01..D-08)."
    - "401 maps to one generic verify-failed catalog string; 429 maps to rate-limited copy; no existence-leak variants (CIT-03 / CIT-04 / D-16 / D-17)."
    - "Success-page status prep URL is locale-prefixed /{locale}/status?reportId=&token= and unprefixed /status redirects into the default locale while preserving query params (D-03 / D-09)."
  artifacts:
    - path: "frontend/src/app/[locale]/status/page.tsx"
      provides: "Civic status form + auto-fetch + result panel"
    - path: "frontend/src/app/api/public/reports/status/route.ts"
      provides: "Public BFF POST proxy with XFF forward"
    - path: "frontend/tests/citizen-status.test.mjs"
      provides: "Route/catalog/locale smoke coverage"
  key_links:
    - from: "frontend/src/app/[locale]/status/page.tsx"
      to: "frontend/src/app/api/public/reports/status/route.ts"
      via: "client POST JSON { report_id, token }"
      pattern: "/api/public/reports/status"
    - from: "frontend/src/app/api/public/reports/status/route.ts"
      to: "backendEndpoint(/api/v1/reports/status)"
      via: "X-Forwarded-For / X-Real-Ip forward + JSON body passthrough"
      pattern: "X-Forwarded-For|/api/v1/reports/status"
---

## Phase Goal

**As a** citizen, **I want to** look up my report with a report ID and access token, **so that** I can see current status and history without creating an account.

<objective>
Deliver Track B frontend vertical slice: Next public BFF, bilingual `/[locale]/status` page (form + one-shot auto-fetch + result states), unprefixed redirect, and success-page locale URL fix (CIT-01 plus client UX for CIT-02/03/04).

Purpose: Close the citizen loop started on the Phase 2 success page so shareable deep links work end-to-end against Track A.
Output: Status BFF, locale status page, message catalog keys, success prep URL fix, frontend smoke tests.
</objective>

<execution_context>
@/Users/minhmice/.codex/gsd-core/workflows/execute-plan.md
@/Users/minhmice/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-citizen-status/04-CONTEXT.md
@.planning/phases/04-citizen-status/04-RESEARCH.md
@.planning/phases/04-citizen-status/04-PATTERNS.md
@.planning/phases/04-citizen-status/04-UI-SPEC.md
@.planning/phases/04-citizen-status/04-VALIDATION.md
@.planning/phases/04-citizen-status/04-01-SUMMARY.md
@frontend/src/app/api/public/reports/analyze/route.ts
@frontend/src/app/[locale]/report/success/page.tsx
@frontend/src/app/[locale]/report/page.tsx
@frontend/src/components/ReportForm.tsx
@frontend/tests/public-shell.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing frontend smoke for status route, BFF, and catalog keys</name>
  <files>frontend/tests/citizen-status.test.mjs</files>
  <behavior>
    - Asserts existence of [locale]/status/page.tsx, status/page.tsx redirect to /en/status, and public BFF route that forwards X-Forwarded-For (CIT-01 / D-03 / D-17).
    - Asserts public.status* catalog keys in en.json and vi.json with identical key trees for new keys (UI-SPEC).
    - Asserts success page builds locale-prefixed status prep (no bare /status? path) (D-03 / D-09).
  </behavior>
  <action>Create frontend/tests/citizen-status.test.mjs mirroring public-shell.test.mjs filesystem and catalog assertions. Cover: locale status page path, unprefixed redirect target, BFF file + XFF forward string, required public.statusHeading / statusVerifyFailed / statusRateLimited / statusNetworkError / statusHistoryEmpty keys (and siblings from 04-UI-SPEC), and success prep locale prefix. Tests must fail until Task 2/3 land. Do not implement UI yet. Cite D-01, D-03, D-09.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/citizen-status.test.mjs 2&gt;&amp;1 | tee /tmp/cit-fe-red.txt; grep -E "not ok|fail" /tmp/cit-fe-red.txt</automated>
  </verify>
  <done>citizen-status.test.mjs exists and is red before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public BFF + bilingual status page form, auto-fetch, and result panel</name>
  <files>frontend/src/app/api/public/reports/status/route.ts, frontend/src/app/[locale]/status/page.tsx, frontend/messages/en.json, frontend/messages/vi.json</files>
  <behavior>
    - BFF POST parses JSON { report_id, token }, forwards XFF/X-Real-Ip like analyze, proxies to FastAPI /api/v1/reports/status, preserves 401/429/Retry-After (D-17 / D-18).
    - /[locale]/status uses public civic chrome (header/footer, max-w-xl), form fields, Check status CTA; query params reportId+token prefill and one-shot auto-fetch on mount (D-01 / D-02 / D-03 / D-04).
    - Success renders only status badge/label, summary, newest-first history with empty-history copy; loading/error/idle empty states per UI-SPEC (D-05 / D-07 / D-08).
    - Client maps 401→statusVerifyFailed only, 429→statusRateLimited, network/5xx→statusNetworkError (D-16 / D-17); privacy note under form (D-09).
  </behavior>
  <action>Implement status BFF by copying analyze XFF forward pattern with JSON Content-Type (04-PATTERNS). Build client status page (or thin RSC shell + client form) per 04-UI-SPEC: Clinic Blue civic chrome, no marketing hero, no AI labels. Use useSearchParams for reportId/token; one-shot useEffect auto-fetch when both trimmed non-empty; keep fields editable after success (D-01 / D-02). Prefer client fetch so token stays out of RSC data fetches (RESEARCH A1). Add all public.status* EN/VI strings from UI-SPEC Copywriting Contract; keep key trees identical. Map HTTP codes to catalog only — ignore distinct API detail text for 401. Do not add officer dashboard work. Cite D-01..D-09, D-16, D-17.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/citizen-status.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <human-check>
    With backend running and a known report_id+token from success flash: open /en/status?reportId=…&amp;token=… and confirm auto-fetch shows status/history; wrong token shows one generic error; switch to /vi/status and confirm VI copy.
  </human-check>
  <done>Deep link and manual form lookup work via BFF against Track A API with bilingual civic UI.</done>
</task>

<task type="auto">
  <name>Task 3: Unprefixed /status redirect + success locale URL fix</name>
  <files>frontend/src/app/status/page.tsx, frontend/src/app/[locale]/report/success/page.tsx, frontend/messages/en.json, frontend/messages/vi.json, frontend/tests/citizen-status.test.mjs</files>
  <behavior>
    - Unprefixed /status redirects to /en/status preserving reportId and token query params when present (D-03).
    - Success statusPrepValue uses /{locale}/status?reportId=&amp;token= via useLocale (or route locale); statusLinkPrep copy updated to live wording (D-03 / D-09 / UI-SPEC).
  </behavior>
  <action>Add frontend/src/app/status/page.tsx redirect patterned on report/page.tsx but append searchParams so deep links survive locale bounce (D-03). Fix success/page.tsx statusPrepValue to include locale prefix — remove bare /status? construction (Pitfall 3). Update public.statusLinkPrep EN/VI from coming-soon to UI-SPEC live strings. Extend smoke asserts if needed. No token re-issue. Cite D-03, D-09.</action>
  <verify>
    <automated>cd frontend &amp;&amp; node --test tests/citizen-status.test.mjs tests/public-shell.test.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <done>Locale-prefixed deep links work from success copy and unprefixed /status bounce.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next BFF | Untrusted report_id/token in JSON body; query params may appear in browser history. |
| Next BFF → FastAPI | Must forward client IP; must not log plaintext tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information disclosure | Status page error UI | high | mitigate | Single catalog string for all 401s (D-16); no not-found vs expired copy |
| T-04-07 | Information disclosure | Query-string tokens | medium | accept | Locked by D-09; privacy note warns citizens; POST body to FastAPI via BFF |
| T-04-08 | DoS | Missing XFF on BFF | high | mitigate | Copy analyze XFF/X-Real-Ip forward (D-17) |
| T-04-09 | Information disclosure | RSC/server logs | medium | mitigate | Client-side auto-fetch preferred (RESEARCH A1); avoid logging request bodies |
| T-04-SC | Tampering | npm installs | low | accept | No new packages; reuse live-region clipboard pattern |
</threat_model>

<verification>
- node --test frontend/tests/citizen-status.test.mjs green
- Manual: deep link auto-fetch, manual form, 401/429 messaging, EN/VI
- Success page copies locale-prefixed status URL
</verification>

<success_criteria>
- CIT-01 delivered; CIT-02/03/04 client UX complete
- D-01..D-09, D-16, D-17 covered on public surface
- Shareable success deep link lands on working status page
</success_criteria>

<output>
Create `.planning/phases/04-citizen-status/04-02-SUMMARY.md` when done
</output>
