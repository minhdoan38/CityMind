---
phase: 10-shadow-rollout-production-evaluation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/datasets/urban-incidents-v1.jsonl
  - evals/datasets/injection-adversarial.jsonl
  - evals/datasets/README.md
  - evals/manifests/phase10-baseline-vs-candidate.json
  - evals/results/.gitkeep
  - src/server/evals/types.ts
  - src/server/evals/load-dataset.ts
  - src/server/evals/load-dataset.test.ts
  - src/server/evals/metrics.ts
  - src/server/evals/metrics.test.ts
  - src/server/evals/aggregate.ts
  - src/server/evals/aggregate.test.ts
  - src/server/evals/run-case.ts
  - src/server/evals/shadow-compare.ts
  - src/server/evals/shadow-compare.test.ts
  - scripts/eval-suite.mjs
  - scripts/verify-eval-gate.mjs
  - src/server/services/report-service.test.ts
  - package.json
  - .gitignore
autonomous: true
requirements: [TRIAGE-08]
user_setup:
  - service: third-party-ai
    why: Live eval runs only (--live); CI uses --mock without API key
    env_vars:
      - name: THIRD_PARTY_API_KEY
        source: Operator secret store
      - name: AI_BASE_URL
        source: .env.local
      - name: AI_MODEL
        source: .env.local
must_haves:
  truths:
    - Operator can run mock eval suite in CI without live API credentials
    - Eval scores production ReportAnalysis fields against expert gold labels (not evaluator 11-key schema)
    - macro-F1, under-triage, grounding, EN/VI parity, and failure-rate metrics computed per evaluator thresholds
    - verify-eval-gate.mjs exits 0 only when pinned manifest thresholds pass
    - Intake survives provider outage — submitReport returns token while triage fails async
    - shadow-compare pure diff logic exists for Plan 10-02 worker hook
  artifacts:
    - path: evals/datasets/urban-incidents-v1.jsonl
      provides: 50 expert-labelled EN/VI cases with gold.is_critical
    - path: src/server/evals/metrics.ts
      provides: macroF1, isUnderTriage, localeParityDelta, groundingPassRate
      exports: [macroF1, isUnderTriage, computeLocaleMetrics, passesThresholds]
    - path: scripts/eval-suite.mjs
      provides: CLI --mock and --live eval runner
    - path: scripts/verify-eval-gate.mjs
      provides: cutover gate exit-code check
    - path: evals/manifests/phase10-baseline-vs-candidate.json
      provides: pinned baseline/candidate provider model prompt routing versions
  key_links:
    - from: scripts/eval-suite.mjs
      to: src/server/evals/metrics.ts
      via: tsx dynamic import or compiled re-export
      pattern: aggregate.*metrics
    - from: scripts/verify-eval-gate.mjs
      to: prompt/citymind_ai_triage_structured_output_evaluator.json
      via: thresholds load
      pattern: thresholds
    - from: src/server/evals/metrics.ts
      to: src/server/validation/analysis-policy.ts
      via: validateAnalysisPolicy for grounding proxy
      pattern: validateAnalysisPolicy
---

## Phase Goal

**As an** operator, **I want** an offline eval suite that scores triage quality against expert labels and evaluator thresholds, **so that** production model/config swaps are gated on under-triage, grounding, EN/VI parity, and failure rate before cutover.

<objective>
Wave 1 vertical slice: versioned eval dataset, pure metric modules with Vitest, mock/live CLI runner, pinned manifest, and threshold gate script. Production keeps ReportAnalysis schema per RESEARCH resolved Q1. No shadow worker hook or officer UI in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-shadow-rollout-production-evaluation/10-RESEARCH.md
@.planning/phases/10-shadow-rollout-production-evaluation/10-PATTERNS.md
@prompt/citymind_ai_triage_structured_output_evaluator.json
@src/server/domain/report-analysis.ts
@src/server/validation/analysis-policy.ts
@scripts/smoke-ai.mjs
@scripts/load-project-env.mjs
@scripts/verify-gate-artifacts.mjs

<interfaces>
From src/server/domain/report-analysis.ts:
- ReportAnalysisSchema, ReportAnalysis — production triage output shape (score these fields only)
- CategorySchema includes pothole|flooding|waste|streetlight|graffiti|obstruction|other

From src/server/validation/analysis-policy.ts:
- validateAnalysisPolicy(analysis, { description? }) → PolicyResult { ok, violations? }
- Use for grounding/rubric proxy metrics (hallucination_pass_rate)

From prompt/citymind_ai_triage_structured_output_evaluator.json:
- evaluation_run.cases: 50, repetitions_per_case: 5
- thresholds: schema_pass_rate_min 0.99, missed_critical_count_max 0, category_consistency_min 0.85, etc.
- pass_criteria.logic: "all"

From scripts/smoke-ai.mjs:
- --require-privacy-approval readline gate before live AI calls
- Exit codes: 0 pass, 1 fail, 2 missing config

Planner discretion (RESEARCH resolved):
- evals/ at repo root (not backend/evals/)
- EN/VI parity epsilon: 0.05 vs baseline per locale metrics
- No ajv; Zod-only validation
- Do NOT migrate production to evaluator 11-key schema
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eval dataset scaffold and pure metric modules</name>
  <files>evals/datasets/urban-incidents-v1.jsonl, evals/datasets/injection-adversarial.jsonl, evals/datasets/README.md, evals/results/.gitkeep, src/server/evals/types.ts, src/server/evals/load-dataset.ts, src/server/evals/load-dataset.test.ts, src/server/evals/metrics.ts, src/server/evals/metrics.test.ts, src/server/evals/shadow-compare.ts, src/server/evals/shadow-compare.test.ts, .gitignore</files>
  <read_first>
    <file>prompt/citymind_ai_triage_structured_output_evaluator.json</file>
    <file>src/server/domain/report-analysis.ts</file>
    <file>src/server/validation/analysis-policy.ts</file>
    <file>.planning/phases/10-shadow-rollout-production-evaluation/10-RESEARCH.md</file>
  </read_first>
  <behavior>
    - EvalCaseSchema: case_id, locale en|vi, report_text, optional image_fixture, gold { category, severity, priority, is_critical }, tags optional
    - urban-incidents-v1.jsonl: exactly 50 lines, 25 en + 25 vi, synthetic text only (no PII), every gold has is_critical
    - injection-adversarial.jsonl: at least 5 cases tagged injection with adversarial instructions in report_text
    - macroF1: multi-class macro average on category pairs
    - isUnderTriage: gold.is_critical true AND (pred.severity &lt; 5 OR pred.priority !== critical)
    - falseCritical: pred priority critical AND gold.is_critical false
    - localeParityDelta: max absolute delta across macro_f1, severity_agreement, under_triage_rate between en and vi bundles
    - shadow-compare: diff category/severity/priority booleans; has_disagreement if any true
    - load-dataset rejects malformed JSONL with Zod safeParse errors
  </behavior>
  <action>
    Create evals/ tree per RESEARCH Pattern 1. Implement types.ts with EvalCaseSchema, GoldLabel, EvalManifest Zod schemas. Implement load-dataset.ts JSONL reader (readline). Implement metrics.ts pure functions per RESEARCH Code Examples — no Supabase or fetch imports. Implement shadow-compare.ts field diff helpers consumed by 10-02. Add metrics.test.ts matrix: macroF1 perfect/partial, under-triage on is_critical gold, false critical, EN/VI parity delta, injection policy pass counting via validateAnalysisPolicy. Add load-dataset.test.ts and shadow-compare.test.ts. Gitignore evals/results/* except .gitkeep. Document labelling rules in evals/datasets/README.md.
  </action>
  <acceptance_criteria>
    <criterion>urban-incidents-v1.jsonl has 50 valid cases with balanced locales.</criterion>
    <criterion>npm run test:unit -- src/server/evals/metrics.test.ts passes all behavior cases.</criterion>
    <criterion>shadow-compare.test.ts covers agreement and disagreement paths.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/evals/metrics.test.ts &amp;&amp; npm run test:unit -- src/server/evals/load-dataset.test.ts &amp;&amp; npm run test:unit -- src/server/evals/shadow-compare.test.ts</automated>
  </verify>
  <done>Pure metric and dataset contracts exist with green unit tests; shadow diff ready for 10-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate thresholds and mock eval CLI end-to-end</name>
  <files>src/server/evals/aggregate.ts, src/server/evals/aggregate.test.ts, src/server/evals/run-case.ts, scripts/eval-suite.mjs, evals/manifests/phase10-baseline-vs-candidate.json, package.json</files>
  <read_first>
    <file>scripts/smoke-ai.mjs</file>
    <file>scripts/load-project-env.mjs</file>
    <file>src/server/ai/openai-compatible.ts</file>
    <file>prompt/citymind_ai_triage_structured_output_evaluator.json</file>
  </read_first>
  <behavior>
    - aggregate.ts loads thresholds from evaluator JSON; passesThresholds returns { pass, failures[] } with logic all
    - Enforce all 11 evaluator threshold keys: schema_pass_rate_min, hallucination_pass_rate_min, rubric_compliance_rate_min, severity_consistency_min, priority_consistency_min, category_consistency_min, mean_confidence_std_max, false_critical_count_max, missed_critical_count_max, unsupported_occurred_injury_claim_count_max, unsupported_cause_claim_count_max
    - Mock mode uses --repetitions 1 (skip multi-rep consistency metrics or treat as N/A with pass); live mode uses repetitions_per_case 5 for consistency metrics
    - run-case.ts --mock mode returns deterministic ReportAnalysis fixtures keyed by case_id prefix (no API)
    - eval-suite.mjs default --mock: load manifest, dataset, run all cases once, aggregate, write evals/results/latest.json and evals/results/run-{iso}.json
    - Manifest resolves ${AI_MODEL} placeholders from env at runtime; stores resolved values in results JSON
    - Manifest pins routing_policy_version from ROUTING_POLICY_VERSION constant
    - Exit 0 on pass, 1 on metric fail, 2 on missing files
  </behavior>
  <action>
    Implement aggregate.ts mapping evaluator threshold keys to computed metrics (schema_pass_rate via ReportAnalysisSchema.safeParse, missed_critical_count, false_critical_count, hallucination_pass_rate via validateAnalysisPolicy, locale parity). Implement run-case.ts with injectable analyzeStructured for live mode and fixture map for mock. Create scripts/eval-suite.mjs mirroring smoke-ai shebang + loadProjectEnv; flags --manifest, --dataset, --mock (default), --live, --repetitions (default 1 in mock, 5 in live). Create evals/manifests/phase10-baseline-vs-candidate.json per RESEARCH Pattern 3. Add package.json scripts: eval:mock, eval:live, eval:gate (verify script wired in Task 3). aggregate.test.ts covers threshold pass/fail edge cases.
  </action>
  <acceptance_criteria>
    <criterion>node scripts/eval-suite.mjs --mock exits 0 and writes evals/results/latest.json.</criterion>
    <criterion>aggregate.test.ts verifies all-pass and single-threshold-fail paths.</criterion>
    <criterion>Results JSON includes baseline/candidate resolved lineage and per-locale metrics.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/evals/aggregate.test.ts &amp;&amp; node scripts/eval-suite.mjs --mock</automated>
  </verify>
  <done>Mock eval pipeline runs end-to-end from dataset to results JSON without live AI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate script, live eval mode, outage test, injection suite</name>
  <files>scripts/verify-eval-gate.mjs, scripts/eval-suite.mjs, src/server/services/report-service.test.ts, package.json</files>
  <read_first>
    <file>scripts/verify-gate-artifacts.mjs</file>
    <file>src/server/services/report-service.ts</file>
    <file>src/server/services/report-service.test.ts</file>
    <file>scripts/smoke-ai.mjs</file>
  </read_first>
  <behavior>
    - verify-eval-gate.mjs reads evals/results/latest.json (or --results path), validates schema, checks pass flag and manifest_id match, compares candidate vs baseline failure_rate and under_triage (candidate must not exceed baseline)
    - --live requires --require-privacy-approval interactive gate like smoke-ai before any API call
    - --live runs 50 cases × 5 repetitions; never invoked from npm test
    - Outage test: submitReport returns report_id + access_token + triage_status pending without calling provider
    - Injection cases counted in separate safety metric; unsupported authority language fails policy
  </behavior>
  <action>
    Create scripts/verify-eval-gate.mjs mirroring verify-gate-artifacts.mjs fail-fast pattern. Support env EVAL_MANIFEST_PATH defaulting to evals/manifests/phase10-baseline-vs-candidate.json. Extend eval-suite.mjs --live path: require THIRD_PARTY_API_KEY, AI_BASE_URL, AI_MODEL; call analyzeStructured from openai-compatible.ts; support --candidate flag overlaying AI_MODEL_CANDIDATE for dual-run offline comparison in results only. Extend report-service.test.ts with outage case per RESEARCH Pattern outage test — intake persisted, triage_status pending, no provider call. Wire npm run eval:gate → verify-eval-gate.mjs. Document in eval-suite help text that CI must use --mock only.
  </action>
  <acceptance_criteria>
    <criterion>node scripts/verify-eval-gate.mjs exits 1 when latest.json status is FAIL.</criterion>
    <criterion>npm run test:unit -- src/server/services/report-service.test.ts passes outage case.</criterion>
    <criterion>npm run eval:mock &amp;&amp; npm run eval:gate succeeds on mock results.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run eval:mock &amp;&amp; npm run eval:gate &amp;&amp; npm run test:unit -- src/server/services/report-service.test.ts</automated>
  </verify>
  <done>Cutover gate script blocks on threshold failure; live eval is operator-only; outage intake contract tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval CLI → third-party AI | Live mode sends synthetic case text to external endpoint |
| evals/datasets → metrics | Case report_text treated as untrusted prompt injection surface |
| results JSON → verify gate | Gate artifact must not be forgeable without manifest match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | eval case injection tags | mitigate | Cases run server-side only; validateAnalysisPolicy on outputs; injection suite asserts policy pass |
| T-10-02 | Information Disclosure | --live eval PII | mitigate | Synthetic fixtures only in dataset README; --require-privacy-approval before live |
| T-10-03 | Elevation | forged evals/results/latest.json | mitigate | verify-eval-gate checks manifest_id, metrics schema, and pass flag; operator reviews artifact |
| T-10-04 | Tampering | candidate endpoint in manifest | mitigate | normalizeAiBaseUrl HTTPS rule; manifest human review before cutover |
| T-10-SC | Tampering | npm installs | accept | Zero new packages; Zod-only per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
Per task automated verifies above.
Wave 1 gate: npm run eval:mock &amp;&amp; npm run eval:gate &amp;&amp; npm run test:unit -- src/server/evals
Full suite: npm run test
Operator gate (manual, not CI): npm run eval:live &amp;&amp; npm run eval:gate
</verification>

<success_criteria>
1. Mock eval suite runs in CI without API credentials and produces evals/results/latest.json
2. All evaluator JSON thresholds enforced by aggregate + verify-eval-gate
3. EN/VI parity computed per locale with 0.05 epsilon vs baseline in gate logic
4. Outage intake test proves report_id + access_token returned while triage stays pending
5. shadow-compare.ts exported for Plan 10-02 worker hook
</success_criteria>

<output>
Create `.planning/phases/10-shadow-rollout-production-evaluation/10-01-SUMMARY.md` when done
</output>
