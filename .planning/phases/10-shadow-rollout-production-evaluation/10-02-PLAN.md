---
phase: 10-shadow-rollout-production-evaluation
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - supabase/migrations/20260722140001_triage_shadow.sql
  - supabase/tests/10_shadow_eval_contract.sql
  - src/server/config/env.ts
  - src/server/evals/shadow-service.ts
  - src/server/evals/shadow-service.test.ts
  - src/server/triage/service.ts
  - src/server/triage/service.test.ts
  - src/server/repositories/reports.ts
  - src/server/officer/filters.ts
  - src/components/reports/ShadowMismatchBadge.tsx
  - src/components/reports/ReportsTable.tsx
  - src/components/reports/ReportsFilters.tsx
  - src/app/dashboard/reports/[reportId]/page.tsx
  - tests/dashboard-table.test.mjs
  - .env.example
autonomous: false
requirements: [TRIAGE-08]
user_setup:
  - service: supabase
    why: Shadow comparison table migration and SQL contract
    env_vars: [SUPABASE_DB_URL]
must_haves:
  truths:
    - TRIAGE_SHADOW_MODE=compare dual-runs candidate triage after baseline completes without mutating reports analysis columns
    - Shadow comparisons persist in triage_shadow_comparisons with baseline/candidate ReportAnalysis snapshots
    - Officers see shadow disagreement badge, filter chip, and detail comparison panel
    - Production cutover requires eval gate PASS before AI_MODEL swap (operator checkpoint)
    - Candidate env uses AI_MODEL_CANDIDATE / AI_BASE_URL_CANDIDATE overlay only for shadow path
  artifacts:
    - path: supabase/migrations/20260722140001_triage_shadow.sql
      provides: triage_shadow_comparisons table + indexes
    - path: src/server/evals/shadow-service.ts
      provides: compareShadowTriage, insertShadowComparison
      exports: [compareShadowTriage]
    - path: src/components/reports/ShadowMismatchBadge.tsx
      provides: officer table badge for has_shadow_disagreement
    - path: src/server/config/env.ts
      provides: TRIAGE_SHADOW_MODE, AI_MODEL_CANDIDATE, EVAL_MANIFEST_PATH
  key_links:
    - from: src/server/triage/service.ts
      to: src/server/evals/shadow-service.ts
      via: post-complete hook when getShadowConfig().mode === compare
      pattern: compareShadowTriage
    - from: src/server/evals/shadow-service.ts
      to: triage_shadow_comparisons
      via: service-role insert only
      pattern: insertShadowComparison
    - from: src/components/reports/ReportsTable.tsx
      to: officer list loader has_shadow_disagreement
      via: badge render
      pattern: ShadowMismatchBadge
---

## Phase Goal

**As an** officer, **I want** shadow triage comparisons visible when baseline and candidate models disagree, **so that** production cutover is observed and gated before citizens see candidate disposition.

<objective>
Wave 2 vertical slice: shadow comparison storage, non-mutating worker dual-run behind TRIAGE_SHADOW_MODE, officer disagreement UX, and feature-flag cutover protocol tied to Plan 10-01 eval gate. Never call complete_triage_report or updateTriageTerminalState for candidate path per RESEARCH Pitfall 2.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/10-shadow-rollout-production-evaluation/10-01-SUMMARY.md
</execution_context>

<context>
@.planning/phases/10-shadow-rollout-production-evaluation/10-RESEARCH.md
@.planning/phases/10-shadow-rollout-production-evaluation/10-PATTERNS.md
@src/server/evals/shadow-compare.ts
@src/server/triage/service.ts
@src/components/reports/TriageStatusBadge.tsx
@src/components/reports/RoutingDestinationBadge.tsx
@supabase/migrations/20260722120002_async_triage_audit.sql
@supabase/tests/08_async_triage_contract.sql

<interfaces>
From 10-01 (must exist before this plan):
- src/server/evals/shadow-compare.ts — buildDisagreement(baseline, candidate) → { category, severity, priority, has_disagreement }
- scripts/verify-eval-gate.mjs — npm run eval:gate

From src/server/triage/service.ts:
- runTriageForReport(reportId, deps?) — insert hook after line ~334 applyTerminalRouting on completed path only
- TriageServiceDeps — inject analyzeStructured for candidate overlay in tests

From src/server/routing/policy.ts:
- ROUTING_POLICY_VERSION — include in shadow row candidate_prompt_version lineage

Planner discretion (RESEARCH resolved):
- TRIAGE_SHADOW_MODE: off | compare (default off)
- Separate triage_shadow_comparisons table (not triage_runs columns)
- No auto manual_review on shadow disagreement — badge + filter + panel only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shadow table migration, shadow-service, SQL contract</name>
  <files>supabase/migrations/20260722140001_triage_shadow.sql, supabase/tests/10_shadow_eval_contract.sql, src/server/evals/shadow-service.ts, src/server/evals/shadow-service.test.ts</files>
  <read_first>
    <file>supabase/migrations/20260722120002_async_triage_audit.sql</file>
    <file>supabase/tests/08_async_triage_contract.sql</file>
    <file>src/server/evals/shadow-compare.ts</file>
    <file>.planning/phases/10-shadow-rollout-production-evaluation/10-RESEARCH.md</file>
  </read_first>
  <behavior>
    - triage_shadow_comparisons columns: comparison_id UUID PK, report_id FK, production_run_id FK triage_runs, candidate_model, candidate_prompt_version, baseline_snapshot JSONB, candidate_snapshot JSONB, disagreement JSONB, has_disagreement BOOLEAN, compared_at TIMESTAMPTZ
    - RLS enabled; REVOKE ALL FROM PUBLIC; GRANT service_role only (mirror audit migration)
    - Index on has_disagreement WHERE true for officer filter
    - insertShadowComparison never UPDATEs reports triage or analysis columns
    - compareShadowTriage calls candidate analyzeStructured with env overlay; on failure stores candidate_snapshot null + error in disagreement metadata without throwing to worker
  </behavior>
  <action>
    Create migration 20260722140001_triage_shadow.sql per RESEARCH Pattern 4 storage table. Create shadow-service.ts with compareShadowTriage(deps, input) using shadow-compare diff helpers and admin client insert. Candidate analyze uses AI_MODEL_CANDIDATE / AI_BASE_URL_CANDIDATE when set; skip shadow when candidate model unset. Create 10_shadow_eval_contract.sql: seed report + production triage_run, insert shadow row, assert reports.category unchanged, assert has_disagreement index usable. shadow-service.test.ts mocks analyzeStructured and Supabase insert.
  </action>
  <acceptance_criteria>
    <criterion>Migration creates triage_shadow_comparisons with FK to reports and triage_runs.</criterion>
    <criterion>shadow-service.test.ts proves no reports UPDATE in compareShadowTriage.</criterion>
    <criterion>10_shadow_eval_contract.sql file exists with _test_assert helper matching Phase 8 style.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/evals/shadow-service.test.ts</automated>
  </verify>
  <done>Shadow persistence contract defined; unit tests green before DB push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push shadow migration and run SQL contract</name>
  <files>supabase/migrations/20260722140001_triage_shadow.sql, supabase/tests/10_shadow_eval_contract.sql</files>
  <read_first>
    <file>scripts/run-supabase-sql.mjs</file>
    <file>supabase/migrations/20260722140001_triage_shadow.sql</file>
    <file>supabase/tests/10_shadow_eval_contract.sql</file>
  </read_first>
  <action>
    [BLOCKING] Requires SUPABASE_DB_URL in .env.local. Apply migration: node scripts/run-supabase-sql.mjs supabase/migrations/20260722140001_triage_shadow.sql. Ensure Phase 8–9 migrations applied first. Run contract: node scripts/run-supabase-sql.mjs supabase/tests/10_shadow_eval_contract.sql. Stop on ASSERTION FAILED. Downstream worker integration remains blocked until pass.
  </action>
  <acceptance_criteria>
    <criterion>Live database has triage_shadow_comparisons callable by service_role.</criterion>
    <criterion>10_shadow_eval_contract.sql exits 0.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-supabase-sql.mjs supabase/tests/10_shadow_eval_contract.sql</automated>
  </verify>
  <done>Shadow table live; worker hook and officer reads unblocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shadow worker hook, env flags, officer disagreement UX, cutover protocol</name>
  <files>src/server/config/env.ts, src/server/triage/service.ts, src/server/triage/service.test.ts, src/server/repositories/reports.ts, src/server/officer/filters.ts, src/components/reports/ShadowMismatchBadge.tsx, src/components/reports/ReportsTable.tsx, src/components/reports/ReportsFilters.tsx, src/app/dashboard/reports/[reportId]/page.tsx, tests/dashboard-table.test.mjs, .env.example</files>
  <read_first>
    <file>src/server/triage/service.ts</file>
    <file>src/components/reports/TriageStatusBadge.tsx</file>
    <file>src/components/reports/RoutingDestinationBadge.tsx</file>
    <file>src/server/repositories/reports.ts</file>
    <file>src/server/officer/filters.ts</file>
    <file>.planning/phases/09-self-help-vs-government-routing/09-04-SUMMARY.md</file>
  </read_first>
  <behavior>
    - env.ts: TRIAGE_SHADOW_MODE z.enum off|compare default off; AI_MODEL_CANDIDATE, AI_BASE_URL_CANDIDATE optional; EVAL_MANIFEST_PATH optional
    - getShadowConfig() returns { mode, candidateModel, candidateBaseUrl } — compare only when mode=compare AND candidate model set
    - Worker hook: after production completed + applyTerminalRouting, await compareShadowTriage; never on failed/manual_review unless planner discretion — skip shadow on non-completed dispositions
    - service.test.ts: shadow analyzeStructured called twice; reports update count unchanged for candidate
    - Officer list: LEFT JOIN latest shadow comparison; expose has_shadow_disagreement boolean
    - ReportsFilters chip shadow_disagreement=true; mirror triage_status chip pattern
    - ShadowMismatchBadge in table column; detail page collapsible Shadow comparison panel side-by-side category/severity/priority
    - Cutover protocol documented in verification: eval:gate PASS → enable TRIAGE_SHADOW_MODE=compare → observe → swap AI_MODEL only after operator checkpoint
  </behavior>
  <action>
    Extend ServerEnvSchema with shadow fields per RESEARCH Pattern 6. Add compareShadowTriage call in runTriageForReport completed path only. Extend reports repository list/detail queries with has_shadow_disagreement via subquery or join on latest comparison per report_id. Create ShadowMismatchBadge mirroring TriageStatusBadge cva variants. Add filter param in filters.ts and dashboard page URL sync. Detail page panel reads baseline_snapshot vs candidate_snapshot JSONB fields — officer-only, no new i18n required. Extend tests/dashboard-table.test.mjs with shadow badge string gate. Update .env.example with TRIAGE_SHADOW_MODE=off, AI_MODEL_CANDIDATE=, AI_BASE_URL_CANDIDATE=, EVAL_MANIFEST_PATH=. Do NOT auto-route shadow disagreements to manual_review per RESEARCH Q4.
  </action>
  <acceptance_criteria>
    <criterion>npm run test:unit -- src/server/triage/service.test.ts passes shadow non-mutation case.</criterion>
    <criterion>npm run test:legacy -- tests/dashboard-table.test.mjs passes shadow badge gate.</criterion>
    <criterion>TRIAGE_SHADOW_MODE defaults to off; compare no-ops when AI_MODEL_CANDIDATE empty.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/server/triage/service.test.ts &amp;&amp; npm run test:legacy -- tests/dashboard-table.test.mjs</automated>
  </verify>
  <done>Shadow dual-run wired in worker; officers can filter and inspect disagreements; cutover gated on 10-01 eval:gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → candidate AI endpoint | Shadow calls use separate candidate URL/model |
| Officer dashboard → shadow rows | Read via report join only; no cross-report leakage |
| service_role → triage_shadow_comparisons | Writes only from shadow-service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | candidate overwriting reports | mitigate | shadow-service insert-only; never call complete_triage_report for candidate |
| T-10-06 | Information Disclosure | shadow snapshots in officer UI | mitigate | Officer auth unchanged; snapshots scoped to report_id join |
| T-10-07 | Denial of Service | shadow doubling API cost | mitigate | TRIAGE_SHADOW_MODE default off; operator enables during window only |
| T-10-08 | Elevation | cutover without eval gate | mitigate | Operator protocol requires npm run eval:gate PASS before AI_MODEL swap |
| T-10-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
After Task 2: node scripts/run-supabase-sql.mjs supabase/tests/10_shadow_eval_contract.sql
After Task 3: npm run test
Cutover protocol (operator checkpoint):
1. npm run eval:live &amp;&amp; npm run eval:gate
2. Set TRIAGE_SHADOW_MODE=compare in .env.local; restart triage:worker
3. Observe shadow disagreement rate in dashboard filter
4. Swap AI_MODEL to candidate only after gate PASS + acceptable disagreement rate
</verification>

<success_criteria>
1. Shadow comparisons stored without mutating production report analysis
2. Officer badge, filter, and detail panel surface baseline vs candidate disagreements
3. TRIAGE_SHADOW_MODE=off by default; compare mode uses candidate env overlay
4. service.test.ts proves candidate path does not change reports disposition
5. SQL contract green on live Supabase
</success_criteria>

<output>
Create `.planning/phases/10-shadow-rollout-production-evaluation/10-02-SUMMARY.md` when done
</output>
